---
phase: 11-inline-demo-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/supervisor/UnifiedBoard.jsx
  - src/components/supervisor/views.jsx
  - src/components/supervisor/WeekFlow.jsx
  - src/components/supervisor/RosterWizard.jsx
  - src/components/supervisor/PositionsScreen.jsx
autonomous: true
requirements: [INLINE-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "מנהל בשלב 'השבוע' (board) של WeekFlow יכול ללחוץ 'x' על אווטאר משובץ כדי להסיר אותו — בלי לצאת מהמסך, בלי לנווט לשלב 'שיבוץ'."
    - "מנהל יכול ללחוץ '+' על משבצת עם מקום פנוי, לבחור שומר/ת מבורר קצר, וההוספה מתבצעת מיד — רק שומרים כשירים לקטגוריה ניתנים לבחירה, אותו gate בדיוק כמו toggleAssignment עצמה."
    - "שתי הפעולות עוברות דרך actions.toggleAssignment הקיימת בלבד — בלי DB write חדש, בלי מנגנון כתיבה שני."
    - "ביומן (CalendarView) ואצל המשתתף (GuardApp) הלוח נשאר לקריאה-בלבד בדיוק כמו קודם — onToggleAssignment לא מועבר משם."
    - "מחיקת עמדת army עם שבוע נוכחי כבר ממומש (RosterWizard או PositionsScreen) לא נכשלת בשגיאת מפתח-זר גולמית."
  artifacts: []
  key_links:
    - "WeekFlow.jsx → UnifiedBoard onToggleAssignment={actions.toggleAssignment} → BoardCard → People.onRemove / picker Modal → actions.toggleAssignment(item.id, guardId)"
    - "RosterWizard.jsx removeItem / PositionsScreen.jsx onDelete → actions.deletePosition(id, weekDates)"
---

<objective>
מחווט את שתי חתיכות ה-UI של INLINE-01 מעל הבסיס ש-Plan 11-01 בנה: (א) עריכה inline על הלוח עצמו — "x" על אווטאר משובץ להסרה, "+" על משבצת פנויה להוספה, שתיהן עוברות דרך `actions.toggleAssignment` הקיימת בלי כתיבה חדשה; (ב) חיווט קריאות `deletePosition` הקיימות ב-`RosterWizard.jsx`/`PositionsScreen.jsx` להעביר `weekDates`, כדי שתיקון ה-FK ש-11-01 בנה יגיע בפועל למנהל.

Purpose: INLINE-01 — "מנהל מוסיף ומוחק שיבוצים ישירות בתוך 'בניית שבוע'" (ROADMAP קריטריון הצלחה 1). האופציה שנבחרה (11-CONTEXT.md decisions, "אופציה ב") היא הכי נאמנה לניסוח "מיד אחרי כל שינוי" — הבאג עצמו (INLINE-04) מרמז שהלוח אמור להיות המקום שמשתנה.

Output: `UnifiedBoard.jsx`/`views.jsx`'s `People`/`WeekFlow.jsx` עם שתי affordances חדשות על הלוח; `RosterWizard.jsx`/`PositionsScreen.jsx` מעבירים `weekDates` ל-`deletePosition`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-inline-demo-cleanup/11-CONTEXT.md
@.planning/phases/11-inline-demo-cleanup/11-01-SUMMARY.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/supervisor/UnifiedBoard.jsx
@src/components/supervisor/WeekFlow.jsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: "x" הסרת שיבוץ מהלוח — מחווט מקצה-לקצה (INLINE-01)</name>

  <files>
src/components/supervisor/UnifiedBoard.jsx
src/components/supervisor/views.jsx
src/components/supervisor/WeekFlow.jsx
  </files>

  <read_first>
- `.planning/phases/11-inline-demo-cleanup/11-CONTEXT.md` קטע `<decisions>` — "INLINE-01 UI: אופציה ב" (כפתורי "+"/"x" ישירות על `UnifiedBoard.jsx`, דרך `toggleAssignment`) וקטע `<findings>` סעיפים 1+5.
- `src/components/supervisor/UnifiedBoard.jsx` — כל הקובץ (270 שורות): `UnifiedBoard` (66-124), `DayColumn` (126-155), `DRAG_MIME` (157-163), `BoardCard` (165-270 — במיוחד `missingOfItem` 61-64, `draggableHere`/`dragProps` 180-202, קריאת `<People .../>` 252-266).
- `src/components/supervisor/views.jsx` שורות 1690-1766 — `People`: פרופ `onDragStart` האופציונלי הקיים (1693-1697, ההערה שמסבירה שהוא רשות ולא ברירת מחדל) הוא הדפוס המדויק ש-`onRemove` צריך לחקות; ההערה בשורות 714-715 (ליד כפתור המחיקה של `ShiftMgmt`) על "hover-gated control is unreachable on a touch screen" — למה ה-"x" חייב להיות תמיד-גלוי.
- `src/components/supervisor/WeekFlow.jsx` שורות 22-31 (imports), 148-170 (רשומת ה-board הקיימת בתוך מערך `body`, כולל `onMove={actions.moveAssignment}`).
- `src/hooks/useGuardian.js` שורות 589-630 — `toggleAssignment` הקיימת (קריאה בלבד, אין לערוך את הקובץ הזה בתוכנית זו).
  </read_first>

  <action>
ב-`UnifiedBoard.jsx`: הוסף `onToggleAssignment` כפרופ אופציונלי חדש (ברירת מחדל `undefined`) ל-`UnifiedBoard` עצמה, באותה צורה בדיוק ש-`onMove` כבר קיים (פרופ רשות, לא ברירת מחדל). השחל אותו דרך `DayColumn` ועד `BoardCard`, באותו דפוס prop-drilling ש-`onMove`/`guards`/`mode` כבר משתמשים בו — אל תמציא מנגנון חדש. ב-`BoardCard`: חשב `const canRemove = Boolean(onToggleAssignment) && !timeless;` (מיד ליד `draggableHere` הקיים, אותו סגנון בדיוק). העבר ל-`<People .../>` הקיימת `onRemove={canRemove ? (guardId) => onToggleAssignment(item.id, guardId) : undefined}`.

ב-`views.jsx`'s `People`: הוסף פרופ אופציונלי `onRemove` (פונקציה `(guardId) => void`, ברירת מחדל `undefined` — אותה צורה בדיוק כמו `onDragStart` הקיים, מתועד ממש ליד). בשני הענפים של רינדור אווטאר (הרגיל/כשיר, והחסום-כשירות) — כשמועבר `onRemove` — רנדר תג-"x" קטן, **תמיד גלוי** (לא hover-only, בדיוק כמו ההערה הקיימת ב-`ShiftMgmt`'s כפתור המחיקה, views.jsx:714-715), ממוקם בפינה שלא מתנגשת עם תג-המנעול הקיים על הענף החסום (המנעול יושב top-right; שים את ה-"x" בפינה הנגדית). לחיצה קוראת ל-`e.stopPropagation(); onRemove(g.id);` — `stopPropagation` הכרחי כי ה-div העוטף את האווטאר הוא `draggable` כש-`onDragStart` גם מועבר (BOARD-05), וקליק על הכפתור המקונן לא אמור "לדלוף" לגרירה. תן לו `aria-label` ששמו את מי שמוסר, באותו סגנון ניסוח כמו `aria-label` הקיים על כפתור המחיקה ב-`ShiftMgmt`.

ב-`WeekFlow.jsx`: על קריאת ה-`<UnifiedBoard key="board" ... onMove={actions.moveAssignment} .../>` הקיימת, הוסף `onToggleAssignment={actions.toggleAssignment}` — `actions.toggleAssignment` כבר קיימת ולא צריכה שום שינוי (useGuardian.js:589-630); המשימה הזו רק מחווטת אותה למשטח UI חדש.

אל תוסיף את הפרופ הזה לשום קריאה ל-`<UnifiedBoard>` ב-`GuardApp.jsx` ולא ל-`CalendarView.jsx` — שתיהן חייבות להישאר לקריאה-בלבד בדיוק כמו היום. `toggleAssignment` עצמה כבר חוסמת כשירות בכיוון ההוספה ולעולם לא חוסמת הסרה (useGuardian.js:594-609) — המשימה הזו לא נוגעת בהתנהגות הזו, ה-"x" רק קורא לפעולה הקיימת בדיוק כמו שקליק ב-AssignView כבר עושה היום.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/UnifiedBoard.jsx','utf8'); if(!/onToggleAssignment/.test(s)) throw new Error('UnifiedBoard.jsx missing onToggleAssignment prop'); if(!/onRemove=\{canRemove/.test(s)) throw new Error('BoardCard must pass onRemove to People gated on canRemove'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/views.jsx','utf8'); const start=s.indexOf('export const People'); const end=s.indexOf('/**', start+50); const body=s.slice(start,end); if(!/onRemove/.test(body)) throw new Error('People must accept onRemove prop'); if(!/onRemove\(g\.id\)/.test(body)) throw new Error('People must call onRemove(g.id) on click'); if(!/stopPropagation/.test(body)) throw new Error('People onRemove handler must stopPropagation (avatar wrapper is draggable)'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/WeekFlow.jsx','utf8'); if(!/onToggleAssignment=\{actions\.toggleAssignment\}/.test(s)) throw new Error('WeekFlow.jsx must pass onToggleAssignment={actions.toggleAssignment} to UnifiedBoard'); console.log('ok')"</automated>
    <automated>node -e "const g=require('fs').readFileSync('src/components/GuardApp.jsx','utf8'); const c=require('fs').readFileSync('src/components/supervisor/CalendarView.jsx','utf8'); if(g.includes('onToggleAssignment')) throw new Error('GuardApp.jsx must stay read-only — onToggleAssignment must not appear there'); if(c.includes('onToggleAssignment')) throw new Error('CalendarView.jsx must stay read-only — onToggleAssignment must not appear there'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, היכנס כמנהל, פתח "בניית שבוע" על צוות עם לפחות משמרת אחת ואדם משובץ אליה. עבור לשלב "השבוע" (הלוח). אמת: (1) כל אווטאר משובץ נושא "x" קטן, גלוי תמיד (לא רק ב-hover); (2) לחיצה עליו מסירה את השיבוץ מיד, הספירות בפס השלבים ובלוח מתעדכנות; (3) רענון הדף מאשר שההסרה נשמרה בשרת; (4) אותו "x" מופיע ועובד גם על אווטאר חסום-כשירות (מסומן במנעול); (5) בכרטיס-לוח שאין בו onMove/onToggleAssignment (למשל: "יומן" מהניווט הראשי, ומסך המשתתף/GuardApp) שום "x" לא מופיע — הלוח שם עדיין לקריאה-בלבד לגמרי.</human-check>
  </verify>

  <acceptance_criteria>
- `UnifiedBoard`/`BoardCard` מקבלים `onToggleAssignment` אופציונלי ומעבירים אותו כ-`onRemove` ל-`People`, גדור ב-`canRemove` (`!timeless`)
- `People` מקבלת `onRemove` אופציונלי, מרנדרת "x" תמיד-גלוי (לא hover-only) בשני הענפים (רגיל + חסום-כשירות), עם `stopPropagation`
- `WeekFlow.jsx` מעביר `onToggleAssignment={actions.toggleAssignment}` ל-`UnifiedBoard` הקיימת
- `GuardApp.jsx` ו-`CalendarView.jsx` לא מכילים `onToggleAssignment` בשום מקום — נשארים read-only
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן על חמש הנקודות ב-`human-check`
  </acceptance_criteria>

  <done>
כל אווטאר משובץ בשלב "השבוע" של "בניית שבוע" נושא "x" עובד, תמיד-גלוי, שקורא ל-`toggleAssignment` הקיימת; הלוח נשאר read-only בכל מקום אחר.
  </done>

  <reversibility rating="reversible">
פרופ אופציונלי חדש; כל קריאה קיימת שלא מעבירה אותו ממשיכה לעבוד בדיוק כמו קודם. חזרה = revert לקומיט.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: "+" הוספת שיבוץ — בורר-שומר קצר על הלוח (INLINE-01)</name>

  <files>
src/components/supervisor/UnifiedBoard.jsx
  </files>

  <read_first>
- Task 1 (אותה תוכנית) — `onToggleAssignment`/`canRemove` שכבר נוספו ל-`BoardCard` באותו קובץ; המשימה הזו מרחיבה את אותו רכיב, לא מוסיפה חיווט חדש מבחוץ.
- `src/components/supervisor/views.jsx` שורות 1204-1266 (`AssignView`) — הדפוס הקיים של בורר-שומר עם חסימת-כשירות (`disabled={!qualified}`, `title` שמסביר את הסירוב): רפרנס-תבנית בלבד, **לא** להעתיק אחד-לאחד — `AssignView` גם בודקת `checkAssignment` (חפיפה/מנוחה) שזה מעבר לסקופ (ר' הערה בהמשך).
- `src/components/ui.jsx` — `Modal` (~591-646) ו-`Avatar` (~285-311): שתי הקומפוננטות שהבורר הזה בנוי עליהן.
- `.planning/phases/11-inline-demo-cleanup/11-CONTEXT.md` קטע `<code_context>` "Established Patterns" — `toggleAssignment` בודקת רק `checkQualification`, לא אילוץ קשיח מלא, "נשאר פתוח בכוונה" — הבורר הזה חייב להתאים בדיוק לאותו scope, לא להרחיב אותו.
  </read_first>

  <action>
ייבא `Modal` ו-`Avatar` ל-`UnifiedBoard.jsx` מ-`../ui.jsx` (ליד ה-import הקיים של `Badge, EmptyState`). ב-`BoardCard`: הוסף `const [pickerOpen, setPickerOpen] = useState(false);` (ה-`useState` כבר מיובא). חשב `const canAdd = Boolean(onToggleAssignment) && !timeless && missing > 0;` (`missing` כבר מחושב מעל, שימוש חוזר — לא לוגיקה חדשה).

רנדר כפתור "+" קטן (Icon name="plus") ליד ערימת האווטארים הקיימת, מוצג רק כש-`canAdd`; לחיצה עליו פותחת את הבורר (`setPickerOpen(true)`).

רנדר `Modal` (`open={pickerOpen}`, `onClose` סוגר, כותרת ששמה את `item.label`) עם שורה אחת לכל שומר/ת שעוד לא ב-`item.assignedGuards`: אווטאר + שם, `disabled` וגם `title={qualRefusal(item.category)}` כש-`!isQualified(g, item.category)` (שתיהן כבר מיובאות/מוגדרות בקובץ הזה — שימוש חוזר, לא להגדיר מחדש). לחיצה על שורה פעילה קוראת ל-`onToggleAssignment(item.id, g.id)` ואז סוגרת את הבורר. הצג שורת-מצב-ריק ("אין {noun.memberPlural} בצוות" / "כל מי שיש כבר משובץ כאן") כשאין אף שומר/ת זמין/ה לרשימה — אל תשאיר גוף מודל ריק.

הבורר הזה מכוון בכוונה לגדר-הכשירות-בלבד של `toggleAssignment` עצמה (דרך `isQualified`), **לא** ל-`checkAssignment` המלאה של `AssignView` (חפיפה/מנוחה) — זה פער קיים ומתועד במפורש כמחוץ לסקופ הפאזה הזו (11-CONTEXT.md).
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/UnifiedBoard.jsx','utf8'); if(!/import\s*\{[^}]*Modal[^}]*\}\s*from\s*[\"']\.\.\/ui\.jsx[\"']/.test(s)) throw new Error('Modal must be imported from ../ui.jsx'); if(!/Avatar/.test(s)) throw new Error('Avatar must be imported/used'); if(!/pickerOpen/.test(s)) throw new Error('missing pickerOpen state'); if(!/onToggleAssignment\(item\.id/.test(s)) throw new Error('picker must call onToggleAssignment(item.id, ...)'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>על משבצת/פריט עם מקום פנוי (מונה "חסרים X" מוצג), לחץ "+"; אמת: (1) הבורר מציג רק שומרים שעוד לא משובצים לפריט הזה; (2) שומר שאינו כשיר לקטגוריה מופיע מנוטרל עם tooltip שמסביר למה; (3) לחיצה על שומר כשיר מוסיפה אותו מיד וסוגרת את הבורר; (4) ה-"+" נעלם ברגע שהמשבצת מתמלאת (missing=0).</human-check>
  </verify>

  <acceptance_criteria>
- `Modal`/`Avatar` מיובאים ומשומשים; `pickerOpen` state קיים; הבורר קורא ל-`onToggleAssignment(item.id, guardId)` על בחירת שומר/ת כשיר/ה
- שומרים כבר-משובצים לא מופיעים ברשימה; שומרים לא-כשירים מופיעים מנוטרלים עם הסבר
- הבורר לא מוסיף בדיקת חפיפה/מנוחה מעבר לכשירות — מתאים בדיוק ל-scope של `toggleAssignment`
- `npm test` ו-`npm run build` עוברים
  </acceptance_criteria>

  <done>
מנהל יכול להוסיף שומר/ת לכל משבצת-לוח עם מקום פנוי, ישירות מהלוח, מבורר מודע-כשירות — בלי ניווט מחוץ ל"בניית שבוע".
  </done>
</task>

<task type="auto">
  <name>Task 3: חיווט תיקון ה-FK של מחיקת עמדת army (INLINE-01)</name>

  <files>
src/components/supervisor/RosterWizard.jsx
src/components/supervisor/PositionsScreen.jsx
  </files>

  <read_first>
- Plan 11-01, Task 3 (SUMMARY או `src/hooks/useGuardian.js` ישירות) — חתימת ה-action החדשה `actions.deletePosition(id, weekDates = [])`.
- `src/components/supervisor/RosterWizard.jsx` שורות 279-288 (`removeItem`, קריאת `actions.deletePosition(item.position.id)` הקיימת) ו-243/269/312 (`weekDates[0]` כבר בשימוש באותו scope — `weekDates` כבר פרופ זמין ברכיב).
- `src/components/supervisor/PositionsScreen.jsx` שורות 47-49 (חתימת הרכיב, `weekDates = []` כבר פרופ עם ברירת מחדל) ושורה 138 (`onDelete={() => actions.deletePosition(p.id)}`).
  </read_first>

  <action>
ב-`RosterWizard.jsx`'s `removeItem`, שנה `actions.deletePosition(item.position.id)` ל-`actions.deletePosition(item.position.id, weekDates)`. ב-`PositionsScreen.jsx`, שנה `onDelete={() => actions.deletePosition(p.id)}` ל-`onDelete={() => actions.deletePosition(p.id, weekDates)}`. שני השינויים בלבד — `weekDates` כבר זמין ב-scope בשני הרכיבים, אין צורך בשום פרופ/state חדש.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/RosterWizard.jsx','utf8'); if(!/actions\.deletePosition\(item\.position\.id,\s*weekDates\)/.test(s)) throw new Error('RosterWizard.jsx removeItem must pass weekDates to deletePosition'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/PositionsScreen.jsx','utf8'); if(!/actions\.deletePosition\(p\.id,\s*weekDates\)/.test(s)) throw new Error('PositionsScreen.jsx onDelete must pass weekDates to deletePosition'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>בצוות במצב army: פתח "בניית שבוע", בקר בשלב "השבוע" (הלוח) לפחות פעם אחת עבור השבוע הנוכחי (כדי ש-ensurePositionsForWeek יממש אותו), חזור לשלב "בניית שבוע" (RosterWizard) ומחק משימה/עמדה שהשבוע הנוכחי שלה כבר ממומש — אמת שהיא נעלמת דרך UndoBar הרגיל, בלי שגיאה גולמית/מכוערת. חזור על אותו תרחיש דרך מסך "עמדות קבועות" העצמאי (PositionsScreen, מגיע דרך "עוד").</human-check>
  </verify>

  <acceptance_criteria>
- `RosterWizard.jsx`'s `removeItem` ו-`PositionsScreen.jsx`'s `onDelete` מעבירים שניהם `weekDates` ל-`actions.deletePosition`
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן משתי נקודות-המחיקה (RosterWizard + PositionsScreen) מראה מחיקה תקינה בלי שגיאת מפתח-זר
  </acceptance_criteria>

  <done>
מחיקת עמדת army עם שבוע נוכחי ממומש עובדת מכל אחד משני מסכי-המחיקה, בזכות תיקון ה-FK שנבנה ב-Plan 11-01.
  </done>

  <reversibility rating="reversible">
תוספת ארגומנט לקריאה קיימת בשני call sites. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| מנהל מחובר → "x"/"+" על הלוח → `actions.toggleAssignment` | UI חדש שקורא לפעולה קיימת ובדוקה — אין DB write חדש |
| מנהל מחובר → מחיקת עמדת army עם שבוע ממומש | פעולה הרסנית קיימת (RosterWizard/PositionsScreen), עכשיו עם תיקון-FK מ-11-01 מאחוריה |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02-A | Elevation of Privilege | שער-הכשירות בבורר "+" | low | mitigate | `disabled`+tooltip ב-UI, וגם `toggleAssignment` עצמה עדיין מריצה `checkQualification` לפני כל כתיבה — עקיפת ה-UI לא יכולה להצליח |
| T-11-02-B | Tampering | טווח הכתיבה של "x"/"+" | low | mitigate | שתיהן עוברות דרך `toggleAssignment` הקיימת, שכבר מוגבלת ע"י `gs_work_item_assignments_write` (team+supervisor) — אין משטח RLS חדש |
| T-11-02-C | Denial of Service | `deletePosition(id, weekDates)` עם `weekDates` שגוי | low | mitigate | שני ה-call sites מעבירים בדיוק את אותו `weekDates` שכבר משמש כל קריאה אחרת באותו מסך — לא ערך חדש שהומצא כאן |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים אחרי כל אחת משלוש המשימות
2. "x" עובד על כל אווטאר משובץ בלוח (כולל חסום-כשירות), "+" עובד על כל משבצת עם מקום פנוי — שתיהן דרך `toggleAssignment`
3. הלוח נשאר read-only ביומן ואצל המשתתף
4. מחיקת עמדת army עם שבוע ממומש עובדת משני מסכי-המחיקה
5. אימות חי בדפדפן על כל אחת משלוש המשימות
</verification>

<success_criteria>
- INLINE-01 מתקיים במלואו: הוספה/מחיקה של שיבוץ ישירות בתוך "בניית שבוע", בלי ניווט למסך אחר (קריטריון הצלחה 1, ROADMAP Phase 11)
- מחיקת עמדת army עם שבוע ממומש כבר לא נכשלת בשגיאת מפתח-זר גולמית
</success_criteria>

<output>
Create `.planning/phases/11-inline-demo-cleanup/11-02-SUMMARY.md` when done.
</output>