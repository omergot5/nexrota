---
phase: 11-inline-demo-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0022_work_items_is_demo.sql
  - src/lib/api.js
  - src/lib/demoData.js
  - src/lib/sequenceGuard.js
  - src/hooks/useGuardian.js
  - scripts/verify-planning.mjs
autonomous: true
requirements: [INLINE-02, INLINE-04, INLINE-01, INLINE-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "gs_work_items.is_demo boolean not null default false exists (migration 0022); shiftFromRow/shiftToRow map it as isDemo in both read and write directions, verified by round-trip assertions in npm test."
    - "seedDemoTeam ו-seedArmyRoster (demoData.js) כותבים is_demo:true על כל שורת gs_work_items שהן יוצרות; כל נתיב כתיבה אחר (createShifts/updateShift/materializeTemplateShifts, שמשרתים מנהל שעובד ידנית) ממשיכים לכתוב is_demo:false כברירת מחדל, בלי שינוי התנהגות לנתונים אמיתיים."
    - "refresh() ב-useGuardian.js מוגן ע\"י sequenceGuard חדש (src/lib/sequenceGuard.js) — רק קריאת ה-refresh האחרונה שהונפקה יכולה לכתוב setData, גם אם היא לא הראשונה שנפתרת (resolves)."
    - "actions.deletePosition(id, weekDates) מבטל מימוש (gs_work_items שנוצרו מהעמדה) של השבוע שהועבר אליה בלבד, לפני מחיקת gs_positions — לא נוגע בשבועות אחרים של אותה עמדה."
    - "actions.deleteDemoDataForWeek(weekDates) קיימת ומוחקת, דרך api.deleteShifts הקיימת, רק שורות is_demo=true בטווח השבוע שהועבר אליה — no-op בלי כתיבה כשאין מה למחוק."
  artifacts:
    - supabase/migrations/0022_work_items_is_demo.sql
    - src/lib/sequenceGuard.js
  key_links:
    - "demoData.js (seedDemoTeam/seedArmyRoster) → shiftRowToWorkItem({..., is_demo:true, ...}) → gs_work_items.is_demo"
    - "useGuardian.js refresh() → sequenceGuard.next()/isCurrent() → setData רק לטוקן העדכני ביותר"
    - "useGuardian.js deletePosition → api.unmaterializePositionWeek(id, weekDates) → api.deletePosition(id)"
    - "useGuardian.js deleteDemoDataForWeek → demoData.js demoShiftIdsForWeek(shifts, weekDates) → api.deleteShifts(ids)"
---

<objective>
בונה את כל שכבת הנתונים והמצב (DB + api.js + demoData.js + useGuardian.js) שהפאזה נשענת עליה — בלי לגעת בשום קובץ UI. שלוש יחידות עבודה בלתי-תלויות זו בזו מבחינה לוגית, אבל כולן חייבות לחיות באותה תוכנית כי הן נוגעות באותם שני קבצי-חנק (`api.js`, `useGuardian.js`) שהפרויקט מזהיר עליהם במפורש: דגל `is_demo` על `gs_work_items` (INLINE-02); תיקון מרוץ ה-`refresh()` שגורם ל"מצב השבוע" הישן לדרוס את החדש (INLINE-04); ותשתית ה-backend לשתי הפעולות ההרסניות החדשות של הפאזה — מחיקת עמדת army בלי להיתקל במפתח-זר גולמי (INLINE-01), ו"מחק נתוני הדגמה לשבוע זה" (INLINE-03). פיצול זה לשלוש תוכניות מקבילות היה מייצר שלוש עריכות-מקבילות לאותם קבצים בדיוק — בדיוק הסיכון ש"אותו קובץ בשני גלים" נועד למנוע.

ה-UI שצורך את כל זה (הלוח, כפתור הניקוי) הוא Plan 11-02/11-03 — תוכנית זו לא נוגעת בשום קובץ `.jsx`.

Purpose: לתת ל-11-02/11-03 בסיס יציב ובדוק (round-trip tests, action signatures) כדי שהעבודה ה-UI-ית שלהן תהיה חיווט בלבד, לא איתור-באגים.
Output: מיגרציה חדשה, api.js/demoData.js/useGuardian.js מעודכנים, `src/lib/sequenceGuard.js` חדש, ו-`scripts/verify-planning.mjs` עם בדיקות round-trip לכל התוספות.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-inline-demo-cleanup/11-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/lib/api.js
@src/lib/demoData.js
@src/hooks/useGuardian.js
@supabase/migrations/0021_fairness_window_setting.sql
@supabase/migrations/0017_work_items.sql
</context>

<tasks>

<task type="tracer">
  <name>Task 1: is_demo end-to-end — מיגרציה → api.js → demoData.js (INLINE-02)</name>

  <files>
supabase/migrations/0022_work_items_is_demo.sql
src/lib/api.js
src/lib/demoData.js
scripts/verify-planning.mjs
  </files>

  <read_first>
- `.planning/phases/11-inline-demo-cleanup/11-CONTEXT.md` קטע `<decisions>` — "is_demo placement": הדגל יושב רק על `gs_work_items`, נגזר (לא דגל נפרד על `gs_work_item_assignments`), אין RLS חדש.
- `supabase/migrations/0021_fairness_window_setting.sql` — תבנית מיגרציה מדויקת לחיקוי (`alter table ... add column if not exists ... default ...`, הערת כותרת עברית).
- `supabase/migrations/0017_work_items.sql` שורות 6-33 — עמודות `gs_work_items` והמדיניות `gs_work_items_write` (`for all ... using (team_code = gs_my_team() and gs_is_supervisor())`) — למה אין צורך במדיניות RLS נוספת.
- `src/lib/api.js` שורות 37-64 (`shiftFromRow`), 66-88 (`shiftToRow`), 207-210 (`SHIFT_SELECT`), 222-229 (`shiftRowToWorkItem`) — ארבע נקודות המיפוי המדויקות.
- `src/lib/demoData.js` שורות 157-184 (`seedDemoTeam`, בניית `shiftPayload`), 379-397 (`positionPlanToShiftRow`, המשמשת רק את `seedArmyRoster`) — שתי נקודות ה-insert היחידות שצריכות `is_demo:true`.
- `scripts/verify-planning.mjs` שורות 886-930 — הדפוס המדויק של בדיקות round-trip ל-`shiftFromRow`/`shiftToRow` (QUAL-03, קטגוריה) שיש לחקות עבור `is_demo`.
  </read_first>

  <action>
צור `supabase/migrations/0022_work_items_is_demo.sql`: הערת כותרת עברית (בסגנון 0021 — למה is_demo יושב רק על `gs_work_items`, ש"שיבוץ הדגמה" נגזר מהדגל הזה בלבד ולא דגל נפרד על `gs_work_item_assignments`/`gs_availability`/`gs_swap_requests` כי ה-`on delete cascade` הקיים (0017/0019) כבר מנקה אותן, וש-`gs_work_items_write` הקיימת כבר מכסה כתיבה בלי מדיניות RLS נוספת), ואז שורה אחת: `alter table gs_work_items add column if not exists is_demo boolean not null default false;`.

ב-`src/lib/api.js`: הוסף `is_demo` ל-`SHIFT_SELECT` (אחרי `position_id`, לפני `created_at`). ב-`shiftFromRow`, הוסף `isDemo: Boolean(row.is_demo),` (אחרי `positionId`). ב-`shiftToRow`, הוסף `is_demo: Boolean(shift.isDemo),` (אחרי `position_id`) — `Boolean(undefined)` הוא `false`, בדיוק כמו ש-`published: Boolean(shift.published)` כבר נוהג באותו קובץ, כך שכל שורה שלא צוין לה `isDemo` (כל כתיבה של מנהל, לא רק דמו) ממשיכה לצאת `is_demo:false` בלי קוד נוסף. אל תיגע ב-`shiftRowToWorkItem` — ה-`...rest` spread שלה כבר מעביר `is_demo` בלי שינוי ברגע ש-`shiftToRow` פולט אותו; הוסף רק הערה קצרה שמסבירה את זה, לא קוד.

ב-`src/lib/demoData.js`: בתוך `seedDemoTeam`, בלולאה שבונה את `shiftPayload` (האובייקט שנדחף ל-`shiftPayload.push({...})`), הוסף `is_demo: true,`. ב-`positionPlanToShiftRow` (הפונקציה היחידה שמזינה את שורות ה-`gs_work_items` שנוצרות מ-`seedArmyRoster`), הוסף `is_demo: true,` לאובייקט המוחזר. אל תיגע בשום נקודת כתיבה אחרת בקובץ הזה או ב-`api.js` (`createShifts`/`updateShift`/`materializeTemplateShifts`) — הן ממשיכות לקבל `is_demo:false` אוטומטית דרך ברירת המחדל של `shiftToRow`.

ב-`scripts/verify-planning.mjs`: הוסף `shiftRowToWorkItem` לרשימת ה-imports הקיימת מ-`../src/lib/api.js` (שורה 31). מיד אחרי בלוק ה-round-trip הקיים של `shiftToRow`/`shiftWithCategory` (שורה ~929), הוסף בלוק בדיקות `is_demo`: (1) `shiftFromRow` על שורה עם `is_demo:true` מחזיר `isDemo:true`; (2) `shiftFromRow` על שורה עם `is_demo:false` מחזיר `isDemo:false`; (3) `shiftFromRow` על שורה בלי `is_demo` בכלל (מיגרציה ישנה שעדיין לא רצה) מחזיר `isDemo:false` ולא נופל; (4) round trip — `shiftToRow` על אובייקט שמופה מ-`shiftFromRow` עם `is_demo:true` משחזר `is_demo:true`; (5) `shiftToRow` על משמרת בלי `isDemo` בכלל (בדיוק מה שכל טופס-עריכה של מנהל שולח) מפיק `is_demo:false`; (6) `shiftRowToWorkItem` על התוצאה של (4) עדיין נושא `is_demo:true` — מוכיח את מעבר ה-rest-spread בלי שינוי מפורש.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0022_work_items_is_demo.sql','utf8'); if(!/alter\s+table\s+gs_work_items/i.test(s)) throw new Error('missing alter table gs_work_items'); if(!/add\s+column\s+if\s+not\s+exists\s+is_demo\s+boolean\s+not\s+null\s+default\s+false/i.test(s)) throw new Error('missing is_demo column definition with default false'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/api.js','utf8'); if(!/SHIFT_SELECT[\s\S]{0,400}is_demo/.test(s)) throw new Error('is_demo missing from SHIFT_SELECT'); if(!/isDemo:\s*Boolean\(row\.is_demo\)/.test(s)) throw new Error('shiftFromRow missing isDemo mapping'); if(!/is_demo:\s*Boolean\(shift\.isDemo\)/.test(s)) throw new Error('shiftToRow missing is_demo mapping'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/demoData.js','utf8'); const c=(s.match(/is_demo:\s*true/g)||[]).length; if(c!==2) throw new Error('expected exactly 2 is_demo:true insert points (seedDemoTeam shiftPayload + positionPlanToShiftRow), found '+c); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
  </verify>

  <acceptance_criteria>
- `gs_work_items.is_demo boolean not null default false` נוסף דרך מיגרציה 0022, בלי מדיניות RLS חדשה
- `SHIFT_SELECT`/`shiftFromRow`/`shiftToRow` מכירים `is_demo`/`isDemo` בשני הכיוונים; `shiftRowToWorkItem` לא נערך (rest spread כבר מעביר את השדה)
- `seedDemoTeam` ו-`positionPlanToShiftRow` (המשמשת את `seedArmyRoster`) הם שני מקומות ה-insert היחידים עם `is_demo: true` בכל `demoData.js`
- `npm test` ירוק עם כל בדיקות ה-round-trip החדשות (`is_demo` קיים/חסר/round-trip/מעבר דרך `shiftRowToWorkItem`)
- `npm run build` עובר
  </acceptance_criteria>

  <done>
`gs_work_items.is_demo` קיים בסכמה, מופה נכון בשני הכיוונים דרך `api.js`, וכל נתוני ההדגמה (civil + army) מסומנים בו — בלי לגעת בשום נתיב כתיבה אחר.
  </done>

  <reversibility rating="reversible">
תוספת עמודה אדיטיבית (default false, בלי NOT NULL על נתונים קיימים לשבור) + מיפוי JS. חזרה = revert לקומיט; drop column אם נדרש.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: refresh() — שער-רצף נגד פתרון-לא-לפי-סדר (INLINE-04)</name>

  <files>
src/lib/sequenceGuard.js
src/hooks/useGuardian.js
scripts/verify-planning.mjs
  </files>

  <read_first>
- `.planning/phases/11-inline-demo-cleanup/11-CONTEXT.md` קטע `<findings>` סעיף 4 במלואו — החשוד העיקרי (`refresh()` בלי sequencing, 12+ נקודות קריאה + subscription realtime שקוראת שוב לאותה כתיבה) ותרחיש השחזור המוצע.
- `.planning/ROADMAP.md` Phase 11, קריטריון הצלחה 4 — הניסוח המדויק ("מיד אחרי כל אחת משלוש הפעולות", "תרחיש השחזור... רץ מחדש ולא משחזר").
- `src/hooks/useGuardian.js` שורות 103-121 (`dataRef`/`mounted` — הדפוס הקיים של refs שלא תלויים ב-render), 183-195 (`refresh()` המדויקת שצריך לתקן), 216-234 (ה-subscription שקוראת `refresh()` על כל אירוע postgres_changes, בלי סינון), 292-338 (`pendingRef`/`flushRef` — עוד דפוס `useRef` קיים לחיקוי מבחינת סגנון).
  </read_first>

  <action>
צור `src/lib/sequenceGuard.js`: מודול טהור, בלי תלות ב-React/DOM, שמייצא `createSequenceGuard()` — factory שמחזירה `{ next(), isCurrent(token) }`. `next()` מגדילה מונה פנימי-לסגירה (closure) ומחזירה את הערך החדש (טוקן). `isCurrent(token)` מחזירה `true` רק אם `token` שווה למונה הנוכחי — כלומר רק הטוקן שהונפק *אחרון* יכול אי-פעם להיחשב "עדכני", בלי קשר לסדר שבו קריאות אסינכרוניות שהחזיקו טוקנים ישנים יותר נפתרות (resolve). כלול הערת כותרת שמסבירה את הבעיה שזה פותר (מרוץ `refresh()`, 11-CONTEXT.md סעיף 4) ושזו לא תלוית-React בכוונה.

ב-`src/hooks/useGuardian.js`: ייבא `createSequenceGuard` מ-`../lib/sequenceGuard.js`. הוסף `const refreshSeqRef = useRef(createSequenceGuard());` ליד שאר ה-refs הקיימים (`dataRef`, `mounted`, שורות 103-121). בתוך `refresh()` (שורות 183-195): מיד אחרי ה-guard-clause של `teamCode` ולפני `await api.loadTeam(teamCode)`, הוסף `const token = refreshSeqRef.current.next();`. בשני הענפים — הצלחה (`try`, לפני `setData(team)`/`setOffline(false)`) וכישלון (`catch`, לפני `setError(e.message)`) — הוסף שער `if (!refreshSeqRef.current.isCurrent(token)) return;` *לפני* קריאת ה-`set*` הראשונה בענף הזה. כך קריאת `refresh()` שכבר הוחלפה ע"י קריאה מאוחרת יותר (גם אם היא נפתרת אחריה בזמן) לעולם לא צובעת מצב — בלי קשר לסדר ה-resolve.

אל תיגע בשום אחת משתים-עשרה נקודות הקריאה הקיימות ל-`await refresh()` בתוך `actions` (למשל `addShifts`, `updateShift`, `applyPlan` וכו') ואל תיגע ב-subscription ה-realtime (`onChange = () => refresh()`, שורות 220-234) — התיקון הוא נקודת-חנק אחת בתוך `refresh()` עצמה, ולכן מכסה אוטומטית את כל שלושת המשטחים (פס השלבים, הלוח, ScheduleMgmt) בלי לגעת בהם, בדיוק כפי ש-11-CONTEXT.md דורש ("לא פיצ'ר per-surface").

ב-`scripts/verify-planning.mjs`: ייבא `createSequenceGuard` מ-`../src/lib/sequenceGuard.js`. הוסף בדיקות: `next()` מחזירה ערכים עולים; `isCurrent()` על הטוקן העדכני ביותר מחזירה `true`; `isCurrent()` על טוקן שהוחלף ע"י טוקן חדש יותר מחזירה `false` — גם אם הוא נבדק לפני שהטוקן החדש "נפתר" בפועל (מדמה resolve-לא-לפי-סדר).
  </action>

  <verify>
    <automated>npm test</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/hooks/useGuardian.js','utf8'); if(!s.includes('createSequenceGuard')) throw new Error('useGuardian.js must import/use createSequenceGuard'); const start=s.indexOf('const refresh = useCallback'); const end=s.indexOf('const refresh', start+1)===-1 ? s.indexOf('// ---------- live updates', start) : -1; const body = end===-1 ? s.slice(start, start+1500) : s.slice(start,end); if(!body.includes('refreshSeqRef')) throw new Error('refresh() must read refreshSeqRef'); if(!body.includes('.next()')) throw new Error('refresh() must call next() before awaiting loadTeam'); if(!body.includes('isCurrent(')) throw new Error('refresh() must gate setData/setError on isCurrent(token)'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/hooks/useGuardian.js','utf8'); const count=(s.match(/refresh\(\)/g)||[]).length; if(count<10) throw new Error('expected the existing ~12 refresh() call sites to remain untouched, found only '+count); console.log('ok')"</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, היכנס כמנהל, פתח "בניית שבוע" על צוות עם לפחות משמרת אחת ואדם אחד. עבור לשלב "שיבוץ" (assign), ולחץ במהירות פעמיים ברצף על אותו אדם מול אותה משמרת (שיבוץ ואז הסרה, או שיבוץ אדם ואז אדם אחר) — לפני שכל "שומר…" הראשון נעלם. אמת: (1) הספירות בפס השלבים ("שיבוץ חכם"/"שיבוץ ידני") מתייצבות על המספר הנכון בהתאם למה שקרה בפועל, לא על מספר ישן; (2) עבור לשלב "השבוע" (הלוח) ואמת שהאווטארים שם תואמים בדיוק את מה שבפועל משובץ; (3) עבור לשלב "שליחה לצוות" (ScheduleMgmt) ואמת שספירת הפרסום תואמת. חזור על התרחיש דרך מחיקה-ואז-הוספה מהירה של משמרת ב"בניית שבוע" (שלב shifts, לא-army) אם ניתן לבצע זאת מספיק מהר. פעולה בודדת (לא מהירה) עדיין מתעדכנת מיידית כמו קודם — אין רגרסיה.</human-check>
  </verify>

  <acceptance_criteria>
- `src/lib/sequenceGuard.js` קיים, טהור, בלי תלות ב-React
- `refresh()` ב-`useGuardian.js` מנפיקה טוקן לפני הקריאה ל-`api.loadTeam`, ובודקת `isCurrent(token)` לפני `setData`/`setOffline` וגם לפני `setError`
- שתים-עשרה נקודות הקריאה הקיימות ל-`refresh()` וה-subscription לא נערכו
- `npm test` ירוק עם בדיקות `createSequenceGuard` החדשות
- אימות חי בדפדפן מראה ספירות נכונות אחרי שתי פעולות מהירות ברצף, בכל אחד משלושת המשטחים
  </acceptance_criteria>

  <done>
`refresh()` מוגנת ע"י שער-רצף — רק הקריאה העדכנית ביותר שהונפקה יכולה לצבוע state, ותרחיש השחזור של 11-CONTEXT.md סעיף 4 כבר לא משחזר.
  </done>

  <reversibility rating="reversible">
תיקון ממוקד לקובץ יחיד (בתוך `refresh()` בלבד) + מודול טהור חדש. חזרה = revert לקומיט.
  </reversibility>
</task>
<task type="auto">
  <name>Task 3: army FK safety net + פעולת "מחק נתוני הדגמה לשבוע זה" (INLINE-01, INLINE-03 — חלק ה-backend)</name>

  <files>
src/lib/api.js
src/lib/demoData.js
src/hooks/useGuardian.js
scripts/verify-planning.mjs
  </files>

  <read_first>
- `.planning/phases/11-inline-demo-cleanup/11-CONTEXT.md` קטע `<decisions>` — "Army FK scope" (בסקופ, לבטל מימוש *השבוע המוצג בלבד* לפני `gs_positions` DELETE) ו-"is_demo placement" (הפסקה שמגדירה בדיוק מה "מחק נתוני הדגמה" מוחק/לא מוחק: לא `gs_profiles`, לא `gs_positions`, רק שורות `gs_work_items` ממומשות של השבוע).
- `.planning/phases/11-inline-demo-cleanup/11-CONTEXT.md` קטע `<findings>` סעיף 4 "חשוד שלישי" — ההסבר המדויק ל-FK בלי cascade.
- `supabase/migrations/0017_work_items.sql` שורה 25 (`position_id uuid references gs_positions(id)` — בלי `on delete cascade`, בניגוד ל-`gs_work_item_assignments`/`gs_availability`/`gs_swap_requests` שכן נושאות cascade).
- `src/lib/api.js` שורות 1040-1046 (`deletePosition`, המדויקת שצריך שילוב-לפניה), 676-680 (`clearAssignments` — הדפוס המדויק של DELETE בלי `.select()`/ספירת-שורות, כי 0 שורות הוא מצב לגיטימי; זה הדפוס לחקות עבור `unmaterializePositionWeek`, **לא** דפוס `deleteShift`/`deleteShifts` עם ה-`.select()`+בדיקת-ספירה), 587-595 (`deleteShifts` הקיימת — תיעשה בה שימוש חוזר, בלי שינוי, עבור מחיקת נתוני-הדגמה).
- `src/hooks/useGuardian.js` שורות 868-873 (`deletePosition` action הקיים), 322-332 (`deferred` — המנגנון שצריך להשתמש בו), 533-538 (`deleteShifts` action — דפוס `deferred` עם `label`+`ids`, הכי קרוב למה ש-`deleteDemoDataForWeek` צריכה), 882-896 (`ensurePositionsForWeek` — דפוס "אם אין מה לעשות, החזר מיד בלי כתיבה ובלי refresh", השער-מפני-DoS שיש לחקות עבור `deleteDemoDataForWeek` כשאין נתוני-הדגמה בשבוע).
- `src/lib/demoData.js` שורות 399-403 (`mergeById` — helper טהור קיים בתחתית הקובץ, מקום טבעי להוסיף לידו helper טהור נוסף).
  </read_first>

  <action>
ב-`src/lib/api.js`, מיד אחרי `deletePosition` (שורה ~1046), הוסף `export async function unmaterializePositionWeek(positionId, dates)`: אם `!dates?.length` — `return` מיידי (אין מה לבטל). אחרת, `DELETE` על `gs_work_items` עם `.eq("position_id", positionId).in("start_date", dates)`, זריקת שגיאה אם `error` קיים — **בלי** `.select()`/בדיקת-ספירת-שורות (מחקה את `clearAssignments`, לא את `deleteShift`): 0 שורות שנמחקו הוא תקין (העמדה לא ממומשת עדיין לשבוע הזה), לא כשל. אל תיגע ב-`deletePosition` עצמה.

ב-`src/hooks/useGuardian.js`: שנה את חתימת ה-action `deletePosition` (שורות 868-873) מ-`(id)` ל-`(id, weekDates = [])`. בתוך ה-`work` (הפונקציה השלישית שמועברת ל-`deferred`), לפני `api.deletePosition(id)`, הוסף `if (weekDates.length) await api.unmaterializePositionWeek(id, weekDates);`. ה-patch האופטימי (הסרה מ-`d.positions`) נשאר בדיוק כפי שהוא.

באותו קובץ: ייבא `demoShiftIdsForWeek` לצד `seedDemoTeam, seedArmyRoster` מ-`../lib/demoData.js` (שורת ה-import הקיימת). הוסף action חדש `deleteDemoDataForWeek: (weekDates) => { ... }` בתוך אובייקט `actions` (מקום טבעי: ליד `deleteShifts`/`clearAssignments`): מחשב `const ids = demoShiftIdsForWeek(dataRef.current.shifts, weekDates);`; אם `!ids.length`, `return` מיידי (בלי כתיבה, בדיוק כמו `ensurePositionsForWeek`); אחרת `return deferred("נתוני ההדגמה נמחקו", (d) => ({ ...d, shifts: d.shifts.filter((s) => !ids.includes(s.id)) }), () => api.deleteShifts(ids));` — שימוש חוזר מלא ב-`api.deleteShifts` הקיימת (כבר נושאת `.select()`+בדיקת-ספירה+`.eq("kind","shift")`), בלי endpoint חדש.

ב-`src/lib/demoData.js`: הוסף בתחתית הקובץ, ליד `mergeById`, `export function demoShiftIdsForWeek(shifts, weekDates)` — פונקציה טהורה: `const weekSet = new Set(weekDates); return shifts.filter((s) => s.isDemo && weekSet.has(s.date)).map((s) => s.id);`. הוסף הערת-כותרת קצרה שמקשרת ל-11-CONTEXT.md open_questions מס' 2: "שיבוץ הדגמה" נגזר מ-`is_demo` על שורת ה-`gs_work_items` עצמה, לא דגל נפרד — ה-cascade הקיים כבר מנקה שיבוצים/זמינות/בקשות-החלפה בחינם ברגע שהשורה הזו נמחקת.

ב-`scripts/verify-planning.mjs`: ייבא `demoShiftIdsForWeek` מ-`../src/lib/demoData.js`. הוסף בדיקות על קבוצת נתוני-דמה (שיבוצי-הדגמה בשבוע המבוקש, שיבוץ אמיתי באותו שבוע, שיבוץ-הדגמה בשבוע *אחר*): הפונקציה מחזירה רק את מזהי שיבוצי-ההדגמה של השבוע המבוקש בדיוק, לא כוללת שיבוץ אמיתי מאותו שבוע, ולא כוללת שיבוץ-הדגמה משבוע אחר.
  </action>

  <verify>
    <automated>npm test</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/api.js','utf8'); if(!/export async function unmaterializePositionWeek/.test(s)) throw new Error('missing unmaterializePositionWeek export'); const i=s.indexOf('unmaterializePositionWeek'); const body=s.slice(i,i+500); if(body.includes('.select(')) throw new Error('unmaterializePositionWeek must not use .select() — 0 rows deleted is a legitimate case, mirror clearAssignments not deleteShift'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/hooks/useGuardian.js','utf8'); if(!/deletePosition:\s*\(id,\s*weekDates\s*=\s*\[\]\)/.test(s)) throw new Error('deletePosition action must accept (id, weekDates = [])'); if(!s.includes('unmaterializePositionWeek')) throw new Error('deletePosition action must call api.unmaterializePositionWeek'); if(!s.includes('deleteDemoDataForWeek')) throw new Error('missing deleteDemoDataForWeek action'); if(!s.includes('demoShiftIdsForWeek')) throw new Error('deleteDemoDataForWeek must use demoShiftIdsForWeek'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/demoData.js','utf8'); if(!/export function demoShiftIdsForWeek/.test(s)) throw new Error('missing demoShiftIdsForWeek export'); console.log('ok')"</automated>
    <automated>npm run build</automated>
    <human-check>לא נדרש human-check עצמאי למשימה זו — שני נתיבי ה-backend שנוספו כאן (מחיקת עמדת army עם שבוע ממומש, ו"מחק נתוני הדגמה") נבדקים חי בדפדפן ב-Plan 11-02 (Task 3) וב-Plan 11-03 בהתאמה, אחרי שה-UI שקורא להם קיים.</human-check>
  </verify>

  <acceptance_criteria>
- `api.unmaterializePositionWeek(positionId, dates)` קיימת, בלי `.select()`/בדיקת-ספירה (0 שורות = תקין)
- `actions.deletePosition(id, weekDates)` מבטלת מימוש השבוע שהועבר אליה *בלבד* לפני מחיקת העמדה
- `actions.deleteDemoDataForWeek(weekDates)` קיימת, no-op בלי כתיבה כשאין נתוני-הדגמה בשבוע, ומשתמשת ב-`api.deleteShifts` הקיימת בלי endpoint חדש
- `demoData.js`'s `demoShiftIdsForWeek` טהורה ומכוסה ב-`npm test` על נתוני-דמה מעורבים (הדגמה/אמיתי, שבוע-זה/שבוע-אחר)
- `npm test` ו-`npm run build` עוברים
  </acceptance_criteria>

  <done>
שתי הפעולות ההרסניות החדשות של הפאזה (מחיקת עמדת army בטוחה-FK, ומחיקת נתוני-הדגמה השבועית) קיימות ב-`useGuardian.js`, מכוסות ב-`npm test`, ומוכנות לחיווט UI.
  </done>

  <reversibility rating="reversible">
תוספת פונקציה חדשה + שינוי חתימה תואם-לאחור (weekDates ברירת מחדל `[]`, שקול להתנהגות הקודמת) + action חדשה. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| מנהל מחובר → כתיבת `is_demo` על `gs_work_items` | דגל חדש על טבלה קיימת, דרך RLS קיימת (`gs_work_items_write`) — לא גבול אמון חדש |
| `refresh()` (fetch לא-מסונכרן) → `setData` (state מקומי) | תוצאת fetch יכולה להגיע מאוחר יותר ממה שהיא "אמורה" — נקודה שכבר הוכחה כפגיעה (11-CONTEXT.md סעיף 4) |
| `deletePosition`/`deleteDemoDataForWeek` → DELETE ישיר על `gs_work_items` | מחיקה יזומה ע"י מנהל, בלי דיאלוג אישור (UndoBar בלבד, עקרון ברזל 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01-A | Tampering | `is_demo` כתיבה (`shiftToRow`) | medium | mitigate | `Boolean()` coercion + `is_demo:true` מפורש רק בשתי נקודות ה-seed; בדיקות round-trip מוודאות ברירת-מחדל `false` כך שכתיבה רגילה של מנהל לעולם לא יכולה להפוך שיבוץ אמיתי לשיבוץ-הדגמה בטעות |
| T-11-01-B | Repudiation | מרוץ `refresh()` (state ישן דורס state חדש) | medium | mitigate | `createSequenceGuard` — רק הקריאה העדכנית ביותר שהונפקה יכולה לצבוע state; מכוסה ב-unit test + human-check שמשחזר את התרחיש מ-11-CONTEXT.md |
| T-11-01-C | Denial of Service | `deletePosition` נכשלת בהפרת מפתח-זר גולמית | low | mitigate | `unmaterializePositionWeek` רצה קודם, מוגבלת לשבוע המוצג בלבד — לא שינוי cascade כללי |
| T-11-01-D | Elevation of Privilege / אובדן נתונים | `deleteDemoDataForWeek` מוחקת בטעות נתונים אמיתיים | high | mitigate | קבוצת ה-id מגיעה אך ורק מ-`demoShiftIdsForWeek` (מסנן `isDemo===true`, מכוסה ב-unit test על נתונים מעורבים); שימוש חוזר ב-`api.deleteShifts` (RLS + ספירת-שורות קיימות); `deferred()` נותן חלון UndoBar של 8 שניות |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים אחרי כל אחת משלוש המשימות
2. `is_demo` ממופה נכון בשני הכיוונים ומסומן `true` בדיוק בשתי נקודות ה-seed
3. `refresh()` מוגנת ע"י `createSequenceGuard`; שתים-עשרה נקודות הקריאה הקיימות לא נערכו
4. `deletePosition`/`deleteDemoDataForWeek` קיימות ומכוסות ב-`npm test`
5. אימות חי בדפדפן על תרחיש המרוץ (Task 2)
</verification>

<success_criteria>
- INLINE-02 מתקיים במלואו ברמת ה-data layer
- INLINE-04 מתוקן בנקודת-חנק אחת (`refresh()`), מכסה את שלושת המשטחים בלי לגעת בהם — מאומת חי
- הבסיס ל-INLINE-01 (FK safety net) ול-INLINE-03 (פעולת מחיקה) קיים, בדוק, ומוכן לחיווט UI ב-Plan 11-02/11-03
</success_criteria>

<output>
Create `.planning/phases/11-inline-demo-cleanup/11-01-SUMMARY.md` when done.
</output>