---
phase: 11-inline-demo-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - src/components/supervisor/WeekFlow.jsx
autonomous: true
requirements: [INLINE-03, INLINE-01, INLINE-02, INLINE-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "כפתור 'מחק נתוני הדגמה לשבוע זה' מופיע בשלב 'השבוע' של WeekFlow רק כשיש נתוני-הדגמה בשבוע המוצג, ומוחק אותם בלחיצה אחת עם UndoBar של 8 שניות — בלי confirm()."
    - "אחרי מחיקת נתוני ההדגמה, שיבוץ אמיתי באותו שבוע ושיבוץ הדגמה בשבוע אחר שורדים — מאומת על שבוע מעורב."
    - "תרחיש השחזור של באג 'מצב השבוע' (מחיקה/הוספה מהירה דרך ה-affordances החדשים של הלוח, Plan 11-02) לא משחזר עוד — פס השלבים, הלוח, ו-ScheduleMgmt מציגים את אותו מספר נכון מיד, גם דרך שלוש הפעולות (הוספה, מחיקה, מחיקת-הדגמה)."
  artifacts: []
  key_links:
    - "WeekFlow.jsx demo-cleanup button → demoData.js demoShiftIdsForWeek(shifts, weekDates) (שער-נראות) → actions.deleteDemoDataForWeek(weekDates)"
---

<objective>
תוכנית הסגירה של הפאזה: מוסיפה את כפתור "מחק נתוני הדגמה לשבוע זה" ל-`WeekFlow.jsx` (INLINE-03, מעל הפעולה ש-Plan 11-01 בנתה), ואז מריצה סבב אימות-אינטגרציה מלא על כל ארבע הדרישות ביחד — כולל שחזור-מחדש של תרחיש הבאג של INLINE-04 (11-CONTEXT.md סעיף 4) דרך ה-affordances **החדשים** של הלוח (Plan 11-02), שהם בדיוק מה שה-CONTEXT מזהיר שיהפוך את המרוץ לשכיח יותר. תלויה גם ב-11-01 (הפעולה) וגם ב-11-02 (חלה על אותו `WeekFlow.jsx` שכבר נערך שם — אותו קובץ, לכן גל נפרד).

Purpose: INLINE-03 + סגירת-לולאה על INLINE-04 (ROADMAP קריטריון הצלחה 3+4) — "תרחיש השחזור של הבאג רץ מחדש ולא משחזר" צריך להיבדק דווקא דרך המקרה הכי-סביר-לשבור עכשיו, לא רק דרך המסלול הישן (AssignView) שכבר נבדק ב-Plan 11-01.

Output: כפתור עובד ב-`WeekFlow.jsx`, ותיעוד-אימות (SUMMARY) שחוצה את כל ארבע הדרישות.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-inline-demo-cleanup/11-CONTEXT.md
@.planning/phases/11-inline-demo-cleanup/11-01-SUMMARY.md
@.planning/phases/11-inline-demo-cleanup/11-02-SUMMARY.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/supervisor/WeekFlow.jsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: כפתור "מחק נתוני הדגמה לשבוע זה" + סבב אימות-אינטגרציה מלא (INLINE-03, וסגירת INLINE-01/02/04)</name>

  <precondition>מיגרציה 0022_work_items_is_demo.sql (Plan 11-01, Task 1) הוחלה על מסד ה-Supabase שמשרת את שרת הפיתוח — עמודת gs_work_items.is_demo קיימת. זהו צעד ידני/deploy מחוץ לתוכנית הזו (אין קובץ קונפיגורציית CLI מקושר בפרויקט הזה) — החל אותה באותו אופן שבו מיגרציות 0001-0021 הוחלו בעבודה הרגילה על הפרויקט (Supabase dashboard SQL editor, או supabase CLI אם מקושר מקומית) לפני שממשיכים.</precondition>

  <files>
src/components/supervisor/WeekFlow.jsx
  </files>

  <read_first>
- `.planning/phases/11-inline-demo-cleanup/11-CONTEXT.md` קטע `<decisions>` "is_demo placement" — מה בדיוק "מחק נתוני הדגמה" מוחק/לא מוחק (לא `gs_profiles`, לא `gs_positions`), וקטע `<findings>` סעיף 4 המלא (תרחיש השחזור המוצע ל-INLINE-04).
- `.planning/ROADMAP.md` Phase 11, קריטריונים 2, 3, 4 — הניסוח המדויק.
- `src/components/supervisor/WeekFlow.jsx` — כל הקובץ (עודכן ב-Plan 11-02 עם `onToggleAssignment`): שורות 22-31 (imports, כולל `useMemo` שכבר מיובא), 62-98 (דפוס `useMemo` הקיים לחישובי-שבוע), 148-170 (רשומת ה-board בתוך מערך `body`, אחרי עדכון 11-02).
- `src/lib/demoData.js`'s `demoShiftIdsForWeek(shifts, weekDates)` (Plan 11-01) — חתימה/התנהגות.
- `src/hooks/useGuardian.js`'s `actions.deleteDemoDataForWeek(weekDates)` (Plan 11-01) — חתימה/התנהגות.
- `src/components/supervisor/views.jsx` שורות 526-533, 588-592 — כפתור "מחק שבוע" הקיים ב-`ShiftMgmt` (`Btn variant="ghost" icon="trash" disabled={busy}`, בלי confirm, UndoBar בלבד): התבנית המדויקת לחיקוי עבור כפתור "מחק נתוני הדגמה" — אותו סוג פעולה הרסנית, אותה מוסכמה.
  </read_first>

  <action>
ב-`WeekFlow.jsx`: הוסף `Btn` לרשימת ה-imports הקיימת מ-`../ui.jsx` (כרגע `{ PrimaryAction, Segmented }`). ייבא `demoShiftIdsForWeek` מ-`../../lib/demoData.js`. הוסף `const demoIds = useMemo(() => demoShiftIdsForWeek(shifts, weekDates), [shifts, weekDates]);` (`useMemo` כבר מיובא, `shifts`/`weekDates` כבר פרופים של הרכיב).

עטוף את רשומת ה-board הקיימת במערך `body` (כרגע `<UnifiedBoard key="board" ... />` בודד) ב-`<div key="board" className="space-y-3">` — הזז את `key="board"` מ-`UnifiedBoard` ל-`div` העוטף. בתוך ה-div, מעל `<UnifiedBoard>`, רנדר תנאי (`demoIds.length > 0`) של שורה מיושרת-ימינה עם `<Btn variant="ghost" icon="trash" onClick={() => actions.deleteDemoDataForWeek(weekDates)} disabled={busy}>מחק נתוני הדגמה לשבוע זה</Btn>` — אותם `variant`/`icon`/`disabled` בדיוק כמו כפתור "מחק שבוע" ב-`ShiftMgmt`, בלי דיאלוג אישור (UndoBar בלבד, עקרון ברזל 3).

הכפתור הזה חי בשלב "השבוע" (board) שמשותף לשני המצבים (army/לא-army) — אין צורך בכפתור מקביל ב-`ShiftMgmt`/`RosterWizard.jsx` הנפרדים.

אחרי שהקוד עובר, בצע סבב אימות-אינטגרציה מלא (ר' סעיף human-check ב-verify למטה) — זה החלק המרכזי של המשימה, לא רק תוספת-קוד: מאמת שהכפתור עובד, ששחזור-מחדש של תרחיש הבאג (INLINE-04) דרך ה-affordances **החדשים** של הלוח (Plan 11-02, לא רק AssignView הישן) לא משחזר, ושהתנהגות שלוש הפעולות (הוספה, מחיקה, מחיקת-הדגמה) עקבית על פני פס-השלבים/הלוח/ScheduleMgmt יחד.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/WeekFlow.jsx','utf8'); if(!/demoShiftIdsForWeek/.test(s)) throw new Error('WeekFlow.jsx must import/use demoShiftIdsForWeek'); if(!/demoShiftIdsForWeek\(shifts,\s*weekDates\)/.test(s)) throw new Error('demoShiftIdsForWeek must be called with (shifts, weekDates)'); if(!/actions\.deleteDemoDataForWeek\(weekDates\)/.test(s)) throw new Error('button must call actions.deleteDemoDataForWeek(weekDates)'); if(!s.includes('מחק נתוני הדגמה לשבוע זה')) throw new Error('missing exact button label מחק נתוני הדגמה לשבוע זה'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/WeekFlow.jsx','utf8'); if(!/\{\s*Btn,\s*PrimaryAction,\s*Segmented\s*\}|\{\s*PrimaryAction,\s*Segmented,\s*Btn\s*\}|Btn/.test(s)) throw new Error('Btn must be imported from ../ui.jsx'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>
הרץ `npm run dev`, היכנס כמנהל. על צוות עם גם שיבוץ אמיתי וגם נתוני-הדגמה **באותו שבוע** (הפעל "הדגמה", ואז הוסף/הסר לפחות שיבוץ אמיתי אחד דרך "+"/"x" החדשים על הלוח):
1. אמת שכפתור "מחק נתוני הדגמה לשבוע זה" מופיע בשלב "השבוע".
2. לחץ עליו — אמת שנתוני ההדגמה נעלמים מיד עם UndoBar רגיל (8 שניות, בלי דיאלוג אישור), והשיבוץ האמיתי שורד.
3. רענן את הדף (אחרי חלון ה-UndoBar) — אמת בשרת שנתוני ההדגמה אכן נמחקו והאמיתיים עדיין שם.
4. אם ניתן להגיע לשבוע נוסף עם נתוני-הדגמה (שבוע אחר) — אמת שהם לא נפגעו.
5. אמת שהכפתור נעלם כשאין יותר נתוני-הדגמה בשבוע המוצג.
6. **שחזור-מחדש של INLINE-04**: לחץ "x" על אווטאר משובץ ומיד "+" להוספת אחר (או ההפך) — לפני שה-UI מפסיק להראות "שומר…" — בשתי-שלוש איטרציות רצופות. אמת שפס השלבים, הלוח, ו-ScheduleMgmt (שלב "שליחה לצוות") תמיד מתייצבים על המספר הנכון, גם תחת הקצב המהיר הזה.
7. חזור על (6) פעם נוספת כשמערבבים גם לחיצה על "מחק נתוני הדגמה לשבוע זה" בין הפעולות המהירות — כדי לוודא ששלוש הפעולות (הוספה/מחיקה/מחיקת-הדגמה) לא שוברות אחת את השנייה.
    </human-check>
  </verify>

  <acceptance_criteria>
- הכפתור מוצג רק כש-`demoIds.length > 0`, קורא ל-`actions.deleteDemoDataForWeek(weekDates)`, בלי דיאלוג אישור
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן מכסה את כל שבע הנקודות ב-`human-check`, כולל שחזור-מחדש מפורש של תרחיש הבאג דרך ה-affordances החדשים של Plan 11-02
  </acceptance_criteria>

  <done>
"מחק נתוני הדגמה לשבוע זה" עובד ומוגבל-שבוע נכון; תרחיש הבאג של "מצב השבוע" לא משחזר גם תחת השימוש האינטנסיבי-ביותר שהפאזה הזו עצמה מוסיפה (עריכה מהירה על הלוח).
  </done>

  <reversibility rating="reversible">
תוספת UI טהורה + סבב אימות. אין שינוי בנתיב כתיבה, בסכימה, או ב-API. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| מנהל מחובר → כפתור "מחק נתוני הדגמה" → `actions.deleteDemoDataForWeek` | UI חדש שקורא לפעולה קיימת ובדוקה (Plan 11-01) — אין DB write חדש כאן |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03-A | Tampering / אובדן נתונים | כפתור מחיקה חדש שמפעיל פעולה הרסנית | high | mitigate | הכפתור רק קורא ל-`deleteDemoDataForWeek` הקיימת (כבר מוגבלת ל-`is_demo=true` ומכוסה ב-unit test, Plan 11-01) — לא מוסיף לוגיקת-מחיקה משלו; `human-check` מאמת חי על שבוע מעורב לפני סגירת הפאזה |
| T-11-03-B | Repudiation | דיווח "עובד" בלי שנצפה חי | medium | mitigate | עקרון ברזל 6 (CLAUDE.md); `human-check` דורש עדות מפורשת על כל אחת משבע הנקודות, כולל שחזור-מחדש מפורש של תרחיש הבאג דרך ה-UI החדש, לא רק דיווח כללי |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים
2. הכפתור מוצג רק כשיש נתוני-הדגמה בשבוע, ומוחק רק אותם
3. תרחיש השחזור של INLINE-04 (11-CONTEXT.md סעיף 4) לא משחזר תחת שימוש אינטנסיבי בכל שלוש הפעולות ההרסניות של הפאזה יחד
4. אימות חי בדפדפן על כל שבע הנקודות ב-`human-check`
</verification>

<success_criteria>
- INLINE-03 מתקיים במלואו: כפתור "מחק נתוני הדגמה לשבוע זה" עובד, מוגבל-שבוע, בלי דיאלוג אישור (קריטריון הצלחה 2+3, ROADMAP Phase 11)
- INLINE-04 מאומת סגור תחת התנאי הקשה ביותר — שימוש אינטנסיבי ב-affordances החדשים של הפאזה עצמה (קריטריון הצלחה 4)
- כל ארבע דרישות הפאזה (INLINE-01..04) מאומתות חיות כמכלול אחד, לא רק כל אחת בנפרד
</success_criteria>

<output>
Create `.planning/phases/11-inline-demo-cleanup/11-03-SUMMARY.md` when done.

חובה לתעד ב-SUMMARY: ממצא כל אחת משבע נקודות ה-human-check בנפרד, כפי שנראו בפועל בדפדפן — כולל שחזור-מחדש המפורש של תרחיש הבאג.
</output>
