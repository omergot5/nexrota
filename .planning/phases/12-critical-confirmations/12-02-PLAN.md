---
phase: 12-critical-confirmations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useGuardian.js
autonomous: true
requirements: [CONFIRM-03, CONFIRM-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "actions.deleteShifts ו-actions.deleteDemoDataForWeek כותבות מיד (run()+refresh()), בלי deferred()/UndoBar — מוכנות להיקרא רק אחרי אישור מפורש בדיאלוג ב-Wave 2."
    - "actions.replaceShifts, actions.removeGuard, actions.deletePosition כותבות מיד באותו דפוס; deletePosition שומרת על הטיפול המיוחד בכישלון-חלקי (refresh() לפני שהשגיאה יוצאת החוצה) שנקבע ב-code review של Phase 11 (CR-02)."
    - "actions.removeRoleCompatibility עברה מ-run() ל-deferred() — מקבלת UndoBar בפעם הראשונה מאז שנוצרה (פער-הגנה שתועד ב-12-CONTEXT.md)."
    - "קיימת טבלת-ביקורת מתועדת בקוד (הערת JSDoc ליד actions), במוסכמה זהה ל-MORE-01 (Phase 10, SupervisorApp.jsx), שמפרטת את כל 10 הפעולות ההרסניות שנסקרו ואת מצב-החיבור של כל אחת: in-scope-for-pre-confirm / stays-UndoBar / newly-protected."
  artifacts:
    - src/hooks/useGuardian.js
  key_links:
    - "actions.deleteShifts / deleteDemoDataForWeek / replaceShifts / removeGuard / deletePosition → run() → api.* → refresh() (לא deferred() יותר)"
    - "actions.removeRoleCompatibility → deferred() → api.removeRoleCompatibility (UndoBar חדש)"
---

<objective>
בונה את שכבת-המנגנון של הפאזה: ממיר את חמש הפעולות שסומנו in-scope-for-pre-confirm (12-CONTEXT.md `<decisions>`) מ-`deferred()` (UndoBar) לכתיבה מיידית (`run()`+`refresh()`, בדיוק כמו `addShifts` הקיים) — כי ההחלטה הנעולה היא שפרה-אישור **מחליף** UndoBar, לא נוסף עליו. שתיים מהחמש (`deleteShifts`, `deleteDemoDataForWeek`) הן הפעולות הנקובות במפורש ב-CONFIRM-03; שלוש נוספות (`replaceShifts`, `removeGuard`, `deletePosition`) מקודמות תחת CONFIRM-05 לפי ההטיה החזקה ב-12-CONTEXT.md `<findings>` §1 ("מועמד חזק"). בנוסף, `removeRoleCompatibility` — הפער-הגנה האמיתי שהתגלה (בלי UndoBar ובלי אישור בכלל) — עוברת ל-`deferred()`, מקבלת רשת-ביטחון מינימלית. התוכנית סוגרת ב-טבלת-ביקורת מתועדת בקוד, באותה מוסכמה כמו MORE-01 (Phase 10).

זו תוכנית מנגנון טהורה — שום קובץ `.jsx` לא נערך כאן, ושום UI לא משתנה. ה-UI שקורא לפעולות האלה עדיין קורא להן בדיוק כמו קודם (אותה חתימה, למעט הסרת פרמטר `label` המיותר מ-`deleteShifts`/`replaceShifts` — שלא נעשה בו שימוש יותר כי אין יותר תווית UndoBar). Wave 2 (12-03/04/05) יחווט כל אחת מהחמש מאחורי `ConfirmDialog` (12-01).

Purpose: CONFIRM-03 (מנגנון) + CONFIRM-05 (מנגנון + ביקורת מתועדת) — הבסיס ש-Wave 2 בונה עליו.
Output: `useGuardian.js` עם חמש פעולות שהומרו לכתיבה מיידית, `removeRoleCompatibility` עם UndoBar חדש, וטבלת-ביקורת מתועדת.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-confirmations/12-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/hooks/useGuardian.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: deleteShifts — מקצה לקצה, deferred() → כתיבה מיידית (CONFIRM-03)</name>

  <files>
src/hooks/useGuardian.js
  </files>

  <read_first>
- `.planning/phases/12-critical-confirmations/12-CONTEXT.md` קטע `<decisions>` — "פרה-אישור REPLACES UndoBar... `deleteShifts`/`deleteDemoDataForWeek`, כשהן נקראות דרך הדיאלוג החדש, צריכות לכתוב מיד... לא להמשיך לעטוף ב-deferred()" — וקטע `<findings>` §3 המלא (הממצא האדריכלי המרכזי).
- `src/hooks/useGuardian.js` שורות 530-534 (`addShifts` — הדפוס המדויק של כתיבה-מיידית-בלי-UndoBar לחיקוי: `run(async () => { await api.X(...); await refresh(); })`), 342-352 (`deferred()` עצמה — המנגנון שיוצאים ממנו), 549-558 (`deleteShifts` הקיימת, כולל ההערה מעליה על "מחיקת אצווה").
  </read_first>

  <action>
ב-`src/hooks/useGuardian.js`: המר את `actions.deleteShifts` (שורות 549-558) מ-`deferred(label, patch, work)` ל-`run(async () => { await api.deleteShifts(ids); await refresh(); })` — בדיוק דפוס `addShifts`. הסר את הפרמטר `label` מהחתימה (`deleteShifts: (ids) => ...`) — אין יותר תווית UndoBar להציג, ואין צרכן שישתמש בו אחרי שינוי זה (12-CONTEXT.md מאשר קורא יחיד: `clearWeek`, שיתעדכן ב-12-03). מחק את ה-patch האופטימי (`(d) => ({ ...d, shifts: d.shifts.filter(...) })`) — לא צריך יותר, `refresh()` מביא את המצב האמיתי מהשרת.

עדכן את ההערה שמעל `deleteShifts` (כרגע מתארת "מחיקת אצווה... עוברת דרך deferred בדיוק כמו מחיקה בודדת") — היא כבר לא נכונה: `deleteShift` (יחיד) נשארת `deferred()`, אבל `deleteShifts` (אצווה) עברה לכתיבה מיידית כי CONFIRM-03 מחברת אותה לדיאלוג-אישור במקום.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/hooks/useGuardian.js','utf8'); const i=s.indexOf('deleteShifts:'); const body=s.slice(i,i+300); if(!/run\(async/.test(body)) throw new Error('deleteShifts must use run(), not deferred()'); if(/deferred\(/.test(body)) throw new Error('deleteShifts must not call deferred() anymore'); if(!/await refresh\(\)/.test(body)) throw new Error('deleteShifts must call refresh() after the write'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
  </verify>

  <acceptance_criteria>
- `actions.deleteShifts(ids)` כותבת מיד דרך `run()`, בלי `deferred()`, בלי פרמטר `label`
- `npm test` ו-`npm run build` עוברים
  </acceptance_criteria>

  <done>
`deleteShifts` ממירה בהצלחה את דפוס הכתיבה — מוכנה להיקרא מאחורי `ConfirmDialog` ב-Wave 2, מוכיחה את התבנית לשאר הפעולות במשימה 2.
  </done>

  <reversibility rating="reversible">
שינוי מנגנון-כתיבה בפעולה אחת, בקובץ יחיד. חזרה = revert לקומיט.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: deleteDemoDataForWeek, replaceShifts, removeGuard, deletePosition — אותה המרה (CONFIRM-03 + CONFIRM-05)</name>

  <files>
src/hooks/useGuardian.js
  </files>

  <read_first>
- Task 1 (אותה תוכנית) — הדפוס שהוכח כרגע ב-`deleteShifts`, לחיקוי בארבע הפעולות האלה.
- `src/hooks/useGuardian.js` שורות 720-736 (`deleteDemoDataForWeek` — שים לב לשער-ה-DoS הקיים `if (!ids.length) return;` **לפני** ה-`deferred`, שצריך להישאר), 560-595 (`replaceShifts`, כולל ההערה הארוכה על "atomic patch" — ההערה כולה כבר לא רלוונטית אחרי המרה לכתיבה מיידית, כי אין יותר שני `deferred` מתחרים למנוע), 756-761 (`removeGuard`), 894-942 (`deletePosition` — שים לב לטיפול המיוחד בכישלון-חלקי בתוך ה-`catch`, ול-`hasMaterializedWeek` שחישבה את התווית המיוחדת ל-UndoBar).
- `.planning/phases/12-critical-confirmations/12-CONTEXT.md` קטע `<findings>` §1 — הביסוס ל"מועמד חזק" של `replaceShifts`/`removeGuard`/`deletePosition`.
  </read_first>

  <action>
בצע את אותה המרה כמו Task 1, לארבע הפעולות:

**`deleteDemoDataForWeek(weekDates)`**: השאר את שער ה-DoS הקיים בראש (`const ids = demoShiftIdsForWeek(...); if (!ids.length) return;`), אבל החלף את גוף ה-`deferred(...)` ב-`return run(async () => { await api.deleteShifts(ids); await refresh(); });`. מחק את ה-patch האופטימי ואת מחרוזת התווית "נתוני ההדגמה נמחקו".

**`replaceShifts(ids, rows)`**: הסר את הפרמטר `label` מהחתימה. הפוך אותה ל-`run(async () => { if (ids.length) await api.deleteShifts(ids); if (rows.length) await api.createShifts(rows, dataRef.current.team?.code); await refresh(); })` — זהה לגוף ה-`work` הפנימי שכבר היה עטוף ב-`run()` בתוך ה-`deferred()` הישן (שורות 590-594), רק בלי השכבה החיצונית. מחק את ה-patch האופטימי ואת ההערה הארוכה שמעליה על "atomic patch" (כבר לא רלוונטית — כתיבה מיידית לא מצריכה תיאום בין שני חלונות-ביטול מתחרים); השאר הערה קצרה חדשה שמפנה ל-12-CONTEXT.md ומסבירה שהפעולה עברה לפרה-אישור כי היא "דריסה" — הדוגמה המפורשת ל-CONFIRM-05 בטקסט הדרישה עצמו.

**`removeGuard(id)`**: הפוך ל-`run(async () => { await api.removeGuard(id); await refresh(); })`. מחק את ה-patch האופטימי ואת מחרוזת התווית.

**`deletePosition(id, weekDates = [])`**: מחק את חישוב `hasMaterializedWeek`/`weekSet` (לא בשימוש יותר — אין תווית UndoBar לבחור). הפוך ל:
`run(async () => { if (weekDates.length) await api.unmaterializePositionWeek(id, weekDates); try { await api.deletePosition(id); } catch (e) { await refresh(); throw e; } await refresh(); })`.
שים לב: זו לא רגרסיה של תיקון CR-02 (Phase 11 code review, שהחליף `rethrow` ב-`refresh()+setError()` בתוך ה-`deferred()` הישן) — הסיבה המקורית לכך הייתה למנוע מ-`flush()`'s ה-`catch` החיצוני "לדרוס" את המצב האמיתי בחזרה ל-snapshot ישן. ב-`run()` אין שום snapshot לצייר-מחדש (`run()` אף פעם לא מצייר patch אופטימי) — אז `refresh()` ואז `throw` בטוח: `refresh()` כבר הציג את המצב האמיתי *לפני* ש-`run()`'s ה-`catch` החיצוני קורא ל-`setError(e.message)` (במקום הקריאה הידנית הישנה ל-`setError`, ש-`run()` כבר עושה בשבילנו — מסיר כפילות קוד).
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/hooks/useGuardian.js','utf8'); for (const name of ['deleteDemoDataForWeek','replaceShifts','removeGuard','deletePosition']) { const i=s.indexOf(name+':'); if(i<0) throw new Error('missing action '+name); const body=s.slice(i,i+700); if(!/run\(/.test(body)) throw new Error(name+' must use run()'); if(/deferred\(/.test(body)) throw new Error(name+' must not call deferred() anymore'); } console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/hooks/useGuardian.js','utf8'); const i=s.indexOf('deletePosition:'); const body=s.slice(i,i+700); if(!/unmaterializePositionWeek/.test(body)) throw new Error('deletePosition must still call unmaterializePositionWeek before deletePosition'); if(!/catch \(e\)/.test(body)) throw new Error('deletePosition must keep the partial-failure catch'); if(!/throw e/.test(body)) throw new Error('deletePosition must rethrow after refresh() inside run()'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/hooks/useGuardian.js','utf8'); if(/hasMaterializedWeek/.test(s)) throw new Error('hasMaterializedWeek must be removed — no longer used for an UndoBar label'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
  </verify>

  <acceptance_criteria>
- `deleteDemoDataForWeek`, `replaceShifts`, `removeGuard`, `deletePosition` כותבות מיד דרך `run()`, בלי `deferred()`
- `deletePosition` שומרת על סדר `unmaterializePositionWeek` → `deletePosition` → refresh-on-partial-failure, בלי `hasMaterializedWeek`
- `replaceShifts`/`deleteShifts` בלי פרמטר `label`
- `npm test` ו-`npm run build` עוברים
  </acceptance_criteria>

  <done>
חמש הפעולות (כולל `deleteShifts` ממשימה 1) כותבות מיד — מוכנות להיקרא מאחורי `ConfirmDialog` ב-Wave 2.
  </done>

  <reversibility rating="reversible">
שינוי מנגנון-כתיבה בארבע פעולות, בקובץ יחיד. חזרה = revert לקומיט.
  </reversibility>
</task>

<task type="auto">
  <name>Task 3: removeRoleCompatibility → UndoBar + טבלת-ביקורת CONFIRM-05 מתועדת</name>

  <files>
src/hooks/useGuardian.js
  </files>

  <read_first>
- `.planning/phases/12-critical-confirmations/12-CONTEXT.md` קטע `<findings>` §1 המלא (הטבלה עם כל 9 פעולות ה-`deferred()` + הממצא הנוסף על `removeRoleCompatibility`) — זה המקור שהטבלה החדשה כאן צריכה לשקף באמונה, לא לגזור מחדש.
- `src/hooks/useGuardian.js` שורות 778-782 (`removeRoleCompatibility` הקיימת — `run()` רגיל, בלי `deferred()` ובלי דיאלוג), 40-52 (`EMPTY` — `compatibility: []` קיים שם, מאשר את שם השדה ב-`data`), 300-311 (בלוק-ההערה הגדול מעל `deferred()` שמנמק "פעולה הפיכה — בלי חלונית אישור" — הנימוק הזה כבר לא חל על כל קריאה ל-`deferred()` אחרי Task 1/2, רק על אלו שנשארו).
- `src/components/SupervisorApp.jsx` שורות ~39-50 — מוסכמת טבלת-הביקורת של MORE-01 (Phase 10): טבלת Markdown בתוך הערת JSDoc, צמודה לקוד הרלוונטי, מפנה ל-CONTEXT.md כמקור. אותה מוסכמה בדיוק כאן.
  </read_first>

  <action>
ב-`removeRoleCompatibility(id)` (שורות 778-782): הפוך אותה מ-`run(async () => { await api.removeRoleCompatibility(id); await refresh(); })` ל-`deferred("החסימה הוסרה", (d) => ({ ...d, compatibility: d.compatibility.filter((c) => c.id !== id) }), () => api.removeRoleCompatibility(id))` — אותו דפוס בדיוק כמו `deleteTask`/`removeGuard`-הישנה. זו הפעם הראשונה שהפעולה הזו מקבלת רשת-ביטחון כלשהי (12-CONTEXT.md: "לא deferred() ולא confirm() — לחיצה על כפתור הפח מוחקת... מיידית, בלי שום רשת ביטחון").

הוסף בלוק-הערת-JSDoc חדש מיד מעל `actions = useMemo(...)` (שורה ~503), באותה מוסכמה כמו MORE-01 (SupervisorApp.jsx ~39-50): כותרת "CONFIRM-05 — טבלת ביקורת הפעולות ההרסניות (Phase 12, 12-CONTEXT.md findings §1)", ואז טבלת Markdown עם עמודות: פעולה | מצב חיבור | נימוק. עשר שורות, נאמנות למקור:

| פעולה | מצב חיבור | נימוק |
|---|---|---|
| `deleteShift` | stays-UndoBar | מחיקת-פריט-בודד תכופה, חומרה נמוכה — עקבי עם עקרון UndoBar לפעולות הפיכות-בקלות |
| `deleteShifts` | in-scope-for-pre-confirm | CONFIRM-03, נקוב במפורש |
| `replaceShifts` | in-scope-for-pre-confirm | דריסת תוכן שבוע שלם — הדוגמה המפורשת ל"דריסות" בטקסט CONFIRM-05 עצמו |
| `clearAssignments` | stays-UndoBar | אין נקודת-UI חיה שקוראת לה כרגע (12-CONTEXT.md אישר) — נשארת מוגנת-UndoBar לכשתחובר |
| `deleteDemoDataForWeek` | in-scope-for-pre-confirm | CONFIRM-03, נקוב במפורש |
| `removeGuard` | in-scope-for-pre-confirm | מסיר אדם מהצוות; cascade לשיבוצים/זמינות/בקשות-החלפה שלו |
| `decideSwap` | stays-UndoBar | הערת-קוד קיימת (useGuardian.js, ליד הפעולה) כבר דנה במפורש ונגד confirm() עבור אישור/דחיית-החלפה — נשמר כתקדים מודע |
| `deleteTask` | stays-UndoBar | מחיקת-פריט-בודד תכופה, חומרה נמוכה — כמו `deleteShift` |
| `deletePosition` | in-scope-for-pre-confirm | מוחקת עמדה קבועה; cascade על שיבוצי-השבוע הנוכחי כשהיא ממומשת |
| `removeRoleCompatibility` | newly-protected (UndoBar, לא pre-confirm) | פער אמיתי שנמצא — לא הייתה לה שום הגנה. הרף המינימלי שנקבע הוא "לפחות UndoBar", לא פרה-אישור מלא |

בסיום הטבלה, הוסף משפט המפנה לוויב-2 (12-03/04/05) לחיווט ה-UI בפועל של חמש השורות "in-scope-for-pre-confirm", ומציין ש-`removeRoleCompatibility` לא דורשת שום שינוי UI — ה-`UndoBar` הגלובלי (`SupervisorApp.jsx`, מוזן מ-`pending`/`undo` שמוחזרים מה-hook) כבר יציג אותה אוטומטית ברגע שהמנגנון השתנה.

עדכן את בלוק-ההערה מעל `deferred()` עצמה (שורות 300-311): הוסף משפט קצר שמפנה לטבלת-הביקורת החדשה — לא כל קריאה ל-`deferred()` בקובץ הזה נהנית מהנימוק "בלי חלונית אישור" באותה מידה יותר; חלק מהפעולות שהיו כאן עברו לפרה-אישור (ר' טבלת CONFIRM-05 למעלה).
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/hooks/useGuardian.js','utf8'); const i=s.indexOf('removeRoleCompatibility:'); const body=s.slice(i,i+300); if(!/deferred\(/.test(body)) throw new Error('removeRoleCompatibility must use deferred() now'); if(!/compatibility: d\.compatibility\.filter/.test(body)) throw new Error('removeRoleCompatibility patch must filter d.compatibility'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/hooks/useGuardian.js','utf8'); if(!/CONFIRM-05/.test(s)) throw new Error('missing CONFIRM-05 audit table comment'); const rows=['deleteShift','deleteShifts','replaceShifts','clearAssignments','deleteDemoDataForWeek','removeGuard','decideSwap','deleteTask','deletePosition','removeRoleCompatibility']; for (const r of rows) { if(!s.includes('`'+r+'`')) throw new Error('audit table missing row for '+r); } console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
  </verify>

  <acceptance_criteria>
- `removeRoleCompatibility` כותבת דרך `deferred()`, מקבלת UndoBar (תווית "החסימה הוסרה")
- קיימת הערת-JSDoc עם טבלת-ביקורת CONFIRM-05 מלאה, כל 10 הפעולות
- בלוק-ההערה מעל `deferred()` מפנה לטבלה החדשה
- `npm test` ו-`npm run build` עוברים
  </acceptance_criteria>

  <done>
`removeRoleCompatibility` מוגנת ב-UndoBar; טבלת-ביקורת CONFIRM-05 מתועדת בקוד, נאמנה ל-12-CONTEXT.md.
  </done>

  <reversibility rating="reversible">
שינוי מנגנון בפעולה אחת + תוספת הערת-תיעוד. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| מנהל מחובר → `run()`-based actions (בלי חלון-ביטול) → DB DELETE/UPDATE | חמש פעולות מאבדות את חלון ה-UndoBar של 8 שניות וכותבות מיד, בלי רשת-ביטחון-אחורית. הפיצוי (ConfirmDialog *לפני* הקריאה) הוא Wave 2, לא כאן. |
| מנהל מחובר → `removeRoleCompatibility` → `deferred()` | פעולה שהייתה בלי שום רשת-ביטחון מקבלת אחת עכשיו — צמצום סיכון, לא הגדלתו |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02-A | Repudiation / אובדן נתונים | חמש הפעולות שהומרו כותבות מיד בלי UndoBar וגם בלי דיאלוג — עד שWave 2 יחווט אותן | medium | mitigate | חלון-הביניים הוא תוך-פאזה בלבד: נקודות ה-UI היחידות שקוראות לפעולות האלה מתעדכנות באותה פאזה (12-03/04/05) לפני שהעבודה נחשבת גמורה — הפאזה לא משתחררת חלקית |
| T-12-02-B | Tampering | `deletePosition`'s טיפול-בכישלון-חלקי (unmaterialize הצליח, delete נכשל) | low | mitigate | `refresh()` לפני `throw` מציג את המצב האמיתי מהשרת לפני שהשגיאה מדווחת — אותו עיקרון שנקבע ב-Phase 11 code review (CR-02), בלי snapshot-rollback שיכול לדרוס אותו |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים אחרי כל אחת משלוש המשימות
2. חמש הפעולות (`deleteShifts`, `deleteDemoDataForWeek`, `replaceShifts`, `removeGuard`, `deletePosition`) כותבות מיד, בלי `deferred()`
3. `removeRoleCompatibility` כותבת דרך `deferred()`
4. טבלת-ביקורת CONFIRM-05 קיימת ונאמנה ל-12-CONTEXT.md, כל 10 השורות
</verification>

<success_criteria>
- CONFIRM-03 (מנגנון): `deleteShifts`/`deleteDemoDataForWeek` מוכנות לכתיבה-מיידית-אחרי-אישור
- CONFIRM-05 (מנגנון + ביקורת): שלוש פעולות נוספות מקודמות, `removeRoleCompatibility` מוגנת, וטבלת-ביקורת מתועדת קיימת
</success_criteria>

<output>
Create `.planning/phases/12-critical-confirmations/12-02-SUMMARY.md` when done.
</output>