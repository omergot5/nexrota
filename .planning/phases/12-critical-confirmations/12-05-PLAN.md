---
phase: 12-critical-confirmations
plan: 05
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - src/components/supervisor/RosterWizard.jsx
  - src/components/supervisor/PositionsScreen.jsx
autonomous: true
requirements: [CONFIRM-05]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "מחיקת עמדה מ-RosterWizard (removeItem, מצב army) פותחת ConfirmDialog רק כשמדובר בעמדה אמיתית (item.position) — הסרת seed טיוטה (עדיין לא נשמר) נשארת מיידית, בלי דיאלוג, כי אין לה נתון שרת למחוק."
    - "מחיקת עמדה מ-PositionsScreen (המסך העצמאי, כל תחום) פותחת ConfirmDialog לפני actions.deletePosition."
  artifacts: []
  key_links:
    - "RosterWizard removeItem → confirmState (position branch only) → ConfirmDialog → actions.deletePosition(id, weekDates)"
    - "PositionsScreen confirmTarget → ConfirmDialog → actions.deletePosition(id, weekDates)"
---

<objective>
מחווט את `ConfirmDialog` (12-01) לשתי נקודות-המחיקה של `deletePosition` — הפעולה המקודמת החמישית והאחרונה של CONFIRM-05 (12-02 המירה אותה לכתיבה מיידית). שתי נקודות-כניסה, שני קבצים נפרדים בלי חפיפה: `RosterWizard.jsx` (אשף-הבנייה במצב army) ו-`PositionsScreen.jsx` (מסך "עמדות קבועות" העצמאי, כל התחומים).

Purpose: משלים את CONFIRM-05 — כל חמש הפעולות המקודמות (12-02) מחווטות ל-UI עד סוף ה-Wave הזו (עם 12-03/04).
Output: שני קבצים עם דיאלוג-אישור לפני מחיקת עמדה.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-confirmations/12-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/supervisor/RosterWizard.jsx
@src/components/supervisor/PositionsScreen.jsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: מחיקת עמדה ב-RosterWizard (army) מקצה-לקצה דרך ConfirmDialog (CONFIRM-05)</name>

  <files>
src/components/supervisor/RosterWizard.jsx
  </files>

  <read_first>
- `.planning/phases/12-critical-confirmations/12-CONTEXT.md` קטע `<findings>` §1 — `deletePosition` "מועמד חזק ל-CONFIRM-05 (הקוד עצמו כבר מכיר בחומרה שלה — תווית מיוחדת כשיש cascade)".
- `src/components/supervisor/RosterWizard.jsx` שורות 20-22 (imports מ-`../ui.jsx` — כרגע `{ Alert, Btn, Card, IconBtn, Input, PageHeader, Segmented, Select }`, `ConfirmDialog` חסרה), 279-291 (`removeItem` הקיימת — שני ענפים: `item.position` (עמדה אמיתית, קוראת ל-`actions.deletePosition`) ו-`item.seed.id` (טיוטה מקומית שעדיין לא נשמרה, `setDraftSeeds` בלבד — **לא** צריכה דיאלוג, אין לה שום דבר בשרת למחוק), 290 (`setActiveKey(null)` שרץ היום ללא תנאי בסוף `removeItem`, גם לפני שהמחיקה בפועל הושלמה).
- Plan 12-02 Task 2 (`actions.deletePosition(id, weekDates = [])` — כותבת מיד, בלי `hasMaterializedWeek`).
  </read_first>

  <action>
הוסף `ConfirmDialog` לרשימת ה-imports מ-`"../ui.jsx"` (שורה 21). הוסף `const [confirmState, setConfirmState] = useState(null);` בתוך הרכיב (`useState` כבר מיובא).

שנה את `removeItem` (שורות 279-291) כך שרק הענף `item.position` פותח דיאלוג, וה-`setActiveKey(null)` על אותו ענף עובר **לתוך** `onConfirm` (רץ רק אחרי שהמחיקה בפועל הצליחה — לא לפניה, כי סגירת פאנל-העריכה מוקדם-מדי הייתה מטעה כשהמשתמש עדיין רואה דיאלוג-אישור פתוח):
```
const removeItem = (item) => {
  if (item.position) {
    setConfirmState({
      title: `למחוק את "${item.position.title}"?`,
      body: "העמדה תימחק לצמיתות, כולל שיבוצים שכבר בוצעו לה השבוע הנוכחי (אם יש). הפעולה לא הפיכה.",
      confirmLabel: "מחק עמדה",
      tone: "danger",
      onConfirm: async () => {
        await actions.deletePosition(item.position.id, weekDates);
        if (item.key === resolvedActiveKey) setActiveKey(null);
      },
    });
    return;
  }
  if (item.seed.id) {
    setDraftSeeds((d) => d.filter((s) => s.id !== item.seed.id));
  }
  if (item.key === resolvedActiveKey) setActiveKey(null);
};
```
שים לב: ענף ה-seed (טיוטה) נשאר מיידי, בלי דיאלוג — היא לא עמדה שקיימת בשרת, אין שום "פעולה הרסנית" אמיתית במחיקתה.

הוסף רינדור `<ConfirmDialog open={Boolean(confirmState)} onClose={() => setConfirmState(null)} onConfirm={confirmState?.onConfirm} title={confirmState?.title} body={confirmState?.body} confirmLabel={confirmState?.confirmLabel} tone={confirmState?.tone} busy={busy} />` בתוך ה-JSX הראשי של הרכיב.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/RosterWizard.jsx','utf8'); if(!/ConfirmDialog/.test(s.slice(0,1200))) throw new Error('ConfirmDialog must be imported from ../ui.jsx'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/RosterWizard.jsx','utf8'); const i=s.indexOf('const removeItem'); const body=s.slice(i,i+700); if(!/setConfirmState/.test(body)) throw new Error('removeItem must open the confirm dialog for the position branch'); if(!/item\\.seed\\.id/.test(body)) throw new Error('the draft-seed branch must remain untouched (no dialog)'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>בצוות במצב army, פתח RosterWizard. (1) הוסף פריט טיוטה חדש ("הוסף משימה") ומחק אותו לפני שמירה — אמת שהוא נעלם מיד, בלי שום דיאלוג. (2) על עמדה אמיתית שכבר נשמרה, לחץ למחיקה — אמת שנפתח דיאלוג לפני שנקרה שינוי; ביטול לא משנה כלום (רענן ואמת); אישור מוחק אותה לצמיתות. (3) אם אפשר, בקר קודם בשלב "השבוע" (כדי לממש את השבוע), חזור למחוק עמדה שהשבוע שלה ממומש — אמת שאין שגיאת מפתח-זר גולמית (תיקון Phase 11 עדיין עובד מעל השינוי הזה).</human-check>
  </verify>

  <acceptance_criteria>
- `ConfirmDialog` מיובא; `removeItem` פותחת דיאלוג רק לענף `item.position`
- ענף `item.seed.id` נשאר מיידי
- `setActiveKey(null)` על מחיקה-אמיתית רץ רק בתוך `onConfirm`, אחרי המחיקה בפועל
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן על שלוש הנקודות
  </acceptance_criteria>

  <done>
מחיקת עמדה אמיתית ב-RosterWizard עוברת דרך `ConfirmDialog`, כותבת מיד; מחיקת-טיוטה נשארת מיידית וללא דיאלוג.
  </done>

  <reversibility rating="reversible">
עיטוף UI על ענף אחד מתוך שניים + הזזת קריאה. חזרה = revert לקומיט.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: מחיקת עמדה ב-PositionsScreen (המסך העצמאי) דרך ConfirmDialog (CONFIRM-05)</name>

  <files>
src/components/supervisor/PositionsScreen.jsx
  </files>

  <read_first>
- `src/components/supervisor/PositionsScreen.jsx` שורות 14-16 (imports מ-`../ui.jsx` — כרגע `{ Badge, Btn, Card, EmptyState, Field, IconBtn, Input, Modal, PageHeader, Segmented, Select }`, `ConfirmDialog` חסרה — `Modal` כבר מיובא ל-`editing`/`form`), 47-49 (חתימת הרכיב), 128-140 (רשימת `PositionCard`, `onDelete={() => actions.deletePosition(p.id, weekDates)}`), 273 (`PositionCard` מקבלת `onDelete` כ-prop, קוראת לו ב-`IconBtn`'s `onClick`).
  </read_first>

  <action>
הוסף `ConfirmDialog` לרשימת ה-imports מ-`"../ui.jsx"` (שורה 15). הוסף `const [confirmTarget, setConfirmTarget] = useState(null);` (position object | null) בתוך `PositionsScreen` (`useState` כבר מיובא, שורה 13).

שנה את `onDelete={() => actions.deletePosition(p.id, weekDates)}` (שורה 138) ל-`onDelete={() => setConfirmTarget(p)}` — `PositionCard`'s prop-signature לא משתנה, רק מה-`PositionsScreen` מעביר בפועל.

הוסף רינדור, ליד ה-`Modal` הקיים של הטופס:
```
<ConfirmDialog
  open={Boolean(confirmTarget)}
  onClose={() => setConfirmTarget(null)}
  onConfirm={() => actions.deletePosition(confirmTarget.id, weekDates)}
  title={confirmTarget ? `למחוק את "${confirmTarget.title}"?` : ""}
  body="העמדה תימחק לצמיתות, כולל שיבוצים שכבר בוצעו לה השבוע הנוכחי (אם יש). הפעולה לא הפיכה."
  confirmLabel="מחק עמדה"
  tone="danger"
  busy={busy}
/>
```
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/PositionsScreen.jsx','utf8'); if(!/ConfirmDialog/.test(s.slice(0,900))) throw new Error('ConfirmDialog must be imported from ../ui.jsx'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/PositionsScreen.jsx','utf8'); if(!/confirmTarget/.test(s)) throw new Error('missing confirmTarget confirm state'); if(/onDelete=\{\(\) => actions\.deletePosition\(p\.id, weekDates\)\}/.test(s)) throw new Error('onDelete must no longer call actions.deletePosition directly'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>פתח מסך "עמדות קבועות" (דרך "עוד"), בכל תחום (לא רק army). לחץ על כפתור-הפח ליד עמדה קיימת. אמת: (1) נפתח דיאלוג אישור לפני שנקרה שינוי; (2) ביטול לא משנה כלום — רענן ואמת; (3) אישור מוחק את העמדה לצמיתות; (4) אם העמדה ממומשת לשבוע הנוכחי, המחיקה עובדת בלי שגיאת מפתח-זר גולמית (תיקון Phase 11 עדיין עובד).</human-check>
  </verify>

  <acceptance_criteria>
- `ConfirmDialog` מיובא ב-`PositionsScreen.jsx`
- `onDelete` פותח דיאלוג; רק `onConfirm` קורא ל-`actions.deletePosition`
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן על ארבע הנקודות
  </acceptance_criteria>

  <done>
מחיקת עמדה מ-`PositionsScreen` עוברת דרך `ConfirmDialog` — שתי נקודות-המחיקה של `deletePosition` מחווטות (עם Task 1).
  </done>

  <reversibility rating="reversible">
עיטוף UI על כפתור בודד. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| מנהל מחובר → ConfirmDialog → actions.deletePosition | פעולה שכבר עוברת RLS+auth בשרת (ללא שינוי) מקבלת שער UI משתי נקודות-כניסה נפרדות |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05-A | Repudiation | לחיצה כפולה על כפתור-האישור בזמן כתיבה בתהליך | low | mitigate | `ConfirmDialog`'s `pending`/`busy` (12-01) — לא קוד חדש כאן |
| T-12-05-B | Denial of Service | סגירת פאנל-עריכה (`setActiveKey(null)`) מוקדם מדי ב-RosterWizard, לפני שהמחיקה בפועל הצליחה | low | mitigate | הועבר לתוך `onConfirm`, אחרי ה-`await actions.deletePosition(...)` — לא לפניו |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים אחרי כל אחת משתי המשימות
2. שתי נקודות-המחיקה (RosterWizard, PositionsScreen) עוברות דרך `ConfirmDialog`
3. מחיקת-טיוטה (seed) ב-RosterWizard נשארת מיידית, בלי דיאלוג
4. אימות חי בדפדפן על כל אחת משתי המשימות
</verification>

<success_criteria>
- CONFIRM-05 (השלמה): כל חמש הפעולות המקודמות (עם 12-03) מחווטות ל-`ConfirmDialog` עד סוף Wave 2
</success_criteria>

<output>
Create `.planning/phases/12-critical-confirmations/12-05-SUMMARY.md` when done.
</output>