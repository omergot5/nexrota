---
phase: 12-critical-confirmations
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - src/components/supervisor/views.jsx
autonomous: true
requirements: [CONFIRM-02, CONFIRM-03, CONFIRM-04, CONFIRM-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "לחיצה על 'מחק שבוע' ב-ShiftMgmt פותחת ConfirmDialog; רק אישור בו קורא ל-actions.deleteShifts (12-02, כותבת מיד) — אין יותר UndoBar על הפעולה הזו."
    - "לחיצה על 'מלא שבוע' כשיש כבר משמרות באותו שבוע פותחת ConfirmDialog לפני הדריסה; כששבוע ריק, המילוי מתבצע מיד בלי דיאלוג — כי אין מה לדרוס."
    - "שלושת כפתורי הפרסום/ביטול-פרסום ב-ScheduleMgmt (פרסם הכל, בטל פרסום, טוגל-יום) עוברים דרך ConfirmDialog משותף אחד (askPublish); הטקסט של כפתור-היום נגזר מ-allPub בזמן הלחיצה, לא מתווית קבועה."
    - "לחיצה על כפתור-הפח ליד אדם ב-TeamView פותחת ConfirmDialog לפני actions.removeGuard."
  artifacts: []
  key_links:
    - "ShiftMgmt clearWeek/fillWeek → local confirmState → ConfirmDialog → actions.deleteShifts / actions.replaceShifts"
    - "ScheduleMgmt askPublish(...) → local confirmState → ConfirmDialog → actions.publish(ids, published)"
    - "TeamView guardToRemove → ConfirmDialog → actions.removeGuard"
---

<objective>
מחווט את `ConfirmDialog` (12-01) לחמש נקודות-הכניסה שחיות ב-`views.jsx`: `ShiftMgmt`'s "מחק שבוע" (CONFIRM-03) ו-"מלא שבוע" (CONFIRM-05, `replaceShifts`), `ScheduleMgmt`'s שלושת כפתורי פרסום/ביטול-פרסום (CONFIRM-02/04), ו-`TeamView`'s הסרת-אדם (CONFIRM-05, `removeGuard`). כל שלוש הפעולות מאחורי הדיאלוגים כותבות מיד (12-02) — אין UndoBar על הפעולות האלה יותר, רק פרה-אישור.

זו נקודת-ההוכחה הראשונה של הפאזה: כאן ConfirmDialog (12-01) ומנגנון-הכתיבה-המיידית (12-02) נפגשים לראשונה עם קליק אמיתי בדפדפן.

Purpose: CONFIRM-02, CONFIRM-03 (חלק), CONFIRM-04, CONFIRM-05 (חלק) — ארבע מחמש הפעולות הנקובות/מקודמות, בקובץ אחד.
Output: `views.jsx` עם `ShiftMgmt`/`ScheduleMgmt`/`TeamView` מחווטים ל-`ConfirmDialog`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-confirmations/12-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/supervisor/views.jsx
@src/lib/terms.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: "מחק שבוע" מקצה-לקצה דרך ConfirmDialog (CONFIRM-03)</name>

  <files>
src/components/supervisor/views.jsx
  </files>

  <read_first>
- `.planning/phases/12-critical-confirmations/12-CONTEXT.md` קטע `<findings>` §3 — "מחיקת שבוע ומחיקת נתוני הדגמה" (הממצא האדריכלי המרכזי: פרה-אישור מחליף UndoBar, לא נוסף עליו) וקטע `<decisions>`.
- `src/components/supervisor/views.jsx` שורות 1-8 (imports מ-`../ui.jsx` — `ConfirmDialog` חסרה מהרשימה), 465-479 (`ShiftMgmt` חתימה + `weekShifts`), 526-533 (`clearWeek` הקיימת — שים לב להערה `/** מחיקת השבוע כולו. \`deleteShifts\` נותן UndoBar, ולכן אין דיאלוג אישור. */` שממש מעליה — היא כבר שגויה אחרי 12-02 וחייבת להתעדכן), 581-601 (`PageHeader` עם כפתור "מחק שבוע", שורה 589).
- Plan 12-01 (`ConfirmDialog` — props: `open, onClose, onConfirm, title, body, confirmLabel, cancelLabel, tone, busy`) ו-Plan 12-02 Task 1 (`actions.deleteShifts(ids)` — בלי פרמטר `label` יותר).
  </read_first>

  <action>
הוסף `ConfirmDialog` לרשימת ה-imports הקיימת מ-`"../ui.jsx"` בראש `views.jsx` (שורות 5-8).

ב-`ShiftMgmt`: הוסף `const [confirmState, setConfirmState] = useState(null);` (`useState` כבר מיובא מ-`"react"`, שורה 1). שנה את `clearWeek` (שורות 526-533) כך שבמקום לקרוא ל-`actions.deleteShifts` ישירות, היא פותחת דיאלוג:
```
const clearWeek = () => {
  if (!weekShifts.length) return;
  setConfirmState({
    title: "למחוק את כל השבוע?",
    body: `${weekShifts.length} ${t("unit.shifts")} יימחקו לצמיתות מהשבוע הזה — כולל שיבוצים שכבר בוצעו. הפעולה לא הפיכה.`,
    confirmLabel: "מחק שבוע",
    tone: "danger",
    onConfirm: () => actions.deleteShifts(weekShifts.map((s) => s.id)),
  });
};
```
עדכן את ההערה מעל `clearWeek` — היא כבר לא נכונה (Task זה בעצמו הופך אותה לשגויה): נמק שהפעולה עברה לפרה-אישור (CONFIRM-03), ו-`deleteShifts` כותבת מיד בלי UndoBar (12-02).

הוסף רינדור `<ConfirmDialog open={Boolean(confirmState)} onClose={() => setConfirmState(null)} onConfirm={confirmState?.onConfirm} title={confirmState?.title} body={confirmState?.body} confirmLabel={confirmState?.confirmLabel} tone={confirmState?.tone} busy={busy} />` בסוף ה-JSX של `ShiftMgmt` (ליד `<WorkItemForm .../>` הקיים) — מופע אחד, משותף לכל דיאלוגי הרכיב הזה (גם `fillWeek` ב-Task 2 ישתמש באותו `confirmState`/`setConfirmState`).
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/views.jsx','utf8'); if(!/ConfirmDialog/.test(s.slice(0,600))) throw new Error('ConfirmDialog must be imported from ../ui.jsx at the top of views.jsx'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/views.jsx','utf8'); const i=s.indexOf('const clearWeek'); const body=s.slice(i,i+500); if(!/setConfirmState/.test(body)) throw new Error('clearWeek must open the confirm dialog via setConfirmState'); if(/actions\\.deleteShifts/.test(body)) throw new Error('clearWeek itself must not call actions.deleteShifts directly — only onConfirm may'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, היכנס כמנהל, פתח "בניית שבוע" עם לפחות משמרת אחת בשבוע. לחץ "מחק שבוע". אמת: (1) נפתח דיאלוג אישור עם כותרת/גוף שמתארים את המחיקה, לפני שנקרה כלום; (2) לחיצה על "ביטול"/Escape/רקע סוגרת את הדיאלוג בלי שנוצר שינוי — רענן את הדף ואמת שהמשמרות עדיין קיימות; (3) לחיצה על "מחק שבוע" (כפתור-האישור בדיאלוג) מוחקת את המשמרות מיד; (4) רענן את הדף — אמת שאין UndoBar (אין חלון-8-שניות), והמשמרות אכן נמחקו בשרת, לא רק במסך.</human-check>
  </verify>

  <acceptance_criteria>
- `ConfirmDialog` מיובא ב-`views.jsx`
- `clearWeek` פותחת דיאלוג; רק `onConfirm` קורא ל-`actions.deleteShifts`
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן על ארבע הנקודות ב-human-check
  </acceptance_criteria>

  <done>
"מחק שבוע" עובר דרך `ConfirmDialog`; המחיקה כותבת מיד אחרי אישור, בלי UndoBar. ההוכחה הראשונה של הפאזה שהארכיטקטורה (12-01+12-02) עובדת מקצה-לקצה.
  </done>

  <reversibility rating="reversible">
עיטוף UI קיים בדיאלוג + שינוי קריאה אחת. חזרה = revert לקומיט.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: פרסום/ביטול-פרסום (ScheduleMgmt, CONFIRM-02/04)</name>

  <files>
src/components/supervisor/views.jsx
  </files>

  <read_first>
- `.planning/phases/12-critical-confirmations/12-CONTEXT.md` קטע `<findings>` §2 המלא (זרימת פרסום/ביטול-הפצה, ארבע נקודות-UI, הבאג הידוע ב-Phase 13) וסעיף §5 (UI placement, נקודה א' — כפתור-הטוגל היחיד הוא גם "פרסם" וגם "בטל" תלוי מצב).
- `src/components/supervisor/views.jsx` שורות 1400-1428 (`ScheduleMgmt` — שלושת כפתורי הפרסום: `publishAll` שורה 1414, `unpublishAll` שורה 1419, `dayShifts` toggle שורות 1457-1463), 1443-1445 (`allPub` מחושב per-day, בתוך ה-`map`).
- `src/lib/terms.js` שורות 62-66 (`action.publishAll`/`action.unpublish`/`action.publishDay`/`action.unpublishShort` — ארבע התוויות הקיימות, civil ו-army שונות).
  </read_first>

  <action>
ב-`ScheduleMgmt`: הוסף `const [confirmState, setConfirmState] = useState(null);`. הוסף פונקציית עזר משותפת אחת, `askPublish`, כדי שלוגיקת-הפתיחה לא תשוכפל שלוש פעמים (12-CONTEXT.md §5, נקודה ב'):
```
const askPublish = ({ ids, publish, scopeLabel, confirmLabel }) => {
  setConfirmState({
    title: publish ? "לפרסם את הסידור?" : "לבטל את הפרסום?",
    body: publish
      ? `${scopeLabel} ${t("unit.shifts")} ייראו מיד אצל כל ${t("noun.memberPlural")} המשובצים.`
      : `${scopeLabel} ${t("unit.shifts")} יחזרו למצב טיוטה — ${t("noun.memberPlural")} לא יראו אותן יותר כמפורסמות.`,
    confirmLabel,
    tone: publish ? "accent" : "outline",
    onConfirm: () => actions.publish(ids, publish),
  });
};
```
עדכן את שלוש נקודות-הקריאה:
- שורה 1414 (`publishAll`): `onClick={() => askPublish({ ids: allIds, publish: true, scopeLabel: "כל השבוע", confirmLabel: t("action.publishAll") })}`.
- שורה 1419 (`unpublishAll`): `onClick={() => askPublish({ ids: allIds, publish: false, scopeLabel: "כל השבוע", confirmLabel: t("action.unpublish") })}`.
- שורה 1460 (טוגל-יום): `onClick={() => askPublish({ ids: dayShifts.map((s) => s.id), publish: !allPub, scopeLabel: formatDateHe(date), confirmLabel: allPub ? t("action.unpublishShort") : t("action.publishDay") })}` — `!allPub`/`confirmLabel` נגזרים מ-`allPub` **בזמן הלחיצה עצמה** (הערך הפנימי של ה-`map`-closure באותו רגע), לא מתווית קבועה על הכפתור — זה פותר במפורש את הנקודה שסומנה ב-12-CONTEXT.md §5(א): הכפתור הזה הוא גם "פרסם" וגם "בטל" תלוי מצב.

הוסף רינדור `<ConfirmDialog open={Boolean(confirmState)} onClose={() => setConfirmState(null)} onConfirm={confirmState?.onConfirm} title={confirmState?.title} body={confirmState?.body} confirmLabel={confirmState?.confirmLabel} tone={confirmState?.tone} busy={busy} />` בסוף ה-JSX של `ScheduleMgmt`.

אל תיגע בזרימת ה-API/state שמתחת ל-`actions.publish` — `12-CONTEXT.md` §2 מדגיש שהבאג הידוע ("ביטול הפצה") הוא ב-Phase 13, לא כאן. המשימה הזו רק גודרת את הקריאה הקיימת (המבוגגת) מאחורי דיאלוג — אל תנסה "לתקן" את הבאג, ואל תשנה שום קוד ב-`useGuardian.js`/`api.js`.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/views.jsx','utf8'); if(!/askPublish/.test(s)) throw new Error('missing shared askPublish helper'); const count=(s.match(/askPublish\\(/g)||[]).length; if(count<4) throw new Error('askPublish must be defined once and called from all 3 publish/unpublish buttons (expected >=4 occurrences incl. definition)'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/views.jsx','utf8'); const i=s.indexOf('allPub ? t(\"action.unpublishShort\")'); if(i<0) throw new Error('day-toggle confirmLabel must derive from allPub at click time'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, פתח "בניית שבוע" עם משמרות בשבוע, עבור לשלב "שליחה לצוות" (ScheduleMgmt). אמת: (1) "פרסם הכל" פותח דיאלוג עם טקסט פרסום (טון לא-danger); (2) אחרי פרסום, "בטל פרסום" פותח דיאלוג עם טקסט ביטול; (3) כפתור-הטוגל היומי מציג "פרסם יום" כשהיום לא-מפורסם ו"בטל" כשהוא מפורסם — ובשני המקרים הדיאלוג שנפתח מתאר את הפעולה הנכונה (לא הפוכה); (4) ביטול/Escape על כל אחד מהשלושה לא משנה כלום — מאומת ברענון דף.</human-check>
  </verify>

  <acceptance_criteria>
- `askPublish` מוגדרת פעם אחת, נקראת משלושת כפתורי הפרסום
- טקסט/כפתור-האישור של הטוגל היומי נגזרים מ-`allPub` בזמן הלחיצה
- אין שינוי בקוד `useGuardian.js`/`api.js` — רק גייטינג UI
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן על ארבע הנקודות
  </acceptance_criteria>

  <done>
שלושת כפתורי הפרסום/ביטול-פרסום ב-ScheduleMgmt עוברים דרך דיאלוג אישור משותף, בלי שינוי מנגנון.
  </done>

  <reversibility rating="reversible">
עיטוף UI + helper משותף. חזרה = revert לקומיט.
  </reversibility>
</task>

<task type="auto">
  <name>Task 3: "מלא שבוע" (CONFIRM-05) + הסרת אדם מהצוות (CONFIRM-05)</name>

  <files>
src/components/supervisor/views.jsx
  </files>

  <read_first>
- Plan 12-02 Task 2 (`actions.replaceShifts(ids, rows)` — בלי `label`, כותבת מיד) ו-Task 2 (שם) (`actions.removeGuard(id)` — כותבת מיד).
- `src/components/supervisor/views.jsx` שורות 499-524 (`fillWeek` הקיימת, כולל בניית `rows`), 618-640 (מסלול `EmptyState`'s `choices` שקורא ל-`fillWeek` כששבוע **ריק** — `weekShifts.length === 0` שם, תמיד), 2668-2680 (`TeamView` חתימה), 2971-2977 (כפתור-הפח `actions.removeGuard(g.id)`).
  </read_first>

  <action>
ב-`ShiftMgmt` (אותו `confirmState`/`setConfirmState` מ-Task 1): שנה את `fillWeek` כך שהיא מבקשת אישור **רק אם יש כבר תוכן לדרוס** — שבוע ריק (המקרה שקורה מתוך `EmptyState`'s choices) הוא יצירה טהורה, לא דריסה, ואינו צריך דיאלוג:
```
const fillWeek = (pattern) => {
  const rows = [...]; // בניית rows נשארת זהה
  const apply = () => actions.replaceShifts(weekShifts.map((s) => s.id), rows);
  if (weekShifts.length > 0) {
    setConfirmState({
      title: "להחליף את תוכן השבוע?",
      body: `${weekShifts.length} ${t("unit.shifts")} קיימות (כולל שיבוצים) יימחקו, ו-${rows.length} ${t("unit.shifts")} חדשות יוצרו במקומן.`,
      confirmLabel: "החלף שבוע",
      tone: "danger",
      onConfirm: apply,
    });
  } else {
    apply();
  }
  setShowFill(false);
};
```
זו לא הקלה בסקופ CONFIRM-05 — ה-"דריסה" שהדרישה מזכירה כדוגמה קיימת רק כש-`weekShifts.length > 0`; מילוי שבוע ריק אינו דורס שום דבר.

ב-`TeamView`: הוסף `const [guardToRemove, setGuardToRemove] = useState(null);`. שנה את `onClick={() => actions.removeGuard(g.id)}` (שורה 2976) ל-`onClick={() => setGuardToRemove(g)}`. הוסף רינדור `<ConfirmDialog open={Boolean(guardToRemove)} onClose={() => setGuardToRemove(null)} onConfirm={() => actions.removeGuard(guardToRemove.id)} title={guardToRemove ? \`להסיר את ${guardToRemove.name} מהצוות?\` : ""} body={\`${guardToRemove?.name || ""} יוסר/תוסר מהצוות, כולל השיבוצים, הזמינות ובקשות ההחלפה שדיווח/ה. הפעולה לא הפיכה.\`} confirmLabel="הסר מהצוות" tone="danger" busy={busy} />` ליד שאר ה-`Modal`ים בסוף ה-JSX של `TeamView` (ליד `SeedDemoDialog`).
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/views.jsx','utf8'); const i=s.indexOf('const fillWeek'); const body=s.slice(i,i+700); if(!/weekShifts\\.length > 0/.test(body)) throw new Error('fillWeek must gate confirmation on existing content'); if(!/setConfirmState/.test(body)) throw new Error('fillWeek must open the confirm dialog when overwriting'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/views.jsx','utf8'); if(!/guardToRemove/.test(s)) throw new Error('TeamView must gate removeGuard behind guardToRemove confirm state'); if(/onClick=\\{\\(\\) => actions\\.removeGuard\\(g\\.id\\)\\}/.test(s)) throw new Error('removeGuard must no longer be called directly from the trash button onClick'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`. (1) בשבוע עם משמרות קיימות, לחץ "מלא שבוע" ובחר תבנית — אמת שנפתח דיאלוג "להחליף את תוכן השבוע?" לפני שנקרה שינוי; ביטול לא משנה כלום (רענן ואמת). (2) בצוות חדש/ריק (0 משמרות), אמת שאחד מכפתורי ה-EmptyState ("3 משמרות ביום" וכו') יוצר משמרות **מיד, בלי דיאלוג** — כי אין מה לדרוס. (3) ב-TeamView, לחץ על כפתור-הפח ליד מישהו מהצוות — אמת שנפתח דיאלוג עם שם האדם; ביטול לא מסיר אותו (רענן ואמת); אישור מסיר אותו לצמיתות.</human-check>
  </verify>

  <acceptance_criteria>
- `fillWeek` פותחת דיאלוג רק כשיש תוכן קיים לדרוס; שבוע ריק ממשיך ליצור מיד
- כפתור-הפח ב-TeamView פותח דיאלוג לפני `actions.removeGuard`
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן על שלוש הנקודות
  </acceptance_criteria>

  <done>
"מלא שבוע" (כשדורס) והסרת אדם מהצוות עוברות דרך `ConfirmDialog` — שתי הפעולות המקודמות הנוספות של CONFIRM-05 שגרות ב-`views.jsx`.
  </done>

  <reversibility rating="reversible">
עיטוף UI + תנאי-נראות על דיאלוג קיים. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| מנהל מחובר → ConfirmDialog → actions.deleteShifts/replaceShifts/publish/removeGuard | ארבע פעולות שכבר עוברות RLS+auth בשרת (ללא שינוי) מקבלות שער UI נוסף — לא גבול-אמון חדש, רק חיכוך מכוון לפני כתיבה בלתי-הפיכה |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03-A | Repudiation | לחיצה על "אישור" פעמיים ברצף (double-submit) | low | mitigate | `ConfirmDialog`'s `pending`/`busy` מנטרלים את כפתור-האישור בזמן שהכתיבה בתהליך (12-01) — לא קוד חדש כאן |
| T-12-03-B | Tampering | `confirmState` נושא `ids`/`onConfirm` שנתפסו בזמן הלחיצה הראשונה, אבל הנתונים משתנים (realtime) לפני האישור בפועל | low | accept | חלון קצר בין פתיחת דיאלוג לאישור; אם משמרת נמחקה ע"י מישהו אחר בינתיים, `api.deleteShifts`/`publish` כבר מטפלים ב-0-שורות-שהושפעו (`.select()`+ספירה) ומדווחים שגיאה — לא כתיבה שקטה שגויה |
| T-12-03-C | Elevation of Privilege | עקיפת ה-UI וקריאה ישירה ל-`actions.X` מה-console | low | accept | ההגנה האמיתית היא RLS בשרת (ללא שינוי בפאזה זו); הדיאלוג הוא UX-safety, לא security boundary — תואם למגבלת-הסמכות של המתכנן (אין שינוי הרשאות בפאזה זו) |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים אחרי כל אחת משלוש המשימות
2. "מחק שבוע", שלושת כפתורי פרסום/ביטול-פרסום, "מלא שבוע" (כשדורס), והסרת-אדם — כולם עוברים דרך `ConfirmDialog`
3. "מלא שבוע" על שבוע ריק לא פותח דיאלוג
4. אין שינוי בקוד `useGuardian.js`/`api.js` בתוכנית זו — רק גייטינג UI
5. אימות חי בדפדפן על כל אחת משלוש המשימות
</verification>

<success_criteria>
- CONFIRM-02, CONFIRM-04: פרסום/ביטול-פרסום עוברים דרך הדיאלוג משלוש נקודות-הכניסה שחיות ב-views.jsx
- CONFIRM-03 (חלק): "מחק שבוע" עובר דרך הדיאלוג, כותב מיד
- CONFIRM-05 (חלק): "מלא שבוע" (כשדורס) והסרת-אדם עוברים דרך הדיאלוג
</success_criteria>

<output>
Create `.planning/phases/12-critical-confirmations/12-03-SUMMARY.md` when done.
</output>
