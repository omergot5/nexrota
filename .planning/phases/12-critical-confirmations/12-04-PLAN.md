---
phase: 12-critical-confirmations
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - src/components/supervisor/WeekFlow.jsx
autonomous: true
requirements: [CONFIRM-02, CONFIRM-03, CONFIRM-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "כפתור ה-CTA 'הסידור מוכן — לשלוח לצוות' בשלב 'שליחה לצוות' של WeekFlow פותח ConfirmDialog לפני actions.publish(...,true) — נקודת-הכניסה הרביעית לפרסום (12-CONTEXT.md findings §2)."
    - "כפתור 'מחק נתוני הדגמה לשבוע זה' ב-WeekFlow פותח ConfirmDialog לפני actions.deleteDemoDataForWeek, שכותבת מיד (12-02) — בלי UndoBar אחרי."
  artifacts: []
  key_links:
    - "WeekFlow action[4].onClick → local confirmState → ConfirmDialog → actions.publish(weekShifts.map(s=>s.id), true)"
    - "WeekFlow demo-cleanup button → local confirmState → ConfirmDialog → actions.deleteDemoDataForWeek(weekDates)"
---

<objective>
מחווט את `ConfirmDialog` (12-01) לשתי נקודות-הכניסה שחיות ב-`WeekFlow.jsx`: כפתור ה-CTA לפרסום בשלב האחרון של הזרימה (נקודת-הכניסה הרביעית לפרסום, כפולה-פעולה ל-`ScheduleMgmt`'s "פרסם הכל" אבל נקודת-UI נפרדת — 12-CONTEXT.md `<findings>` §2) וכפתור "מחק נתוני הדגמה לשבוע זה" (Phase 11, INLINE-03, CONFIRM-03).

Purpose: משלים את CONFIRM-02/04 (הנקודה הרביעית) ו-CONFIRM-03 (הפעולה הנקובה השנייה).
Output: `WeekFlow.jsx` עם שני הכפתורים מחווטים ל-`ConfirmDialog`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-confirmations/12-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/supervisor/WeekFlow.jsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: CTA-בר פרסום מקצה-לקצה דרך ConfirmDialog (CONFIRM-02/04)</name>

  <files>
src/components/supervisor/WeekFlow.jsx
  </files>

  <read_first>
- `.planning/phases/12-critical-confirmations/12-CONTEXT.md` קטע `<findings>` §2 (ארבע נקודות-הכניסה לפרסום — WeekFlow.jsx:292 היא #4, כפולה ל-#1 מבחינת הפעולה עצמה אבל נקודת-UI נפרדת שחייבת חיווט משלה).
- `src/components/supervisor/WeekFlow.jsx` שורות 22-24 (imports מ-`../ui.jsx` — כרגע `{ Btn, PrimaryAction, Segmented }`, `ConfirmDialog` חסרה), 55-58 (חתימת `WeekFlow`), 288-300 (`action[4]` — ה-CTA "הסידור מוכן — לשלוח לצוות" / `t("action.publish")`, `onClick: () => actions.publish(weekShifts.map((s) => s.id), true)`), 303-367 (ה-JSX שמרנדר `{body}` ו-`<PrimaryAction {...action} />`).
  </read_first>

  <action>
הוסף `ConfirmDialog` לרשימת ה-imports הקיימת מ-`"../ui.jsx"` (שורה 23).

הוסף `const [confirmState, setConfirmState] = useState(null);` בתוך `WeekFlow` (`useState` כבר מיובא, שורה 22).

שנה את `onClick` של `action[4]` (שורה 292, כרגע `() => actions.publish(weekShifts.map((s) => s.id), true)`) לפתיחת דיאלוג:
```
onClick: () =>
  setConfirmState({
    title: "לשלוח את הסידור לצוות?",
    body: `${weekShifts.length} ${t("unit.shifts")} ייראו מיד אצל כל ${t("noun.memberPlural")} המשובצים.`,
    confirmLabel: t("action.publish"),
    tone: "accent",
    onConfirm: () => actions.publish(weekShifts.map((s) => s.id), true),
  }),
```
`action[4]` הוא רק פרסום (אף פעם לא ביטול — הכפתור מנוטרל כש-`published === weekShifts.length`), אז אין צורך ב-helper משותף כמו `askPublish` ב-12-03 — נקודת-כניסה אחת, לא שלוש.

הוסף רינדור `<ConfirmDialog open={Boolean(confirmState)} onClose={() => setConfirmState(null)} onConfirm={confirmState?.onConfirm} title={confirmState?.title} body={confirmState?.body} confirmLabel={confirmState?.confirmLabel} tone={confirmState?.tone} busy={busy} />` בתוך ה-`return` הראשי של `WeekFlow`, אחרי `<PrimaryAction {...action} />` — מופע יחיד, משותף גם ל-Task 2 (כפתור מחיקת-הדגמה).

אל תיגע בשום קוד קשור ל-publish מתחת ל-`actions.publish` עצמה — הבאג הידוע ("ביטול הפצה") הוא Phase 13, לא כאן, וממילא `action[4]` הזו אף פעם לא קוראת ל-`publish(ids, false)`.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/WeekFlow.jsx','utf8'); if(!/ConfirmDialog/.test(s.slice(0,1200))) throw new Error('ConfirmDialog must be imported from ../ui.jsx'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/WeekFlow.jsx','utf8'); const i=s.indexOf('children: hasShifts && published'); const body=s.slice(i,i+700); if(!/setConfirmState/.test(body)) throw new Error('the publish CTA onClick must open the confirm dialog'); if(/onClick: \\(\\) => actions\\.publish/.test(body)) throw new Error('the publish CTA must not call actions.publish directly anymore'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, פתח "בניית שבוע" עם משמרות ולפחות שיבוץ אחד, נווט עד שלב "שליחה לצוות" (הכי ימני בפס השלבים) בלי ללחוץ שם על שום כפתור-פרסום פנימי. לחץ על כפתור ה-CTA התחתון ("הסידור מוכן — לשלוח לצוות"). אמת: (1) נפתח דיאלוג אישור לפני שנקרה שינוי; (2) ביטול לא משנה כלום — רענן דף ואמת; (3) אישור מפרסם את השבוע מיד; (4) רענן — אמת שהמשמרות מפורסמות בשרת, ושה-CTA משתנה בהתאם (מציג "הסידור אצל הצוות").</human-check>
  </verify>

  <acceptance_criteria>
- `ConfirmDialog` מיובא ב-`WeekFlow.jsx`
- ה-CTA לפרסום פותח דיאלוג; רק `onConfirm` קורא ל-`actions.publish`
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן על ארבע הנקודות
  </acceptance_criteria>

  <done>
נקודת-הכניסה הרביעית לפרסום (CTA-בר) עוברת דרך `ConfirmDialog` — כל ארבע נקודות הפרסום/ביטול-פרסום מחווטות (יחד עם 12-03).
  </done>

  <reversibility rating="reversible">
עיטוף UI + שינוי onClick אחד. חזרה = revert לקומיט.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: "מחק נתוני הדגמה לשבוע זה" דרך ConfirmDialog (CONFIRM-03)</name>

  <files>
src/components/supervisor/WeekFlow.jsx
  </files>

  <read_first>
- `.planning/phases/12-critical-confirmations/12-CONTEXT.md` קטע `<findings>` §3 (הממצא האדריכלי המרכזי — פרה-אישור מחליף UndoBar).
- `src/components/supervisor/WeekFlow.jsx` שורות 100-103 (`demoIds` — `useMemo` שכבר קיים, שער-הנראות של הכפתור), 161-176 (הכפתור עצמו + ההערה שמעליו — שים לב: ההערה **כרגע קובעת במפורש** "בלי דיאלוג אישור, UndoBar בלבד (עקרון ברזל 3)" — זו בדיוק ההערה שהופכת שגויה בתוכנית הזו וחייבת להתעדכן, לא רק הקוד תחתיה).
- Plan 12-02 Task 2 (`actions.deleteDemoDataForWeek(weekDates)` — כותבת מיד, בלי `deferred()`, שער-DoS פנימי `if (!ids.length) return;`).
  </read_first>

  <action>
ב-`WeekFlow` (אותו `confirmState`/`setConfirmState` מ-Task 1): שנה את `onClick` של כפתור "מחק נתוני הדגמה לשבוע זה" (שורה ~170, כרגע `() => actions.deleteDemoDataForWeek(weekDates)`) ל:
```
onClick: () =>
  setConfirmState({
    title: "למחוק את נתוני ההדגמה של השבוע?",
    body: `${demoIds.length} רשומות הדגמה יימחקו מהשבוע הזה. שיבוצים אמיתיים באותו שבוע ונתוני הדגמה בשבועות אחרים לא ייפגעו.`,
    confirmLabel: "מחק נתוני הדגמה",
    tone: "danger",
    onConfirm: () => actions.deleteDemoDataForWeek(weekDates),
  }),
```
(שים לב: זה `onClick` ישיר על `<Btn>`, לא אובייקט-`action` כמו ב-Task 1 — עטוף בפונקציית-חץ, לא `onClick=` שטוח).

עדכן את גוף-ההערה מעל הכפתור (כרגע מתארת את המוסכמה הישנה — "בלי דיאלוג אישור, UndoBar בלבד") לתאר את המצב החדש: הפעולה עברה לפרה-אישור (CONFIRM-03), כותבת מיד אחרי אישור, בלי UndoBar יותר. אל תשאיר את הניסוח הישן כפי שהוא.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/WeekFlow.jsx','utf8'); const i=s.indexOf('demoIds.length > 0'); const body=s.slice(i,i+700); if(!/setConfirmState/.test(body)) throw new Error('the demo-cleanup button must open the confirm dialog'); if(/onClick=\\{\\(\\) => actions\\.deleteDemoDataForWeek\\(weekDates\\)\\}/.test(body)) throw new Error('the button must no longer call actions.deleteDemoDataForWeek directly'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>על שבוע עם גם שיבוץ אמיתי וגם נתוני-הדגמה (הפעל "הדגמה" אם צריך), פתח שלב "השבוע" ב-WeekFlow. אמת: (1) כפתור "מחק נתוני הדגמה לשבוע זה" פותח דיאלוג לפני שנקרה שינוי; (2) ביטול לא משנה כלום — רענן ואמת; (3) אישור מוחק את נתוני ההדגמה מיד, בלי UndoBar; (4) רענן — אמת שהשיבוץ האמיתי שרד ושנתוני ההדגמה נמחקו בשרת.</human-check>
  </verify>

  <acceptance_criteria>
- כפתור מחיקת-ההדגמה פותח דיאלוג; רק `onConfirm` קורא ל-`actions.deleteDemoDataForWeek`
- ההערה הישנה ("בלי דיאלוג אישור") עודכנה
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן על ארבע הנקודות
  </acceptance_criteria>

  <done>
"מחק נתוני הדגמה לשבוע זה" עובר דרך `ConfirmDialog`, כותב מיד — הפעולה הנקובה השנייה של CONFIRM-03 מחווטת.
  </done>

  <reversibility rating="reversible">
עיטוף UI + עדכון הערה. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| מנהל מחובר → ConfirmDialog → actions.publish/deleteDemoDataForWeek | שתי פעולות שכבר עוברות RLS+auth בשרת (ללא שינוי) מקבלות שער UI נוסף |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04-A | Repudiation | לחיצה כפולה על כפתור-האישור בזמן כתיבה בתהליך | low | mitigate | `ConfirmDialog`'s `pending`/`busy` (12-01) — לא קוד חדש כאן |
| T-12-04-B | Information Disclosure | `demoIds.length` בגוף הדיאלוג חושף מספר-רשומות לפני מחיקה | low | accept | מידע לגיטימי למנהל שכבר רואה את השבוע כולו — לא חשיפה מעבר למה שכבר גלוי במסך |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים אחרי כל אחת משתי המשימות
2. CTA-פרסום וכפתור מחיקת-הדגמה עוברים דרך `ConfirmDialog`
3. אין שינוי בקוד `useGuardian.js`/`api.js`
4. אימות חי בדפדפן על כל אחת משתי המשימות
</verification>

<success_criteria>
- CONFIRM-02, CONFIRM-04: נקודת-הכניסה הרביעית לפרסום מחווטת — כל ארבע נקודות הפרסום (עם 12-03) עוברות דרך הדיאלוג
- CONFIRM-03: שתי הפעולות הנקובות (מחק שבוע, מחק נתוני הדגמה) מחווטות במלואן (עם 12-03)
</success_criteria>

<output>
Create `.planning/phases/12-critical-confirmations/12-04-SUMMARY.md` when done.
</output>
