---
phase: 12-critical-confirmations
plan: 06
type: execute
wave: 3
depends_on: ["12-03", "12-04", "12-05"]
files_modified:
  - CLAUDE.md
  - .claude/CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [CONFIRM-01, CONFIRM-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CLAUDE.md (root) עקרון ברזל 3, .claude/CLAUDE.md (Dependency Patterns + Architectural Constraints), ו-.planning/PROJECT.md (Validated) מתארים את שני המסלולים (UndoBar כברירת מחדל לרוב הפעולות ההרסניות; פרה-אישור לרשימה סגורה ומפורשת) — לא סותרים את הקוד אחרי Wave 2."
    - "לחיצה על 'ביטול' בכל אחד מדיאלוגי האישור שנוספו בפאזה, ואז רענון דף, משאירה את הנתונים בדיוק כפי שהיו לפני הלחיצה על הכפתור שפתח את הדיאלוג — מאומת חי על לפחות שתי פעולות שונות (מחיקה ופרסום)."
    - "סריקת קוד מאשרת שכל דיאלוגי האישור בפרויקט בנויים על ConfirmDialog האחד (ui.jsx) — אין רכיב-אישור מקביל/כפול בשום מסך (CONFIRM-01 קריטריון הצלחה 1)."
  artifacts: []
  key_links:
    - "12-CONTEXT.md finding §5 → root CLAUDE.md line ~26 (עקרון 3), .claude/CLAUDE.md lines ~91 (Dependency Patterns) ו-~169 (Architectural Constraints), PROJECT.md line ~48 (Validated)"
---

<objective>
תוכנית הסגירה של הפאזה: (1) מעדכנת את שלושת מיקומי התיעוד שהקוד עכשיו סותר — `CLAUDE.md` (root) עקרון ברזל 3, `.claude/CLAUDE.md`, ו-`.planning/PROJECT.md` — כדי שהתיעוד יתאר נאמנה את שני המסלולים שקיימים עכשיו (UndoBar כברירת מחדל; פרה-אישור לרשימה סגורה של שמונה פעולות). (2) מריצה סבב אימות-אינטגרציה מלא שחוצה את כל חמש התוכניות הקודמות: CONFIRM-06 (ביטול = בלי אפקט-צד, מאומת ברענון דף) על פני כמה דיאלוגים, ו-CONFIRM-01 קריטריון-הצלחה 1 (אין לוגיקת-אישור כפולה בשום מסך — סריקת קוד).

זו התוכנית היחידה בפאזה שלא נוגעת בשום קובץ `.jsx`/`.js` — רק תיעוד + אימות.

Purpose: CONFIRM-01 (סגירה) + CONFIRM-06 — ותיעוד שלא מסתיר את מה שהקוד עושה, בדיוק כמו ש-Core Value של הפרויקט דורש ("מה שהמערכת אומרת על עצמה נכון").
Output: שלושה קבצי תיעוד מעודכנים, וסבב-אימות מלא מתועד ב-SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-critical-confirmations/12-CONTEXT.md
@.planning/phases/12-critical-confirmations/12-02-SUMMARY.md

@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: עדכון שלושת מיקומי התיעוד שהקוד סותר (12-CONTEXT.md finding §5)</name>

  <files>
CLAUDE.md
.claude/CLAUDE.md
.planning/PROJECT.md
  </files>

  <read_first>
- `.planning/phases/12-critical-confirmations/12-CONTEXT.md` קטע `<findings>` §5 המלא — שלושת המיקומים המדויקים, כולל הציטוט הנוכחי המדויק של כל אחד ומה בדיוק צריך להשתנות (לא ניסוח-מחדש מלא בשום מקום).
- `CLAUDE.md` (root), שורה ~26, טבלת "עקרונות ברזל", עיקרון #3 — הניסוח הנוכחי.
- `.claude/CLAUDE.md`, שורה ~91 (סעיף "Dependency Patterns", לא "Constraints") — השורה "Undo pattern: delay write 8 seconds, allow cancel without reverting", ושורה ~169 (סעיף "Architectural Constraints", "Offline-read only") — אזכור `optimistic()`/`deferred()` כשני מנגנוני-הכתיבה היחידים.
- `.planning/PROJECT.md`, שורה ~48, סעיף "Validated" — השורה "✓ ביטול פעולה במקום דיאלוג אישור, RTL מלא, מצב בהיר/כהה/מערכת — existing" (שלושה נושאים באותה שורה — לגעת רק בחלק הראשון).
  </read_first>

  <action>
ב-`CLAUDE.md` (root): עדכן עיקרון ברזל #3 בטבלת "עקרונות ברזל" מ"ביטול במקום אישור — הפעולה מתבצעת מיד, עם `UndoBar` של 8 שניות. בלי `confirm()`." לניסוח שמתאר שני מסלולים: UndoBar כברירת-מחדל לרוב הפעולות ההרסניות, ופרה-אישור (`ConfirmDialog`) לרשימה סגורה ומפורשת — פרסום/ביטול-פרסום, מחיקת שבוע, מחיקת נתוני הדגמה, מחיקת עמדה, החלפת-תוכן-שבוע (דריסה), והסרת אדם מהצוות. הפנה לטבלת-הביקורת ב-`useGuardian.js` (12-02) כמקור-האמת המלא, במקום למנות את כל שמונה הפעולות בטבלת עקרונות-הברזל עצמה (שנשארת אינדקס, לא פירוט).

ב-`.claude/CLAUDE.md`: בסעיף "Dependency Patterns", עדכן את השורה "Undo pattern: delay write 8 seconds, allow cancel without reverting" להוסיף משפט קצר: פעולות מסוימות (רשימה סגורה) עוברות פרה-אישור במקום UndoBar — ר' `CLAUDE.md` עקרון 3. בסעיף "Architectural Constraints", תחת "Offline-read only", עדכן את האזכור ל-`optimistic()`/`deferred()` כשני מנגנוני-הכתיבה היחידים — הוסף `run()` (כתיבה מיידית בלי חלון-ביטול, המסלול השלישי שפעולות מאחורי `ConfirmDialog` משתמשות בו).

ב-`.planning/PROJECT.md`: פצל את השורה בסעיף "Validated" — "✓ ביטול פעולה במקום דיאלוג אישור, RTL מלא, מצב בהיר/כהה/מערכת — existing" — כך שחלק ה-RTL/מצב-תצוגה נשאר "existing" בלי שינוי, וחלק ה"ביטול-במקום-אישור" מתעדכן לתאר את שני המסלולים (או מפוצל לשורה נפרדת עם "Phase 12"). אל תיגע בשום חלק אחר של הקובץ.

בכל שלושת הקבצים: שמור על הטון והפורמט הקיימים (טבלאות Markdown, עברית), אל תוסיף פירוט חדש שלא נדרש — ההנחיה המפורשת ב-`CLAUDE.md` (root) עצמו היא "CLAUDE.md לא גדל. תוכן חדש הולך ל-docs/".
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('CLAUDE.md','utf8'); if(/בלי .?confirm\(\).?\./.test(s) && !/פרה-אישור/.test(s)) throw new Error('root CLAUDE.md still claims a blanket no-confirm() rule with no mention of the pre-confirm track'); if(!/פרה-אישור/.test(s)) throw new Error('root CLAUDE.md must mention the pre-confirm track'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('.claude/CLAUDE.md','utf8'); if(!/פרה-אישור/.test(s)) throw new Error('.claude/CLAUDE.md Dependency Patterns must mention the pre-confirm track'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('.planning/PROJECT.md','utf8'); if(/ביטול פעולה במקום דיאלוג אישור, RTL מלא/.test(s)) throw new Error('PROJECT.md Validated line must be split — the blanket cancel-instead-of-confirm claim can no longer sit unqualified next to RTL/theme'); console.log('ok')"</automated>
  </verify>

  <acceptance_criteria>
- שלושת הקבצים מתארים שני מסלולים (UndoBar כברירת מחדל; פרה-אישור לרשימה סגורה), לא כלל גורף אחד
- RTL/מצב-תצוגה ב-PROJECT.md נשארו ללא שינוי
- שום קובץ לא גדל בפירוט מעבר לנדרש
  </acceptance_criteria>

  <done>
שלושת מיקומי התיעוד תואמים את הקוד אחרי Wave 2 — אין יותר סתירה בין CLAUDE.md לבין מה שהמערכת עושה בפועל.
  </done>

  <reversibility rating="reversible">
עריכת תיעוד בלבד. חזרה = revert לקומיט.
  </reversibility>
</task>

<task type="tracer">
  <name>Task 2: סבב אימות-אינטגרציה סוגר — CONFIRM-06 + CONFIRM-01 קריטריון 1</name>

  <files>
אין — משימת אימות בלבד, לא עורכת קבצים (קוראת מ-src/components/ לצורך סריקת-הקוד ב-verify)
  </files>

  <read_first>
- `.planning/ROADMAP.md` Phase 12, קריטריוני-הצלחה 1-4 — הניסוח המדויק, במיוחד קריטריון 4 ("לחיצה על 'בטל' בדיאלוג משאירה את המערכת בדיוק במצב הקודם — מאומת ברענון דף אחרי ביטול, לא רק במראה המסך").
- `.planning/phases/12-critical-confirmations/12-CONTEXT.md` קטע `<decisions>` — אישור CONFIRM-06: "אין 'ביטול' אמיתי נדרש... 'ביטול' פשוט מבטל את הקריאה".
- SUMMARY-ים של 12-01 עד 12-05 (אם קיימים בזמן ריצת התוכנית הזו) — לוודא שכל חמש התוכניות אכן נחתמו לפני הסבב הזה (`depends_on` כבר אוכף את זה ברמת ה-wave, אבל שווה קריאה לפני הסבב).
  </read_first>

  <action>
בצע סבב אימות-אינטגרציה בדפדפן (`npm run dev`) שחוצה את כל חמש התוכניות הקודמות — זה החלק המרכזי של המשימה, לא רק תיעוד:

1. **CONFIRM-06 על פני שתי פעולות שונות** (לא רק אחת — כדי לא לפספס באג ספציפי-לפעולה): בחר פעולת-מחיקה אחת (למשל "מחק שבוע") ופעולת-פרסום אחת (למשל "פרסם הכל"). לכל אחת: פתח את הדיאלוג, לחץ "ביטול", **רענן את הדף** (לא רק תסתכל על המסך — CONFIRM-06 דורש רענון בפירוש), ואמת שהנתונים בדיוק כמו שהיו לפני הלחיצה על הכפתור שפתח את הדיאלוג.
2. **סריקת-קוד ל-CONFIRM-01 קריטריון 1**: חפש בכל `src/components/` אחר כל רכיב-Modal/דיאלוג-אישור שאינו `ConfirmDialog` (`ui.jsx`) — ודא שאין רכיב-אישור מקביל/כפול (`SeedDemoDialog` ממשיך להתקיים כרכיב-קלט נפרד, לא רכיב-אישור — הוא לא מתחרה ב-`ConfirmDialog`, וזה תקין; ר' ROADMAP Phase 9 הערה מפורשת "אלה שני דברים שונים"). תעד את ממצא הסריקה ב-SUMMARY.
3. **עקביות ניסוח**: עבור חי על כל שישה-שבעה הדיאלוגים שנוספו (פרסום/ביטול-פרסום, מחיקת-שבוע, מחיקת-הדגמה, מלא-שבוע, הסרת-אדם, מחיקת-עמדה × 2) ואמת שכל אחד מציג כותרת+גוף ברורים, כפתור-אישור עם טון מתאים (danger למחיקות, accent/outline לפרסום), וכפתור-ביטול.
4. **בדיקת-רגרסיה קצרה**: אמת ש-`deleteShift`/`deleteTask`/`decideSwap`/`removeRoleCompatibility` (הפעולות שנשארו/הפכו ל-UndoBar) **לא** מציגות דיאלוג-אישור בטעות — הן עדיין לחיצה-מיידית-עם-UndoBar, בדיוק כמו לפני הפאזה (חוץ מ-`removeRoleCompatibility` שעכשיו *כן* מקבלת UndoBar לראשונה — אמת שהיא מציגה UndoBar, לא דיאלוג).

תעד ב-SUMMARY את ממצא כל אחת מארבע הנקודות בנפרד, כפי שנראו בפועל — לא רק "עובד".
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const path=require('path'); const dirs=['src/components','src/components/supervisor']; let confirmLike=[]; for (const dir of dirs) { for (const f of fs.readdirSync(dir)) { if(!f.endsWith('.jsx')) continue; const s=fs.readFileSync(path.join(dir,f),'utf8'); const matches = s.match(/function \w*(Confirm|AreYouSure)\w*\(/gi) || []; confirmLike.push(...matches.map((m)=>f+':'+m)); } } const unexpected = confirmLike.filter((m) => !m.includes('ConfirmDialog')); if (unexpected.length) throw new Error('found confirm-like components outside ConfirmDialog: '+unexpected.join(', ')); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>בצע את ארבע הנקודות ב-action, בסדר: (1) CONFIRM-06 על מחיקה ועל פרסום, מאומת ברענון דף אחרי כל ביטול; (2) סריקת-קוד ל"אין רכיב-אישור כפול"; (3) עקביות ניסוח על כל הדיאלוגים; (4) `deleteShift`/`deleteTask`/`decideSwap` עדיין מיידיים-עם-UndoBar (לא דיאלוג), ו-`removeRoleCompatibility` מציגה UndoBar בפעם הראשונה. תעד כל אחת בנפרד ב-SUMMARY.</human-check>
  </verify>

  <acceptance_criteria>
- CONFIRM-06 מאומת ברענון דף על שתי פעולות שונות לפחות
- אין רכיב-אישור כפול מחוץ ל-`ConfirmDialog`
- כל הדיאלוגים עקביים בניסוח/טון
- הפעולות שנשארו UndoBar (כולל `removeRoleCompatibility` החדשה) לא מציגות דיאלוג-אישור
- `npm test` ו-`npm run build` עוברים
  </acceptance_criteria>

  <done>
הפאזה סגורה: כל שש דרישות CONFIRM-01..06 מאומתות כמכלול אחד, לא רק כל אחת בנפרד; התיעוד תואם את הקוד.
  </done>

  <reversibility rating="reversible">
אימות בלבד, בלי שינוי קוד. חזרה = לא רלוונטי.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| תיעוד (CLAUDE.md/PROJECT.md) → הנחיית-פיתוח עתידית | תיעוד שגוי הוא סיכון-תהליך, לא סיכון-ריצה — מנחה מפתח/סוכן עתידי לחשוב שהקוד עושה משהו שהוא לא עושה |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06-A | Repudiation | תיעוד ממשיך לתאר "בלי confirm() לעולם" אחרי שהקוד כבר סותר את זה | medium | mitigate | Task 1 מעדכן את שלושת המיקומים המדויקים שאותרו ב-12-CONTEXT.md §5, מאומת ב-grep שהניסוח הגורף הישן לא נשאר בלי הקשר |
| T-12-06-B | Repudiation | "עובד" מוצהר בלי אימות חי (עקרון-ברזל 6, CLAUDE.md) | low | mitigate | human-check דורש רענון-דף מפורש אחרי כל ביטול, ותיעוד נפרד לכל אחת מארבע הנקודות ב-SUMMARY — לא דיווח כללי |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. שלושת קבצי התיעוד מעודכנים ותואמים את הקוד
2. CONFIRM-06 מאומת ברענון דף על שתי פעולות שונות
3. אין רכיב-אישור כפול מחוץ ל-`ConfirmDialog`
4. `npm test` ו-`npm run build` עוברים
</verification>

<success_criteria>
- CONFIRM-01 (סגירה): אין לוגיקת-אישור כפולה בשום מסך — מאומת בסריקת-קוד
- CONFIRM-06: "ביטול" משאיר את המערכת במצב הקודם בדיוק, מאומת ברענון דף על יותר מפעולה אחת
- שלושת מיקומי התיעוד תואמים את הקוד — אין יותר סתירה בין CLAUDE.md לפאזה 12
</success_criteria>

<output>
Create `.planning/phases/12-critical-confirmations/12-06-SUMMARY.md` when done.
</output>
