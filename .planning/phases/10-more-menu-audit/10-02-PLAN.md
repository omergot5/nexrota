---
phase: 10-more-menu-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SupervisorApp.jsx
  - src/components/supervisor/ResourceView.jsx
  - docs/architecture/system-overview.md
autonomous: true
requirements: [MORE-03]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "תפריט 'עוד' מציג בדיוק 4 פריטים (swaps, tasks, positions, analytics) — resources הוסר מ-moreItems() וממפת views (MORE-03)."
    - "src/components/supervisor/ResourceView.jsx נמחק מהקוד; אין אף ייבוא או אזכור שלו שנשאר בשום מקום תחת src/."
    - "ResourceGrid.jsx ו-buildResourceRows (src/lib/resourceView.js) ממשיכים לשמש את CalendarView.jsx ו-RosterWizard.jsx בלי שום שינוי — לא נוגעים בהם."
    - "יומן (CalendarView, ברירת מחדל מצב שבוע) ממשיך להציג את אותו מבנה שורת-עמדה/עמודות-יום ש-resources הציג — שום פונקציונליות לא אבדה."
    - "docs/architecture/system-overview.md לא מזכיר יותר את ResourceView.jsx כקובץ קיים או כצרכן של ResourceGrid — המסמך משקף את מבנה הקוד בפועל."
  artifacts:
    - src/components/SupervisorApp.jsx
    - docs/architecture/system-overview.md
  key_links:
    - "moreItems() (4 פריטים כעת) → 'more' view ב-SupervisorApp.jsx (MORE.map) → go(id) → views map (ללא מפתח resources)."
    - "CalendarView (מצב week, ברירת מחדל) → ResourceGrid/buildResourceRows — הנתיב היחיד הנותר לאותו נתון שresources הציג."
---

<objective>
להסיר את `resources` ("מבט משאבים") מתפריט "עוד" — פעולת ההסרה היחידה שהביקורת (10-CONTEXT.md MORE-01) מצאה: כפילות מלאה מול `CalendarView` במצב השבועי שלו (ברירת המחדל של "יומן"). הסרה כוללת: `moreItems()` ו-`views` map ב-`SupervisorApp.jsx`, מחיקת הקובץ המת שנשאר `ResourceView.jsx`, ותיקון שתי הפניות מיושנות אליו ב-`docs/architecture/system-overview.md`.

Purpose: MORE-03 (10-CONTEXT.md). "יומן" כבר מכסה את אותו נתון בדיוק, בלחיצה אחת מהניווט הראשי, ובנוסף עם זום יום/חודש ש-`ResourceView` לא הציע — אין אובדן פונקציונליות. השארת קובץ מת וקישור תיעוד לקובץ שנמחק סותרת את הליבה של המחזור הזה: "מה שהמערכת אומרת על עצמה נכון".

Output: `SupervisorApp.jsx` בלי `resources` (לא ב-`moreItems()`, לא ב-`views` map, לא ב-import), `ResourceView.jsx` נמחק, ו-`docs/architecture/system-overview.md` בלי הפניה לקובץ שנמחק.

**החלטה נעולה (10-CONTEXT.md, MORE-03) — לא נפתחת מחדש:** פעולת הסרה אחת בלבד — `resources`. `ResourceGrid.jsx` ו-`src/lib/resourceView.js` (`buildResourceRows`) עצמם **לא** נוגעים — הם ממשיכים לשמש את `CalendarView.jsx`/`RosterWizard.jsx` ישירות, לא דרך `ResourceView.jsx`. `positions` **לא** מוסר (הכרעה נפרדת, MORE-02) — נשאר בדיוק כפי שהוא.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-more-menu-audit/10-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: הסרת resources מ"עוד", מחיקת ResourceView.jsx, ועדכון מפת הארכיטקטורה (MORE-03)</name>

  <files>
src/components/SupervisorApp.jsx
src/components/supervisor/ResourceView.jsx
docs/architecture/system-overview.md
  </files>

  <read_first>
- `src/components/SupervisorApp.jsx` שורות 1-16 — imports, כולל שורה 8 (`import ResourceView from "./supervisor/ResourceView.jsx";`) שהופכת ללא-בשימוש.
- `src/components/SupervisorApp.jsx` שורות 41-47 — `moreItems()`, כולל שורת ה-`resources` (id/label/icon/hint) שיוצאת.
- `src/components/SupervisorApp.jsx` שורה 283 — מפת `views`, השורה `resources: <ResourceView guards={guards} shifts={shifts} tasks={tasks} />,` שיוצאת.
- `src/components/SupervisorApp.jsx` שורות 183-191 — ההערה הקיימת שכבר מתעדת למה `CalendarView` במצב "week" הוא התחליף המלא ל"מבט משאבים" — אישור-רקע שההחלטה כבר נלקחה בחשבון בקוד.
- `src/components/supervisor/ResourceView.jsx` — הקובץ המלא: `export default` יחיד, בלי exports נוספים; הצרכן היחיד שלו הוא `SupervisorApp.jsx`.
- `docs/architecture/system-overview.md` שורות 16-56 (עץ הספריות, כולל שורה 25 — הפניה מיושנת ל-`ResourceView.jsx`) ושורות 69-73 (§🧩 "דפוס תצוגה משותף", כולל שורה 71 — `ResourceView.jsx` ברשימת הצרכנים).
- `.planning/phases/10-more-menu-audit/10-CONTEXT.md` קטע `## MORE-01` (שורת הטבלה של resources) — הביקורת המלאה שמבססת את ההסרה.
  </read_first>

  <action>
ב-`src/components/SupervisorApp.jsx`: הסר את איבר ה-`resources` (`{ id: "resources", label: "מבט משאבים", icon: "grid", hint: "מי מאייש כל עמדה, יום־יום, במבט אחד" }`) ממערך `moreItems()` — המערך נשאר עם 4 איברים (swaps, tasks, positions, analytics), באותו סדר יחסי שהיה. הסר את השורה `resources: <ResourceView guards={guards} shifts={shifts} tasks={tasks} />,` ממפת `views`. הסר את שורת ה-import של `ResourceView` שהופכת ללא-בשימוש.

מחק לגמרי את הקובץ `src/components/supervisor/ResourceView.jsx` — לאחר ההסרות למעלה זה קוד מת: הצרכן היחיד שלו היה `SupervisorApp.jsx`. `ResourceGrid.jsx` ו-`src/lib/resourceView.js` (`buildResourceRows`) נשארים ללא שינוי — הם כבר משרתים ישירות את `CalendarView.jsx` ו-`RosterWizard.jsx`, לא דרך `ResourceView.jsx`.

ב-`docs/architecture/system-overview.md`: הסר את שורת `ResourceView.jsx` מעץ הספריות (בין `CalendarView.jsx` ל-`RosterWizard.jsx`) — הקובץ שהיא מתארת כבר לא קיים. בפסקת §🧩 ("דפוס תצוגה משותף"), עדכן את רשימת הצרכנים של `ResourceGrid` כך שתכלול רק את שני הצרכנים הנותרים בפועל — `RosterWizard.jsx` (מצב army) ו-`CalendarView.jsx` — בלי `ResourceView.jsx`, ובלי לשנות שום טענה אחרת בפסקה (הרכיב עדיין `ResourceGrid.jsx` בלבד, עדיין לא תלוי ב-`resourceView.js` בעצמו, מספר הפריטים עדיין נגזר מהנתונים).

אל תיגע ב-`CalendarView.jsx`, ב-`RosterWizard.jsx`, ב-`ResourceGrid.jsx`, או ב-`src/lib/resourceView.js` — כולם נשארים בדיוק כפי שהם, כולל הערות שמזכירות את השם ההיסטורי `ResourceView` כהסבר-רקע ארכיטקטוני (Phase 6) שנשאר נכון היסטורית ואינו טוען שהקובץ קיים היום. אל תסיר ואל תשנה את הפריט `positions` ב-`moreItems()`/`views` — הוא נשאר; רק ההחלטה נגד תוספת קיצור-דרך חדש עבורו שייכת לתוכנית האחרת (10-01-PLAN.md, MORE-02).
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/SupervisorApp.jsx','utf8'); const start=s.indexOf('const moreItems = ()'); const end=s.indexOf('const titles = ('); if(start===-1||end===-1) throw new Error('moreItems boundaries not found'); const body=s.slice(start,end); if(body.includes('resources')) throw new Error('resources entry still present in moreItems()'); const ids=(body.match(/id:\s*\"(\w+)\"/g)||[]); if(ids.length!==4) throw new Error('expected exactly 4 moreItems entries, found '+ids.length); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/SupervisorApp.jsx','utf8'); if(/\bresources:\s*</.test(s)) throw new Error('resources entry still present in views map'); if(/import\s+ResourceView\s+from/.test(s)) throw new Error('unused ResourceView import still present'); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); if(fs.existsSync('src/components/supervisor/ResourceView.jsx')) throw new Error('ResourceView.jsx still exists on disk'); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'),path=require('path'); function walk(dir,out){for(const f of fs.readdirSync(dir)){const p=path.join(dir,f);const st=fs.statSync(p);if(st.isDirectory())walk(p,out);else if(/\.(js|jsx)$/.test(f))out.push(p);}return out;} const files=walk('src',[]); const hits=files.filter(f=>fs.readFileSync(f,'utf8').includes('ResourceView')); if(hits.length) throw new Error('ResourceView still referenced in: '+hits.join(', ')); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('docs/architecture/system-overview.md','utf8'); if(s.includes('ResourceView')) throw new Error('stale ResourceView reference remains in system-overview.md'); for(const n of ['ResourceGrid','RosterWizard.jsx','CalendarView.jsx']) if(!s.includes(n)) throw new Error('expected reference missing after edit: '+n); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, היכנס כמנהל, פתח "עוד". אמת: (1) התפריט מציג בדיוק 4 כרטיסים — חילופים, משימות, עמדות קבועות, דוחות/ניתוח — בלי "מבט משאבים"; (2) פתח "יומן" מהניווט הראשי (לא מתוך "עוד") — הוא נפתח כברירת מחדל במצב "שבוע" ומציג את אותו מבנה שורת-עמדה/עמודות-יום שכרטיס "מבט משאבים" הציג קודם, בלי חוסר במידע; (3) "עמדות קבועות" עדיין נגיש ועובד מתוך "עוד", בלי שינוי.</human-check>
  </verify>

  <acceptance_criteria>
- `moreItems()` מכיל בדיוק 4 איברים (swaps, tasks, positions, analytics), בלי resources
- מפת `views` ב-`SupervisorApp.jsx` לא מכילה מפתח `resources`
- `import ResourceView` הוסר מ-`SupervisorApp.jsx`
- הקובץ `src/components/supervisor/ResourceView.jsx` נמחק מהדיסק
- אפס אזכורים למחרוזת `ResourceView` בכל עץ `src/`
- `docs/architecture/system-overview.md` לא מזכיר `ResourceView` ומשקף רק את שני הצרכנים הנותרים (`RosterWizard.jsx`, `CalendarView.jsx`)
- `ResourceGrid.jsx`, `src/lib/resourceView.js`, `CalendarView.jsx`, `RosterWizard.jsx` לא נערכו
- `positions` נשאר ללא שינוי ב-`moreItems()`/`views`
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע על שלוש הנקודות ב-`human-check`, כולל אימות שיומן מכסה את הפונקציונליות שהוסרה
  </acceptance_criteria>

  <done>
"עוד" מציג בדיוק 4 פריטים; הקובץ המת נמחק ואינו מוזכר בשום מקום, כולל התיעוד; יומן (מצב שבוע) מוכח כמכסה את הפונקציונליות שהוסרה.
  </done>

  <reversibility rating="reversible">
הסרת ניווט + מחיקת קובץ קוד + תיקון תיעוד. אין שינוי בסכימה, ב-API, או בנתיב כתיבה. חזרה = revert לקומיט (הקובץ, שורת התפריט, ושורות התיעוד חוזרים בדיוק כפי שהיו).
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| מנהל מחובר → תפריט "עוד" (4 פריטים כעת) → `go(id)` → `views` map | שינוי ניווט צד-לקוח בלבד — אין שינוי ב-API/RLS |
| מחיקת קובץ קוד → שאר עץ המקור | מחיקה שעלולה להשאיר ייבוא/הפניה יתומה במקום שנשכח |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-A | Tampering / Availability | `ResourceView.jsx` (מחיקה) | medium | mitigate | שער אוטומטי סורק רקורסיבית את כל `src/**/*.{js,jsx}` ומוודא אפס אזכורים למחרוזת `ResourceView`; `npm run build` נכשל ממילא על import שבור אם נשכח |
| T-10-02-B | Availability (אובדן פיצ'ר בטעות) | resources → יומן (תחליף) | medium | mitigate | human-check מאמת חי שיומן (מצב שבוע) מציג את אותו מבנה נתונים; 10-CONTEXT.md כבר קבע כפילות מלאה מבוססת-קוד, לא השערה |
| T-10-02-C | Repudiation (תיעוד שלא תואם קוד) | `docs/architecture/system-overview.md` | low | mitigate | שער אוטומטי מוודא שהמסמך כבר לא מזכיר `ResourceView`, ושהוא כן מזכיר את שני הצרכנים הנותרים |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים
2. `moreItems()`/`views` ב-`SupervisorApp.jsx` בלי `resources`; `ResourceView.jsx` נמחק ואינו מוזכר בשום מקום, כולל `docs/architecture/system-overview.md`
3. `ResourceGrid.jsx`/`src/lib/resourceView.js`/`CalendarView.jsx`/`RosterWizard.jsx` ללא שינוי
4. אימות חי בדפדפן על שלוש הנקודות ב-`human-check`
</verification>

<success_criteria>
- MORE-03 מתקיים: הכפילות היחידה שהביקורת מצאה (resources) הוסרה, בלי לשבור פיצ'ר קיים (קריטריון הצלחה 3 ב-ROADMAP Phase 10)
- "יומן" מוכח כמסלול חלופי מלא לפונקציונליות שהוסרה
- `positions` ושאר פריטי "עוד" נשארו ללא שינוי
</success_criteria>

<output>
Create `.planning/phases/10-more-menu-audit/10-02-SUMMARY.md` when done.
</output>
