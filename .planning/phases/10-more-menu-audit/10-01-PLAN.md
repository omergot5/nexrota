---
phase: 10-more-menu-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/supervisor/views.jsx
autonomous: true
requirements: [MORE-02]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "כרטיס 'עומס ה{חברים}' בדשבורד (SupDashboard) מציג קישור 'לדוח המלא' שמנווט בלחיצה אחת למסך הדוחות המלא (analytics) — MORE-02."
    - "שני קיצורי הדרך הקיימים (StatCard 'חילופים'→swaps, StatCard 'משימות פתוחות'→tasks) לא נערכו ועדיין עובדים כפי שהיו."
    - "לא נוסף שום קיצור-דרך ל-positions בדשבורד — הוכרע נגד במפורש ב-10-CONTEXT.md MORE-02 (עמדות מוגדרות פעם אחת וחוזרות לבד, מסך שימוש-נמוך בכוונה)."
    - "הקישור החדש מוצג תמיד בכרטיס — לא מותנה בקיום שורות loadRows — כי הדוח המלא קיים גם כשאין עדיין נתוני עומס להציג."
  artifacts:
    - src/components/supervisor/views.jsx
  key_links:
    - "כרטיס 'עומס' (SupDashboard) → Btn 'לדוח המלא' → onNavigate('analytics') → common.onNavigate=go (SupervisorApp.jsx) → views.analytics (AnalyticsDash, lazy-loaded)."
---

<objective>
להוסיף ל-`SupDashboard` קיצור-דרך חדש אחד ויחיד: קישור "לדוח המלא" על כרטיס "עומס ה{חברים}" הקיים, שמנווט ל-`analytics` דרך `onNavigate` — אותו דפוס ניווט בדיוק שכבר קיים היום בשני ה-`StatCard`ים (חילופים, משימות פתוחות) ובכפתור "לבניית השבוע".

Purpose: MORE-02 (10-CONTEXT.md). כרטיס "עומס" מציג היום רק "טיזר" — תת-קבוצה קלה מאותו נתון שמסך הדוחות המלא מציג, בלי המשך. הוספת הקישור היא קיצור-הדרך היחיד שהביקורת (10-CONTEXT.md MORE-01) המליצה עליו בפועל; קיצור ל-positions הוכרע נגדו במפורש, ו-swaps/tasks כבר יש להם קיצורים קיימים שרק צריך לאמת שהם עדיין עובדים.

Output: `src/components/supervisor/views.jsx` עם קישור "לדוח המלא" חדש בתוך כרטיס העומס של `SupDashboard`, מנווט ל-`analytics`.

**החלטה נעולה (10-CONTEXT.md, MORE-02) — לא נפתחת מחדש:** קיצור-דרך אחד בלבד, ל-`analytics`. אין להוסיף קיצור ל-`positions`. אין לגעת ב-`moreItems()`/`views` map ב-`SupervisorApp.jsx` — זו תוכנית נפרדת (MORE-03, 10-02-PLAN.md).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-more-menu-audit/10-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: קישור "לדוח המלא" מכרטיס "עומס" ← analytics (MORE-02)</name>

  <files>
src/components/supervisor/views.jsx
  </files>

  <read_first>
- `src/components/supervisor/views.jsx` שורות 128-130 — חתימת `SupDashboard`: `onNavigate` כבר מגיע כפרופ, אין צורך בשום שינוי מעבר לקומפוננטה הזו.
- `src/components/supervisor/views.jsx` שורות 402-443 — כרטיס "עומס ה{חברים}" המלא: `Card` נפתח בשורה 402 ונסגר בשורה 443; שורות 403-417 הן שורת הכותרת (h2 + Segmented); שורות 418-442 הן הבלוק המותנה (loadRows ריק מול רשימת שורות) — זו הנקודה שהקישור נכנס אחריה, לפני `</Card>`.
- `src/components/supervisor/views.jsx` שורה 297 — דפוס `Btn` קיים עם `onNavigate` ("לבניית השבוע", `variant="outline" size="sm" icon="left"`) — הפניה לדפוס הניווט הכללי, לא לסגנון עצמו.
- `src/components/ui.jsx` שורות 149-159 — `BTN_VARIANTS`, כולל `ghost` ("text-muted hover:text-content hover:bg-surface-hover") — הווריאנט המתאים לקישור משני בתוך תוכן כרטיס, לא CTA ראשי.
- `.planning/phases/10-more-menu-audit/10-CONTEXT.md` קטע `## MORE-01` (שורת הטבלה של analytics) וקטע `## MORE-02` — הניסוח הנעול המדויק: קישור אחד בלבד, ל-analytics, בלי positions.
  </read_first>

  <action>
ב-`SupDashboard` (`src/components/supervisor/views.jsx`), בתוך ה-`Card` שמכיל את כרטיס "עומס ה{t("noun.memberPlural")}" (פותח בשורה 402, נסגר ב-`</Card>` בשורה 443), הוסף קישור חדש מיד אחרי הבלוק המותנה שמרנדר את `loadRows` או את הודעת המצב-הריק (אחרי ה-`)}` שסוגר את התנאי `loadRows.length === 0 ? ... : ...`, ולפני `</Card>`). הקישור הוא `Btn` עם `variant="ghost"`, `size="sm"`, `icon="left"`, הטקסט "לדוח המלא", ו-`onClick={() => onNavigate("analytics")}` — `onNavigate` כבר מגיע כפרופ ל-`SupDashboard`. עטוף את הכפתור ב-`div` עם `className="mt-4 flex justify-end"` כדי שיישב כשורה נפרדת מתחת לרשימת השורות, לא בתוך שורת הכותרת (שכבר תפוסה ב-h2+Segmented).

הקישור מוצג תמיד — לא מותנה ב-`loadRows.length` — כי הדוח המלא קיים גם כשאין עדיין מה להציג בכרטיס המצומצם.

אל תיגע בשני ה-`StatCard`ים הקיימים (שורות 351-358 עבור swaps, 359-366 עבור tasks) — הם כבר עובדים, המשימה הזו רק מוסיפה קישור חדש. אל תוסיף שום קיצור-דרך עבור `positions` בשום מקום ב-`SupDashboard` — 10-CONTEXT.md MORE-02 מכריע נגד זה במפורש. אל תיגע ב-`moreItems()`/`views` map ב-`SupervisorApp.jsx` — זו משימה נפרדת (MORE-03, תוכנית אחרת, 10-02-PLAN.md). `Btn` ו-`Icon` כבר מיובאים בראש הקובץ ומשמשים במקומות אחרים גם היום — אין צורך בשינוי imports.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/supervisor/views.jsx','utf8'); const start=s.indexOf('export function SupDashboard('); const end=s.indexOf('export function ShiftMgmt('); if(start===-1||end===-1||end<start) throw new Error('SupDashboard boundaries not found'); const body=s.slice(start,end); if(!body.includes('onNavigate(\"analytics\")')) throw new Error('missing onNavigate(\"analytics\") call in SupDashboard'); if(!body.includes('לדוח המלא')) throw new Error('missing לדוח המלא label'); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/supervisor/views.jsx','utf8'); const start=s.indexOf('export function SupDashboard('); const end=s.indexOf('export function ShiftMgmt('); const body=s.slice(start,end); const swaps=(body.match(/onNavigate\(\"swaps\"\)/g)||[]).length; const tasks=(body.match(/onNavigate\(\"tasks\"\)/g)||[]).length; if(swaps!==1) throw new Error('expected exactly one onNavigate(\"swaps\") call, found '+swaps); if(tasks!==1) throw new Error('expected exactly one onNavigate(\"tasks\") call, found '+tasks); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/supervisor/views.jsx','utf8'); const start=s.indexOf('export function SupDashboard('); const end=s.indexOf('export function ShiftMgmt('); const body=s.slice(start,end); if(body.includes('onNavigate(\"positions\")')) throw new Error('positions shortcut must not be added — 10-CONTEXT.md MORE-02 decides against it'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, היכנס כמנהל, פתח את הדשבורד. אמת: (1) כרטיס "עומס ה{חברים}" מציג קישור "לדוח המלא" מתחת לרשימת השורות; (2) לחיצה עליו מנווטת למסך הדוחות המלא (AnalyticsDash — גרפים/טבלת הוגנות); (3) StatCard "חילופים" עדיין מנווט למסך ניהול ההחלפות; (4) StatCard "משימות פתוחות" עדיין מנווט לניהול המשימות; (5) שום קיצור-דרך חדש ל"עמדות קבועות" לא מופיע בשום מקום בדשבורד.</human-check>
  </verify>

  <acceptance_criteria>
- כרטיס "עומס ה{חברים}" ב-`SupDashboard` מציג קישור "לדוח המלא" (`Btn variant="ghost" size="sm" icon="left"`) שקורא ל-`onNavigate("analytics")`
- הקישור מוצג ללא תלות ב-`loadRows.length` (גם כשאין עדיין נתוני עומס)
- שני ה-`StatCard`ים הקיימים (swaps, tasks) לא נערכו — קריאה אחת בלבד לכל אחד מהם נשארת בגוף `SupDashboard`
- שום `onNavigate("positions")` לא נוסף בשום מקום ב-`SupDashboard`
- `moreItems()`/`views` map ב-`SupervisorApp.jsx` לא נערכו בתוכנית הזו
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע על חמש הנקודות ב-`human-check`
  </acceptance_criteria>

  <done>
כרטיס "עומס" בדשבורד פותח בלחיצה אחת את מסך הדוחות המלא; שני הקיצורים הקיימים (swaps, tasks) ממשיכים לעבוד ללא שינוי; אין קיצור חדש ל-positions.
  </done>

  <reversibility rating="reversible">
תוספת UI טהורה — קישור חדש שקורא לפונקציית ניווט קיימת. אין שינוי בנתיב כתיבה, בסכימה או ב-API. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| מנהל מחובר → כרטיס "עומס" → `onNavigate("analytics")` → מסך קיים כבר | ניווט צד-לקוח בלבד — אין קריאת API/כתיבה חדשה, אין שינוי RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01-A | Tampering | מיפוי ה-id שהקישור מנווט אליו | low | mitigate | שער אוטומטי מוודא את המחרוזת המדויקת `onNavigate("analytics")` בגוף `SupDashboard`; human-check מאמת ניווט בפועל למסך הנכון |
| T-10-01-B | Denial of Service (UX — שבירת קיצור קיים) | StatCard "חילופים"/"משימות פתוחות" | low | mitigate | שער אוטומטי סופר בדיוק קריאה אחת לכל אחד מ-`onNavigate("swaps")`/`onNavigate("tasks")` בגוף `SupDashboard` — עריכה שפוגעת בהם נתפסת לפני human-check |
| T-10-01-C | Elevation of Privilege (הרחבת סקופ שלא הוכרעה) | קיצור-דרך ל-`positions` | low | mitigate | שער אוטומטי כושל אם `onNavigate("positions")` מופיע בגוף `SupDashboard` — 10-CONTEXT.md MORE-02 מכריע נגד תוספת כזו |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים
2. כרטיס "עומס" מציג קישור "לדוח המלא" שקורא ל-`onNavigate("analytics")`, ושני הקיצורים הקיימים (swaps/tasks) לא נפגעו
3. אין `onNavigate("positions")` חדש בשום מקום ב-`SupDashboard`
4. אימות חי בדפדפן על חמש הנקודות ב-`human-check`
</verification>

<success_criteria>
- MORE-02 מתקיים חלקית: קיצור-הדרך שהומלץ ב-10-CONTEXT.md (analytics) נוסף ומוביל בפועל למסך הנכון (קריטריון הצלחה 2 ב-ROADMAP Phase 10)
- שני הקיצורים הקיימים (swaps, tasks) מאומתים עדיין עובדים
- אין תוספת לא-מוכרעת (positions)
</success_criteria>

<output>
Create `.planning/phases/10-more-menu-audit/10-01-SUMMARY.md` when done.
</output>