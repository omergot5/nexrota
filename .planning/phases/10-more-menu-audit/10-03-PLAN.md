---
phase: 10-more-menu-audit
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/components/SupervisorApp.jsx
autonomous: true
requirements: [MORE-01, MORE-02, MORE-03]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "טבלת ביקורת מלאה (5 פריטים × מיקום נוכחי × כפול? × המלצה) קיימת כהערה קבועה בקוד, צמודה ל-moreItems() — המקור-האמת שהיא מתעדת (MORE-01)."
    - "כל אחד מ-5 פריטי 'עוד' המקוריים (swaps, tasks, positions, resources, analytics) אומת חי בדפדפן: ארבעה מהם דרך 'עוד' עצמו, ו-resources דרך יומן (מצב שבוע, המסלול היחיד הנותר) — בלי אובדן פונקציונליות (MORE-03, קריטריון הצלחה 3 ב-ROADMAP)."
    - "קיצור-הדרך החדש 'לדוח המלא' (analytics) ושני הקיצורים הקיימים (swaps/tasks) אומתו חיים מהדשבורד יחד עם שאר התפריט, כמכלול אחד — קריטריון הצלחה 2 ב-ROADMAP."
    - "אין קיצור-דרך ל-positions בדשבורד — מאומת כהחלטה מכוונת, לא כפריט שנשכח."
  artifacts:
    - src/components/SupervisorApp.jsx
  key_links:
    - "moreItems() ↔ הערת הביקורת (MORE-01) הצמודה אליו ↔ 10-CONTEXT.md (מקור ההחלטות הנעולות)."
---

<objective>
לעגן בקוד את טבלת הביקורת המלאה של MORE-01 (5 פריטי "עוד" × מיקום נוכחי × כפול? × המלצה) כהערה צמודה ל-`moreItems()` — המקור-האמת שהיא מתעדת — ולבצע סבב אימות לייב בדפדפן שחוצה את כל 5 הפריטים המקוריים יחד עם קיצור-הדרך החדש, כמכלול אחד אחרי ששתי התוכניות הקודמות (10-01, 10-02) כל אחת אימתה רק את החלק שלה.

Purpose: MORE-01 (10-CONTEXT.md), וקריטריוני ההצלחה 2+3 ב-ROADMAP Phase 10, שדורשים אימות לייב "על כל אחד" ו"על כל חמשת הפריטים, לא רק על אלו שהשתנו" — לא רק שהקוד השתנה נכון, אלא שהמצב הסופי של התפריט, כפי שמנהל רואה אותו בפועל, שלם ועקבי.

Output: `src/components/SupervisorApp.jsx` עם הערת ביקורת (MORE-01) חדשה מעל `moreItems()`, וסבב אימות מתועד ב-SUMMARY על כל 5 הפריטים + הקיצור החדש.

**החלטה נעולה (10-CONTEXT.md) — לא נפתחת מחדש:** הביקורת נבנתה מראש על ניתוח קוד מקדים (לא השערה) — התוכן המדויק כבר קיים תחת `10-CONTEXT.md` → `<decisions>` → "MORE-01 — טבלת הביקורת" ו-"המסקנה התפעולית". זו העברה נאמנה של תוכן קיים ונעול לקוד, לא ניסוח מחדש. הביקורת נחתת כהערה בקוד (`בקוד`, כלשון MORE-01 ב-REQUIREMENTS.md) — לא כמסמך חדש תחת `docs/`, ולא כתוספת ל-`CLAUDE.md` (עקרון "CLAUDE.md לא גדל").
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-more-menu-audit/10-CONTEXT.md
@.planning/phases/10-more-menu-audit/10-01-SUMMARY.md
@.planning/phases/10-more-menu-audit/10-02-SUMMARY.md

@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: עיגון טבלת הביקורת בקוד (MORE-01) ואימות לייב על כל 5 הפריטים + הקיצור החדש</name>

  <files>
src/components/SupervisorApp.jsx
  </files>

  <read_first>
- `.planning/phases/10-more-menu-audit/10-CONTEXT.md` קטע `<decisions>` המלא — טבלת הביקורת בת 5 השורות ("MORE-01 — טבלת הביקורת") ופסקת "המסקנה התפעולית מהטבלה": התוכן המדויק והנעול שעובר לקוד, לא מנוסח מחדש.
- `src/components/SupervisorApp.jsx` שורות 18-47 — סגנון ההערות הקיים מעל `navItems()`/`moreItems()` ("ארבעה פריטים. לא יותר." וכו') — הדפוס המדויק שהערת הביקורת החדשה מצטרפת אליו, ישירות מעל שורה 41 (`const moreItems = () => [`).
- `.planning/phases/10-more-menu-audit/10-01-SUMMARY.md`, `10-02-SUMMARY.md` — מה כבר אומת חי בכל תוכנית בנפרד (כדי לא לחזור על אותה בדיקה, אלא להוסיף את מה שחוצה את התפריט כמכלול).
- `.planning/ROADMAP.md` Phase 10, קריטריוני הצלחה 1/2/3 — הניסוח המדויק שהאימות הזה צריך לספק.
- `CLAUDE.md` קטע "📚 מפת התיעוד" ו-"✍️ כשמוסיפים תיעוד" (קובץ אחד = שאלה אחת; CLAUDE.md לא גדל) — מאשר שהערת קוד + SUMMARY הם הבית הנכון לביקורת הזו, לא מסמך `docs/` חדש.
  </read_first>

  <action>
הוסף הערת Hebrew רב-שורתית ב-`src/components/SupervisorApp.jsx`, ממוקמת ישירות מעל `const moreItems = () => [` (אחרי ההערה הקיימת "היעדים המשניים..."), ומסומנת בבירור עם "MORE-01" כדי שהיא ניתנת לאיתור. ההערה חייבת לכלול, לכל אחד מחמשת פריטי "עוד" המקוריים (swaps, tasks, positions, resources, analytics): שם הפריט, מיקומו הנוכחי (כולל כל מיקום כפול/משני שהביקורת מצאה), האם הוא כפול (כן/לא/חלקי) וההמלצה — התוכן המדויק כבר קיים, מוכן ונעול, ב-`10-CONTEXT.md` תחת `<decisions>` → "MORE-01 — טבלת הביקורת" ו-"המסקנה התפעולית מהטבלה"; זו העברה נאמנה של תוכן קיים, לא ניסוח מחדש. ציין בפירוש גם את שתי ההחלטות התפעוליות שכבר יושמו בתוכניות הקודמות: resources הוסר (MORE-03, 10-02-PLAN.md), ו-analytics קיבל קיצור-דרך חדש על כרטיס "עומס" בעוד ש-swaps/tasks כבר יש להם קיצורים קיימים שרק אומתו (MORE-02, 10-01-PLAN.md) — ו-positions לא קיבל קיצור-דרך, במכוון.

אל תגדיל את `CLAUDE.md` ואל תוסיף מסמך חדש תחת `docs/` — הביקורת הזו שייכת לקוד עצמו ("בקוד/ב-PR description", כלשון MORE-01 ב-`REQUIREMENTS.md`), והיא מתועדת גם ב-SUMMARY.md של התוכנית הזו (ר' `<output>`).

בנוסף להערה, בצע סבב אימות לייב בדפדפן (`npm run dev`) שחוצה את כל 5 הפריטים המקוריים ואת הקיצור החדש יחד — זו הפעם הראשונה שהמצב הסופי המלא של התפריט נבדק כמכלול אחד, אחרי ששתי התוכניות הקודמות כל אחת אימתה רק את החלק שלה. אל תערוך שום קובץ קוד נוסף מעבר להערה שתוארה למעלה — זו תוכנית של תיעוד ואימות, לא שינוי פונקציונלי.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/SupervisorApp.jsx','utf8'); const anchorIdx=s.indexOf('MORE-01'); if(anchorIdx===-1) throw new Error('MORE-01 audit anchor not found'); const moreIdx=s.indexOf('const moreItems = ()'); if(moreIdx===-1) throw new Error('moreItems() not found'); if(anchorIdx>moreIdx) throw new Error('MORE-01 audit comment must sit above moreItems()'); const block=s.slice(anchorIdx,moreIdx); for(const tok of ['swaps','tasks','positions','resources','analytics','כפול','המלצה']) if(!block.includes(tok)) throw new Error('audit comment missing token: '+tok); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/SupervisorApp.jsx','utf8'); const start=s.indexOf('const moreItems = ()'); const end=s.indexOf('const titles = ('); const body=s.slice(start,end); if(body.includes('resources')) throw new Error('resources must remain removed — final integration check for MORE-03'); const ids=(body.match(/id:\s*\"(\w+)\"/g)||[]); if(ids.length!==4) throw new Error('expected exactly 4 moreItems entries, found '+ids.length); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const v=fs.readFileSync('src/components/supervisor/views.jsx','utf8'); const start=v.indexOf('export function SupDashboard('); const end=v.indexOf('export function ShiftMgmt('); const body=v.slice(start,end); if(!body.includes('onNavigate(\"analytics\")')) throw new Error('analytics shortcut must remain present — final integration check for MORE-02'); if(!body.includes('לדוח המלא')) throw new Error('לדוח המלא label must remain present'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, היכנס כמנהל. אמת בלחיצה, אחד-אחד: (1) "עוד" מציג בדיוק 4 פריטים — חילופים, משימות, עמדות קבועות, דוחות/ניתוח — בלי "מבט משאבים"; (2) לחיצה על "חילופים" בתוך "עוד" פותחת את ניהול ההחלפות (SwapMgmt) כרגיל; (3) לחיצה על "משימות" בתוך "עוד" פותחת את ניהול המשימות (TaskMgmt) כרגיל; (4) לחיצה על "עמדות קבועות" בתוך "עוד" פותחת את PositionsScreen כרגיל, בלי שום קיצור-דרך חדש אליו בדשבורד; (5) לחיצה על "דוחות/ניתוח" בתוך "עוד" פותחת את מסך הדוחות (AnalyticsDash) כרגיל — הדוח המלא עדיין נגיש גם דרך "עוד", לא רק דרך הקיצור החדש; (6) בדשבורד, לחיצה על "לדוח המלא" בכרטיס "עומס ה{חברים}" מנווטת לאותו מסך דוחות — הקיצור החדש של MORE-02; (7) בדשבורד, StatCard "חילופים" ו-StatCard "משימות פתוחות" עדיין מנווטים נכון בהתאמה — הקיצורים הקיימים לא נשברו; (8) לחיצה על "יומן" בניווט הראשי (לא בתוך "עוד") נפתחת במצב "שבוע" כברירת מחדל ומציגה את אותו מבנה שורת-עמדה/עמודות-יום ש"מבט משאבים" הציג — resources פונקציונלית עדיין נגישה במסלול הזה, בלי אובדן מידע.</human-check>
  </verify>

  <acceptance_criteria>
- הערת ביקורת מסומנת "MORE-01" יושבת ישירות מעל `moreItems()` ב-`SupervisorApp.jsx`, ומכילה לכל אחד מ-5 הפריטים המקוריים: מיקום נוכחי, כפול?, והמלצה — תואמת את 10-CONTEXT.md
- ההערה מזכירה במפורש את שתי ההחלטות התפעוליות (resources הוסר; analytics קיבל קיצור, positions לא)
- `CLAUDE.md` לא גדל; לא נוסף מסמך חדש תחת `docs/`
- שער אוטומטי מאמת מחדש (integration check) ש-`moreItems()` נשאר עם 4 איברים בלי resources, ושקיצור ה-analytics עדיין קיים ב-`SupDashboard`
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע על כל שמונה הנקודות ב-`human-check` — כל 5 הפריטים המקוריים + הקיצור החדש + שני הקיצורים הקיימים
  </acceptance_criteria>

  <done>
הביקורת המלאה (MORE-01) מתועדת בקוד צמוד ל-`moreItems()`, וכל 5 הפריטים המקוריים + הקיצור החדש אומתו חיים כמכלול אחד.
  </done>

  <reversibility rating="reversible">
הוספת הערת תיעוד בלבד + סבב אימות — אין שינוי פונקציונלי. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| אין גבול אמון חדש | תיעוד + סבב אימות על ניווט צד-לקוח קיים כבר; אין קריאת API/כתיבה חדשה |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03-A | Repudiation (תיעוד שסוטה מהקוד) | הערת הביקורת מעל `moreItems()` | low | mitigate | ההערה יושבת צמוד פיזית ל-`moreItems()` עצמה (לא במסמך נפרד), ושער אוטומטי מוודא שתוכן ההערה (5 פריטים) תואם את מצב `moreItems()` בפועל (4 איברים, בלי resources) באותו קובץ |
| T-10-03-B | Repudiation (דיווח "עובד" בלי שנצפה) | תהליך האימות עצמו | medium | mitigate | עקרון ברזל 6 (CLAUDE.md) מחייב דיווח לפי מה שנראה; human-check דורש עדות מפורשת על כל אחת מ-8 הנקודות, כולל resources דרך יומן — לא רק הפריטים שהשתנו (קריטריון הצלחה 3, ROADMAP) |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים
2. הערת MORE-01 קיימת מעל `moreItems()` וכוללת את חמשת הפריטים, מיקום/כפול/המלצה לכל אחד
3. שערי אינטגרציה מוודאים ש-MORE-02 (קיצור analytics) ו-MORE-03 (הסרת resources) עדיין בתוקף בקוד
4. אימות חי בדפדפן על כל שמונה הנקודות ב-`human-check`
</verification>

<success_criteria>
- MORE-01 מתקיים במלואו: טבלת ביקורת מתועדת בקוד, ממפה את כל 5 הפריטים → מיקום → כפול? → המלצה (קריטריון הצלחה 1, ROADMAP Phase 10)
- MORE-02 מאומת חי כמכלול עם שאר התפריט (קריטריון הצלחה 2)
- MORE-03 מאומת חי על כל 5 הפריטים המקוריים, לא רק אלו שהשתנו (קריטריון הצלחה 3)
</success_criteria>

<output>
Create `.planning/phases/10-more-menu-audit/10-03-SUMMARY.md` when done.

חובה לתעד ב-SUMMARY: ממצא כל אחת משמונה נקודות ה-human-check בנפרד, כפי שנראו בפועל בדפדפן.
</output>
