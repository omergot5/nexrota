---
phase: 06-resource-view-pattern
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - docs/architecture/system-overview.md
autonomous: true
requirements: [RESVIEW-01, RESVIEW-02, RESVIEW-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "שינוי ויזואלי בודד ב-ResourceGrid.jsx משתקף בשלושת המסכים — מוכח בפועל, לא בהיסק (קריטריון 4 ב-ROADMAP)."
    - "הדפוס החזותי חי בקובץ אחד בלבד; אף אחד משלושת המסכים לא מצייר טבלה משלו."
    - "עמדה/קטגוריה עם 1, 2, 3 ו-5 פריטים באותו יום מוצגת נכון בשלושת המסכים — מספר הפריטים נגזר מהנתונים (RESVIEW-03)."
    - "מפת הארכיטקטורה בתיעוד מזהה את הרכיב המשותף כנקודת הבעלות על הדפוס, כדי ש-Phase 7 ו-8 יאמצו ולא ימציאו."
  artifacts:
    - docs/architecture/system-overview.md
  key_links:
    - "ResourceGrid.jsx → ResourceView.jsx / RosterWizard.jsx / CalendarView.jsx — שלושה צרכנים, מקור אחד; זו הטענה שהתוכנית הזו מוכיחה."
---

<objective>
לסגור את הפאזה בהוכחה ולא בהנחה: לאמת אוטומטית שהדפוס החזותי חי במקור אחד ושלושת המסכים צורכים אותו, לאמת חי בדפדפן ששינוי בודד ברכיב המשותף אכן משתנה בשלושתם, ולעגן את הבעלות בתיעוד כדי ש-Phase 7 ו-Phase 8 יאמצו את הדפוס במקום להמציא אחד משלהן.

Purpose: קריטריון ההצלחה הרביעי של הפאזה ב-ROADMAP הוא היחיד שאי אפשר לאמת בתוך תוכנית בודדת — הוא חוצה שלושה מסכים ולכן שייך לגל שאחרי שכולם נחתו. בלעדיו הפאזה מסתיימת בטענה לא-בדוקה.

Output: ביקורת מקור-יחיד אוטומטית שעברה, סריקת דפדפן מתועדת על שלושת המסכים, ופסקה אחת ב-`docs/architecture/system-overview.md` שמצביעה על הבעלים של הדפוס.

**החלטות רלוונטיות (מפה מלאה ב-06-01):** D-04 (רכיב אחד לשלושה מסכים) ו-D-05 (מספר פריטים דינמי) הם בדיוק מה שהתוכנית הזו מוכיחה. D-06 (שני מנגנוני צבע נפרדים) ו-D-07 (תחום הפעילות מהחנות) נאכפים כאן כשער.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-resource-view-pattern/06-CONTEXT.md
@.planning/phases/06-resource-view-pattern/06-01-SUMMARY.md
@.planning/phases/06-resource-view-pattern/06-02-SUMMARY.md
@.planning/phases/06-resource-view-pattern/06-03-SUMMARY.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/supervisor/ResourceGrid.jsx
</context>

<artifacts>
## Artifacts this plan produces

אין קוד חדש ואין export חדש. הפלט הוא ראיה: תוצאות שערים אוטומטיים, סריקת דפדפן מתועדת, ופסקת ארכיטקטורה ב-`docs/architecture/system-overview.md` שמצהירה מי הבעלים של דפוס קטגוריה×יום.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: ביקורת מקור-יחיד — הדפוס חי בקובץ אחד ושלושת המסכים צורכים אותו</name>

  <files>
(ביקורת בלבד — לא נערך שום קובץ מקור בתוך המשימה הזו)
  </files>

  <read_first>
- `src/components/supervisor/ResourceGrid.jsx` — הקובץ שאמור להיות הבעלים היחיד של ה-markup
- `src/components/supervisor/ResourceView.jsx`, `src/components/supervisor/RosterWizard.jsx`, `src/components/supervisor/CalendarView.jsx` — שלושת הצרכנים אחרי 06-01/02/03
- שלושת קובצי ה-SUMMARY של הפאזה — במיוחד כל סטייה מחוזה ה-API שדווחה ב-06-01
- `CLAUDE.md` עקרון ברזל 6 ("אימות בדפדפן") — מה שלא נבדק מדווח כלא נבדק
  </read_first>

  <action>
הרץ את סדרת השערים שלמטה, ותקן ממצא רק אם הוא הפרה של הביקורת עצמה (למשל ייבוא כפול או שריד markup שנשכח). אם שער נכשל מסיבה מהותית — כלומר אחד משלושת המסכים לא באמת עבר לרכיב המשותף — **עצור ודווח**; אל תשכתב את השער כדי שיעבור.

הביקורת בודקת ארבעה דברים:

1. **מקור יחיד ל-markup.** בכל עץ `src/components/supervisor/`, רק `ResourceGrid.jsx` מכיל אלמנט `<table` השייך לדפוס הזה. שלושת הצרכנים מייבאים אותו ואינם מכילים אלמנט `<table` משל עצמם.
2. **שלושה צרכנים בפועל.** כל אחד מ-`ResourceView.jsx`, `RosterWizard.jsx` ו-`CalendarView.jsx` גם מייבא את הרכיב וגם מרנדר אותו.
3. **אין הרדקוד של מספר משמרות (D-05 / RESVIEW-03).** ב-`ResourceGrid.jsx`, בשורות שאינן הערה, אין `.slice(0, N)` ואין `Math.min(` על מערך פריטים. הכלל: מסננים את שורות ההערה לפני הספירה, כי הערה שמזכירה את המונח היא לא מימוש שלו.
4. **שני מנגנוני הצבע נשארו נפרדים (D-06).** `ResourceGrid.jsx` מייבא מ-`../../design/categoryPalette.js` בלבד, ואינו מייבא `categoryColor` או `positionColorKey` — אלה נבנים ב-Phase 7 לצורך אחר, ואיחוד מוקדם שלהם היה מבטל החלטה מפורשת.

מעבר לשערים, קרא בעין את `ResourceGrid.jsx` ואשר שלוש נקודות שאי אפשר לגרפ: (א) כל המחלקות של Tailwind שנגזרות מגוון נקראות מ-`TONE_CLASSES[tone]` כמחרוזות שלמות ולא מורכבות במחרוזת-תבנית (ההנמקה בראש `src/design/categoryPalette.js`); (ב) אין שימוש ב-`dangerouslySetInnerHTML` ואין בניית HTML ממחרוזת — סגירת T-06-01 ו-T-06-06; (ג) הרכיב אינו מייבא מ-`../../lib/resourceView.js`, כלומר הפיבוט נשאר אצל הקוראים (D-02).
  </action>

  <verify>
    <automated>npm run build</automated>
    <automated>npm test</automated>
    <automated>node -e "const fs=require('fs'),d='src/components/supervisor/'; const owners=fs.readdirSync(d).filter(f=>f.endsWith('.jsx')&&/<table/.test(fs.readFileSync(d+f,'utf8'))); if(owners.join()!=='ResourceGrid.jsx') throw new Error('table markup is not single-source, owners: '+owners.join(',')); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'),d='src/components/supervisor/'; for(const f of ['ResourceView.jsx','RosterWizard.jsx','CalendarView.jsx']){const s=fs.readFileSync(d+f,'utf8'); if(!/import\s+ResourceGrid\s+from/.test(s)) throw new Error(f+' does not import ResourceGrid'); if(!/<ResourceGrid/.test(s)) throw new Error(f+' does not render ResourceGrid');} console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const body=fs.readFileSync('src/components/supervisor/ResourceGrid.jsx','utf8').split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n'); if(/\.slice\(\s*0\s*,\s*\d+\s*\)/.test(body)) throw new Error('item cap found'); if(/Math\.min\(/.test(body)) throw new Error('item clamp found'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/ResourceGrid.jsx','utf8'); if(!/design\/categoryPalette\.js/.test(s)) throw new Error('palette import missing'); if(/categoryColor|positionColorKey/.test(s)) throw new Error('phase-7 colour mechanism leaked into the shared grid'); if(/lib\/resourceView\.js/.test(s)) throw new Error('grid computes its own pivot'); console.log('ok')"</automated>
  </verify>

  <acceptance_criteria>
- `ResourceGrid.jsx` הוא הקובץ היחיד תחת `src/components/supervisor/` שמכיל אלמנט `<table`
- שלושת הצרכנים מייבאים **וגם** מרנדרים את `ResourceGrid`
- בגוף הלא-מוער של `ResourceGrid.jsx` אין `.slice(0, N)` ואין `Math.min(` (D-05 / RESVIEW-03)
- `ResourceGrid.jsx` מייבא מ-`design/categoryPalette.js` ואינו מייבא `categoryColor`/`positionColorKey` (D-06)
- `ResourceGrid.jsx` אינו מייבא מ-`lib/resourceView.js` (D-02)
- אין `dangerouslySetInnerHTML` ואין הרכבת HTML ממחרוזת ברכיב (T-06-01 / T-06-06)
- מחלקות Tailwind של גוון נקראות מ-`TONE_CLASSES` כמחרוזות שלמות, לא מורכבות דינמית
- `npm run build` ו-`npm test` עוברים
  </acceptance_criteria>

  <done>
כל שערי הביקורת עוברים והקריאה בעין אישרה את שלוש הנקודות שאינן ניתנות לגריפה; אין צרכן שמצייר טבלה משלו.
  </done>
</task>

<task type="auto">
  <name>Task 2: סריקת דפדפן על שלושת המסכים ועיגון הבעלות בתיעוד</name>

  <files>
docs/architecture/system-overview.md
  </files>

  <read_first>
- `docs/architecture/system-overview.md` — מבנה המסמך וסגנון הכתיבה בו; מצא את המקום הנכון לפסקה על שכבת התצוגה של מסכי המפקד
- `CLAUDE.md` — טבלת "ארבע נקודות חנק" וקטע "כשמוסיפים תיעוד" (קובץ אחד = שאלה אחת; `CLAUDE.md` לא גדל)
- שלושת קובצי ה-SUMMARY של הפאזה — מה כבר אומת חי בכל תוכנית, כדי לא לחזור על אותה בדיקה אלא להוסיף את מה שחוצה מסכים
- `.planning/ROADMAP.md`, Phase 6, קריטריון הצלחה 4 — הניסוח המדויק שצריך להיות מסופק
  </read_first>

  <action>
**חלק א' — סריקת הדפדפן שחוצה את שלושת המסכים.** הרץ `npm run dev`. תחילה ודא שיש נתונים שמכסים את המקרה הדינמי: קטגוריה אחת עם משמרת אחת ביום, אחת עם שתיים, אחת עם שלוש ואחת עם חמש באותו יום. אם נתוני הצוות הנוכחיים לא מכסים זאת, השתמש בצוות ההדגמה במצב army (`src/lib/demoData.js` — עמדה 24/7 מחולקת מייצרת בדיוק את המצב הזה) או הוסף משמרות ידנית. תעד באיזה מסלול השתמשת.

עבור על שלושת המסכים ותעד מה **נראה בפועל**, לא מה היה אמור (עקרון ברזל 6):
- "מבט משאבים"
- "השבוע" → שלב בניית השבוע (מצב army)
- היומן → תצוגת שבוע

בכל אחד מהם ודא: אותו מבנה שורה/עמודה, אותה טיפוגרפיה ואותו ריווח; עמודה ראשונה נעוצה בגלילה אופקית; תא עם 1, 2, 3 ו-5 פריטים מציג את כולם בלי חיתוך ובלי מספר עמודות קבוע; RTL תקין בשלושתם.

**חלק ב' — הוכחת המקור היחיד, חי.** הכנס שינוי ויזואלי בודד וזמני ל-`ResourceGrid.jsx` — למשל החלפת `border-r-[3px]` בתא-הפריט ל-`border-r-[6px]`, שינוי בודד וברור לעין. רענן את שלושת המסכים ואשר שהשינוי נראה **בשלושתם**. **שחזר את השינוי מיד** ואשר ש-`git status` נקי מ-`src/` לפני סיום המשימה. זהו קריטריון ההצלחה הרביעי של הפאזה, והדרך היחידה לספק אותו היא להריץ אותו בפועל.

**חלק ג' — עיגון בתיעוד.** הוסף ל-`docs/architecture/system-overview.md` פסקה קצרה (או שורת טבלה, לפי המבנה הקיים במסמך) שקובעת: דפוס התצוגה "שורת עמדה/קטגוריה × עמודות יום, תא = רשימת פריטים ממוינת עם שעה ושמות" חי ב-`src/components/supervisor/ResourceGrid.jsx` בלבד, נזון מהמנוע הטהור `src/lib/resourceView.js`, ונצרך על ידי שלושה מסכים — מבט משאבים, בניית השבוע והיומן בתצוגת שבוע. ציין במפורש שמסך חדש שמציג את אותו נתון אמור לצרוך את הרכיב ולא לצייר טבלה משלו, ושהצבעים בתא מגיעים מ-`categoryTone`/`TONE_CLASSES` — מנגנון נפרד ובמכוון מזה שנבנה ב-Phase 7 לתצוגת הפרסום והשיתוף.

**אל תגדיל את `CLAUDE.md`** — ההנחיה בו מפורשת: תוכן חדש הולך ל-`docs/`. אם נראה לך שטבלת "ארבע נקודות חנק" צריכה שורה חמישית, זו הצעה שמדווחת ב-SUMMARY להכרעת בעל המוצר, לא שינוי שנעשה כאן.
  </action>

  <verify>
    <automated>npm run build</automated>
    <automated>npm test</automated>
    <automated>node -e "const cp=require('child_process'); const out=cp.execSync('git status --porcelain -- src',{encoding:'utf8'}).trim(); if(out) throw new Error('temporary visual probe was not reverted:\n'+out); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('docs/architecture/system-overview.md','utf8'); for(const n of ['ResourceGrid','resourceView.js','categoryTone']) if(!s.includes(n)) throw new Error('architecture doc missing: '+n); console.log('ok')"</automated>
    <human-check>שלושת המסכים נסרקו חיים בדפדפן, כולל מקרי 1/2/3/5 פריטים בתא, וממצא כל מסך תועד כפי שנראה. שינוי ויזואלי בודד ברכיב המשותף נצפה בפועל בשלושת המסכים ולאחר מכן שוחזר.</human-check>
  </verify>

  <acceptance_criteria>
- שלושת המסכים נסרקו חיים ותוצאת כל אחד מהם תועדה ב-SUMMARY במונחי מה שנראה בפועל
- מקרה הבדיקה של 1, 2, 3 ו-5 פריטים בתא אומת חי בשלושת המסכים, והמסלול שבו נוצרו הנתונים תועד
- שינוי ויזואלי בודד ב-`ResourceGrid.jsx` נצפה בשלושת המסכים — קריטריון 4 של הפאזה סופק בהרצה, לא בהיסק
- השינוי הזמני שוחזר במלואו; `git status --porcelain -- src` ריק
- `docs/architecture/system-overview.md` מציין את `ResourceGrid` כבעלים היחיד של הדפוס, את `resourceView.js` כמנוע שמזין אותו, את שלושת הצרכנים, ואת ההפרדה מול מנגנון הצבעים של Phase 7
- `CLAUDE.md` לא גדל
- `npm run build` ו-`npm test` עוברים
  </acceptance_criteria>

  <done>
שלושת המסכים אומתו חיים, המקור-היחיד הוכח בהרצה עם שינוי זמני ששוחזר, והבעלות על הדפוס מתועדת ב-`docs/architecture/system-overview.md` עבור Phase 7 ו-Phase 8.
  </done>

  <reversibility rating="reversible">
    ביקורת ותיעוד. השינוי הוויזואלי הזמני מוחזר בתוך אותה משימה ונאכף בשער אוטומטי.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| שינוי זמני בקוד מקור → עץ העבודה | בדיקה חיה שמכניסה שינוי מכוון לקובץ מקור ומסתמכת על שחזורו |
| תיעוד ארכיטקטורה → פאזות עתידיות | Phase 7 ו-8 קוראות את המסמך ומקבלות ממנו החלטות |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13 | Tampering | שינוי-הבדיקה הזמני ב-`ResourceGrid.jsx` | medium | mitigate | שער אוטומטי על `git status --porcelain -- src` בתוך אותה משימה; המשימה אינה יכולה להסתיים כשהעץ מלוכלך |
| T-06-14 | Repudiation | דיווח "עובד" בלי שנצפה | medium | mitigate | עקרון ברזל 6 של הפרויקט מחייב דיווח לפי מה שנראה; ה-SUMMARY חייב לתעד את מסלול יצירת הנתונים ואת ממצא כל מסך בנפרד, כך שטענה לא-מכוסה ניתנת לזיהוי |
| T-06-15 | Information Disclosure | נתוני הדגמה שנוצרו לצורך הבדיקה | low | accept | מסלול ההדגמה הקיים יוצר צוות מבודד; ניקוי מפורש של נתוני הדגמה הוא סקופ של Phase 9 (REST-03/04) ולא נגרר לכאן. אם נוצרו נתונים, הדבר מדווח ב-SUMMARY |
| T-06-16 | Tampering | תיעוד ארכיטקטורה שיסטה מהקוד | low | mitigate | שער אוטומטי מוודא שהמסמך נוקב בשמות האמיתיים (`ResourceGrid`, `resourceView.js`, `categoryTone`), כך שמחיקה או שינוי-שם עתידיים ייתפסו |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. כל שערי הביקורת של Task 1 עוברים
2. `npm run build` ו-`npm test` עוברים
3. `git status --porcelain -- src` ריק בסוף הפאזה
4. שלושת המסכים אומתו חיים, כולל מקרי 1/2/3/5
5. `docs/architecture/system-overview.md` מצביע על הבעלים של הדפוס
</verification>

<success_criteria>
- קריטריון 4 של Phase 6 ב-ROADMAP סופק בהרצה ולא בהיסק
- RESVIEW-01, RESVIEW-02 ו-RESVIEW-03 אומתו חיים בשלושת המסכים
- הדפוס בעל מקור יחיד מוכח, ו-Phase 7 ו-8 יודעות מהתיעוד לאן לפנות
</success_criteria>

<output>
Create `.planning/phases/06-resource-view-pattern/06-04-SUMMARY.md` when done.

חובה לתעד ב-SUMMARY: ממצא כל אחד משלושת המסכים בנפרד כפי שנראה בדפדפן; המסלול שבו נוצרו נתוני 1/2/3/5; מה בדיוק היה השינוי הוויזואלי הזמני ואישור שהוחזר; וכל הצעה לעדכון `CLAUDE.md` שהועלתה ולא בוצעה.
</output>
