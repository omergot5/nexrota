---
phase: 06-resource-view-pattern
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/supervisor/RosterWizard.jsx
autonomous: true
requirements: [RESVIEW-01, RESVIEW-03]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "מנהל פותח \"בניית שבוע\" (שלב 1 ב-WeekFlow, מצב army) ורואה את אותו מבנה שהוא רואה ב\"מבט משאבים\": שורת עמדה/קטגוריה נעוצה, עמודות יום, תא עם שעה ושמות משובצים (RESVIEW-01)."
    - "טופס עריכת העמדות נשאר במקומו ובתפקודו המלא — הגריד יושב לצידו ולא מחליף אותו (D-03)."
    - "מנהל שמקליד משימה חדשה שעדיין לא נשמרה רואה אותה מיד בגריד כשורה מקווקוות עם התגית \"בעריכה\" — המשוב החי של האשף נשמר."
    - "עמדה עם 1, 2, 3 או 5 משמרות ביום מוצגת נכון גם במסך הזה (RESVIEW-03)."
  artifacts:
    - src/components/supervisor/RosterWizard.jsx
  key_links:
    - "WeekFlow.jsx `common` → RosterWizard: `shifts` ו-`tasks` כבר מועברים היום ורק לא נצרכים — הקישור החסר היחיד הוא ה-destructure."
    - "buildResourceRows (src/lib/resourceView.js) → rows → ResourceGrid — אותו מסלול נתונים בדיוק כמו ב-ResourceView (D-02/D-04)."
    - "form (draft בעריכה) → pendingRows → ResourceGrid extraRow עם pending:true — המשוב החי ששרד את החלפת הפאנל."
---

<objective>
להחליף את פאנל התצוגה "ככה השבוע נראה עד עכשיו" ב-`RosterWizard.jsx` ברכיב הגריד המשותף `ResourceGrid`, כך שמסך בניית השבוע ידבר את אותה שפה חזותית כמו "מבט משאבים" — וטופס עריכת העמדות שלצידו יישאר בדיוק כפי שהוא.

Purpose: RESVIEW-01. היום מסך בניית השבוע מציג רצועת-ימים עם שמות עמדות בלבד — בלי שורת עמדה, בלי שעות, בלי שמות משובצים. זו שפת עיצוב שנייה על אותו נתון, וזו בדיוק הבעיה שהפאזה סוגרת.

Output: `RosterWizard.jsx` שמרנדר `ResourceGrid` בעמודת התצוגה של הפריסה הדו-טורית הקיימת, כולל שורת-רפאים לפריט שבעריכה.

**החלטות רלוונטיות (מפה מלאה ב-06-01):**
- **D-02** — הנתונים מגיעים מ-`buildResourceRows`; אין מיזוג חדש ואין נגיעה במנוע הטהור
- **D-03** — הגריד הוא פאנל-תצוגה **לצד** טופס עריכת העמדות; **הטופס לא מוחלף ולא נוגעים בו**
- **D-04** — צורכים את `ResourceGrid` מ-06-01; אסור לשכפל JSX של טבלה כאן ואסור לערוך את `ResourceGrid.jsx`
- **D-05** — מספר פריטים דינמי בתא — נרש מהרכיב המשותף, לא ממומש מחדש
- **D-07** — תחום הפעילות מ-`subscribeTerms`/`termProfile`

**הכרעת מתכנן שיש לדעת עליה (J-2):** ה-markup של רצועת-הימים הקיימת (`dayItems`, שורות ~299–315 ו-~382–410) **נסגר** והגריד המשותף תופס את מקומו באותו כרטיס ובאותה עמודה. `06-CONTEXT.md` קובע "לא מחליף אותו" על **טופס העריכה** — הטופס אכן נשאר שלם. להשאיר גם את רצועת-הימים הישנה וגם את הגריד החדש היה מציג שתי שפות עיצוב זו לצד זו על אותו מסך, בדיוק מה ש-RESVIEW-01 בא לסגור. שני דברים ששמרנו במכוון: המשוב החי של "בעריכה" (דרך שורת-רפאים), והכותרת "ככה השבוע נראה עד עכשיו".

**הבדל התנהגותי שנוצר מהמעבר, ומדוע הוא רצוי:** הרצועה הישנה ציירה מ-`expectedDatesForWeek(position, sunday)` — כלומר "איפה העמדה *אמורה* לרוץ". הגריד מצייר מ-`shifts`/`tasks` — כלומר "מה באמת קיים בשבוע". כשהשניים חלוקים (עמדה שהוגדרה אבל `ensurePositionsForWeek` עוד לא מימשה את פריטי העבודה שלה), המסך יראה מעתה את המציאות ולא את הכוונה. זה בדיוק ה-Core Value של המחזור: "מה שהמערכת אומרת לו על עצמה הוא נכון".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-resource-view-pattern/06-CONTEXT.md
@.planning/phases/06-resource-view-pattern/06-01-SUMMARY.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/supervisor/ResourceGrid.jsx
@src/components/supervisor/RosterWizard.jsx
</context>

<artifacts>
## Artifacts this plan produces

אין קובץ חדש ואין export חדש. סמלים פנימיים חדשים בתוך `RosterWizard.jsx`:
- `mode` — ערך מ-`useSyncExternalStore(subscribeTerms, termProfile, termProfile)`
- `rows` — `useMemo` סביב `buildResourceRows({ shifts, tasks, weekDates, mode })`
- `pendingRows` — `useMemo` שמייצר שורת-רפאים אחת (או אפס) עבור ה-draft שבעריכה

סמלים שנסגרים: `dayItems` (המשתנה שהזין את רצועת-הימים) והייבוא `expectedDatesForWeek`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: הזרמת נתוני השבוע ל-RosterWizard ובניית שורות הגריד (כולל שורת-רפאים)</name>

  <files>
src/components/supervisor/RosterWizard.jsx
  </files>

  <read_first>
- `src/components/supervisor/RosterWizard.jsx` — כל הקובץ. במיוחד: חתימת הרכיב (שורה ~130), `items`/`resolvedActiveKey`/`form` (שורות ~151–202), ו-`dayItems` (שורות ~299–315) שזה מה שמוחלף
- `src/components/supervisor/WeekFlow.jsx` שורות ~134–136 ו-~163 — אובייקט `common` **כבר** מעביר `shifts`, `tasks`, `guards`, `weekDates`, `team`; `RosterWizard` פשוט לא עושה להם destructure. אין צורך לשנות כלום ב-`WeekFlow.jsx`
- `.planning/phases/06-resource-view-pattern/06-01-SUMMARY.md` — החתימה הסופית של `ResourceGrid` ותמיכת `pending`
- `src/components/supervisor/ResourceGrid.jsx` — צורת `rows` ו-`item` שהרכיב מצפה לה
- `src/lib/resourceView.js` — פלט `buildResourceRows`. **לא נוגעים בקובץ** (D-02)
- `src/lib/categories.js` — `folderIcon(category)`, לאייקון של שורת-הרפאים
  </read_first>

  <action>
הרחב את `RosterWizard.jsx` כך שיחזיק את שורות הגריד, בלי לגעת עדיין ב-JSX של התצוגה (זה Task 2).

**חתימה** — הוסף לרשימת ה-destructure של הפרופס את `shifts = []` ו-`tasks = []`. שאר הפרופס נשארים בדיוק כפי שהם. `WeekFlow.jsx` כבר מעביר אותם — אל תערוך אותו.

**תחום הפעילות (D-07)** — הוסף `useSyncExternalStore` ל-import של `react`, והוסף `subscribeTerms, termProfile` ל-import הקיים מ-`../../lib/terms.js` (שממנו כבר מיובא `t`). בתוך הרכיב: `const mode = useSyncExternalStore(subscribeTerms, termProfile, termProfile);`. הרכיב הזה מוצג רק במצב army, אבל הערך עדיין נקרא מהחנות ולא ננעל למחרוזת — מקור אמת אחד לכל המסכים.

**שורות אמיתיות** — ייבא `buildResourceRows` מ-`../../lib/resourceView.js` והוסף `const rows = useMemo(() => buildResourceRows({ shifts, tasks, weekDates, mode }), [shifts, tasks, weekDates, mode]);`. זה בדיוק אותו מסלול נתונים שמסך "מבט משאבים" משתמש בו (D-02).

**שורת-רפאים לפריט שבעריכה** — הוסף `const pendingRows = useMemo(...)` שמחזיר מערך ריק כשאין `form`, או כש-`form.id` קיים (פריט שמור — הוא כבר מופיע ב-`rows` דרך פריטי העבודה שלו). אחרת הוא מחזיר מערך עם שורה יחידה:
- `category` — `form.category` אם נבחרה, אחרת המחרוזת `משימה חדשה`
- `icon` — `folderIcon(form.category)` (ייבא `folderIcon` מ-`../../lib/categories.js`, לצד `foldersFor` שכבר מיובא משם)
- `pending: true` — גורם ל-`ResourceGrid` לצייר את תגית "בעריכה" על תווית השורה
- `days` — מיפוי של `weekDates`: לכל תאריך חשב את יום השבוע שלו עם `fromISODate(date).getDay()` (כבר מיובא בקובץ). הימים הפעילים הם `form.weekdays` כש-`form.shape === "template"`, וכל שבעת הימים כש-`form.shape === "weekly"`. ביום פעיל התא מקבל פריט יחיד עם `id` יציב שנגזר מ-`resolvedActiveKey` (לא מ-`Date.now()` ולא מאינדקס — דטרמיניזם, עקרון ברזל 1), `pending: true`, `label` = `form.title` לאחר `trim` או `משימה חדשה` כשהוא ריק, ו-`startTime`/`endTime` מהטופס כש-`shape === "template"` או `null` כש-`shape === "weekly"`. ביום שאינו פעיל התא מקבל מערך פריטים ריק.
- תלויות ה-`useMemo`: `form`, `weekDates`, `resolvedActiveKey`.

**סגירת המנגנון הישן** — מחק את חישוב `dayItems` ואת המשתנה `sundayISO` שמזין רק אותו, והסר את `expectedDatesForWeek` מה-import של `../../lib/positions.js` (`buildDivisionRows` נשאר — הוא עדיין בשימוש בשמירה ובתצוגת החלוקה). אל תיגע בשום פונקציה אחרת בקובץ: `save`, `saveAndNext`, `removeItem`, `addBlank`, `fillFixedPattern`, `restWarning`, `smallestRestGapBetween`, `intervalsFor` — כולן נשארות כפי שהן (D-03).

עדכן את כותרת הקובץ בראש `RosterWizard.jsx` במשפט אחד: פאנל התצוגה של האשף מצייר מעתה את אותו דפוס משותף שמסך המשאבים והיומן מציגים, מתוך פריטי העבודה בפועל ולא מתוך העמדות הצפויות.
  </action>

  <verify>
    <automated>npm run build</automated>
    <automated>npm test</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/RosterWizard.jsx','utf8'); for(const n of ['buildResourceRows','pendingRows','folderIcon','useSyncExternalStore']) if(!s.includes(n)) throw new Error('missing '+n); if(s.includes('expectedDatesForWeek')) throw new Error('old expected-dates path still present'); if(/Date\.now\(\)/.test(s.split('pendingRows')[1]||'')) throw new Error('non-deterministic id in pending row'); console.log('ok')"</automated>
  </verify>

  <acceptance_criteria>
- `RosterWizard.jsx` עושה destructure ל-`shifts = []` ו-`tasks = []` מהפרופס
- `src/components/supervisor/WeekFlow.jsx` **לא** שונה
- הקובץ מכיל `useSyncExternalStore(subscribeTerms, termProfile, termProfile)` (D-07)
- קיים `rows` שנבנה מ-`buildResourceRows({ shifts, tasks, weekDates, mode })`
- קיים `pendingRows` שמחזיר מערך ריק כשאין `form` או כש-`form.id` קיים, ושורה אחת אחרת
- המזהה של פריט-הרפאים נגזר מ-`resolvedActiveKey` — אין `Date.now()` ואין `Math.random()` במסלול הזה (עקרון ברזל 1: דטרמיניזם)
- `dayItems`, `sundayISO` והייבוא `expectedDatesForWeek` הוסרו מהקובץ
- `buildDivisionRows` עדיין מיובא ובשימוש
- `src/lib/resourceView.js` ו-`src/components/supervisor/ResourceGrid.jsx` **לא** שונו (D-02/D-04)
- `npm run build` ו-`npm test` עוברים
  </acceptance_criteria>

  <done>
`RosterWizard.jsx` מחזיק `rows` ו-`pendingRows` מוכנים לרינדור, המסלול הישן מבוסס-`expectedDatesForWeek` הוסר, והבנייה והבדיקות עוברות.
  </done>
</task>

<task type="auto">
  <name>Task 2: החלפת פאנל התצוגה ב-ResourceGrid, לצד טופס העריכה</name>

  <files>
src/components/supervisor/RosterWizard.jsx
  </files>

  <read_first>
- `src/components/supervisor/RosterWizard.jsx` שורות ~379–411 — הפריסה הדו-טורית `grid gap-4 lg:grid-cols-[1.3fr_1fr] items-start` והכרטיס "ככה השבוע נראה עד עכשיו" שבעמודה הימנית; שורות ~413–541 הן טופס העריכה **שאסור לגעת בו**
- `src/components/supervisor/ResourceView.jsx` (אחרי 06-01) — הדוגמה לאיך קוראים ל-`ResourceGrid` עם `rows`/`dates`/`guards`
- `src/components/supervisor/ResourceGrid.jsx` — בפרט ההתנהגות כש-`rows.length === 0` (מחזיר `null`), כדי לדעת מה הקורא חייב לכסות
- `.claude/CLAUDE.md` — קטע Comments (הערות עברית לתחום המוצר)
  </read_first>

  <action>
החלף את גוף כרטיס התצוגה בפריסה הדו-טורית הקיימת ברכיב הגריד המשותף (D-04), והשאר את עמודת טופס העריכה נוגעת-לא-נוגעת (D-03).

ייבא את `ResourceGrid` מ-`./ResourceGrid.jsx`.

**בעמודה הימנית של `grid gap-4 lg:grid-cols-[1.3fr_1fr] items-start`** — בתוך אותו `Card` ותחת אותה כותרת `h3` הקיימת ("ככה השבוע נראה עד עכשיו", אותן מחלקות), הסר את כל ה-`div` בעל `grid grid-cols-4 sm:grid-cols-7 gap-2` ואת כל מה שבתוכו (כותרות הימים, תיבות ה-placeholder המקווקוות, והצ'יפים `bg-accent/15 text-accent`). במקומו:
- חשב `const allRows = [...rows, ...pendingRows];`
- כש-`allRows.length === 0` הצג פסקה אחת: `text-[12px] text-faint leading-relaxed` עם הטקסט `עוד לא הוגדרה אף משימה לשבוע הזה. הגדירו אחת בטופס שלצד — היא תופיע כאן מיד.` — זה המצב-הריק של הקורא, כי הרכיב המשותף מחזיר `null` במצב הזה.
- אחרת רנדר `<ResourceGrid rows={allRows} dates={weekDates} guards={guards} />` בתוך עוטף `-mx-4 -mb-4` (או מרווח שקול) כך שהגלילה האופקית של הטבלה תגיע לקצה הכרטיס במקום להיחתך בתוך ה-padding שלו. `firstColLabel` נשאר ברירת המחדל.
- שנה את ה-`Card` שמכיל את הכרטיס ל-`p-4 overflow-hidden` כדי שהעמודה הנעוצה והגלילה האופקית לא יזלגו מחוץ לפינות המעוגלות.

**מה שאסור להשתנות בתוכנית הזו:** עמודת טופס העריכה (מ-`{form && (` ועד סוף הטופס), פס הטאבים של בחירת המשימה, הבאנר "הכול חוסם הכול", `PageHeader` וכפתור "מלא לי שבוע במבנה קבוע". אף אחד מהם לא נגרר לשינוי.

הוסף הערה קצרה בעברית מעל הכרטיס שמסבירה את ההחלטה, במונחים ולא בציטוט: הפאנל מציג מעתה את פריטי העבודה בפועל של השבוע דרך הרכיב המשותף — אותו מבנה בדיוק שמסך המשאבים והיומן מציגים — והמשימה שבעריכה מופיעה בו כשורה מקווקוות עד שהיא נשמרת.
  </action>

  <verify>
    <automated>npm run build</automated>
    <automated>npm test</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/RosterWizard.jsx','utf8'); if(!/<ResourceGrid/.test(s)) throw new Error('ResourceGrid not rendered'); if(/grid-cols-4 sm:grid-cols-7/.test(s)) throw new Error('old day-strip markup still present'); if(!/lg:grid-cols-\[1\.3fr_1fr\]/.test(s)) throw new Error('two-column layout lost'); if(!/form\.requiredGuards/.test(s)) throw new Error('editing form was damaged'); if(!/saveAndNext/.test(s)) throw new Error('editing form was damaged'); console.log('ok')"</automated>
    <human-check>הרץ `npm run dev`, היכנס לצוות במצב army, פתח "השבוע" → שלב בניית השבוע, ואמת בזה אחר זה: (1) עמודת התצוגה מציגה טבלה עם שורת עמדה/קטגוריה נעוצה מימין ושבע עמודות יום — אותו מראה כמו "מבט משאבים"; (2) טופס העריכה מימינה עובד במלואו — בחירת קטגוריה, ימים, שעות, מונה הכפופים, אזהרת המנוחה ו"המשך למשימה הבאה"; (3) לחיצה על "הוסף משימה" והקלדת שם מציגה מיד שורה מקווקוות עם התגית "בעריכה" בגריד; (4) אחרי שמירה, אותה משימה מופיעה כשורה רגילה עם טווח השעות שלה; (5) קטגוריה שיש בה 1, 2, 3 ו-5 משמרות באותו יום מציגה את כולן — התא גדל, לא נחתך.</human-check>
  </verify>

  <acceptance_criteria>
- `RosterWizard.jsx` מרנדר `<ResourceGrid rows={...} dates={weekDates} guards={guards} />`
- ה-markup של רצועת-הימים (`grid grid-cols-4 sm:grid-cols-7`) הוסר מהקובץ
- הפריסה הדו-טורית `lg:grid-cols-[1.3fr_1fr]` נשמרה — הגריד בעמודה אחת, טופס העריכה בשנייה (D-03)
- כל שדות טופס העריכה קיימים ופועלים: `title`, `category`, מתג 24/7, `divisionHours`, בוחר הימים, שעות, `requiredGuards`, `restWarning`, `saveAndNext`
- קיים מצב-ריק טקסטואלי כש-`rows` ו-`pendingRows` ריקים שניהם
- `src/components/supervisor/ResourceGrid.jsx` **לא** שונה בתוכנית הזו (D-04 — חוזה ה-API מ-06-01 הספיק)
- `npm run build` ו-`npm test` עוברים
- אימות חי בדפדפן בוצע על חמש הנקודות שב-`human-check` והתוצאה מדווחת כפי שנראתה (CLAUDE.md, עקרון ברזל 6)
  </acceptance_criteria>

  <done>
מסך בניית השבוע מציג את דפוס הגריד המשותף בעמודת התצוגה, טופס העריכה שלם וללא שינוי לצידו, המשימה שבעריכה נראית כשורה מקווקוות, ושלוש-עד-חמש משמרות באותה קטגוריה ביום אחד מוצגות כולן.
  </done>

  <reversibility rating="reversible">
    שינוי JSX בקובץ אחד. חזרה = revert לקומיט. אין נתונים, אין סכימה, אין חוזה חיצוני.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `shifts`/`tasks` מ-`useGuardian` → פאנל התצוגה של האשף | נתוני שיבוץ אמיתיים של הצוות נכנסים למסך שקודם הציג רק שמות עמדות |
| טיוטת הטופס (`form`) → שורת-רפאים ב-DOM | קלט חופשי שהמנהל מקליד ברגע זה מרונדר מיידית |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Information Disclosure | פאנל התצוגה ב-`RosterWizard.jsx` | low | accept | הפאנל מציג את אותם `shifts`/`tasks` ש-`WeekFlow` כבר מעביר ומרנדר בשלבים הסמוכים (`UnifiedBoard`, `AvailView`). אין הרחבה של גבול הנתונים — רק הצגה של מה שכבר בהישג יד באותו מסך |
| T-06-06 | Tampering (XSS) | שורת-הרפאים — `form.title`, `form.category` | medium | mitigate | `label` ו-`category` עוברים כטקסט-ילד של JSX בלבד (React escaping). אסור `dangerouslySetInnerHTML` ואסורה הרכבת HTML ממחרוזת |
| T-06-07 | Repudiation | ההבדל בין "עמדה צפויה" ל"פריט עבודה קיים" | low | accept | השינוי מגדיל דיוק: המסך מדווח את מה שקיים בפועל ולא את הכוונה. מתועד ב-`<objective>` וב-SUMMARY כדי שהפער לא ייקרא כבאג |
| T-06-08 | Tampering | דטרמיניזם מזהה שורת-הרפאים | low | mitigate | ה-`id` נגזר מ-`resolvedActiveKey` ולא מ-`Date.now()`/`Math.random()` — נאכף כקריטריון קבלה ובבדיקה אוטומטית ב-Task 1 |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm run build` ו-`npm test` עוברים
2. `ResourceGrid.jsx` לא נערך — 06-02 צרכה בלבד
3. `WeekFlow.jsx` לא נערך
4. טופס עריכת העמדות שלם: כל שדה קיים ופועל
5. אימות חי בדפדפן על חמש הנקודות ב-`human-check` של Task 2, כולל מקרה 1/2/3/5 המשמרות
</verification>

<success_criteria>
- RESVIEW-01 מתקיים: מבנה שורת-עמדה/עמודות-יום/שמות-משובצים במסך בניית השבוע, זהה למסך "מבט משאבים"
- טופס עריכת העמדות לא נפגע (D-03)
- המשוב החי "בעריכה" שרד את החלפת הפאנל
- RESVIEW-03 נראה בפועל גם במסך הזה
</success_criteria>

<output>
Create `.planning/phases/06-resource-view-pattern/06-02-SUMMARY.md` when done.

חובה לתעד ב-SUMMARY: תוצאת האימות החי על חמש הנקודות (מה נראה בפועל, לא מה היה אמור), והאם נדרשה התאמת מרווחים/`overflow` מעבר למתוכנן.
</output>
