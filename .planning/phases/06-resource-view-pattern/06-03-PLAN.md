---
phase: 06-resource-view-pattern
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/supervisor/CalendarView.jsx
  - src/components/supervisor/WeekTimeGrid.jsx
  - src/components/supervisor/WeekTimeGrid.css
  - src/components/supervisor/UnifiedBoard.jsx
  - src/design/categoryPalette.js
autonomous: true
requirements: [RESVIEW-02, RESVIEW-03]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "מנהל פותח את היומן בתצוגת שבוע ורואה את אותו מבנה שורות/עמודות, אותה טיפוגרפיה ואותו ריווח שהוא רואה ב\"מבט משאבים\" — אין יותר שתי שפות עיצוב על אותו נתון (RESVIEW-02)."
    - "תצוגות חודש ויום ביומן ממשיכות לעבוד בדיוק כפי שהן — MonthGrid ו-DayList לא נגעו."
    - "משמרת חסרת-איוש עדיין מסומנת ככזו בתצוגת השבוע — המידע ש-WeekTimeGrid הציג לא אבד."
    - "עמדה עם 1, 2, 3 או 5 משמרות ביום מוצגת נכון גם ביומן (RESVIEW-03)."
    - "אין בעץ המקור קובץ יתום או ייבוא מת שנשארו מהרכיב שהוחלף."
  artifacts:
    - src/components/supervisor/CalendarView.jsx
    - src/design/categoryPalette.js
    - src/components/supervisor/UnifiedBoard.jsx
  key_links:
    - "CalendarView.jsx ענף `mode === \"week\"` → buildResourceRows → ResourceGrid — נקודת ההחלפה היחידה; ענפי month/day לא נוגעים בה."
    - "TONE_VARS (src/design/categoryPalette.js) → MonthGrid/DayList/UnifiedBoard — נשאר בשימוש גם אחרי המחיקה; רק ההנמקה בהערה מתעדכנת."
---

<objective>
להחליף את תצוגת השבוע ביומן — היום רכיב עטיפה סביב react-big-calendar עם ציר שעות אנכי — במבנה הטבלה קטגוריה×יום של הרכיב המשותף `ResourceGrid`, ולמחוק את הרכיב שהוחלף יחד עם גיליון הסגנונות שלו.

Purpose: RESVIEW-02. אותו נתון מוצג היום בשתי שפות עיצוב שונות לגמרי — טבלה במסך המשאבים, ציר-שעות ביומן. מנהל שעובר ביניהם מרגיש שעבר אפליקציה.

Output: `CalendarView.jsx` שמרנדר `ResourceGrid` בענף השבוע; `WeekTimeGrid.jsx` ו-`WeekTimeGrid.css` נמחקים; שתי הערות מקור שמפנות אליהם מתוקנות.

**החלטות רלוונטיות (מפה מלאה ב-06-01):**
- **D-01** — החלפה מבנית מלאה, לא restyle. תצוגות חודש/יום ביומן נשארות כפי שהן
- **D-02** — הנתונים מ-`buildResourceRows`; אין מיזוג חדש
- **D-04** — צורכים את `ResourceGrid` מ-06-01; אסור לשכפל JSX ואסור לערוך את `ResourceGrid.jsx`
- **D-05/D-06/D-07** — כמו בשאר התוכניות

**ממצא שסותר הנחה ב-`06-CONTEXT.md` — קרא לפני שמתחילים:** `06-CONTEXT.md` מציין ש"RBC נשאר בשימוש בתצוגות חודש/יום של `CalendarView.jsx`". בפועל `MonthGrid` ו-`DayList` הם רכיבים כתובים-ביד באותו קובץ (שורות ~203–370) ואינם נוגעים ב-react-big-calendar כלל. הצרכן היחיד של הספרייה בכל עץ המקור הוא הרכיב שמוחלף כאן. משמעות: תצוגות חודש/יום אינן נפגעות (ההחלטה עומדת בתוקפה), אבל מחיקת הרכיב משאירה מאחוריה גם צרכנים מתים נוספים — ראו "לא בסקופ" למטה.

**הכרעת מתכנן שיש לדעת עליה (J-1) — ה-`lazy()` יורד:** `06-CONTEXT.md` מבקש "לשמור על אותו lazy-loading pattern ברכיב החדש". הסיבה המקורית ל-`lazy()` מתועדת בקוד עצמו: react-big-calendar שוקל כ-190KB gzip ואין טעם להעמיס אותה על מי שנכנס ליומן ולא מגיע לשבוע. אחרי ההחלפה הספרייה לא נטענת בכלל — כלומר **המטרה מושגת במלואה ובאופן חזק יותר**. `ResourceGrid` הוא טבלה קטנה בלי תלויות חיצוניות, ו-`ResourceView.jsx` כבר מייבא אותו ישירות בחבילה הראשית; `lazy()` עליו היה מוסיף גבול `Suspense` שמהבהב ספינר על מודול שכבר טעון. לכן: ייבוא ישיר, בלי `lazy` ובלי `Suspense`. **אם המפעיל מעדיף לשמר את ה-`lazy()` כלשונו — זו נקודת עצירה לשאול לפניה, לא להחליט בשקט.**

**במפורש לא בסקופ בפאזה הזו (הכרעת מתכנן J-4, לא השמטה):**
- `src/lib/calendarEvents.js` ו-`scripts/verify-calendar-events.mjs` — נשארים. זהו מודול טהור עם בדיקת Node משלו ב-`npm test`; אחרי המחיקה אין לו צרכן ב-UI, אבל Phase 8 (WEEKBUILD-05, "לוח שבועי ממוקד שמראה בדיוק אילו שעות העמדה תפוסה") היא בדיוק המקרה שעשוי לצרוך אותו שוב. מחיקתו עכשיו היא ניחוש, ועלותו כשהוא יושב ללא שימוש היא אפס.
- `react-big-calendar` ו-`dayjs` ב-`package.json` — נשארים מאותה סיבה. אף מודול לא מייבא אותם אחרי התוכנית הזו, ולכן הם אינם נכנסים ל-bundle; העלות היחידה היא `node_modules`. הסרה מה-manifest שייכת להכרעה של Phase 8.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-resource-view-pattern/06-CONTEXT.md
@.planning/phases/06-resource-view-pattern/06-01-SUMMARY.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/supervisor/ResourceGrid.jsx
@src/components/supervisor/CalendarView.jsx
</context>

<artifacts>
## Artifacts this plan produces

אין קובץ חדש ואין export חדש.

**סמלים פנימיים חדשים ב-`CalendarView.jsx`:**
- `weekRows` — `useMemo` סביב `buildResourceRows({ shifts, tasks, weekDates: dates, mode: teamMode })`, מחושב רק בענף השבוע

**נמחקים:**
- `src/components/supervisor/WeekTimeGrid.jsx` (194 שורות) — הרכיב, ה-default export שלו, ו-`EventContent`/`eventPropGetter` הפנימיים
- `src/components/supervisor/WeekTimeGrid.css` (81 שורות)
- ב-`CalendarView.jsx`: הייבוא ב-`lazy()`, עטיפת ה-`Suspense`, והייבוא של `Spinner`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: תצוגת השבוע ביומן עוברת ל-ResourceGrid</name>

  <files>
src/components/supervisor/CalendarView.jsx
  </files>

  <read_first>
- `src/components/supervisor/CalendarView.jsx` — כל הקובץ. במיוחד: שורה 14 (`lazy`), שורות ~66–82 (`dates`, `merged`, `byDate`), שורות ~168–178 (ענף `mode === "week"` ועטיפת ה-`Suspense`), ושורות ~203–370 (`MonthGrid`/`DayList` — **לא נוגעים**)
- `src/components/supervisor/WeekTimeGrid.jsx` שורות 93–130 — מה בדיוק התצוגה הישנה הציגה לכל אירוע (שעה, כותרת, שמות, תג חוסר-איוש), כדי לוודא שלא אובד מידע במעבר
- `.planning/phases/06-resource-view-pattern/06-01-SUMMARY.md` — החתימה הסופית של `ResourceGrid`
- `src/components/supervisor/ResourceView.jsx` (אחרי 06-01) — הדוגמה לקריאה תקינה ל-`ResourceGrid`
- `src/lib/resourceView.js` — פלט `buildResourceRows`. **לא נוגעים** (D-02)
  </read_first>

  <action>
החלף את גוף ענף השבוע ב-`CalendarView.jsx` ברכיב הגריד המשותף (D-01/D-04).

**ייבוא** — הוסף `import ResourceGrid from "./ResourceGrid.jsx";` ו-`import { buildResourceRows } from "../../lib/resourceView.js";`. הסר את שורת ה-`lazy()` בראש הקובץ יחד עם הערת ה-190KB שמעליה (היא מתעדת החלטה שכבר לא רלוונטית), הסר את `lazy` ו-`Suspense` מייבוא ה-`react`, והסר את `Spinner` מהייבוא של `../ui.jsx` (שאר הסמלים משם — `Badge`, `Btn`, `Card`, `EmptyState`, `IconBtn`, `PageHeader`, `guardColor`, `readableInk` — נשארים בשימוש ב-`MonthGrid`/`DayList`; ודא לפני ההסרה).

**שורות השבוע** — הוסף `const weekRows = useMemo(() => (mode === "week" ? buildResourceRows({ shifts, tasks, weekDates: dates, mode: teamMode }) : []), [mode, shifts, tasks, dates, teamMode]);`. ה-hook נקרא ללא תנאי; רק הגוף שלו מותנה, כדי שפיבוט של רשת חודש בת 42 יום לא יחושב לשווא. שים לב לשתי המשמעויות של השם `mode` בקובץ: המשתנה המקומי `mode` הוא רזולוציית התצוגה (`month`/`week`/`day`), ואילו תחום הפעילות הוא `teamMode` — זה הערך שנכנס ל-`buildResourceRows`.

**הענף עצמו** — במקום ה-`Suspense` והרכיב הישן, רנדר בענף `mode === "week"`:
- כש-`weekRows.length === 0`: אל תרנדר כלום בתוך ה-`Card`. ה-`EmptyState` הקיים בתחתית הרכיב (שורות ~185–198, מותנה ב-`shown.length === 0 && mode !== "day"`) כבר מכסה את המצב הזה ואין ליצור שני מצבים-ריקים מתחרים.
- אחרת: `<ResourceGrid rows={weekRows} dates={dates} guards={guards} />` בעוטף שמנטרל את ה-padding של ה-`Card` בצדדים (`-mx-3 sm:-mx-4`) כך שהגלילה האופקית והעמודה הנעוצה יגיעו לקצה הכרטיס. `firstColLabel` נשאר ברירת המחדל.
- הוסף `overflow-hidden` ל-`Card` העוטף (היום `p-3 sm:p-4`) כדי שהתוכן הגולש לא יזלוג מהפינות המעוגלות.

**מה שאסור להשתנות:** `MODES` ובורר הרזולוציה, חישוב `dates`, `merged`/`byDate`, `step()`, `title`, `coverageOf`, `MonthGrid`, `DayList`, וה-`EmptyState` בתחתית. `teamMode` נשאר נצרך על ידי `MonthGrid`/`DayList` ולכן `useSyncExternalStore` נשאר במקומו.

עדכן את כותרת ה-`CALENDAR` בראש הקובץ: "One dataset, three zoom levels" עדיין נכון, אבל תצוגת השבוע היא מעתה אותו מבנה קטגוריה×יום שמסך המשאבים ומסך בניית השבוע מציגים — רזולוציה שונה, לא שפה שונה.

בנוסף — עדכן את ההערה בשורות ~278–283 (הפסקה על הסרת WeekStrip) כך שתפנה למנגנון הנוכחי במקום לרכיב שנמחק: שיבוץ מחדש בגרירה חי ב-`UnifiedBoard.jsx` (שלב "אסדר בעצמי" בבניית השבוע) ולא ביומן, ואותו `DRAG_MIME` לא משוכפל כאן.
<!-- planner-discipline-allow: WeekTimeGrid -->
  </action>

  <verify>
    <automated>npm run build</automated>
    <automated>npm test</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/CalendarView.jsx','utf8'); if(!/<ResourceGrid/.test(s)) throw new Error('ResourceGrid not rendered'); for(const n of ['lazy(','Suspense','Spinner']) if(s.includes(n)) throw new Error('stale symbol: '+n); for(const n of ['MonthGrid','DayList','coverageOf','TONE_VARS']) if(!s.includes(n)) throw new Error('month/day path damaged: '+n); if(!/weekDates:\s*dates/.test(s)) throw new Error('buildResourceRows not wired to the shown dates'); console.log('ok')"</automated>
    <human-check>הרץ `npm run dev`, פתח את היומן. (1) תצוגת שבוע: טבלה עם עמודה ראשונה נעוצה מימין ושבע עמודות יום — זהה למראה של "מבט משאבים"; טווחי שעות מיושרים, שמות משובצים קריאים, ומשמרת חסרת-איוש נושאת תג `{יש}/{צריך}`. (2) לחץ "הקודם"/"הבא" — השבוע מתחלף והטבלה מתעדכנת. (3) עבור לתצוגת חודש ולתצוגת יום וחזור — שתיהן עובדות בדיוק כמו קודם, כולל צביעת הנקודות בחודש ורצועות הצבע ביום. (4) ודא שאין הבהוב ספינר בכניסה לתצוגת השבוע.</human-check>
  </verify>

  <acceptance_criteria>
- `CalendarView.jsx` מרנדר `<ResourceGrid rows={weekRows} dates={dates} guards={guards} />` בענף `mode === "week"`
- הקובץ אינו מכיל `lazy(`, `Suspense` או `Spinner`
- `weekRows` נבנה מ-`buildResourceRows({ shifts, tasks, weekDates: dates, mode: teamMode })` — עם `teamMode` (תחום הפעילות), לא עם `mode` (רזולוציית התצוגה)
- `MonthGrid`, `DayList`, `coverageOf`, `MODES`, `step`, `merged`/`byDate` ו-`EmptyState` התחתון קיימים ולא שונו
- אין מצב-ריק חדש בתוך ענף השבוע — ה-`EmptyState` הקיים בתחתית נשאר היחיד
- `src/components/supervisor/ResourceGrid.jsx` ו-`src/lib/resourceView.js` **לא** שונו (D-02/D-04)
- `npm run build` ו-`npm test` עוברים
- אימות חי בוצע על ארבע הנקודות ב-`human-check` והתוצאה מדווחת כפי שנראתה
  </acceptance_criteria>

  <done>
תצוגת השבוע ביומן מרונדרת דרך הרכיב המשותף, תצוגות חודש ויום לא נפגעו, אין גבול `Suspense` מיותר, והמידע על חוסר-איוש שרד את המעבר.
  </done>

  <reversibility rating="costly">
    ההחלפה עצמה הפיכה בקומיט, אבל היא קובעת את כיוון תצוגת-השבוע לכל המוצר (Phase 7 ו-8 מאמצות את הדפוס הזה). חזרה אחרי שהן נבנו עליו יקרה. הפחתת סיכון: המחיקה בפועל מופרדת ל-Task 2, כך שיש חלון ביניים שבו הרכיב הישן עדיין בעץ.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: מחיקת הרכיב שהוחלף וניקוי ההפניות שנותרו</name>

  <files>
src/components/supervisor/WeekTimeGrid.jsx (נמחק)
src/components/supervisor/WeekTimeGrid.css (נמחק)
src/components/supervisor/UnifiedBoard.jsx
src/design/categoryPalette.js
  </files>

  <read_first>
- `src/components/supervisor/UnifiedBoard.jsx` שורה ~171 — הערה שמפנה לרכיב שנמחק; ודא מה היא באמת אומרת לפני שאתה מנסח אותה מחדש
- `src/design/categoryPalette.js` שורות ~60–74 — ההנמקה של `TONE_VARS` תלויה בספרייה שיוצאת מהשימוש, אבל `TONE_VARS` **עצמו נשאר בשימוש** ב-`MonthGrid`, ב-`DayList` וב-`UnifiedBoard`
- `src/lib/calendarEvents.js` ו-`scripts/verify-calendar-events.mjs` — קרא כדי לאשר שהם עצמאיים ואינם נשברים מהמחיקה. **הם נשארים** (ראו "לא בסקופ" ב-`<objective>`)
- `package.json` — אישור שאף סקריפט בנייה/בדיקה לא מפנה לקבצים הנמחקים
  </read_first>

  <action>
מחק את הרכיב שהוחלף ונקה אחריו, כדי שלא יישאר קובץ יתום ולא הפניה משקרת (הנחיית `06-CONTEXT.md`: לא להשאיר גיליון סגנונות מיותם).

מחק את שני הקבצים: `src/components/supervisor/WeekTimeGrid.jsx` ו-`src/components/supervisor/WeekTimeGrid.css`. השתמש ב-`git rm` כדי שהמחיקה תיכנס ל-index.

נקה שתי הפניות שנשארו תלויות באוויר:
- `src/components/supervisor/UnifiedBoard.jsx` שורה ~171 — ההערה מסבירה שהגוון נלקח מאותו מקור שהיומן משתמש בו. נסח אותה מחדש כך שתפנה ל-`CalendarView.jsx` בלבד, במונחים ולא בציטוט של שם קובץ שנמחק. **ההערה בלבד משתנה — אין לגעת בשורת קוד כלשהי בקובץ הזה.**
- `src/design/categoryPalette.js` שורות ~60–74 — ההנמקה מעל `TONE_VARS` מסבירה למה נדרש ערך צבע חי ל-`style` מוטבע, ומצדיקה זאת בכך שהספרייה החיצונית כותבת `backgroundColor` ישירות ל-DOM. הצדקה זו כבר לא קיימת. נסח מחדש כך שתצדיק את אותו מנגנון בצרכנים האמיתיים שנשארו — נקודות החודש ב-`MonthGrid`, רצועות הצבע ב-`DayList`, ו-`UnifiedBoard` — שכולם כותבים `style={{ background: ... }}` מוטבע ולא מחלקת Tailwind. הנימוק על `rgb(var(--x))` מול hex קבוע (התחלפות אוטומטית בין מצב בהיר לכהה) נכון ונשאר. **ההערה בלבד משתנה — `TONE_VARS`, `TONE_CLASSES` ו-`categoryTone` נשארים בדיוק כפי שהם** (D-06).

אל תיגע ב-`src/lib/calendarEvents.js`, ב-`scripts/verify-calendar-events.mjs` וב-`package.json` — ההחלטה להשאיר אותם מתועדת ב-`<objective>` ושייכת ל-Phase 8. ההערות ב-`src/lib/resourceView.js` שמצליבות מול `calendarEvents.js` נשארות נכונות ואין לגעת בהן.
<!-- planner-discipline-allow: WeekTimeGrid -->
<!-- planner-discipline-allow: react-big-calendar -->
  </action>

  <verify>
    <automated>test ! -f src/components/supervisor/WeekTimeGrid.jsx &amp;&amp; test ! -f src/components/supervisor/WeekTimeGrid.css &amp;&amp; echo ok</automated>
    <automated>node -e "const cp=require('child_process'); const out=cp.execSync('git grep -n WeekTimeGrid -- src scripts || true',{encoding:'utf8'}).trim(); if(out) throw new Error('dangling references remain:\n'+out); console.log('ok')"</automated>
    <automated>node -e "const cp=require('child_process'); const out=cp.execSync('git grep -l react-big-calendar -- src/components src/design || true',{encoding:'utf8'}).trim(); if(out) throw new Error('library still referenced in UI layer: '+out); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/design/categoryPalette.js','utf8'); for(const n of ['TONE_VARS','TONE_CLASSES','categoryTone','cat-red','faint']) if(!s.includes(n)) throw new Error('palette API damaged: '+n); console.log('ok')"</automated>
    <automated>npm run build</automated>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
- שני הקבצים שנמחקו אינם קיימים בעץ העבודה ומחיקתם רשומה ב-git index
- `git grep` על `src` ו-`scripts` לא מחזיר אף הפניה לרכיב שנמחק — כולל הערות
- אין בעץ המקור ייבוא כלשהו של הספרייה החיצונית שהוחלפה
- `src/design/categoryPalette.js` — רק ההערה שונתה; `categoryTone`, `TONE_CLASSES` ו-`TONE_VARS` זהים ביט-בביט להתנהגותם הקודמת (D-06)
- `src/components/supervisor/UnifiedBoard.jsx` — רק ההערה שונתה; אין שינוי בשורת קוד
- `src/lib/calendarEvents.js`, `scripts/verify-calendar-events.mjs` ו-`package.json` לא שונו
- `npm run build` עובר; `npm test` עובר במלואו, כולל `verify-calendar-events.mjs`
  </acceptance_criteria>

  <done>
הרכיב שהוחלף וגיליון הסגנונות שלו נמחקו, אין בעץ המקור הפניה או ייבוא שנותרו מאחור, לוח הצבעים לא נפגע, והבנייה והבדיקות עוברות.
  </done>

  <reversibility rating="reversible">
    מחיקת קבצים ב-git — שחזור בקומיט אחד מההיסטוריה. התלויות ב-`package.json` נשארות במכוון, כך ששחזור אינו דורש התקנה מחדש.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `shifts`/`tasks` מ-`useGuardian` → תצוגת השבוע ביומן | אותם נתוני צוות, מסלול רינדור חדש |
| קוד ספרייה חיצונית → DOM | הספרייה החיצונית שכתבה סגנונות ישירות ל-DOM יוצאת מהמסלול; מוחלפת ב-JSX מקומי בלבד |
| מחיקת קבצים → תלויות בזמן בנייה | ייבוא שנשאר תלוי באוויר שובר את הבנייה |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Denial of Service | `CalendarView.jsx` — ייבוא שנשאר אחרי המחיקה | medium | mitigate | `npm run build` + בדיקת `git grep` על שתי תיקיות המקור כשער חובה ב-Task 2; המחיקה מופרדת מההחלפה כדי שהבנייה תיבדק פעמיים |
| T-06-10 | Information Disclosure | תג חוסר-איוש שאבד במעבר | low | mitigate | התג הועבר במכוון לרכיב המשותף ב-06-01; נאכף בקריטריון קבלה ובאימות חי ב-Task 1 — מניעת אובדן מידע תפעולי, לא הוספת מידע חדש |
| T-06-11 | Tampering (XSS) | רינדור אירועי היומן | low | mitigate | מסלול הרינדור החדש מרנדר טקסט בלבד דרך JSX ואינו כותב סגנון או HTML ל-DOM ממחרוזת — צמצום של משטח התקיפה לעומת המצב הקודם |
| T-06-12 | Elevation of Privilege | תלויות חיצוניות שנשארות ב-manifest בלי צרכן | low | accept | ללא ייבוא הן אינן נכנסות ל-bundle ואינן רצות. הן נשארות בהישג יד ל-Phase 8; ההחלטה מתועדת במפורש ב-`<objective>` ובקטע "לא בסקופ", לא מושמטת בשקט |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל. **הסרת** חבילות קיימת אף היא מחוץ לסקופ, במכוון.
</threat_model>

<verification>
1. `npm run build` ו-`npm test` עוברים אחרי כל אחת משתי המשימות
2. `git grep` על `src` ועל `scripts` נקי מהפניות לרכיב שנמחק
3. אין בעץ המקור ייבוא של הספרייה החיצונית שהוחלפה
4. `MonthGrid` ו-`DayList` ללא שינוי — נבדק במקור ובדפדפן
5. `categoryTone`/`TONE_CLASSES`/`TONE_VARS` ללא שינוי התנהגותי
6. אימות חי: תצוגת שבוע נראית כמו "מבט משאבים"; חודש ויום עובדים כרגיל; אין הבהוב ספינר
</verification>

<success_criteria>
- RESVIEW-02 מתקיים: מבנה, טיפוגרפיה וריווח זהים בין תצוגת השבוע ביומן לבין "מבט משאבים"
- תצוגות חודש ויום ללא רגרסיה
- אין קובץ יתום ואין הפניה משקרת בעץ המקור
- ההחלטות שנלקחו בניגוד להנחות `06-CONTEXT.md` (ביטול ה-`lazy`, השארת התלויות והמודול הטהור) מתועדות ב-SUMMARY
</success_criteria>

<output>
Create `.planning/phases/06-resource-view-pattern/06-03-SUMMARY.md` when done.

חובה לתעד ב-SUMMARY: (1) תוצאת האימות החי על ארבע הנקודות; (2) אישור מפורש ש-`lazy`/`Suspense` הוסרו ומדוע (J-1) — Phase 8 תקרא את זה; (3) הרשימה המדויקת של מה שנשאר בכוונה ללא צרכן (המודול הטהור, בדיקת ה-Node שלו, ושתי התלויות ב-manifest) כמועמדים להכרעה ב-Phase 8.
</output>
