---
phase: 09-rest-time-demo-button
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/supervisor/views.jsx
autonomous: true
requirements: [REST-03, REST-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "לחיצה על 'מלא לי נתוני הדגמה' (כרטיס ההקמה בדשבורד, מוצג כל עוד isNew) פותחת דיאלוג פרמטרים במקום לכתוב מיד — שום רשומת הדגמה לא נוצרת עד אישור בתוך הדיאלוג (REST-03/REST-04)."
    - "לחיצה על 'מלא נתוני הדגמה' (TeamView empty-state, מוצג רק כש-guards.length === 0) פותחת את אותו רכיב דיאלוג משותף בדיוק — לא רכיב שהומצא פעמיים (REST-03)."
    - "הדיאלוג המשותף חושף בורר כמות (7/14/15/20, ברירת מחדל 20) בגוף שלו — לפחות מספר אנשים, כנדרש ב-REST-04."
    - "רק אישור בתוך הדיאלוג קורא בפועל ל-onSeedDemo(guardCount) (המחווט ל-actions.seedDemo); ביטול, Escape, או קליק מחוץ לפאנל סוגרים בלי ליצור שום רשומה."
    - "שני הכפתורים נשארים בטקסט ובתנאי-ההצגה הנוכחיים שלהם ('מלא לי נתוני הדגמה' תלוי ב-isNew, 'מלא נתוני הדגמה' תלוי ב-guards.length === 0) — לא אוחדו לתנאי אחד."
    - "startGuestDemo / AuthPage.jsx לא נערכו — מסלול ההדגמה של האורח האנונימי נשאר לחיצה-אחת-בלי-דיאלוג כפי שהוא היום."
  artifacts:
    - src/components/supervisor/views.jsx
  key_links:
    - "כפתור 'מלא לי נתוני הדגמה' (SupDashboard) → setDemoDialogOpen(true) → <SeedDemoDialog onConfirm={onSeedDemo}> → אישור → onSeedDemo(guardCount) → actions.seedDemo (startDemo)."
    - "כפתור 'מלא נתוני הדגמה' (TeamView) → setDemoDialogOpen(true) → אותה <SeedDemoDialog onConfirm={onSeedDemo}> → אישור → onSeedDemo(guardCount) → actions.seedDemo (startDemo)."
---

<objective>
לבנות קומפוננטת דיאלוג משותפת אחת, `SeedDemoDialog`, שגוזרת בגוף שלה את בורר-הכמות הקיים (7/14/15/20), ולחווט את שני הכפתורים הקיימים שממלאים נתוני הדגמה לתוך הצוות האמיתי — כרטיס ההקמה ב-`SupDashboard` וה-empty-state ב-`TeamView` — כך ששניהם פותחים אותה קומפוננטה, ורק אישור בתוכה קורא בפועל ל-`onSeedDemo(guardCount)`.

Purpose: REST-03/REST-04 (09-CONTEXT.md). נתוני הדגמה, בכל מקום שמנהל מחובר יכול להזין אותם לצוות האמיתי שלו, נכנסים למערכת רק אחרי שהוא אישר במפורש כמה — לא בלחיצה אחת שכותבת מיד.

Output: `src/components/supervisor/views.jsx` עם `SeedDemoDialog` חדשה (מוגדרת פעם אחת, לפני `SupDashboard`), ושני הכפתורים הקיימים מחווטים אליה במקום לקרוא ל-`onSeedDemo` ישירות.

**החלטות נעולות (09-CONTEXT.md, REST-03/REST-04) — לא נפתחות מחדש:**
- הדיאלוג חל **רק** על שני המסלולים שממלאים נתונים לתוך הצוות האמיתי. **לא** נוגעים ב-`startGuestDemo`/`AuthPage.jsx` (צוות-הדגמה זמני ואנונימי, מחוץ לסקופ במפורש).
- שני הכפתורים **נשארים**, במיקומים ובתנאי-ההצגה הנוכחיים שלהם — לא מאוחדים לתנאי אחד (התנאים שונים במהותם: שלב-הקמה-לא-גמור מול צוות-בלי-אנשים).
- הדיאלוג נבנה על `Modal` הקיים (`src/components/ui.jsx:591`), לפי תבנית `Modal`+`footer` הקיימת (עורך הכשירויות, `views.jsx:2860-2922`) — לא פטרן קלט חדש. ה-`Segmented` הקיים עובר פנימה לגוף ה-Modal.
- `actions.seedDemo`'s חתימה לא משתנה — רק מי שקורא לו (דרך `onSeedDemo`) עובר דרך אישור בדיאלוג קודם.

**סיבת ה-wave/depends_on:** תלוי ב-09-01 לא מסיבה לוגית (שתי היכולות עצמאיות זו מזו) אלא כי שתיהן עורכות את אותו קובץ (`views.jsx`) — הרצה מקבילה של שתי תוכניות על אותו קובץ מסתכנת בהתנגשות עריכה, ולכן זה גל 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rest-time-demo-button/09-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/ui.jsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: SeedDemoDialog משותפת + חיווט כפתור TeamView (מסלול-מוכח-ראשון)</name>

  <files>
src/components/supervisor/views.jsx
  </files>

  <read_first>
- `src/components/supervisor/views.jsx` שורות 2540-2547 — קליטת `onSeedDemo` ו-ה-state הקיים לכמות ב-`TeamView` (זה ה-state שמוחלף במשימה הזו — שים לב לשם המדויק).
- `src/components/supervisor/views.jsx` שורות 2727-2745 — הכרטיס עם התנאי `guards.length === 0 && onSeedDemo`, ה-`Segmented` הקיים לצדו, וה-`onClick` הישיר הנוכחי על `onSeedDemo(demoCount)`.
- `src/components/supervisor/views.jsx` שורות 2860-2922 — תבנית ה-`Modal`+`footer` הקיימת (עורך הכשירויות) — הדפוס המדויק ש-`SeedDemoDialog` מאמצת: `Btn` ראשי disabled/loading-aware בתוך `footer`, ו-`Btn variant="secondary"` "ביטול" לצדו; פעולת השמירה אסינכרונית וסוגרת את ה-state של העריכה רק אחרי ה-`await` (ר' `saveQualifications`/`saveWkndPreference`).
- `src/components/ui.jsx` שורות 591-644 — API מלא של `Modal` (`open`, `onClose`, `title`, `subtitle`, `children`, `footer`, `wide`).
- `src/components/ui.jsx` שורות 561-585 — API מלא של `Segmented` (`value`, `onChange`, `options`, `size`).
- `.planning/phases/09-rest-time-demo-button/09-CONTEXT.md` קטע "REST-03/04 — היקף דיאלוג הפרמטרים" ו-"מיקום הכפתור" — הניסוח הנעול המדויק.
  </read_first>

  <action>
הגדר קומפוננטה חדשה `SeedDemoDialog({ open, onClose, onConfirm, busy })` ב-`src/components/supervisor/views.jsx`, ממוקמת מיד אחרי מערך `WEEK_PATTERNS` ולפני הערת המקטע `// DASHBOARD` (לפני `export function SupDashboard`) — כך ששני הצרכנים (`SupDashboard` ו-`TeamView`) חולקים את אותה הגדרה אחת, בלי תלות בסדר-קריאה. הוסף מעליה הערת מקטע קצרה בסגנון ההערות הקיימות בקובץ (למשל `// SEED DEMO DIALOG (REST-03/REST-04, שותף לשני נקודות-הכניסה)`).

הקומפוננטה מחזיקה `useState` פנימי לכמות בשם `guardCount` (ברירת מחדל 20), ומרנדרת את אותו `Segmented` שכבר קיים היום ליד שני הכפתורים (אפשרויות 7/14/15/20) — עכשיו בגוף ה-`Modal` במקום לצד כפתור חיצוני. בנה את ה-`Modal` בדיוק לפי תבנית עורך-הכשירויות (`read_first`): `title` שמתאר בבירור שמדובר בפרמטרים ליצירת נתוני הדגמה; גוף שמסביר בטקסט גלוי שהיצירה מוסיפה נתונים אמיתיים לצוות הנוכחי, ושרק אישור כאן יוצר אותם — לפני כן אין שום כתיבה; ו-`footer` עם `Btn` ראשי (`loading={busy}`, ומכבה-קליק מיותר באמצעות אותו מנגנון `disabled/loading` הקיים בכל שאר הדיאלוגים בקובץ) ו-`Btn variant="secondary"` "ביטול" שסוגר בלי לקרוא ל-`onConfirm` בכלל. פעולת האישור אסינכרונית: מחכה ל-`onConfirm(guardCount)` ורק אחרי שהיא מסתיימת קוראת ל-`onClose()` — אותו דפוס בדיוק כמו `saveQualifications`/`saveWkndPreference`.

בתוך `TeamView`: המר את ה-state הקיים לכמות (שורה 2546, ר' `read_first`) ל-state בוליאני חדש `demoDialogOpen` (מתחיל `false`) — ה-state של הכמות עצמו כבר לא נחוץ ברמת `TeamView`, הוא עבר לגמרי לתוך `SeedDemoDialog`. בכרטיס עם התנאי `guards.length === 0 && onSeedDemo` (שורות 2727-2745): הסר משם את ה-`Segmented`; השאר כפתור בודד עם אותו טקסט המדויק שכבר שם היום ("מלא נתוני הדגמה"), אותו אייקון (`sparkles`), ואותם `variant`/`size` — רק ה-`onClick` שלו משתנה: במקום לקרוא ישירות לפעולת הכתיבה, הוא פותח את הדיאלוג (`setDemoDialogOpen(true)`). הוסף `<SeedDemoDialog open={demoDialogOpen} onClose={() => setDemoDialogOpen(false)} onConfirm={onSeedDemo} busy={busy} />` בתוך ה-JSX של `TeamView`, לצד שני ה-`Modal`-ים הקיימים בסוף הקומפוננטה (ליד `qualEditing`/`wkndEditing`).

אל תיגע עדיין ב-`SupDashboard` — זו המשימה הבאה, שמחווטת אותו בדיוק אל אותה `SeedDemoDialog` בלי להגדיר עותק שני. אל תיגע ב-`AuthPage.jsx` או ב-`startGuestDemo` — מחוץ לסקופ במפורש.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/supervisor/views.jsx','utf8'); const dialogDefIdx=s.indexOf('function SeedDemoDialog('); if(dialogDefIdx===-1) throw new Error('SeedDemoDialog not defined'); const dashStart=s.indexOf('export function SupDashboard'); if(dashStart===-1) throw new Error('SupDashboard anchor not found'); if(dialogDefIdx>dashStart) throw new Error('SeedDemoDialog must be defined before SupDashboard so both consumers share one definition'); const teamStart=s.indexOf('export function TeamView'); const teamBody=s.slice(teamStart); for (const token of ['demoDialogOpen','setDemoDialogOpen','<SeedDemoDialog','onConfirm={onSeedDemo}']) { if (!teamBody.includes(token)) throw new Error('missing '+token+' in TeamView'); } console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/supervisor/views.jsx','utf8'); const m=s.match(/function SeedDemoDialog\(/g)||[]; if(m.length!==1) throw new Error('SeedDemoDialog must be defined exactly once, found '+m.length); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, היכנס כמנהל עם צוות ריק (guards.length === 0), פתח מסך "הצוות שלי"/"הכפופים לי". אמת: (1) הכפתור "מלא נתוני הדגמה" גלוי ומסומן בבירור ככפתור הדגמה; (2) לחיצה עליו פותחת דיאלוג — עדיין אין אף שומר/guard חדש בעמוד ברקע; (3) בורר הכמות (7/14/15/20) עובד בתוך הדיאלוג, ברירת המחדל 20; (4) לחיצה על "ביטול" סוגרת את הדיאלוג בלי שנוצרה שום רשומה (guards.length נשאר 0); (5) פתיחה חוזרת ולחיצה על כפתור האישור יוצרת בפועל את מספר האנשים שנבחר, וסוגרת את הדיאלוג.</human-check>
  </verify>

  <acceptance_criteria>
- `SeedDemoDialog({ open, onClose, onConfirm, busy })` מוגדרת פעם אחת, לפני `SupDashboard`
- הדיאלוג בנויה מעל `Modal` הקיים, עם `Segmented` (7/14/15/20, ברירת מחדל 20) בגוף וכפתורי footer בדפוס עורך-הכשירויות
- `onConfirm(guardCount)` נקרא רק בתוך פעולת האישור בדיאלוג, אף פעם לא בטעינת הקומפוננטה או בפתיחתה
- `TeamView` מחזיקה `demoDialogOpen`/`setDemoDialogOpen`, פותחת את הדיאלוג דרך הכפתור הקיים, ומעבירה `onConfirm={onSeedDemo}`
- הטקסט/אייקון/תנאי-ההצגה של כפתור "מלא נתוני הדגמה" ב-TeamView לא השתנו
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע על חמש הנקודות ב-`human-check`, כולל אימות שביטול לא יוצר רשומות
  </acceptance_criteria>

  <done>
כפתור "מלא נתוני הדגמה" ב-TeamView פותח דיאלוג פרמטרים משותף; רק אישור בתוכו יוצר נתוני הדגמה בפועל.
  </done>

  <reversibility rating="reversible">
קומפוננטת Modal חדשה + שינוי חיווט ב-state מקומי אחד. חזרה = revert לקומיט. אין שינוי בסכימה או בחתימת actions.seedDemo.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: חיווט כפתור SupDashboard לאותה SeedDemoDialog</name>

  <files>
src/components/supervisor/views.jsx
  </files>

  <read_first>
- `src/components/supervisor/views.jsx` שורות 59-65 — קליטת `onSeedDemo` וה-state הקיים לכמות ב-`SupDashboard` (זה ה-state שמוחלף במשימה הזו — שים לב לשם המדויק, שורה 65).
- `src/components/supervisor/views.jsx` שורות 199-221 — כרטיס ההקמה (`isNew`), ה-`Segmented` הקיים לצד הכפתור "מלא לי נתוני הדגמה", וה-`onClick` הישיר הנוכחי על `onSeedDemo(demoCount)`.
- `src/components/supervisor/views.jsx` — הגדרת `SeedDemoDialog` שנוספה ב-Task 1 (למעלה בקובץ), וחיווטה בפועל ב-`TeamView` — דוגמה מדויקת לחיקוי, אותה קומפוננטה, לא עותק נוסף.
  </read_first>

  <action>
החל בדיוק את אותו מהלך כמו ב-`TeamView` (Task 1), הפעם על `SupDashboard`: המר את ה-state הקיים לכמות ליד כרטיס ההקמה (שורה 65, ר' `read_first`) ל-state בוליאני חדש `demoDialogOpen` (מתחיל `false`). בתוך הכרטיס המותנה ב-`isNew` (שורות 199-221): הסר משם את ה-`Segmented` הקיים; השאר כפתור בודד עם אותו טקסט המדויק שכבר שם היום ("מלא לי נתוני הדגמה"), אותו אייקון (`sparkles`), ואותם `variant`/`size` — רק ה-`onClick` שלו משתנה: פותח את הדיאלוג (`setDemoDialogOpen(true)`) במקום לקרוא ישירות לפעולת הכתיבה. הוסף `<SeedDemoDialog open={demoDialogOpen} onClose={() => setDemoDialogOpen(false)} onConfirm={onSeedDemo} busy={busy} />` בתוך ה-JSX של `SupDashboard` — זו **אותה** קומפוננטה שהוגדרה ב-Task 1 ומשמשת כבר את `TeamView`; אסור להגדיר עותק שני או לשכפל את ה-JSX שלה.

אל תיגע בשום כרטיס/מקטע אחר של `SupDashboard` — לא בכרטיס "מה חסר להשלים השבוע", לא בשורת הצעדים (`steps`), ולא בשום חישוב אחר בקומפוננטה. אל תיגע ב-`AuthPage.jsx` או ב-`startGuestDemo`.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/supervisor/views.jsx','utf8'); const dashStart=s.indexOf('export function SupDashboard'); const dashEnd=s.indexOf('export function ShiftMgmt'); const dashBody=s.slice(dashStart,dashEnd); for (const token of ['demoDialogOpen','setDemoDialogOpen','<SeedDemoDialog','onConfirm={onSeedDemo}']) { if (!dashBody.includes(token)) throw new Error('missing '+token+' in SupDashboard'); } console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/supervisor/views.jsx','utf8'); const m=s.match(/function SeedDemoDialog\(/g)||[]; if(m.length!==1) throw new Error('SeedDemoDialog must still be defined exactly once, found '+m.length); const openCount=(s.match(/<SeedDemoDialog\b/g)||[]).length; if(openCount!==2) throw new Error('SeedDemoDialog must be rendered exactly twice (SupDashboard + TeamView), found '+openCount); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const auth=fs.readFileSync('src/components/AuthPage.jsx','utf8'); if(auth.includes('SeedDemoDialog')) throw new Error('AuthPage.jsx must stay out of scope — no SeedDemoDialog reference expected there'); const hook=fs.readFileSync('src/hooks/useGuardian.js','utf8'); if(!hook.includes('startGuestDemo')) throw new Error('startGuestDemo must remain untouched in useGuardian.js'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, צור/היכנס לצוות חדש ונקי (בלי guards, בלי shifts — isNew=true). אמת: (1) כרטיס ההקמה בדשבורד מציג את הכפתור "מלא לי נתוני הדגמה", מסומן בבירור ככפתור הדגמה; (2) לחיצה עליו פותחת את אותו דיאלוג בדיוק שנראה במסך הצוות (Task 1) — אותה כותרת, אותו בורר כמות, אותם כפתורי footer; (3) לפני אישור, אין שום רשומת הדגמה בצוות — לוח הבקרה עדיין מציג 0 guards/0 shifts; (4) אישור עם כמות נבחרת יוצר בפועל את מספר האנשים שנבחר ומנווט לשיבוץ חכם (ההתנהגות הקיימת של startDemo, ללא שינוי); (5) מנהל שנכנס לצוות נקי ולא לוחץ על שום כפתור הדגמה כלל — לא ב-Dashboard ולא ב-TeamView — לא מקבל שום רשומת הדגמה (success criterion 5 ב-ROADMAP Phase 9), מאומת ברענון דף בלי ללחוץ שום כפתור.</human-check>
  </verify>

  <acceptance_criteria>
- `SupDashboard` מחזיקה `demoDialogOpen`/`setDemoDialogOpen`, פותחת את הדיאלוג דרך הכפתור הקיים, ומעבירה `onConfirm={onSeedDemo}`
- הטקסט/אייקון/תנאי-ההצגה (`isNew`) של כפתור "מלא לי נתוני הדגמה" לא השתנו
- `SeedDemoDialog` מוגדרת פעם אחת בלבד בקובץ ומרונדרת בשני מקומות (SupDashboard + TeamView) — אין עותק שני
- `AuthPage.jsx` ו-`startGuestDemo` לא נערכו
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע על חמש הנקודות ב-`human-check`, כולל אימות מפורש שצוות נקי לא מקבל רשומות בלי לחיצה
  </acceptance_criteria>

  <done>
שני כפתורי מילוי-ההדגמה (Dashboard + TeamView) פותחים את אותה קומפוננטת דיאלוג משותפת; נתוני הדגמה בצוות האמיתי נכתבים רק אחרי אישור מפורש בתוכה.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| מנהל מחובר (בחירת כמות מוגבלת ל-4 ערכים קבועים) → `SeedDemoDialog` → `onConfirm` → `actions.seedDemo` → כתיבה בפועל ל-guards/positions/shifts של הצוות האמיתי | אותו נתיב כתיבה קיים בדיוק כמו היום — רק נדחה עד אישור מפורש, לא נתיב חדש |
| `guests`/`startGuestDemo` (מחוץ לסקופ) | נשאר לחיצה-אחת-בלי-דיאלוג, יוצר צוות-הדגמה זמני ואנונימי בלבד — לא נוגע בצוות אמיתי |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Elevation of Privilege (עקיפת אישור) | `onSeedDemo`/`actions.seedDemo` | medium | mitigate | שני כפתורי-הכניסה היחידים שקוראים ל-`onSeedDemo` בקובץ (SupDashboard, TeamView) עוברים דרך `SeedDemoDialog.onConfirm` בלבד — grep מאמת שאין `onClick` שקורא ל-`onSeedDemo` ישירות בשום מקום אחר בקובץ |
| T-09-03 | Denial of Service (UX — כתיבה כפולה) | `Btn` ראשי ב-`SeedDemoDialog` | low | mitigate | `loading={busy}` מנטרל את הכפתור בזמן שהקריאה ל-`onConfirm` עדיין רצה — אותו דפוס `Btn` בכל שאר הדיאלוגים ב-`views.jsx` |
| T-09-04 | Tampering | כמות השומרים (`guardCount`) | low | accept | הבחירה מוגבלת ל-4 ערכים קבועים (7/14/15/20) דרך `Segmented`, לא שדה חופשי — אותו אילוץ שכבר קיים היום ב-`actions.seedDemo` |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים
2. `SeedDemoDialog` מוגדרת פעם אחת ומרונדרת פעמיים (SupDashboard + TeamView), עם `onConfirm={onSeedDemo}` בשני המקומות
3. אימות חי בדפדפן: שני הכפתורים פותחים את אותו דיאלוג, ביטול לא כותב, אישור כותב את הכמות שנבחרה, וצוות נקי בלי לחיצה לא מקבל שום רשומה
4. `AuthPage.jsx`/`startGuestDemo` ללא שינוי
</verification>

<success_criteria>
- REST-03 מתקיים: כפתור "הדגמה" מפורש (שני כפתורים, מסומנים בבירור) מחליף כל מילוי סמוי/אוטומטי של נתוני הדגמה בצוות האמיתי
- REST-04 מתקיים: לחיצה על כל אחד מהכפתורים פותחת דיאלוג פרמטרים (בורר כמות) לפני יצירת הנתונים — ורק אישור בדיאלוג יוצר נתונים
- מנהל שנכנס לצוות נקי ולא לוחץ על שום כפתור הדגמה לא מקבל שום רשומת הדגמה
</success_criteria>

<output>
Create `.planning/phases/09-rest-time-demo-button/09-02-SUMMARY.md` when done.
</output>
