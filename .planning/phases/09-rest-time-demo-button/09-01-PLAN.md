---
phase: 09-rest-time-demo-button
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/supervisor/views.jsx
autonomous: true
requirements: [REST-01, REST-02]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "לוח הבקרה הראשי (SupDashboard) לא מציג יותר את כרטיס 'מנוחה מינימלית בין {משמרות}' בשום מקום — הוסר מה-JSX לגמרי, לא הוסתר בתנאי (REST-01)."
    - "מסך הצוות (TeamView — 'כפופים שלי' במצב army) מציג את אותו כרטיס, byte-identical בתוכן (כותרת, תת-כותרת, שני כפתורי הטוגל 10/12 שעות) למה שהיה בדשבורד — קורא team.restHours, כותב דרך actions.updateTeamSettings({ restHours }) (REST-02)."
    - "הכרטיס המועבר יושב כסיבלינג ישיר של FairnessWindowSettings/CategoryWeightSettings/CategoryConflictSettings ב-TeamView, לא בפריסה חדשה או במיקום מנותק."
    - "ההערה המיושנת שהסבירה למה הכרטיס חי בדשבורד לא שורדת כהערה יתומה אחרי ההעברה — מוחלפת בהערה שמסבירה נכון את המיקום החדש."
    - "לחיצה על 10 או 12 שעות במיקום החדש מתנהגת בדיוק כפי שהתנהגה בדשבורד — אותו נתיב כתיבה, אותו מצב disabled בזמן busy."
  artifacts:
    - src/components/supervisor/views.jsx
  key_links:
    - "RestHoursSettings({ team, actions, busy }) → actions.updateTeamSettings({ restHours: hours }) → gs_teams (DB) — אותו נתיב כתיבה בדיוק כמו קודם, רק ממוקם אחרת."
    - "TeamView render → <RestHoursSettings team={team} actions={actions} busy={busy} /> כסיבלינג ל-FairnessWindowSettings/CategoryWeightSettings/CategoryConflictSettings."
---

<objective>
להעביר את כרטיס "מנוחה מינימלית" הקיים מ-`SupDashboard` (הכרטיס הראשון בתוכן שלה, מיד אחרי ה-`PageHeader`) אל `TeamView`, כקומפוננטה חדשה `RestHoursSettings` שמצטרפת לשלוש קומפוננטות-האחיות הקיימות שם (`FairnessWindowSettings`, `CategoryWeightSettings`, `CategoryConflictSettings`) — כולן Card שקורא שדה `team` אחד וכותב דרך `actions.updateTeamSettings`.

Purpose: REST-01/REST-02 (09-CONTEXT.md). לוח הבקרה הראשי מציג רק מה ששייך לו — "מנוחה מינימלית" היא הגדרת-צוות, לא סטטיסטיקה של הדשבורד, ושייכת למסך שבו שאר הגדרות-הצוות חיות.

Output: `src/components/supervisor/views.jsx` עם `RestHoursSettings` חדשה, `SupDashboard` בלי הכרטיס (ובלי ההערה המיושנת שמעליו), ו-`TeamView` שמרנדר אותה כסיבלינג לשלוש הקומפוננטות הקיימות.

**החלטה נעולה (09-CONTEXT.md, REST-01/REST-02) — לא נפתחת מחדש:** זו lift ישיר של קומפוננטה קיימת, לא עיצוב מחדש. אין אובדן מידע — הכרטיס נושא רק כותרת+תת-כותרת+טוגל 10/12 שעות, ושום דבר אחר בדשבורד לא תלוי בו.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rest-time-demo-button/09-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: הוצאת כרטיס "מנוחה מינימלית" מ-SupDashboard ל-RestHoursSettings ב-TeamView</name>

  <files>
src/components/supervisor/views.jsx
  </files>

  <read_first>
- `src/components/supervisor/views.jsx` שורות 159-198 — תחילת ה-JSX של `SupDashboard` (`PageHeader`), ואחריו מיד בלוק ההערה+ה-Card של כרטיס המנוחה — זה בדיוק הבלוק שעובר. שים לב לתוכן המדויק (כותרת עם אייקון `bed`, תת-כותרת, שני כפתורי `Btn` בלולאת `[10, 12].map`).
- `src/components/supervisor/views.jsx` שורות 2420-2454 — `FairnessWindowSettings`, כולל הערת ה-JSDoc שמעליה. זה דפוס-האח המדויק: קומפוננטה עצמאית שמקבלת רק `{ team, actions, busy }`, `Card` יחיד, `h2` עם `Icon`+כותרת, `p` תת-כותרת, ובקרה שכותבת דרך `actions.updateTeamSettings`.
- `src/components/supervisor/views.jsx` שורות 2631-2649 — סדר הרינדור הנוכחי ב-`TeamView`: `ProfilePicker` → `DeadlineSettings` → `FairnessWindowSettings` → `CategoryWeightSettings` → `CategoryConflictSettings`. כאן נכנס הסיבלינג החדש.
- `.planning/phases/09-rest-time-demo-button/09-CONTEXT.md` קטע "REST-01/02" — הניסוח הנעול המדויק, כולל ההערה על ההערה המיושנת.
  </read_first>

  <action>
ב-`src/components/supervisor/views.jsx`, הוצא את בלוק ה-Card הראשון בתוך `SupDashboard` — זה שמופיע מיד אחרי ה-`PageHeader` ולפני הכרטיס המותנה ב-`isNew` — יחד עם הערת ה-JSDoc-style שמעליו (ששת השורות שמסבירות למה הכרטיס חי היום בדשבורד). ההסבר הזה תלוי-מיקום ולא נכון יותר אחרי ההעברה, ולכן הוא לא נשאר בשום צורה: לא כהערה יתומה מעל מה שהיה שם, ולא בתור טקסט מועתק במיקום החדש.

צור קומפוננטה חדשה `RestHoursSettings({ team, actions, busy })` (props זהים בדיוק לשלוש קומפוננטות-האחיות — לא guards/shifts/tasks) שמכילה את אותו JSX פנימי שהיה בכרטיס המקורי, byte-identical: אותה כותרת (עם אותו אייקון וטקסט), אותה תת-כותרת, ואותם שני כפתורי הטוגל בלולאת `[10, 12].map` שכותבים דרך `actions.updateTeamSettings({ restHours: hours })` בדיוק כפי שעשו קודם. מעל ההגדרה, הוסף הערת JSDoc קצרה בסגנון ההערה שמעל `FairnessWindowSettings` (לא מועתקת ממנה — כתובה מחדש) שמסבירה: מנוחה מינימלית היא הגדרת-צוות קבועה (10/12 שעות בלבד, בלי שדה חופשי), נשמרת על `gs_teams` עצמו כדי שכל בדיקת אילוץ באפליקציה תסכים על אותו ערך, וחיה עכשיו במסך הצוות לצד שאר הגדרות-הצוות — לא בלוח הבקרה.

מקם את `RestHoursSettings` בקובץ ישירות לפני `FairnessWindowSettings` (שורה 2420) — כך שהיא הראשונה מבין קומפוננטות-ההגדרות שמופיעות ב-`TeamView`, בהתאם לעיקרון שאילוץ קשיח (מנוחה מינימלית, "לעולם לא ייווצר שיבוץ שמפר אותה") מקדים קריטריון רך (חלון הוגנות, ניקוד בלבד) — אותו סדר עדיפויות שכבר קיים במנוע (`autoAssign.js`: אילוצים קשיחים לפני קריטריונים רכים).

בתוך `TeamView`, בין `<DeadlineSettings ... />` ל-`<FairnessWindowSettings ... />` (שורות 2637-2639), הוסף `<RestHoursSettings team={team} actions={actions} busy={busy} />` — סיבלינג ישיר, אותו דפוס props מצומצם.

אל תיגע בשום Card/קומפוננטה אחרת: לא בכרטיס ההקמה (`isNew`) ב-`SupDashboard`, לא בכרטיס "מה חסר להשלים השבוע", לא ב-`DeadlineSettings`/`CategoryWeightSettings`/`CategoryConflictSettings`/`ProfilePicker` ב-`TeamView`, ולא בשום פונקציה אחרת בקובץ. אל תשנה imports — `Icon`, `Card`, `Btn`, `t` כבר מיובאים בראש הקובץ ומשמשים במקומות אחרים גם היום.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/supervisor/views.jsx','utf8'); const dashStart=s.indexOf('export function SupDashboard'); const dashEnd=s.indexOf('export function ShiftMgmt'); if(dashStart===-1||dashEnd===-1) throw new Error('anchor functions not found'); const dashBody=s.slice(dashStart,dashEnd); if(dashBody.includes('מנוחה מינימלית')) throw new Error('rest-hours card text still present in SupDashboard'); const restDefIdx=s.indexOf('function RestHoursSettings('); if(restDefIdx===-1) throw new Error('RestHoursSettings not defined'); const teamStart=s.indexOf('export function TeamView'); if(restDefIdx>teamStart) throw new Error('RestHoursSettings must be defined before TeamView, alongside its sibling settings components'); const teamBody=s.slice(teamStart); if(!teamBody.includes('<RestHoursSettings')) throw new Error('RestHoursSettings not rendered inside TeamView'); if(!teamBody.includes('מנוחה מינימלית')) throw new Error('rest-hours card text missing from TeamView region'); console.log('ok')"</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/supervisor/views.jsx','utf8'); const m=s.match(/\[10, 12\]\.map/g)||[]; if(m.length!==1) throw new Error('expected exactly one 10/12-hour toggle block, found '+m.length); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, היכנס כמנהל (כל תחום — civil/army). אמת: (1) לוח הבקרה הראשי לא מציג יותר את כרטיס "מנוחה מינימלית" בשום מקום — לא בגלילה, לא במצב isNew ולא במצב ותיק; (2) מסך "הצוות שלי"/"הכפופים לי" מציג את הכרטיס הזה, עם שני כפתורי 10/12 שעות; (3) לחיצה על "12 שעות" במיקום החדש הופכת את הכפתור למצב פעיל (variant="primary") ומתעדכנת בפועל ב-team.restHours — רענון הדף שומר את הבחירה; (4) שלוש קומפוננטות ההגדרות השכנות (חלון הוגנות, משקלי קטגוריה, התנגשויות קטגוריה) עדיין מוצגות ופועלות בלי שינוי.</human-check>
  </verify>

  <acceptance_criteria>
- `SupDashboard` לא מרנדרת יותר את כרטיס המנוחה, ולא נושאת את הערת ה-JSDoc-style המיושנת שהייתה מעליו
- `RestHoursSettings({ team, actions, busy })` קיימת, מוגדרת לפני `FairnessWindowSettings`, ונושאת הערת JSDoc חדשה שמתארת נכון את המיקום החדש
- `TeamView` מרנדרת `<RestHoursSettings team={team} actions={actions} busy={busy} />` כסיבלינג ישיר בין `DeadlineSettings` ל-`FairnessWindowSettings`
- תוכן הכרטיס (כותרת, תת-כותרת, שני כפתורי 10/12 שעות, נתיב הכתיבה) byte-identical למה שהיה בדשבורד — בלוק `[10, 12].map` קיים פעם אחת בלבד בקובץ
- שאר Card/קומפוננטות ב-`SupDashboard` וב-`TeamView` לא נערכו
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע על ארבע הנקודות ב-`human-check`
  </acceptance_criteria>

  <done>
כרטיס "מנוחה מינימלית" הוסר מלוח הבקרה הראשי ומוצג במקומו במסך הצוות, כסיבלינג לשלוש קומפוננטות-ההגדרות הקיימות, בלי אובדן מידע ובלי הערה יתומה.
  </done>

  <reversibility rating="reversible">
העברת קומפוננטת UI קיימת בין שני מיקומים באותו קובץ, בלי שינוי בנתיב הכתיבה או בסכימה. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| אין גבול אמון חדש | הזזת קומפוננטת UI קיימת בין שני מסכים באותה אפליקציה — נתיב הכתיבה (`actions.updateTeamSettings`) ונתיב הקריאה (`team.restHours`) לא משתנים, רק המיקום הוויזואלי |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | `RestHoursSettings` → `actions.updateTeamSettings({ restHours })` | low | accept | נתיב כתיבה קיים ולא-משתנה (ולידציה + RLS כבר קיימים בשכבת ה-API); רק הרינדור המקומי זז בין מסכים |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים
2. כרטיס המנוחה נעדר מ-`SupDashboard` ומופיע ב-`RestHoursSettings` ב-`TeamView`, ללא כפילות ובלי הערה יתומה
3. אימות חי בדפדפן על ארבע הנקודות ב-`human-check`
</verification>

<success_criteria>
- REST-01 מתקיים: לוח הבקרה הראשי לא מציג יותר את כרטיס "מנוחה מינימלית"
- REST-02 מתקיים: "כפופים שלי"/"הצוות שלי" מציג את אותו כרטיס עם אותו מידע
- אין אובדן מידע ואין הערה יתומה שנשארה מהמיקום הישן
</success_criteria>

<output>
Create `.planning/phases/09-rest-time-demo-button/09-01-SUMMARY.md` when done.
</output>
