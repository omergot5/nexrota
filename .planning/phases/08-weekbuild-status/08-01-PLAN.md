---
phase: 08-weekbuild-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/terms.js
autonomous: true
requirements: [WEEKBUILD-01]

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "מנהל במצב army שפותח את מה שהיה נקרא 'בניית סד\"כ' רואה בכל מקום UI שבו התווית הזו הופיעה את המילים 'בניית שבוע' במקום — פס הניווט, פס השלבים בתוך 'השבוע', וכל טקסט אחר שנגזר מ-t(\"nav.shifts\") (WEEKBUILD-01)."
    - "הערך הגנרי (הלא-army) של nav.shifts נשאר \"בניית השבוע\" (עם ה' הידיעה) בלי שינוי — המפרט לא ביקש לגעת בפרופיל civil/security."
    - "שאר דריסות המילון הצבאי (action.publish=\"הפץ סד\\\"כ\", nav.smart=\"בנה לי סד\\\"כ\", unit.shifts=\"תורנויות\" וכו') נשארות בדיוק כפי שהן — רק nav.shifts השתנה."
  artifacts:
    - src/lib/terms.js
  key_links:
    - "PROFILE_TERMS.army[\"nav.shifts\"] → t(\"nav.shifts\") → כל קורא בקוד (WeekFlow.jsx meta[shifts].label, SupervisorApp.jsx navItems/titles, RosterWizard.jsx PageHeader) — מקור אמת אחד, בלי מחרוזת קשיחה מקבילה באף רכיב."
---

<objective>
לשנות שורה אחת ב-`src/lib/terms.js`: דריסת המצב הצבאי של `nav.shifts` — מ-`"בניית סד\"כ"` ל-`"בניית שבוע"` — כך שמצב army קורא ל"בניית שבוע" באותו שם שהמצב הגנרי קורא לו.

Purpose: WEEKBUILD-01. מאחד את אוצר המילים בין הפרופילים לפני שהפאזה נוגעת בכל דבר אחר סביב אותו מסך (WEEKBUILD-02).

Output: `src/lib/terms.js` עם דריסה אחת מעודכנת, ואפס שינוי בכל דריסה/ערך אחר.

**החלטה נעולה (08-CONTEXT.md, WEEKBUILD-01) — לא נפתחת מחדש:** רק ה-override הצבאי של `nav.shifts` משתנה. הערך הגנרי ב-`BASE` (`"בניית השבוע"`, עם ה' הידיעה) נשאר בדיוק כפי שהוא — יישור שני הערכים למחרוזת אחת הוא הרחבת-סקופ לא-מבוקשת שה-CONTEXT דוחה במפורש (ר' `<deferred>`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-weekbuild-status/08-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/lib/terms.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: דריסת nav.shifts במצב army — "בניית סד\"כ" → "בניית שבוע"</name>

  <files>
src/lib/terms.js
  </files>

  <read_first>
- `src/lib/terms.js` שורות 87–125 — `PROFILE_TERMS`, ובפרט ענף `army` (שורות 102–124) ושורה 105 (`"nav.shifts": "בניית סד\"כ",`) שהיא היעד היחיד
- `src/lib/terms.js` שורות 24–78 — `BASE`, ובפרט שורה 37 (`"nav.shifts": "בניית השבוע",`) — **לא נוגעים בה** (הערך הגנרי נשאר כמו שהוא)
- `.planning/phases/08-weekbuild-status/08-CONTEXT.md` קטע `WEEKBUILD-01` — הניסוח הנעול המדויק
  </read_first>

  <action>
ב-`src/lib/terms.js`, בתוך `PROFILE_TERMS.army`, שנה את ערך המפתח `"nav.shifts"` מ-`"בניית סד\"כ"` ל-`"בניית שבוע"` (בלי ה' הידיעה — זה בדיוק הניסוח שה-CONTEXT נועל, שונה מהערך הגנרי `"בניית השבוע"` שכן כולל ה' הידיעה ונשאר ללא שינוי).

אל תיגע בשום מפתח אחר בענף `army` (`noun.member`, `nav.availability`, `nav.smart`, `action.publish` וכו') ואל תיגע ב-`BASE`, ב-`PROFILE_TERMS.restaurant`, ב-`PROFILE_TERMS.security`, או ב-`PROFILES` (מסך בחירת הפרופיל — השדה `hint` שם מתאר את הפרופיל עצמו, לא את `nav.shifts`, ונשאר כמו שהוא). אל תיגע בהערה בשורות 100–101 (מסבירה את לשון-הפיקוד הכללית של הפרופיל, לא ספציפית ל-`nav.shifts`) — היא עדיין נכונה בדיוק כפי שהיא.

אין קריאה קשיחה למחרוזת "בניית סד\"כ" בשום רכיב UI (מאומת בגרפ בקטע ה-verify) — `t("nav.shifts")` הוא נקודת הקריאה היחידה, כך שהשינוי כאן משתקף אוטומטית בכל מקום: פס השלבים ב-`WeekFlow.jsx`, וכל מקום עתידי שקורא ל-`t("nav.shifts")`.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/terms.js','utf8'); const armyBlock=s.slice(s.indexOf('army: {'), s.indexOf('};', s.indexOf('army: {'))); if(!/\"nav\.shifts\":\s*\"בניית שבוע\"/.test(armyBlock)) throw new Error('army nav.shifts not updated to expected literal'); if(!/\"nav\.shifts\":\s*\"בניית השבוע\"/.test(s.slice(0, s.indexOf('PROFILE_TERMS')))) throw new Error('generic BASE nav.shifts must stay unchanged'); for(const k of ['action.publish','nav.smart','unit.shifts','nav.board']) if(!armyBlock.includes(k)) throw new Error('unrelated army override missing/removed: '+k); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, הירשם/היכנס עם צוות במצב army (או עבור למצב army בהגדרות הצוות), ואמת: (1) פריט הניווט הראשי שנקרא היום "בניית שבוע" בסרגל הצד ובכותרת המסך עדיין כתוב כך (זה `titles.week`/`navItems` הקשיחים ב-SupervisorApp.jsx, שלא משתנים בתוכנית הזו); (2) בתוך מסך "השבוע", פס השלבים מציג עבור שלב בניית המשמרות/העמדות את התווית "בניית שבוע" ולא "בניית סד\"כ"; (3) יתר התוויות הצבאיות (כפוף/כפופים, "הפץ סד\"כ", "בנה לי סד\"כ" וכו') לא השתנו.</human-check>
  </verify>

  <acceptance_criteria>
- `PROFILE_TERMS.army["nav.shifts"]` שווה `"בניית שבוע"` בדיוק
- `BASE["nav.shifts"]` שווה `"בניית השבוע"` — ללא שינוי
- כל שאר המפתחות בענף `army` קיימים ולא שונו
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע ותועד (CLAUDE.md, עקרון ברזל 6)
  </acceptance_criteria>

  <done>
מצב army מציג "בניית שבוע" בכל מקום שבו הוא הציג "בניית סד\"כ", דרך מקור האמת היחיד ב-`terms.js`, בלי רגרסיה לשום דריסה אחרת ובלי נגיעה בערך הגנרי.
  </done>

  <reversibility rating="reversible">
שינוי ליטרל מחרוזת אחד בקובץ אחד. חזרה = revert לקומיט.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| אין גבול אמון חדש | שינוי ליטרל תצוגה סטטי בלבד — אין קלט משתמש, אין נתיב נתונים חדש |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `PROFILE_TERMS.army["nav.shifts"]` | low | accept | ליטרל תצוגה קבוע ללא קלט משתמש; שינוי ידני בקוד בלבד, נבדק ב-grep ובדפדפן |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים
2. `grep` על `terms.js` מאמת שרק `PROFILE_TERMS.army["nav.shifts"]` השתנה
3. אימות חי בדפדפן על שלוש הנקודות ב-`human-check`
</verification>

<success_criteria>
- WEEKBUILD-01 מתקיים: מצב army מציג "בניית שבוע" בכל מקום שבו הוא הציג "בניית סד\"כ"
- הערך הגנרי (הלא-army) של nav.shifts ללא שינוי
- אין הרחבת-סקופ לרכיבים/routes
</success_criteria>

<output>
Create `.planning/phases/08-weekbuild-status/08-01-SUMMARY.md` when done.
</output>
