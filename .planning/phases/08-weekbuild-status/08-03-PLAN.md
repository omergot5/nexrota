---
phase: 08-weekbuild-status
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/supervisor/ResourceGrid.jsx
  - src/components/supervisor/RosterWizard.jsx
autonomous: true
requirements: [WEEKBUILD-04, WEEKBUILD-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "מנהל שלוחץ (עכבר או מקלדת) על שם עמדה/קטגוריה בפאנל התצוגה של 'בניית שבוע' רואה תצוגה ממוקדת לעמדה הזו בלבד, על פני כל ימי השבוע, באותו דפוס row=עמדה/col=יום שכבר קיים ב-ResourceGrid — לא פריסה חדשה (WEEKBUILD-05)."
    - "קיימת דרך ברורה וגלויה לחזור מהתצוגה הממוקדת לתצוגה הכללית של כל העמדות."
    - "שורת-הרפאים (pending — טיוטה שעוד לא נשמרה) אינה לחיצה לתצוגה ממוקדת — אין טעם בצלילה לעמדה שעדיין לא קיימת."
    - "שני הצרכנים האחרים של ResourceGrid (מבט משאבים ב-ResourceView.jsx, תצוגת שבוע ב-CalendarView.jsx) אינם משתנים חזותית או התנהגותית — יכולת הלחיצה מותנית ב-prop אופציונלי שהם לא מעבירים."
    - "מנהל שלוחץ על כפתור ייעודי בפאנל התצוגה רואה רק עמדות שממומשו בפועל (rows), בלי שורת-הרפאים של הטיוטה שבעריכה כרגע — ולחיצה חוזרת מחזירה את תצוגת הטיוטה (WEEKBUILD-04)."
  artifacts:
    - src/components/supervisor/ResourceGrid.jsx
    - src/components/supervisor/RosterWizard.jsx
  key_links:
    - "ResourceGrid.jsx onRowClick (חדש, אופציונלי) → row מלא (כולל key/category/days) מועבר לקורא בלי טרנספורמציה — אותה צורת row שכבר יוצאת מ-buildResourceRows."
    - "RosterWizard.jsx focusedKey (state חדש) → derive focusedRow מתוך allRows בכל רינדור (לא snapshot קפוא) → ResourceGrid rows={[focusedRow]} — אותו רכיב בדיוק, מערך בגודל 1."
    - "RosterWizard.jsx hideDrafts (state חדש) → visibleRows = hideDrafts ? rows : allRows — משפיע רק על התצוגה הכללית (הלא-ממוקדת), לא על focusedRow."
---

<objective>
להוסיף שתי יכולות תצוגה לפאנל "ככה השבוע נראה עד עכשיו" ב-`RosterWizard.jsx`, שתיהן בנויות מעל `ResourceGrid.jsx` הקיים בלי להמציא פריסה חדשה: (1) לחיצה על שם עמדה פותחת תצוגה ממוקדת לעמדה הזו בלבד על פני השבוע, עם דרך חזרה; (2) כפתור ייעודי מציג רק עמדות שממומשו בפועל, מסתיר את שורת-הטיוטה.

Purpose: WEEKBUILD-05 (החלק המרכזי — רכיב חדש לגמרי, לא קיים היום בשום צורה) ו-WEEKBUILD-04 (טוגל קטן על הבחנה שכבר קיימת ב-`RosterWizard.jsx` בין `rows`/`pendingRows`). שתי היכולות חיות באותו בלוק JSX בדיוק (כרטיס התצוגה ב-`RosterWizard.jsx`), ולכן מתוכננות יחד בתוכנית אחת כדי למנוע התנגשות עריכה בין שתי תוכניות על אותו אזור בקובץ.

Output: `ResourceGrid.jsx` עם `onRowClick` אופציונלי חדש (byte-identical לצרכנים שלא מעבירים אותו), ו-`RosterWizard.jsx` עם תצוגה ממוקדת + טוגל-טיוטה בפאנל התצוגה שלו.

**החלטות נעולות (08-CONTEXT.md, WEEKBUILD-04/05) — לא נפתחות מחדש:**
- WEEKBUILD-05: אינטראקציה = לחיצה על שורה בגריד המשותף, לא בורר/dropdown נפרד. חייב לאמץ את `ResourceGrid`/`buildResourceRows` — לא פריסה חדשה. "תצוגה ממוקדת" = אותו גריד עם שורה אחת בלבד, עדיין row=עמדה/col=יום, לא ציר-שעות. נפרדת מהתצוגה הכללית — צריך דרך לחזור.
- WEEKBUILD-04: ממומש כטוגל בפאנל התצוגה של `RosterWizard.jsx` **בלבד** — לא בשלב ה-board. מבחין `rows` (עמדות ממומשות) מול `pendingRows` (טיוטה). **לא** מוסיפים מנגנון סינון "אמיתי/טיוטה" לשלב ה-board (נדחה במפורש ב-`<deferred>`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-weekbuild-status/08-CONTEXT.md
@.planning/phases/06-resource-view-pattern/06-01-SUMMARY.md
@.planning/phases/06-resource-view-pattern/06-02-SUMMARY.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/supervisor/ResourceGrid.jsx
@src/components/supervisor/RosterWizard.jsx
@src/lib/resourceView.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: תצוגה ממוקדת לעמדה בודדת — onRowClick ב-ResourceGrid, ומיקוד ב-RosterWizard</name>

  <files>
src/components/supervisor/ResourceGrid.jsx
src/components/supervisor/RosterWizard.jsx
  </files>

  <read_first>
- `src/components/supervisor/ResourceGrid.jsx` — כל הקובץ (187 שורות). במיוחד: הערת הכותרת (1–17, ובפרט הרשימה "מפורשות לא באחריות הרכיב"), חתימת הרכיב (25), לולאת `rows.map` (62–182), ותא-העמדה הראשון (69–83, `<td>` עם ה-`<div>` שמכיל נקודת-הגוון/אייקון/קטגוריה/תג-בעריכה)
- `src/components/supervisor/RosterWizard.jsx` — כל הקובץ (576 שורות). במיוחד: ה-imports (19–27), `allRows` (316–363), בלוק התצוגה (427–442, הכרטיס "ככה השבוע נראה עד עכשיו")
- `src/lib/resourceView.js` שורות 82–98 — `buildResourceRows` מחזיר `{category, icon, days}` בלי `key` (רק שורת-הרפאים ב-`RosterWizard.jsx` מוסיפה `key` משלה) — זו הסיבה ש-`row.key ?? row.category` הוא דפוס הזיהוי הנכון, כבר בשימוש ב-`ResourceGrid.jsx` שורה 69
- `.planning/phases/06-resource-view-pattern/06-01-SUMMARY.md` — חוזה ה-API הסופי של `ResourceGrid` (בלי `mode` כפרופ, `mode` נקרא פנימית)
  </read_first>

  <action>
**חלק א' — `ResourceGrid.jsx`: `onRowClick` אופציונלי, בלי לשנות שום דבר קיים.**

הוסף `onRowClick` לרשימת הפרמטרים המפורקים של הרכיב (בלי ברירת מחדל — `undefined` כשלא מסופק). בתוך `rows.map`, מיד אחרי חישוב `tone`, חשב משתנה `clickable` שאומר האם השורה הזו ניתנת ללחיצה: `onRowClick` הוא פונקציה **וגם** `row.pending` אינו `true`. שורת-רפאים (`row.pending === true`, מגיעה מ-`RosterWizard.jsx`) לעולם לא לחיצה גם כש-`onRowClick` מסופק ברמת הרכיב — היא עדיין לא נשמרה, ואין לה מה "לצלול" אליו.

בתוך `<td>` הראשון של כל שורה (זה שמכיל היום `<div className="flex items-center gap-1.5 font-bold text-content text-xs">` עם נקודת-הגוון, האייקון, שם הקטגוריה, ותג "בעריכה" המותנה): כש-`clickable` הוא `true`, החלף את ה-`<div>` ב-`<button type="button">` — נגיש למקלדת (native button, focusable מאליו), עם `onClick` שקורא ל-`onRowClick(row)` (מעביר את אובייקט ה-row השלם, בלי טרנספורמציה), `aria-label` שמתאר את הפעולה ומזכיר את `row.category` (למשל "פתח תצוגה ממוקדת לעמדה {row.category}"), ואת **אותה** מחלקת Tailwind שהייתה על ה-`<div>` (`flex items-center gap-1.5 font-bold text-content text-xs`) בתוספת `text-right w-full cursor-pointer hover:text-brand focus:outline-none focus:ring-2 focus:ring-brand/30 rounded transition-colors` (פוקוס-ring באותו דפוס כמו `CONTROL` ב-`ui.jsx` שורה 499 — `focus:ring-2 focus:ring-brand/30 focus:outline-none`). התוכן הפנימי של הכפתור זהה: נקודת-הגוון, ה-`Icon`, ו-`{row.category}` — **בלי** תג "בעריכה" (שורה לחיצה לעולם לא `pending`, אז זה תמיד מיותר בענף הזה). כש-`clickable` הוא `false` (כולל המקרה שבו `onRowClick` בכלל לא סופק — ResourceView.jsx ו-CalendarView.jsx), השאר את ה-`<div>` המקורי **בדיוק** כפי שהוא כיום, כולל תג "בעריכה" — אפס שינוי חזותי לשני הצרכנים האחרים.

הוסף שורת הערה קצרה מעל השינוי המסבירה: `onRowClick` אופציונלי (WEEKBUILD-05) — כשמסופק, שם העמדה הופך לכפתור נגיש שפותח תצוגה ממוקדת אצל הקורא; שורת-רפאים (`row.pending`) אף פעם לא לחיצה. אל תיגע בשום דבר אחר בקובץ — לא בעמודות התאריך, לא בתאי הפריטים, לא ב-`firstColLabel`, לא ב-`nameOf`.

**חלק ב' — `RosterWizard.jsx`: state למיקוד, ורינדור מותנה.**

הוסף שני `useState` חדשים ליד ההצהרות הקיימות (`activeKey`/`form`): `focusedKey` (מתחיל `null`) — מפתח השורה הממוקדת כרגע, או `null` לתצוגה הכללית.

מיד אחרי החישוב הקיים של `allRows` (שורה 363: `const allRows = [...rows, ...pendingRows];`), הוסף חישוב `focusedRow`: אם `focusedKey` הוא `null`, `focusedRow` הוא `null`; אחרת חפש ב-`allRows` שורה שבה `row.key ?? row.category` שווה ל-`focusedKey`, והחזר אותה או `null` אם לא נמצאה (עמדה שנמחקה בזמן שהייתה ממוקדת). **חשוב:** `focusedRow` נגזר מ-`allRows` **בכל רינדור מחדש** — לא snapshot קפוא שנשמר ב-state בזמן הלחיצה — כדי שהתצוגה הממוקדת תישאר עדכנית אם השיבוץ משתנה תוך כדי שהיא פתוחה.

בכרטיס התצוגה (הבלוק שמתחיל ב-`<Card className="p-4 overflow-hidden">`, שורות 431–442): שנה את הכותרת `<h3>` כך שכש-`focusedRow` קיים היא מציגה את שם הקטגוריה שלו ("{focusedRow.category} · תצוגה ממוקדת"), ואחרת את הכותרת הקיימת "ככה השבוע נראה עד עכשיו" ללא שינוי. מעל תוכן הגריד עצמו, כש-`focusedRow` קיים, הוסף כפתור "חזרה לכל העמדות" — השתמש ברכיב `Btn` הקיים (כבר מיובא בקובץ) עם `variant="ghost"` ו-`size="sm"` ו-`icon="x"`, ו-`onClick` שמאפס את `focusedKey` ל-`null`.

לתוכן הגריד עצמו, שלושה ענפים: (1) כש-`focusedRow` קיים — רנדר `<ResourceGrid rows={[focusedRow]} dates={weekDates} guards={guards} />` בתוך אותו עוטף `-mx-4 -mb-4` הקיים; (2) אחרת, כש-`allRows.length === 0` — פסקת מצב-הריק הקיימת ללא שינוי; (3) אחרת — הגריד הכללי `<ResourceGrid rows={allRows} dates={weekDates} guards={guards} onRowClick={(row) => setFocusedKey(row.key ?? row.category)} />` (הוספת `onRowClick` היא כל השינוי כאן — שאר ה-props נשארים כפי שהם).

אל תיגע בעמודת טופס העריכה (`{form && (...)}`), בפס הבחירה (`role="tablist"`), בבאנר "הכול חוסם הכול", או בכל פונקציה קיימת (`save`, `saveAndNext`, `removeItem`, `addBlank`, `fillFixedPattern`).
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/ResourceGrid.jsx','utf8'); if(!/onRowClick/.test(s)) throw new Error('onRowClick prop missing'); if(!/row\.pending\s*!==\s*true/.test(s)) throw new Error('pending rows must be excluded from clickable check'); if(!/type=\"button\"/.test(s)) throw new Error('clickable row label must be a real <button>'); if(!/aria-label/.test(s)) throw new Error('clickable row button needs an accessible name'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/RosterWizard.jsx','utf8'); for(const n of ['focusedKey','setFocusedKey','focusedRow','onRowClick','חזרה לכל העמדות']) if(!s.includes(n)) throw new Error('missing '+n); if(!/rows=\{\[focusedRow\]\}/.test(s)) throw new Error('focused view must pass a single-row array to ResourceGrid'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/ResourceView.jsx','utf8'); if(/onRowClick/.test(s)) throw new Error('ResourceView.jsx must not opt into row clicking in this plan'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/CalendarView.jsx','utf8'); if(/onRowClick/.test(s)) throw new Error('CalendarView.jsx must not opt into row clicking in this plan'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, היכנס לצוות army עם כמה עמדות שממומשו (או הרץ הדגמה), פתח "השבוע" → שלב בניית השבוע. אמת: (1) שם עמדה בפאנל "ככה השבוע נראה עד עכשיו" נראה לחיץ (סמן עכבר, hover צובע בברנד); (2) לחיצה עליו פותחת תצוגה שמראה **רק** את העמדה הזו על פני כל השבוע, עדיין row=עמדה/col=יום; (3) כפתור "חזרה לכל העמדות" גלוי ולחיצה עליו מחזירה את התצוגה הכללית עם כל העמדות; (4) ניווט מקלדת בלבד (Tab להגיע לשם העמדה, Enter או Space להפעיל) עובד זהה ללחיצת עכבר; (5) בזמן שמקלידים משימה חדשה בטופס (שורת-רפאים "בעריכה" מופיעה בגריד), השורה הזו **אינה** לחיצה — אין עליה hover/cursor-pointer; (6) פתח את "מבט משאבים" ("עוד" → מבט משאבים) ואמת ששום שורה שם לא לחיצה — התנהגות זהה לפני השינוי.</human-check>
  </verify>

  <acceptance_criteria>
- `ResourceGrid.jsx` מקבל `onRowClick` אופציונלי; שורה לחיצה רק כש-`onRowClick` מסופק וגם `row.pending !== true`
- שורה לחיצה מרונדרת כ-`<button type="button">` אמיתי עם `aria-label`, לא `<div onClick>` (נגישות מקלדת)
- ResourceView.jsx ו-CalendarView.jsx **לא** נערכו ולא מעבירים `onRowClick` — התנהגותם ומראם ללא שינוי
- `RosterWizard.jsx` מחזיק `focusedKey`/`focusedRow`, `focusedRow` נגזר מ-`allRows` בכל רינדור (לא snapshot)
- כש-`focusedRow` קיים, הגריד מקבל `rows={[focusedRow]}` וכפתור "חזרה לכל העמדות" מופיע
- כש-אין מיקוד, הגריד הכללי מקבל `onRowClick` חדש; שאר ה-props שלו לא השתנו
- טופס העריכה, פס הבחירה, והבאנר לא נערכו
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע על שש הנקודות ב-`human-check`, כולל ניווט מקלדת
  </acceptance_criteria>

  <done>
לחיצה (עכבר או מקלדת) על שם עמדה בפאנל התצוגה של "בניית שבוע" פותחת תצוגה ממוקדת לעמדה הזו על פני השבוע, עם דרך חזרה ברורה; שני הצרכנים האחרים של ResourceGrid לא הושפעו.
  </done>

  <reversibility rating="reversible">
Prop אופציונלי חדש ברכיב משותף + state מקומי בקורא אחד. חזרה = revert לקומיט. אין נתונים, אין סכימה.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: טוגל "מה שיש עד עכשיו" — הסתרת שורת-הטיוטה בתצוגה הכללית</name>

  <files>
src/components/supervisor/RosterWizard.jsx
  </files>

  <read_first>
- `src/components/supervisor/RosterWizard.jsx` אחרי Task 1 — בלוק התצוגה המעודכן (עם `focusedRow`/`focusedKey`), ו-`rows`/`pendingRows`/`allRows` (שורות 316–363 במקור)
- `.planning/phases/08-weekbuild-status/08-CONTEXT.md` קטע `WEEKBUILD-04` — הניסוח הנעול המדויק
- `.planning/ROADMAP.md` קטע Phase 8, Success Criteria #4 — הביטוי המדויק "מה שיש עד עכשיו" שהכפתור צריך לשקף
  </read_first>

  <action>
הוסף `useState` שלישי: `hideDrafts` (מתחיל `false`) — כש-`true`, פאנל התצוגה **הכללי** (הלא-ממוקד) מציג רק `rows` (עמדות שממומשו בפועל), מסתיר את `pendingRows` (שורת-הרפאים של הטיוטה שבעריכה). חשב `visibleRows`: `hideDrafts ? rows : allRows`.

בבלוק התצוגה מ-Task 1, בענף הלא-ממוקד בלבד (כש-`focusedRow` הוא `null`): במקום כפתור "חזרה לכל העמדות" (ששייך לענף הממוקד), הצג כפתור טוגל — שוב עם `Btn` הקיים, `variant="ghost"` ו-`size="sm"`, עם `aria-pressed={hideDrafts}` ו-`onClick` שהופך את `hideDrafts`. הטקסט: כש-`hideDrafts` הוא `false` (ברירת המחדל, מציג הכול כולל טיוטה) — "מה שיש עד עכשיו" (הביטוי המדויק מ-Success Criteria #4 ב-ROADMAP, כדי שהכפתור יהיה מזוהה וניתן לחיפוש טקסט חזרה למקור הדרישה); כש-`hideDrafts` הוא `true` (כבר מסונן) — "הצג גם טיוטה" (מאפשר לחזור).

עדכן את שני המקומות בענף הלא-ממוקד שהשתמשו ב-`allRows` (בדיקת האורך למצב-הריק, וה-`rows` prop של `<ResourceGrid>`) לשימוש ב-`visibleRows` במקום — **רק** בענף הזה. ה-`onRowClick` שנוסף ב-Task 1 ממשיך להעביר את השורה שנלחצה בדיוק כפי שהיא (מ-`visibleRows`, לא מ-`allRows` — כשהטוגל פעיל, עמדת-טיוטה גם ככה לא הייתה שם קודם). הענף הממוקד (כש-`focusedRow` קיים) **לא משתנה** — `hideDrafts` לא משפיע עליו כלל, כי אי אפשר להגיע אליו דרך שורת-רפאים מלכתחילה (Task 1).

אל תיגע ב-`rows`/`pendingRows`/`allRows` עצמם (החישוב שלהם נשאר בדיוק כפי שהיה) — רק ב-`visibleRows` החדש וברינדור.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/RosterWizard.jsx','utf8'); for(const n of ['hideDrafts','setHideDrafts','visibleRows','מה שיש עד עכשיו']) if(!s.includes(n)) throw new Error('missing '+n); if(!/aria-pressed=\{hideDrafts\}/.test(s)) throw new Error('toggle must expose aria-pressed'); console.log('ok')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/RosterWizard.jsx','utf8'); if(!/rows=\{visibleRows\}/.test(s)) throw new Error('unfocused grid must render visibleRows, not allRows'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, פתח "השבוע" → שלב בניית השבוע במצב army, עם לפחות עמדה אחת שממומשה ועוד משימה בעריכה (טרם נשמרה). אמת: (1) כפתור "מה שיש עד עכשיו" גלוי מעל הגריד הכללי; (2) לחיצה עליו מסתירה מיד את שורת-הרפאים המקווקוות, משאירה רק עמדות שנשמרו בפועל, והטקסט על הכפתור משתנה ל"הצג גם טיוטה"; (3) לחיצה חוזרת מחזירה את שורת-הטיוטה; (4) בזמן שהטוגל פעיל (מציג רק rows), לחיצה על שם עמדה עדיין פותחת תצוגה ממוקדת תקינה (Task 1 לא נפגע); (5) פתיחת תצוגה ממוקדת ואז לחיצה על "חזרה לכל העמדות" מחזירה את מצב הטוגל כפי שהיה לפני הכניסה למיקוד (לא מתאפס בטעות).</human-check>
  </verify>

  <acceptance_criteria>
- `RosterWizard.jsx` מחזיק `hideDrafts`/`setHideDrafts` ו-`visibleRows = hideDrafts ? rows : allRows`
- הכפתור בענף הלא-ממוקד קורא `aria-pressed={hideDrafts}` ומציג את הטקסט "מה שיש עד עכשיו" (כבוי) / "הצג גם טיוטה" (דלוק)
- הגריד הכללי (הלא-ממוקד) מרונדר `rows={visibleRows}`, לא `rows={allRows}`
- הענף הממוקד (`focusedRow`) לא משתנה ולא מושפע מ-`hideDrafts`
- `rows`/`pendingRows`/`allRows` עצמם לא נערכו
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע על חמש הנקודות ב-`human-check`
  </acceptance_criteria>

  <done>
כפתור "מה שיש עד עכשיו" מסתיר ומציג את שורת-הטיוטה בתצוגה הכללית, בלי להשפיע על התצוגה הממוקדת מ-Task 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `row.category`/`item.label` (טקסט שהמנהל הקליד) → כפתור/aria-label חדש ב-ResourceGrid | אותו קלט שכבר מוצג בתא הרגיל, עכשיו גם בתוך attribute וטקסט כפתור |
| לחיצה → focusedKey (state) → ResourceGrid rows=[focusedRow] | אין נתיב נתונים חדש — אותם shifts/tasks/guards שכבר ב-allRows, רק מסוננים בצד הלקוח |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering (XSS) | `row.category` בטקסט הכפתור וב-`aria-label` | medium | mitigate | React מרנדר את שניהם כטקסט (JSX child / attribute string), לא HTML — אין `dangerouslySetInnerHTML` בשום מקום בתוכנית הזו |
| T-08-06 | Information Disclosure | תצוגה ממוקדת מציגה עמדה בודדת | low | accept | אותם נתונים בדיוק שכבר גלויים בתצוגה הכללית (`allRows`) באותו מסך, לאותו מנהל — סינון תצוגתי בלבד, לא הרחבת גישה |
| T-08-07 | Denial of Service (UX) | שורת-רפאים לחיצה בטעות | low | mitigate | `clickable` נבדק פר-שורה (`row.pending !== true`) — נאכף אוטומטית ובדפדפן (Task 1, human-check נקודה 5) |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים
2. `ResourceGrid.jsx`: `onRowClick` אופציונלי, שורת-רפאים לעולם לא לחיצה, שני הצרכנים האחרים לא נערכו
3. `RosterWizard.jsx`: תצוגה ממוקדת עובדת עם דרך חזרה, וטוגל "מה שיש עד עכשיו" מסנן נכון
4. אימות חי בדפדפן על שתי המשימות, כולל ניווט מקלדת לתצוגה הממוקדת
</verification>

<success_criteria>
- WEEKBUILD-05 מתקיים: לחיצה על עמדה פותחת תצוגה ממוקדת בנויה מעל ResourceGrid הקיים, עם דרך חזרה
- WEEKBUILD-04 מתקיים: כפתור ייעודי מסתיר/מציג את שורת-הטיוטה בתצוגה הכללית
- שני הצרכנים האחרים של ResourceGrid (מבט משאבים, יומן) ללא שינוי
</success_criteria>

<output>
Create `.planning/phases/08-weekbuild-status/08-03-SUMMARY.md` when done.

חובה לתעד ב-SUMMARY: תוצאת האימות החי (כולל ניווט מקלדת) על שתי המשימות, ואת הטקסט המדויק שנבחר לכפתורים.
</output>