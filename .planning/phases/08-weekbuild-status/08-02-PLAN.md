---
phase: 08-weekbuild-status
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/supervisor/WeekFlow.jsx
autonomous: true
requirements: [WEEKBUILD-02, WEEKBUILD-03]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "מנהל שנכנס ל'השבוע' רואה את פס השלבים בסדר: בניית שבוע (1) → תמונת מצב שבועית (2) → מי הגיש (3) → שיבוץ (4) → שליחה (5) — 'תמונת מצב שבועית' ממוקמת מיד אחרי 'בניית שבוע', לא לפניה (WEEKBUILD-02)."
    - "לחיצה על הכפתור הראשי בשלב 'בניית שבוע' מעבירה לשלב 'תמונת מצב שבועית' — לא מדלגת ישירות לזמינות."
    - "לחיצה על הכפתור הראשי בשלב 'תמונת מצב שבועית' מעבירה ל'מי הגיש', וחסומה כל עוד לא נבנתה אף משמרת/משימה השבוע — אותו אילוץ עסקי שהיה קיים קודם על המעבר משמרות←זמינות, רק שיושב עכשיו על המעבר הבא בתור."
    - "ניווט ישיר לשלב לפי מזהה (STEP_OF/go()) ממשיך לעבוד ללא שינוי עבור כל שבעת המזהים (board, shifts, availability, smart, assignment, assign, schedule, publish)."
    - "מצב-הריק של הלוח (כשאין עדיין משמרות/משימות השבוע) מפנה את המנהל לשם השלב 'בניית שבוע', לא לכפתור שמתחתיו — כי הכפתור שמתחת ללוח כבר לא בונה משמרות אחרי ההזזה (תיעוד השינוי ב-G-05-1, לא רק הזזה שקטה)."
    - "ניווט בין שבועות בחצים (WeekNav, weekOffset) ממשיך לעבוד זהה לפני ואחרי ההזזה, כולל כששלב 'תמונת מצב שבועית' פעיל — התאריכים המוצגים בלוח מתעדכנים נכון (WEEKBUILD-03)."
  artifacts:
    - src/components/supervisor/WeekFlow.jsx
  key_links:
    - "STEP_OF ↔ אינדקס במערכי meta/body/action — שלושתם חייבים להישאר בלוקסטפ אחרי ההזזה, כולל טקסט תלוי-סדר (לשעבר goBuildLabel) ויעדי ה-onClick של כל CTA."
    - "SupervisorApp.jsx go(id) → STEP_OF[id] → setWeekStep — לא נערך בתוכנית הזו; ממשיך לצרוך את STEP_OF המיוצא בלי לדעת שהמפתחות הוזזו."
    - "weekDates זורם SupervisorApp → common → UnifiedBoard כ-dates prop, בלי תלות במיקום השלב במערך — זה מה ש-WEEKBUILD-03 מאמת שלא נשבר."
---

<objective>
להזיז את שלב "הלוח המאוחד" (`UnifiedBoard`, מוצג במצב army בשם "תמונת מצב שבועית") בתוך `WeekFlow.jsx` מלהיות **לפני** שלב "בניית שבוע" (`RosterWizard`/`ShiftMgmt`) להיות **אחריו** — סדר חדש: בניית שבוע → תמונת מצב שבועית → מי הגיש → שיבוץ → שליחה — ולאמת שניווט בין שבועות בחצים ממשיך לעבוד ללא שינוי אחרי ההזזה.

Purpose: WEEKBUILD-02 + WEEKBUILD-03. פס השלבים של `WeekFlow.jsx` **הוא** הניווט המקומי בפועל של המסך הזה — מיקום "תמונת מצב שבועית" מיד אחרי "בניית שבוע" בפס הזה הוא המימוש הכי נאמן של "מסך שממוקם מיד אחרי בניית שבוע בניווט", בלי לבנות מסך/רכיב חדש ובלי לשנות את השם `UnifiedBoard.jsx` (ר' ROADMAP.md Phase 8, הגילוי האדריכלי מ-2026-09-22 שמחליף את ההחלטה הקודמת מ-17/9).

Output: `WeekFlow.jsx` עם `STEP_OF` ומערכי `meta`/`body`/`action` בסדר החדש, כולל טקסט CTA ומצב-ריק שמתועדים מחדש כדי לשקף את המעבר הנכון (לא רק אינדקסים שהוזזו).

**החלטה נעולה (08-CONTEXT.md, WEEKBUILD-02) — לא נפתחת מחדש:** הסדר החדש הוא `shifts → board → availability → assign → schedule`. **לא** נדרש מסך/קומפוננטה חדשים, **לא** שינוי שם ל-`UnifiedBoard.jsx`. חובה לתעד את ההשפעה על היגיון מצב-הריק של הלוח (G-05-1, Phase 5 D-04) — לא רק להזיז את הקוד בלי להסביר למה ההנמקה המקורית עדיין תקפה בצורה חדשה.

**קריאה נכונה של "לא רק אינדקסים" (CONTEXT.md, במפורש):** `STEP_OF.shifts`/`STEP_OF.board` מתחלפים (0↔1); `availability`/`smart`/`assignment`/`assign`/`schedule`/`publish` **נשארים** באותו אינדקס (2/3/4 בהתאמה) — הם לא זזים, כי רק שני השלבים הראשונים מתחלפים ביניהם. בגלל זה, ה-CTA שהיה "בניתי את השבוע — זמינות?" (מוביל ישירות מ-shifts לזמינות) **חייב לעבור** לשלב הלוח (כי הלוח יושב עכשיו באמצע), וה-CTA שהיה "המשך לבניית שבוע" (`goBuildLabel`, מוביל מהלוח לבניית שבוע) **חייב להתחלף** ב-CTA שמוביל מהלוח לזמינות — אחרת המעבר "בניתי משמרות ⟶ תראה את הלוח ⟶ תמשיך לזמינות" נשבר לכדי דילוג על הלוח או ללולאה חזרה לבניית שבוע.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-weekbuild-status/08-CONTEXT.md

@CLAUDE.md
@.claude/CLAUDE.md

@src/components/supervisor/WeekFlow.jsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: הזזת שלב הלוח בפס השלבים — STEP_OF, meta/body/action, וטקסט תלוי-סדר</name>

  <files>
src/components/supervisor/WeekFlow.jsx
  </files>

  <read_first>
- `src/components/supervisor/WeekFlow.jsx` — כל הקובץ (327 שורות). במיוחד: הערת הכותרת (1–18), הערת התיעוד מעל `STEP_OF` (31–37) ו-`STEP_OF` עצמו (38–47), `meta` (95–126), `goBuildLabel` (131), `common` (134–136), `body` (138–195, ובפרט אלמנט `UnifiedBoard` 143–157 ו-`empty.body` שלו 149–151), `action` (200–261, ובפרט אינדקסים 0 ו-1, שורות 201–220)
- `.planning/phases/08-weekbuild-status/08-CONTEXT.md` קטע `WEEKBUILD-02` — הניסוח הנעול המדויק והדרישה לתעד את השינוי בהיגיון מצב-הריק
- `.planning/ROADMAP.md` קטע "Phase 8" — ההערה למתכנן שמסבירה את הגילוי האדריכלי ואת נימוק "פס השלבים הוא הניווט המקומי"
  </read_first>

  <action>
בצע ארבעה שינויים תואמים ב-`WeekFlow.jsx`, כולם נדרשים יחד כדי שהקובץ יישאר עקבי (D: לא לבצע חלקית).

**1) `STEP_OF` (שורות 38–47).** החלף את `board: 0` ו-`shifts: 1` בזה בזה, כך שהמפה החדשה היא: `shifts: 0`, `board: 1`, `availability: 2`, `smart: 3`, `assignment: 3`, `assign: 3`, `schedule: 4`, `publish: 4`. עדכן את הערת התיעוד שמעליו (שורות 31–37, שמסבירה כיום ש"שלב 0 הוא הלוח המאוחד... כל שאר השלבים הוזזו ב-1") כדי לתאר את המצב החדש: שלב 0 הוא "בניית שבוע" (הצעד הפעיל, לא לקריאה בלבד), שלב 1 הוא הלוח המאוחד — שממשיך להיות "השבוע במבט אחד"/"תמונת מצב שבועית" — ושהמיקום החדש (מיד אחרי בניית שבוע, לא לפניה) הוא החלטת WEEKBUILD-02 מ-2026-09-22 שמחליפה את מיקום ה"לוח קודם" שנקבע ב-Phase 5 (D-04), בלי לבטל את הרעיון שהלוח נגיש מוקדם ובלי חיפוש — הוא עדיין השלב השני מתוך חמישה, לא קבור בסוף.

**2) `meta` (שורות 95–126).** החלף את מיקומי האובייקט `{ id: "shifts", ... }` ואת האובייקט `{ id: "board", ... }` — תוכן כל אחד מהם (`label`/`count`/`done`) נשאר בדיוק כפי שהוא, רק סדר שני האיברים הראשונים במערך מתחלף. האיברים `availability`/`smart`/`schedule` (כיום שלישי/רביעי/חמישי) נשארים בדיוק במקומם — הם לא זזים כי הם כבר לא צמודים לשינוי.

**3) `body` (שורות 138–195).** החלף את מיקומי אלמנט ה-JSX של `RosterWizard`/`ShiftMgmt` (הענף `team?.mode === "army" ? <RosterWizard .../> : <ShiftMgmt .../>`) ואת אלמנט ה-`<UnifiedBoard .../>` — כך שאלמנט המשמרות מגיע ראשון במערך ו-`UnifiedBoard` שני. שני האלמנטים האחרים (`AvailView`, ה-`div` של שלב השיבוץ, `ScheduleMgmt`) נשארים במקומם. בתוך אלמנט `UnifiedBoard` עדכן את ה-prop `empty.body` (ר' סעיף 4 למטה) — שום דבר אחר בו לא משתנה (לא `shifts`/`tasks`/`guards`/`dates`/`onMove`/`mode`).

**4) `action` (שורות 200–261) ומצב-הריק של הלוח — כאן ההיגיון האמיתי, לא רק הזזה.**

מחק את המשתנה `goBuildLabel` (שורה 131) ואת הערת התיעוד שמעליו — המשמעות שלו ("המשך לבניית שבוע") כבר לא רלוונטית לשום כפתור אחרי ההזזה, כי בניית שבוע היא עכשיו השלב הראשון ואין לפניה שלב שמפנה אליה.

בנה את שני האיברים הראשונים החדשים של `action` (הזוגות הישנים באינדקס 0/1 מוחלפים לגמרי בתוכן, לא רק מוזזים):

- **`action[0]` (הכפתור של שלב "בניית שבוע", מוביל ללוח):** `children` הוא התבנית `` `המשך ל${t("nav.board")}` `` (משקף בדיוק את הדפוס הישן של `goBuildLabel`, רק עם `nav.board` במקום `nav.shifts`). `icon: "left"`. `onClick: () => setStep(1)`. **בלי** `disabled` — לצפות בלוח זה תמיד תקין, גם בלי משמרות (בדיוק כמו שה-CTA הישן שהוביל ללוח מעולם לא היה חסום). `hint`: `hasShifts ? \`${weekShifts.length} ${t("unit.shifts")} בשבוע הזה\` : undefined` (בלי טקסט "צריך לפחות..." — זה כבר לא חסם כאן).
- **`action[1]` (הכפתור של שלב הלוח, מוביל לזמינות):** `children` הוא התבנית `` `תמונת המצב ברורה — ${t("nav.availability")}?` ``. `icon: "left"`. `onClick: () => setStep(2)`. `disabled: !hasShifts` — האילוץ העסקי שהיה על המעבר shifts→availability ("אי אפשר לאסוף זמינות בלי משמרות") עובר לכאן במדויק, כי זה עכשיו המעבר שמוביל לזמינות. `hint`: `hasShifts ? \`${weekShifts.length} ${t("unit.shifts")} בשבוע הזה\` : "צריך לפחות משמרת אחת כדי להמשיך"` — זהה מילה-במילה ל-hint שהיה על ה-CTA הישן של shifts (רק עבר שלב אחד קדימה).

`action[2]`, `action[3]`, `action[4]` (זמינות→שיבוץ, שיבוץ→שליחה, פרסום) **לא משתנים כלל** — לא בתוכן ולא במיקום.

**מצב-הריק של הלוח (G-05-1, לא רק הזזה — זה מה שה-CONTEXT דורש לתעד):** עדכן את `empty.body` בתוך `<UnifiedBoard>` מ-`` `בנה ${t("unit.shifts")} או משימות, והלוח ייבנה מעצמו — לחצו למטה על "${goBuildLabel}".` `` ל-`` `בנה ${t("unit.shifts")} או משימות בשלב "${t("nav.shifts")}", והלוח ייבנה מעצמו.` ``. הסיבה: לפני ההזזה, הכפתור שממש מתחת ללוח (אז `action[0]`) היה `goBuildLabel` עצמו — פעולה שממש בונה משמרות — ומותר היה לקרוא לו בשמו (זה בדיוק מה שההערה הקיימת מעל ה-`if (totalItems === 0)` ב-`UnifiedBoard.jsx` מתעדת כחריגה המותרת ל-D-15). אחרי ההזזה, הכפתור שמתחת ללוח (`action[1]` החדש) כבר לא בונה משמרות — הוא מוביל הלאה לזמינות, ולפעמים אפילו חסום (`disabled: !hasShifts`). הפניית משתמש עם לוח ריק ל"לחצו למטה" הייתה עכשיו מטעה: הכפתור שם לא עושה את מה שהטקסט מבטיח. הפתרון: מצב-הריק מפנה בשם ה**שלב** ("בניית שבוע", דרך `t("nav.shifts")`) ולא בשם כפתור ספציפי — עדיין שם מפורש ומועיל (מותר לפי אותה חריגה — WeekFlow עדיין יודע שהוא לא ברירת המחדל הכללית של `UnifiedBoard`), רק לא תלוי-מיקום בכפתור שמתחתיו.

עדכן גם את הערת התיעוד הקיימת מעל אלמנט ה-`UnifiedBoard` (שורות 139–142, שמתחילה ב"מצב ריק משלו לשלב 0 (G-05-1)") — היא מתייחסת כיום ל"שלב 0" ול-"action[0] למטה" שכבר לא נכונים. נסח מחדש במונחים (לא ציטוט מילולי): הלוח יושב עכשיו בשלב 1 (אחרי בניית שבוע, WEEKBUILD-02); ברירת המחדל הכללית של `UnifiedBoard` עדיין לא קוראת בשם שום כפתור (D-15, כי היא גם מוצגת ביומן ואצל המשתתף); כאן, בתוך `WeekFlow`, מצב-הריק המותאם עדיין מותר לקרוא בשם מפורש — אבל בשם השלב "בניית שבוע", לא בשם כפתור שכבר לא עושה את הפעולה שהטקסט מתאר.

בנוסף, עדכן את הערת הכותרת של הקובץ כולו (שורות 1–18) בשורה שאומרת כיום "כאן זה מסך אחד עם חמישה שלבים — הלוח המאוחד (Phase 5, D-04) ואחריו ארבעת השלבים המקוריים" — זה כבר לא מדויק (הלוח כבר לא ראשון). נסח מחדש במונחים: חמישה שלבים, הראשון הוא בניית שבוע/סד"כ, מיד אחריו הלוח המאוחד (Phase 5, D-04 — עבר למיקום הזה ב-Phase 8, WEEKBUILD-02, 2026-09-22), ואחריו שלושת השלבים המקוריים הנותרים.
  </action>

  <verify>
    <automated>node -e "
const fs=require('fs');
const s=fs.readFileSync('src/components/supervisor/WeekFlow.jsx','utf8');
const stepOf=s.slice(s.indexOf('export const STEP_OF'), s.indexOf('};', s.indexOf('export const STEP_OF')));
if(!/shifts:\s*0/.test(stepOf)) throw new Error('STEP_OF.shifts must be 0');
if(!/board:\s*1/.test(stepOf)) throw new Error('STEP_OF.board must be 1');
if(!/availability:\s*2/.test(stepOf)) throw new Error('STEP_OF.availability must stay 2');
if(!/schedule:\s*4/.test(stepOf)) throw new Error('STEP_OF.schedule must stay 4');
if(s.includes('goBuildLabel')) throw new Error('goBuildLabel must be fully removed, not just unused');
const metaBlock=s.slice(s.indexOf('const meta ='), s.indexOf('const goBuildLabel') > -1 ? s.indexOf('const goBuildLabel') : s.indexOf('const body ='));
const shiftsMetaIdx=metaBlock.indexOf('id: \"shifts\"');
const boardMetaIdx=metaBlock.indexOf('id: \"board\"');
if(shiftsMetaIdx === -1 || boardMetaIdx === -1 || shiftsMetaIdx > boardMetaIdx) throw new Error('meta array must list shifts before board');
const bodyBlock=s.slice(s.indexOf('const body ='), s.indexOf('const action ='));
const shiftsBodyIdx=bodyBlock.search(/RosterWizard|ShiftMgmt/);
const boardBodyIdx=bodyBlock.indexOf('UnifiedBoard');
if(shiftsBodyIdx === -1 || boardBodyIdx === -1 || shiftsBodyIdx > boardBodyIdx) throw new Error('body array must render shifts step before board step');
const actionBlock=s.slice(s.indexOf('const action ='), s.indexOf('return ('));
if(!/setStep\(1\)/.test(actionBlock)) throw new Error('an action must advance to step 1 (board)');
if(!/setStep\(2\)/.test(actionBlock)) throw new Error('an action must advance to step 2 (availability)');
if(!/nav\.board/.test(actionBlock)) throw new Error('the shifts-step CTA must reference nav.board');
if(!/nav\.availability/.test(actionBlock)) throw new Error('the board-step CTA must reference nav.availability');
if(s.includes('לחצו למטה')) throw new Error('stale click-below empty-state phrasing must be removed');
console.log('ok');
"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, היכנס לצוות (army או security — הסדר החדש חל על שניהם, זו זרימה גנרית אחת), פתח "השבוע", ואמת את כל הזרימה בזה אחר זה: (1) פס השלבים מציג בסדר: שלב 1 = בניית שבוע/סד"כ, שלב 2 = השבוע במבט אחד/תמונת מצב שבועית, שלב 3 = מי הגיש/דיווח, שלב 4 = שיבוץ, שלב 5 = שליחה/פרסום; (2) בצוות ריק (בלי משמרות), שלב 2 (הלוח) מציג מצב-ריק שמפנה בשם לשלב "בניית שבוע"/"בניית סד\"כ", לא לכפתור "למטה"; (3) בשלב 1, לחיצה על הכפתור הראשי מעבירה לשלב 2 (הלוח) — לא מדלגת לזמינות; (4) בנה משמרת אחת, עבור לשלב 2, ואמת שהלוח מציג אותה ושהכפתור הראשי שם פעיל (לא חסום) ומוביל לשלב 3 (זמינות); (5) נסה ללחוץ על שם השלב "תמונת מצב שבועית"/"השבוע במבט אחד" ישירות בפס השלבים (בלי לעבור דרך הכפתור) — עובד ומציג את הלוח; (6) שלבים 3–5 מתפקדים בדיוק כמו לפני השינוי (לא נבדקים כאן לעומק — רק שלא נשברו).</human-check>
  </verify>

  <acceptance_criteria>
- `STEP_OF` מיוצא עם `shifts: 0`, `board: 1`, `availability: 2`, `smart/assignment/assign: 3`, `schedule/publish: 4`
- `meta`, `body` ו-`action` — שלושתם מציגים את שלב המשמרות (RosterWizard/ShiftMgmt) לפני שלב הלוח (UnifiedBoard), ואת שאר השלבים (זמינות/שיבוץ/פרסום) באותו סדר כמו קודם
- `goBuildLabel` הוסר לגמרי מהקובץ
- ה-CTA של שלב המשמרות מוביל לשלב הלוח (`setStep(1)`) ומזכיר `t("nav.board")`, בלי `disabled`
- ה-CTA של שלב הלוח מוביל לזמינות (`setStep(2)`), מזכיר `t("nav.availability")`, וחסום כש-`!hasShifts` עם אותו טקסט hint שהיה על ה-CTA הישן של שלב המשמרות
- מצב-הריק של הלוח (`empty.body`) מפנה בשם השלב "בניית שבוע" (`t("nav.shifts")`), לא בביטוי "לחצו למטה"
- ההערות שהתייחסו למיקום הישן (כותרת הקובץ, תיעוד `STEP_OF`, תיעוד מצב-הריק) עודכנו לשקף את הסדר החדש ואת הנימוק
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע על שש הנקודות ב-`human-check`
  </acceptance_criteria>

  <done>
פס השלבים ב-"השבוע" מציג את הסדר shifts→board→availability→assign→schedule, כל ה-CTA-ים מובילים לשלב הנכון עם טקסט נכון, מצב-הריק של הלוח מתעד את השינוי בהיגיון ולא רק מוזז, ואימות חי בדפדפן מוכיח שהזרימה שלמה מקצה לקצה.
  </done>

  <reversibility rating="reversible">
שינוי JSX/אובייקטים בקובץ אחד, בלי סכימה ובלי חוזה חיצוני. חזרה = revert לקומיט.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: אימות WEEKBUILD-03 — ניווט בין שבועות בחצים אחרי ההזזה</name>

  <files>(אין — משימת אימות בלבד, WEEKBUILD-03 לא דורשת קוד חדש)</files>

  <read_first>
- `src/components/SupervisorApp.jsx` שורות 58–77 (`WeekNav`), 92–113 (`weekStep`/`weekOffset`/`weekDates`), 181 (`common` שמעביר `weekDates`), 400–424 (מיקום `WeekNav` בהדר, גלוי כל עוד `isWeek === true`)
- `.planning/phases/08-weekbuild-status/08-CONTEXT.md` קטע `WEEKBUILD-03` — הקביעה המפורשת שזו משימת אימות, לא בנייה
  </read_first>

  <action>
אין שינוי קוד במשימה הזו. המטרה: להוכיח (לא להניח) ש-`WeekNav` (חצי `weekOffset`) ממשיך לעבוד נכון אחרי הזזת שלב הלוח ב-Task 1, כולל שהתאריכים שהלוח מציג מתעדכנים כשעוברים שבוע.

ההוכחה המבנית: `weekDates` נגזר ב-`SupervisorApp.jsx` (`useMemo(() => weekByOffset(weekOffset), [weekOffset])`) ברמת ההורה, בלי שום תלות ב-`weekStep`/במיקום השלב בתוך `WeekFlow`, ומוזרם דרך `common` ל-`WeekFlow` ומשם כ-`dates={weekDates}` ל-`UnifiedBoard` בדיוק כמו קודם ההזזה — Task 1 לא נגע בקישור הזה בכלל. `WeekNav` עצמו מוצג ב-header של `SupervisorApp.jsx` כל עוד `isWeek === true` (`view === "week"`), גם הוא בלי תלות ב-`weekStep`. מכיוון ששני אלה לא השתנו, האימות הוא ריצה חיה שמוכיחה שההנחה המבנית נכונה בפועל ולא רק בקוד.

הרץ את הבדיקה האוטומטית (שמוודאת ש-`SupervisorApp.jsx` לא נערך כלל בפאזה הזו — ראיה שהתשתית הקיימת הספיקה, בדיוק כפי שה-CONTEXT קבע), ואז בצע את אימות הדפדפן.
  </action>

  <verify>
    <automated>git diff --quiet HEAD -- src/components/SupervisorApp.jsx && echo "ok: SupervisorApp.jsx untouched" || (echo "FAIL: SupervisorApp.jsx was modified — WEEKBUILD-03 should require zero code changes" && exit 1)</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/supervisor/WeekFlow.jsx','utf8'); if(/const\s*\[\s*weekOffset/.test(s)) throw new Error('WeekFlow.jsx must not own weekOffset state — SupervisorApp.jsx remains the sole owner'); if(!/dates=\{weekDates\}/.test(s)) throw new Error('UnifiedBoard must still receive dates={weekDates} unchanged'); console.log('ok')"</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>הרץ `npm run dev`, פתח "השבוע", ועבור לשלב 2 (הלוח, "תמונת מצב שבועית"/"השבוע במבט אחד"). אמת: (1) חיצי `WeekNav` (הקודם/הבא) גלויים בהדר בזמן ששלב הלוח פעיל, בדיוק כמו בשלבים האחרים; (2) לחיצה על החץ "שבוע הבא" מקדמת את התאריכים בכותרת ה-`WeekNav`, והלוח עצמו מציג את שבעת ימי השבוע החדש (כותרות התאריך בעמודות משתנות בהתאם); (3) אם יש משמרות בשבוע היעד, הן מופיעות בלוח; אם אין, מוצג מצב-הריק החדש (מ-Task 1); (4) לחיצה על "היום" מחזירה לשבוע הנוכחי והלוח מתעדכן בהתאם; (5) אותה בדיקה חוזרת על שלב 1 (בניית שבוע) ושלב 3 (זמינות) לוודא שההתנהגות זהה בכל השלבים — לא רגרסיה ממוקדת בלוח בלבד.</human-check>
  </verify>

  <acceptance_criteria>
- `src/components/SupervisorApp.jsx` לא נערך בתוכנית הזו כלל (git diff ריק)
- `WeekFlow.jsx` לא מחזיק state עצמאי של `weekOffset` — עדיין תלוי לגמרי ב-`weekDates` שמגיע כפרופ
- `UnifiedBoard` בתוך `WeekFlow.jsx` ממשיך לקבל `dates={weekDates}` ללא שינוי
- `npm test` ו-`npm run build` עוברים
- אימות חי בדפדפן בוצע על חמש הנקודות ב-`human-check`, כולל מעבר שבוע בזמן ששלב הלוח פעיל
  </acceptance_criteria>

  <done>
ניווט בין שבועות בחצים מאומת כעובד ללא רגרסיה אחרי הזזת שלב הלוח — כולל עדכון נכון של התאריכים בתוך שלב "תמונת מצב שבועית" עצמו — בלי ששורת קוד אחת נדרשה מחוץ ל-Task 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| אין גבול אמון חדש | שינוי סדר תצוגה/ניווט בתוך מסך קיים — אותם shifts/tasks/guards שכבר זורמים ל-WeekFlow, בלי הרחבת נתיב נתונים |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Repudiation / Integrity | זרימת ה-CTA בין שלבי `WeekFlow.jsx` | high | mitigate | סדר-שלבים שבור (למשל CTA שמדלג על שלב או חוזר ללולאה) גורם למנהל לפרסם סידור חלקי מבלי לדעת שהוא דילג על שלב — בדיוק סוג הכשל שהמחזור הזה בא לסגור ("מה שהמערכת אומרת לו על עצמה הוא נכון"). מאומת באוטומציה (STEP_OF/meta/body/action בלוקסטפ) ובהליכה חיה מקצה-לקצה בדפדפן על כל חמשת השלבים |
| T-08-03 | Denial of Service (UX) | מצב-הריק של הלוח אחרי ההזזה | medium | mitigate | טקסט שמפנה לכפתור שכבר לא עושה את מה שהוא מבטיח ("לחצו למטה") תוקע משתמש שלא מבין למה שום דבר לא קורה. הטקסט עודכן לתאר את השלב הנכון בשמו, ונאכף ב-grep שהביטוי הישן לא נשאר |
| T-08-04 | Repudiation | קישור `weekDates`/`weekOffset` בין `SupervisorApp.jsx` ל-`WeekFlow.jsx` (WEEKBUILD-03) | low | accept | אין שינוי קוד בקישור הזה בתוכנית הזו; הסיכון היחיד הוא רגרסיה לא-מכוונת מ-Task 1, שנבדקת ישירות (git diff ריק על SupervisorApp.jsx + הליכה חיה על ניווט שבועות בשלב הלוח) |

אין בתוכנית הזו התקנת חבילות (npm/pip/cargo) — שער חוקיות-חבילות אינו חל.
</threat_model>

<verification>
1. `npm test` ו-`npm run build` עוברים
2. `STEP_OF`/`meta`/`body`/`action` בלוקסטפ מלא — נבדק אוטומטית ב-grep וידנית בהליכה על כל חמשת השלבים
3. `SupervisorApp.jsx` לא נערך כלל (WEEKBUILD-03 מאומת, לא נבנה)
4. ניווט בין שבועות בחצים עובד בכל שלב, כולל שלב הלוח, מאומת חי בדפדפן
</verification>

<success_criteria>
- WEEKBUILD-02 מתקיים: "תמונת מצב שבועית" ממוקמת מיד אחרי "בניית שבוע" בפס השלבים, עם CTA-ים תקינים בשני הכיוונים ומצב-ריק מתועד מחדש
- WEEKBUILD-03 מתקיים: ניווט בין שבועות בחצים מאומת עובד ללא רגרסיה, כולל בתוך שלב הלוח שהוזז
- אין מסך/רכיב חדש, אין שינוי שם ל-UnifiedBoard.jsx
</success_criteria>

<output>
Create `.planning/phases/08-weekbuild-status/08-02-SUMMARY.md` when done.

חובה לתעד ב-SUMMARY: את הטקסט המדויק שנבחר לשני ה-CTA-ים החדשים ולמצב-הריק, ואת תוצאת האימות החי על שתי המשימות (מה נראה בפועל, לא מה היה אמור).
</output>