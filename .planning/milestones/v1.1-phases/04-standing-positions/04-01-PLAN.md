---
phase: 04-standing-positions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0007_standing_positions.sql
  - scripts/verify-backend.mjs
  - src/lib/positions.js
  - src/lib/api.js
  - src/hooks/useGuardian.js
  - scripts/verify-positions.mjs
  - package.json
autonomous: false
requirements: [POS-01, POS-02, POS-03, POS-04]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "POS-01 · עמדה פעילה שהוגדרה פעם אחת מייצרת את שורות השבוע שלה בלי פעולה נוספת של המנהל — `ensurePositionsForWeek(sundayISO)` כותב אותן, ומחזיר `{created: 0}` בלי שום כתיבה כשהן כבר קיימות."
    - "POS-03 · זהות השורה השבועית היא הצמד `(positionId, identityDate)` בלבד, והתאריך נגזר אך ורק מיום ראשון של שבוע היעד — לא משעון הקיר, לא מסדר שורות שחזר מ-Postgres."
    - "POS-04 · הרצה שנייה של אותו שבוע מייצרת אפס שורות חדשות: `missingRowsForWeek` מחזיר מערך ריק, וגם אם נשלחה כתיבה בכל זאת — האינדקס החלקי `(position_id, date)` ו-`ignoreDuplicates: true` הופכים אותה ל-no-op."
    - "POS-02/D-04 · בריכת הכשירים לעמדה נגזרת מ-`isQualified(guard, position.category)` הקיים מפאזה 3, ללא שינוי בו וללא טבלת כשירות שנייה — שיוך אדם לעמדה אינו כתיבה בכלל."
    - "D-02 · שורת משמרת שנושאת `position_id` נבחרת ומאוישת על ידי `autoAssign()` הקיים בדיוק כמו כל משמרת אחרת — בלי מקרה מיוחד ובלי שינוי בקובץ המנוע."
    - "`npm test` מדפיס ok על כל בדיקות `verify-positions.mjs` ויוצא בקוד 0; הרצה על אותם נתונים בסדר קלט מעורבב מחזירה תוצאה זהה בית-בית."
  artifacts:
    - "supabase/migrations/0007_standing_positions.sql — טבלת `gs_positions` + RLS באותו קובץ + שתי עמודות `position_id` nullable + שני אינדקסים ייחודיים חלקיים"
    - "src/lib/positions.js — מודול טהור חדש: `expectedDatesForWeek`, `plannedRowsForWeek`, `missingRowsForWeek`, `qualifiedGuardsForPosition`, `workingGuardIdsForWeek`"
    - "src/lib/api.js — `positionFromRow`, `positionToRow`, `createPosition`, `updatePosition`, `deletePosition`, `materializeTemplateShifts`, ו-`positions` בתוך `loadTeam`"
    - "src/hooks/useGuardian.js — `positions: []` ב-EMPTY, ו-actions `addPosition` / `updatePosition` / `deletePosition` / `ensurePositionsForWeek`"
    - "scripts/verify-positions.mjs — בדיקת Node עצמאית חדשה, מחווטת ל-`npm test`"
    - "scripts/verify-backend.mjs — סעיף חי חדש שמוכיח RLS על `gs_positions` ואת האינדקס הייחודי"
  key_links:
    - "`position.category` → `isQualified()` — משטח הכשירות היחיד (D-04). אין `gs_position_qualifications`."
    - "שורת `gs_shifts` עם `position_id` → `autoAssign()` הקיים ללא שינוי (D-02)."
    - "`(position_id, date)` partial unique index → `upsert(..., {onConflict, ignoreDuplicates: true})` → אידמפוטנטיות (POS-04)."
    - "`loadTeam` → `positions` → `useGuardian` data spread → כל מסך שצורך עמדות."
    - "`gs_positions.id` ← `on delete set null` על שתי העמודות המפנות — היסטוריית השיבוצים שורדת מחיקת הגדרה."
  prohibitions:
    - "זהות שורה שבועית לעולם אינה נגזרת משעון הקיר של תהליך ההרצה — לא ישירות ולא דרך ברירת מחדל של פונקציית תאריך."
    - "מחיקת הגדרת עמדה לעולם אינה מוחקת את השורות שכבר התממשו ממנה — היסטוריית העומס וההוגנות חייבת לשרוד."
    - "טבלה חדשה לעולם אינה נשלחת בלי RLS באותו קובץ מיגרציה שיוצר אותה."
    - "כתיבה לבסיס הנתונים לעולם אינה נחשבת מוצלחת בלי `.select()` שמחזיר שורה — RLS שמסננת הכול מחזירה `error: null` ומערך ריק."
  flagged_assumptions:
    - "POS-01 — בדיקת הקצה האוטומטית נמנעה מסיווג (unclassified/unresolved). הדרישה נגזרה כאן ידנית מ-REQUIREMENTS.md §POS ומ-D-01/D-02: 'נכנסת לכל שבוע אוטומטית' = חישוב חוסרים בכל צפייה בשבוע, לא כפתור 'צור' ידני."
    - "POS-02 — בדיקת הקצה האוטומטית נמנעה מסיווג. נגזרה ידנית מ-D-04: 'שיוך' אינו כתיבה כלל אלא גזירה טהורה מעל `qualifiedCategories` הקיים."
    - "POS-03 — בדיקת הקצה האוטומטית נמנעה מסיווג. נגזרה ידנית מ-04-RESEARCH.md Pattern 1: 'מזהה דטרמיניסטי' = מפתח טבעי `(position_id, date)`, לא UUID שנגזר מ-hash."
    - "POS-04 — בדיקת הקצה האוטומטית נמנעה מסיווג. נגזרה ידנית: 'אותן שורות בדיוק' נמדדת גם על מספר השורות וגם על יציבות ה-`id` הקיים, לא רק על היעדר כפילות."
    - "A1 (04-RESEARCH.md) — nullability של `gs_shifts.start_time`/`end_time` לא נקראה ישירות. המשימה מאמתת אותה מול `information_schema` לפני שהיא נשענת על ההנחה."
    - "פתוחה מהמחקר — `required_guards` על עמדה: ברירת מחדל 1, השדה נחשף אבל אינו חובה. אם יתברר שעמדה תמיד מאיישת אדם אחד, העמודה נשארת לא-מזיקה."
    - "פתוחה מהמחקר — עמדה שהושבתה: המימוש מסתכל קדימה מעמדות `active` בלבד ואינו מוחק שורות עתידיות שכבר התממשו. הן נשארות משמרות רגילות שאפשר למחוק ידנית."
---

<objective>
לבנות את הנתיב המלא של עמדה קבועה מסוג "תבנית משמרת" — מהסכמה ועד למשמרת מאוישת — כפרוסה אנכית אחת שעוברת דרך כל שכבה שהפאזה נוגעת בה: מיגרציה חיה, מודול טהור, שכבת הנתונים, וה-state המרכזי.

Purpose: זו הפרוסה שמוכיחה שהארכיטקטורה עומדת. אם `(position_id, date)` לא מספיק כדי לקבל אידמפוטנטיות, או אם שורת משמרת שנושאת `position_id` לא נאספת על ידי `autoAssign()` הקיים ללא שינוי (D-02) — עדיף לגלות את זה אחרי commit אחד ולא אחרי חמישה. שאר הפאזה מתרחבת החוצה מהפרוסה הזאת: המסך (04-02) והצורה השנייה, "שבועית ללא שעות" (04-03).

Output: `gs_positions` חיה ב-Supabase עם RLS, `src/lib/positions.js`, מיפויי `api.js`, actions ב-`useGuardian`, ו-`scripts/verify-positions.mjs` מחווט ל-`npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-standing-positions/04-CONTEXT.md
@.planning/phases/04-standing-positions/04-RESEARCH.md
@.planning/phases/04-standing-positions/04-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: נעילת צורת הסכמה של gs_positions (דלת חד-כיוונית)</name>

  <read_first>
    - `.planning/phases/04-standing-positions/04-RESEARCH.md` — §"Code Examples" (סקיצת המיגרציה המלאה), §"Architecture Patterns" Pattern 1, §"Open Questions", §"Assumptions Log" A1/A3/A4
    - `.planning/phases/04-standing-positions/04-CONTEXT.md` — D-01 (Reversibility: costly), §"Claude's Discretion"
    - `supabase/migrations/0004_task_templates_and_compatibility.sql` — צורת "טבלה + RLS באותו קובץ" שהמיגרציה החדשה מחקה
    - `supabase/migrations/0006_qualification.sql` — פילוסופיית additive-only / nullable-first
  </read_first>

  <reversibility rating="one-way">
    טבלה חדשה + שתי עמודות FK על שתי טבלאות חמות. ביטול דורש מיגרציה נגדית מול פרויקט Supabase חי שכבר מחזיק נתוני צוותים; D-01 כבר מסומנת costly, והוספת צורת עמדה שלישית מאוחר יותר תדרוש מעבר דומה על שתי הצורות הקיימות.
  </reversibility>

  <decision>
    לאשר את צורת הסכמה של `gs_positions` לפני שהיא נכתבת ונדחפת לפרויקט Supabase החי. ארבע נקודות שהמחקר השאיר פתוחות ושמשנות עמודות:

    1. **`required_guards` על העמדה** — ברירת מחדל 1, אבל העמודה קיימת (המלצת המחקר, Open Question 1). האלטרנטיבה: לא להוסיף אותה בכלל ולקבע עמדה = אדם אחד.
    2. **מפתח הזהות של הצורה "שבועית ללא שעות"** — המחקר הציע שורת `gs_tasks` עם `due_date = יום ראשון`. ההמלצה כאן שונה: `start_date = יום ראשון` ו-`due_date = יום שבת`, כך שהעמדה נקראת על המסך כפריט שנפרש על השבוע כולו במקום כמשימה של יום ראשון. הזהות נשארת דטרמיניסטית לגמרי כי שבת נגזרת אך ורק מיום ראשון של שבוע היעד, והאינדקס הייחודי נשאר על `(position_id, due_date)`. תופעת לוואי רצויה: `isSingleDayTask` מחזירה false, ולכן השורה קפואה מחוץ למנוע גם אם מישהו יזין לה שעות בטעות בעתיד.
    3. **A1 — nullability של `gs_shifts.start_time`/`end_time`** — לא נקראה ישירות במחקר. המשימה הבאה מאמתת אותה מול `information_schema.columns` לפני שהיא נשענת עליה. ההחלטה כאן: גם אם הן nullable, הצורה "שבועית" עדיין מנותבת ל-`gs_tasks` — כדי שהיא תישאר מחוץ למילוי שנושא שעות של `autoAssign()` מעצם המבנה, כפי ש-D-03 דורשת.
    4. **A3/A4 — בסיס הרוטציה** — ספירה שטוחה של תורות על פני כל ההיסטוריה (ולא חלון מתגלגל של 14 יום כמו `fairness.js`, ולא משקל-זמן). ההנמקה: כל תור הוא בדיוק שבוע, ולכן "הכי מעט זמן" ו"הכי מעט תורות" זהים כאן. זו החלטה בלוגיקה טהורה ולכן הפיכה בלי מיגרציה — היא מוצגת כאן רק כדי שלא תתגלה כהפתעה ב-04-03.
  </decision>

  <options>
    - **א' (מומלץ): לאשר את ארבע ההכרעות כפי שנוסחו.** הסכמה נכתבת בדיוק כמו בסקיצה של 04-RESEARCH.md §"Code Examples", עם השינוי היחיד של סעיף 2 (יום שבת כתאריך הזהות של הצורה השבועית).
    - **ב': לאשר עם שינוי בסעיף 2** — להישאר עם המלצת המחקר, `due_date = יום ראשון` לצורה השבועית. זול יותר לכתוב, אבל עמדה שחלה על השבוע כולו תופיע במסכים כפריט של יום ראשון.
    - **ג': לוותר על `required_guards`** — עמדה = אדם אחד תמיד. פחות עמודה אחת; הרחבה עתידית תדרוש מיגרציה.
  </options>

  <recommendation>אפשרות א'.</recommendation>

  <acceptance_criteria>
    - המשתמש בחר במפורש אחת מ-א'/ב'/ג' (או ניסח חלופה משלו) לפני שנכתבה שורת SQL אחת.
    - ההכרעה שנבחרה מתועדת מילולית בגוף ה-SUMMARY של התוכנית הזאת, כולל הנימוק, כך ש-04-03 יורשת אותה ולא מסיקה אותה מחדש.
    - אם נבחרה ב' או ג' — Task 2 ו-Task 3 מעדכנים את שמות/סוגי העמודות בהתאם לפני הכתיבה, ולא אחריה.
  </acceptance_criteria>

  <done>ההכרעה נבחרה ותועדה; המיגרציה טרם נכתבה.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] מיגרציית 0007 — הטבלה, ה-RLS, האינדקסים — ודחיפה חיה ל-Supabase</name>

  <precondition>
    לפרויקט Supabase `biauxcgphdhwewszupsq` יש נתיב זמין להחלת DDL — כלי `apply_migration` של Supabase MCP, CLI מקושר, או גישה לעורך ה-SQL בלוח הבקרה. אם אף אחד מהשלושה לא זמין, עצור ובקש מהמשתמש להריץ את הקובץ ידנית לפני שממשיכים; אין להמשיך ל-Task 3 על סמך קובץ שנכתב אך לא הוחל.
  </precondition>

  <files>supabase/migrations/0007_standing_positions.sql, scripts/verify-backend.mjs</files>

  <read_first>
    - `supabase/migrations/0004_task_templates_and_compatibility.sql` — הצורה המדויקת שיש לחקות: `create table if not exists` → `enable row level security` → `drop policy if exists` → `create policy` קריאה → `create policy` כתיבה, הכול בקובץ אחד
    - `supabase/migrations/0006_qualification.sql` — כותרת מוסברת בעברית, additive-only, בלי backfill
    - `supabase/2026-08-18-fix-duplicates-and-preferences.sql` — התקדים בתוך המאגר ל"מפתח טבעי כאינדקס ייחודי חלקי" (`gs_profiles_one_name_per_team`)
    - `scripts/verify-backend.mjs` — מבנה `check(label, cond, extra)`, יצירת אחמ"ש/שומר זמניים, `fresh()`, ואיך הסקריפט כבר מוכיח דחיית RLS היום
    - `.planning/phases/04-standing-positions/04-RESEARCH.md` — §"Code Examples" (סקיצת המיגרציה), §"Security Domain"
  </read_first>

  <action>
    צור את `supabase/migrations/0007_standing_positions.sql` כקובץ אחד שמכיל את כל אלה, בסדר הזה, עם כותרת מוסברת בעברית בראשו בסגנון 0006:

    (א) `create table if not exists gs_positions` עם העמודות: `id uuid primary key default gen_random_uuid()`, `team_code text not null references gs_teams(code) on delete cascade`, `shape text not null check (shape in ('template','weekly'))`, `title text not null`, `category text not null`, `weekdays jsonb`, `start_time time`, `end_time time`, `required_guards int not null default 1`, `active boolean not null default true`, `created_at timestamptz not null default now()`.

    (ב) `alter table gs_positions enable row level security;` ואחריו שתי מדיניויות בדיוק בצורת 0004: `gs_positions_read` — `for select using (team_code in (select team_code from gs_profiles where user_id = auth.uid()))`; `gs_positions_write` — `for all using (team_code in (select team_code from gs_profiles where user_id = auth.uid() and role = 'supervisor'))`. לפני כל אחת `drop policy if exists`, כדי שהרצה חוזרת של הקובץ תהיה בטוחה.

    (ג) `alter table gs_shifts add column if not exists position_id uuid references gs_positions(id) ...` ואותו הדבר על `gs_tasks`. שתי ההפניות משתמשות בהתנהגות שמאפסת את ההפניה למחיקה ומשאירה את השורה — לא בהתנהגות שמוחקת את השורות המפנות. הצורה האסורה מנוסחת מפורשות בקריטריוני הקבלה של המשימה הזאת; היסטוריית השיבוצים חייבת לשרוד מחיקת הגדרת עמדה, כי `nextRotationGuard` (04-03) והוגנות הנטל נשענות עליה.

    (ד) שני אינדקסים ייחודיים חלקיים: `gs_shifts_position_date_idx on gs_shifts (position_id, date) where position_id is not null`, ו-`gs_tasks_position_date_idx on gs_tasks (position_id, due_date) where position_id is not null`. החלקיות אינה קישוט: בלעדיה כל השורות הקיימות שבהן `position_id` הוא NULL היו מתנגשות זו בזו.

    לאחר מכן **החל את הקובץ בפועל** על הפרויקט החי — `apply_migration` של Supabase MCP אם קיים, אחרת CLI מקושר, אחרת עצור לפי ה-precondition. אל תסתפק ב-`npm run build` שעובר: הפרויקט הזה כתוב JS לא-מוטיפס, ובנייה מוצלחת אינה מעידה דבר על הסכמה החיה.

    בזמן שאתה מול הסכמה החיה, אמת גם את A1: שאילתה על `information_schema.columns` עבור `gs_shifts` בעמודות `start_time`/`end_time`, ורשום את התשובה (`is_nullable`) ב-SUMMARY. זו אימות של הנחה שהמחקר סימן כלא-מאומתת, לא סקרנות.

    לבסוף הרחב את `scripts/verify-backend.mjs` בסעיף חדש בשם `=== standing positions (gs_positions) ===`, שמוסיף — בתוך תרחיש האחמ"ש/שומר שהסקריפט כבר בונה — את הבדיקות: (1) האחמ"ש מכניס עמדה ומקבל שורה חזרה מ-`.select()`; (2) השומר (לא-אחמ"ש) שמנסה להכניס עמדה לאותו צוות אינו מצליח — או שגיאה, או אפס שורות חזרה; (3) השומר יכול לקרוא את העמדה של הצוות שלו; (4) הכנסה כפולה של שתי שורות `gs_shifts` עם אותו `position_id` ואותו `date` — השנייה נדחית, ועם `ignoreDuplicates: true` היא חוזרת כמערך ריק בלי שגיאה. השתמש ב-`check()` הקיים, בשמות תוויות בעברית כמו שאר הקובץ, ובלי לשנות בדיקה קיימת.
  </action>

  <verify>
    <automated>grep -vE '^\s*--' supabase/migrations/0007_standing_positions.sql | grep -c 'create policy' — חייב להחזיר 2</automated>
    <automated>grep -E 'references gs_positions\(id\)' supabase/migrations/0007_standing_positions.sql | grep -c 'on delete set null' — חייב להחזיר 2</automated>
    <automated>grep -E 'references gs_positions\(id\)' supabase/migrations/0007_standing_positions.sql | grep -c 'on delete cascade' — חייב להחזיר 0</automated>
    <automated>grep -vE '^\s*--' supabase/migrations/0007_standing_positions.sql | grep -c 'where position_id is not null' — חייב להחזיר 2</automated>
    <automated>npm run test:backend — הסעיף החדש מדפיס ok על כל ארבע הבדיקות, והסקריפט יוצא בקוד 0</automated>
  </verify>

  <acceptance_criteria>
    - `supabase/migrations/0007_standing_positions.sql` קיים, ומכיל `enable row level security` ושתי `create policy` באותו קובץ שיוצר את הטבלה.
    - `grep -vE '^\s*--' supabase/migrations/0007_standing_positions.sql | grep -c 'create policy'` = 2.
    - `grep -E 'references gs_positions\(id\)' supabase/migrations/0007_standing_positions.sql | grep -c 'on delete cascade'` = 0 (הצורה המוחקת אסורה על שתי עמודות ה-FK).
    - `grep -E 'references gs_positions\(id\)' supabase/migrations/0007_standing_positions.sql | grep -c 'on delete set null'` = 2.
    - `grep -vE '^\s*--' supabase/migrations/0007_standing_positions.sql | grep -c 'where position_id is not null'` = 2 — שני האינדקסים חלקיים.
    - המיגרציה **הוחלה בפועל** על הפרויקט החי, וההוכחה היא ש-`npm run test:backend` מדפיס ok על הסעיף החדש — לא ש-`npm run build` עבר.
    - `npm run test:backend` מדפיס ok על: כתיבת אחמ"ש מצליחה עם שורה חוזרת; כתיבת שומר נדחית; קריאת שומר מהצוות שלו מצליחה; הכנסה כפולה של `(position_id, date)` מחזירה אפס שורות חדשות.
    - תשובת `information_schema` על `gs_shifts.start_time`/`end_time` (`is_nullable`) רשומה מילולית ב-SUMMARY.
    - `npm test` ו-`npm run build` ממשיכים לעבור (המיגרציה לא נוגעת בקוד היישום).
  </acceptance_criteria>

  <done>הטבלה, ה-RLS, שתי עמודות ה-FK ושני האינדקסים חיים בפרויקט Supabase, ו-`verify-backend.mjs` מוכיח את זה מקצה לקצה.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: הפרוסה האנכית — עמדת תבנית אחת: הגדרה ⟵ מימוש שבועי אידמפוטנטי ⟵ איוש על ידי המנוע הקיים</name>

  <files>src/lib/positions.js, src/lib/api.js, src/hooks/useGuardian.js, scripts/verify-positions.mjs, package.json</files>

  <read_first>
    - `src/lib/dates.js` — במלואו. `startOfWeek`, `weekFrom`, `weekByOffset`, `addDays`, `fromISODate`, `toISODate`, מוסכמת `Date.getDay()`, ו-`isSingleDayTask`/`isTaskEngineEligible`/`taskAsShiftShape`
    - `src/lib/autoAssign.js` שורות 110–170 — `isQualified`, `checkQualification`, ומוסכמת `tieBreak`; ושורות 423–520 — חתימת `autoAssign()` ומה היא מצפה לקבל
    - `src/lib/api.js` במלואו — `shiftFromRow`/`shiftToRow`/`taskFromRow`/`taskColumns` (מוסכמת null מפורש ולא השמטה), `loadTeam` (למה `taskTemplates`/`compatibility` מכוונות **לא** להיכלל ב-`firstError`), `setGuardQualifications` (שומר ה-`.select()` נגד עיוורון-RLS), `assignGuard` (תקדים `upsert` + `onConflict`), ו-`MIGRATION_HINT`/`asTaskError`
    - `src/hooks/useGuardian.js` שורות 28–41 (`EMPTY`), 220–321 (`run`/`optimistic`/`deferred`), 455–680 (מבנה `actions` והדרישה שהוא יישאר יציב רפרנציאלית)
    - `scripts/verify-planning.mjs` שורות 1–40 ו-30 השורות האחרונות — מבנה `check()`, סגנון התוויות, וצורת ה-`process.exit`
    - `.planning/phases/04-standing-positions/04-RESEARCH.md` — Pattern 1, Pattern 2, Pattern 3, §"Anti-Patterns to Avoid"
    - `.planning/phases/04-standing-positions/04-VALIDATION.md` — §"Phase Requirements → Test Map"
  </read_first>

  <behavior>
    בדיקות שנכתבות ב-`scripts/verify-positions.mjs` **לפני** המימוש, בסגנון `check()` של `verify-planning.mjs`:

    - POS-03 · `expectedDatesForWeek({shape:"template", weekdays:[0,1,2,3,4]}, "2026-09-06")` מחזיר בדיוק את חמשת התאריכים 2026-09-06..2026-09-10, ושלוש קריאות רצופות מחזירות מערכים זהים ב-`JSON.stringify`.
    - POS-03 · אותה עמדה על יום ראשון בעבר הרחוק ועל יום ראשון בעתיד הרחוק מחזירה תמיד היסט זהה מיום ראשון — התוצאה תלויה בארגומנטים בלבד.
    - POS-03 · `expectedDatesForWeek({shape:"weekly"}, sunday)` מחזיר בדיוק איבר אחד, והוא `addDays(sunday, 6)`.
    - POS-04 · `missingRowsForWeek(position, sunday, [])` מחזיר 5 שורות; לאחר הזנת אותן שורות בחזרה כ-`realized`, קריאה שנייה מחזירה מערך ריק.
    - POS-04 · אותה קריאה עם `realized` בסדר מעורבב מחזירה תוצאה זהה; ו-`realized` שמכיל שורה של עמדה *אחרת* באותו תאריך אינו מדכא שורה.
    - POS-02/D-04 · `qualifiedGuardsForPosition` — שומר עם `qualifiedCategories: null` כשיר; עם `[]` כשיר; עם `["מטבח"]` אינו כשיר לעמדה בקטגוריית "שמירות"; עמדה בלי קטגוריה — כולם כשירים.
    - POS-05 · שתי הרשימות אינן נגזרות מאותו שדה: שומר לא-כשיר שנמצא ב-`assignedGuards` של שורה שהתממשה מופיע ב-`workingGuardIdsForWeek` ואינו מופיע ב-`qualifiedGuardsForPosition`; שומר כשיר בלי שורה מופיע רק בשנייה.
    - D-02 · מקצה לקצה: מזינים את חמש השורות שהתממשו ל-`autoAssign()` האמיתי (מיובא מ-`src/lib/autoAssign.js`, ללא שינוי בו) יחד עם בריכת שומרים כשירים ולא-כשירים; העמדה מאוישת, `assignmentMeta` נושא `source` של המנוע, ואף שומר לא-כשיר אינו נבחר.
    - דטרמיניזם · הרצת אותו נתיב פעמיים כשמערך השומרים מעורבב מחזירה תוצאה זהה ב-`JSON.stringify`.
    - מיפוי · `shiftToRow(shiftFromRow(row))` משמר את `position_id`; `taskColumns(taskFromRow(row))` משמר את `position_id`; `positionToRow(positionFromRow(row), code)` משחזר את `shape`/`category`/`weekdays`/`required_guards`.
  </behavior>

  <action>
    **(1) `src/lib/positions.js` — מודול טהור חדש.** בלי React, בלי Supabase, בלי ייבוא מ-`api.js`. כותרת מוסברת בעברית בראש הקובץ, בסגנון `dates.js`/`fairness.js`. מייצא:

    - `expectedDatesForWeek(position, sundayISO)` — תאריכי **הזהות** של העמדה בשבוע. `shape === "template"`: `weekFrom(sundayISO)` מסונן לפי `new Set(position.weekdays || [])` מול `fromISODate(d).getDay()`. `shape === "weekly"`: `[addDays(sundayISO, 6)]` לפי ההכרעה של Task 1 (יום שבת כתאריך הזהות; יום ראשון הוא ה-`startDate`).
    - `plannedRowsForWeek(position, sundayISO)` — אובייקטי אפליקציה ב-camelCase, מוכנים למיפוי. לתבנית: `{positionId, date, label: position.title, startTime, endTime, requiredGuards, category, type: "custom", published: false}` לכל תאריך. לשבועית: שורת משימה אחת `{positionId, title, category, startDate: sundayISO, dueDate: addDays(sundayISO,6), startTime: null, endTime: null, assignees: [], priority: "medium"}`.
    - `missingRowsForWeek(position, sundayISO, realized)` — מסנן מ-`plannedRowsForWeek` כל שורה שתאריך הזהות שלה (`row.date || row.dueDate`) כבר מופיע ב-`realized` **עבור אותו `positionId`**. `realized` שמכיל עמדה אחרת אינו מדכא.
    - `qualifiedGuardsForPosition(position, guards)` — `guards.filter((g) => isQualified(g, position.category))`, כשה-`isQualified` מיובא מ-`autoAssign.js` **ללא שינוי בו** (D-04).
    - `workingGuardIdsForWeek(position, {shifts, tasks}, sundayISO)` — איחוד ממוין לקסיקוגרפית של `assignedGuards` משורות `gs_shifts` שנושאות את ה-`positionId` ותאריכן בתוך `weekFrom(sundayISO)`, ו-`assignees` משורות `gs_tasks` שנושאות אותו `positionId` ותאריך הזהות שלהן בשבוע.

    זהות שורה נגזרת אך ורק מ-`sundayISO` שהועבר כארגומנט ומ-`position.id`. הקובץ אינו קורא לאף API של שעון או של אקראיות; הזיהויים האסורים מנויים מפורשות בקריטריוני הקבלה של המשימה הזאת. כל מיון מפורש (`localeCompare`), לעולם לא סדר ההגעה של המערך.

    **(2) `src/lib/api.js`.** הוסף `positionFromRow` / `positionToRow` באזור המפות בראש הקובץ, לצד `taskFromRow`: `weekdays` דרך `Array.isArray(...) ? ... : []`, שעות דרך `hhmm` עם `null` כשחסר, `requiredGuards: row.required_guards ?? 1`, `active: row.active !== false`. `positionToRow` כותב `null` מפורש ולא משמיט מפתח — אותה מוסכמה של `taskColumns`.

    הוסף `positionId: row.position_id || null` ל-`shiftFromRow` ול-`taskFromRow`, ו-`position_id: shift.positionId || null` / `position_id: task.positionId || null` ל-`shiftToRow` ול-`taskColumns`. בלי המפתח בכיוון הכתיבה, עריכת משמרת דרך הטופס הייתה מנתקת אותה בשקט מהעמדה שלה.

    ב-`loadTeam` הוסף שאילתה שמינית ל-`Promise.all`: `supabase.from("gs_positions").select("*").eq("team_code", teamCode).order("created_at")`. `.order()` הוא חובה ולא סגנון — קריאה בלי סדר מפורש היא בדיוק אי-הדטרמיניזם ש-Pitfall 1 מתאר. אל תכלול את התוצאה ב-`firstError`, בדיוק מאותה סיבה ש-`taskTemplates` ו-`compatibility` אינן נכללות: בסיס נתונים שהמיגרציה טרם רצה עליו חייב להמשיך לטעון. החזר `positions: (posRes.data || []).map(positionFromRow)`.

    הוסף `createPosition(position, teamCode)` (insert + `.select().single()`), `updatePosition(id, patch)` (update + `.select()`, וזריקת שגיאה בעברית אם חזר מערך ריק — שומר עיוורון-RLS כמו ב-`setGuardQualifications`), `deletePosition(id)`, ו-`materializeTemplateShifts(rows, teamCode)`: `.upsert(rows.map((r) => shiftToRow(r, teamCode)), { onConflict: "position_id,date", ignoreDuplicates: true })` עם `.select("*, gs_assignments(guard_id, source, score, reason)")`, שמחזיר את השורות שנוצרו בפועל דרך `shiftFromRow`. עדכן את `MIGRATION_HINT` כך שיציין `0007_standing_positions.sql` כמיגרציה האחרונה, והוסף `position_id` לביטוי הרגולרי של `asTaskError`.

    **(3) `src/hooks/useGuardian.js`.** הוסף `positions: []` ל-`EMPTY`. הוסף ל-`actions`, בלי לשנות את רשימת התלויות של ה-`useMemo` מעבר למה שכבר שם (`[run, optimistic, deferred, refresh]`), ובלי לקרוא ל-`data` ישירות — רק דרך `dataRef.current`:
    - `addPosition(position)` ו-`updatePosition(id, patch)` — דרך `run`, ואז `refresh()`.
    - `deletePosition(id)` — דרך `deferred("העמדה נמחקה", ...)`, כמו `deleteShift`.
    - `ensurePositionsForWeek(sundayISO)` — דרך `run`. קורא `positions`/`shifts`/`tasks` מ-`dataRef.current`, מסנן ל-`position.active`, מחשב `missingRowsForWeek` לכל עמדה, ואם הסכום ריק — **חוזר מיד עם `{created: 0}` בלי שום כתיבה ובלי `refresh()`**. זה לא ייעול: `refresh()` ללא תנאי מתוך פעולה שמסך קורא לה בכל שינוי שבוע הוא לולאה. אם יש חוסרים — קורא ל-`materializeTemplateShifts` על שורות התבנית, ואז `refresh()`, ומחזיר `{created: n}`. בפרוסה הזאת מטופלת רק `shape === "template"`; עמדות `shape === "weekly"` מדולגות במפורש עם הערה שמפנה ל-04-03.

    **(4) `scripts/verify-positions.mjs` + `package.json`.** כתוב את הבדיקות מ-`<behavior>` לפני המימוש, בסגנון `verify-planning.mjs` בדיוק: `let failures = 0`, `check(label, cond, extra)`, כותרות סעיפים ב-`console.log`, וסיום ב-`process.exit(failures === 0 ? 0 : 1)`. הסקריפט חייב להישאר לגמרי לא-מקוון — ייבוא מ-`api.js` מותר (בניית לקוח Supabase היא סינכרונית ואינה פונה לרשת, בדיוק כפי ש-`verify-planning.mjs` כבר נשען), אבל אף קריאת רשת. שרשר לסקריפט `test` ב-`package.json`: `&& node scripts/verify-positions.mjs`.
  </action>

  <verify>
    <automated>node scripts/verify-positions.mjs — כל השורות מדפיסות ok, הסקריפט מדפיס PASS ויוצא בקוד 0</automated>
    <automated>npm test — שלושת הסקריפטים עוברים ברצף ויוצאים בקוד 0</automated>
    <automated>npm run build — עובר</automated>
    <automated>grep -vE '^\s*(//|\*|/\*)' src/lib/positions.js | grep -cE 'Math\.random|Date\.now|new Date\(|todayISO\(' — חייב להחזיר 0</automated>
    <automated>grep -c 'ignoreDuplicates: true' src/lib/api.js — חייב להחזיר לפחות 1</automated>
    <human-check>הרץ `npm run dev`, הגדר עמדת תבנית דרך ה-console (`window` אינו חשוף — במקום זה קרא ל-`actions.addPosition` מתוך React DevTools, או המתן ל-04-02 שמספק את הטופס), ואמת בלוח הבקרה של Supabase ששורות המשמרת נוצרו עם `position_id`, ושהרצה שנייה של `ensurePositionsForWeek` על אותו שבוע לא הוסיפה שורה. סמן כלא-נבדק אם לא נצפה בפועל.</human-check>
  </verify>

  <acceptance_criteria>
    - `src/lib/positions.js` קיים ומייצא בדיוק: `expectedDatesForWeek`, `plannedRowsForWeek`, `missingRowsForWeek`, `qualifiedGuardsForPosition`, `workingGuardIdsForWeek`.
    - `grep -vE '^\s*(//|\*|/\*)' src/lib/positions.js | grep -cE 'Math\.random|Date\.now|new Date\(|todayISO\('` = 0 — אין תלות בשעון הקיר ואין אקראיות בקוד הפעיל.
    - `grep -c "from \"./autoAssign.js\"" src/lib/positions.js` ≥ 1 ו-`git diff --stat src/lib/autoAssign.js` ריק — `isQualified` נצרך כפי שהוא, `autoAssign.js` לא נגעה (D-02, D-04).
    - `node scripts/verify-positions.mjs` מדפיס ok על כל אחת מהבדיקות שב-`<behavior>`, כולל בדיקת `autoAssign()` מקצה לקצה ובדיקת הדטרמיניזם בסדר מעורבב.
    - `npm test` יוצא בקוד 0 ומריץ שלושה סקריפטים; `grep -c 'verify-positions.mjs' package.json` ≥ 1.
    - `src/lib/api.js` מכיל `positionFromRow`, `positionToRow`, `createPosition`, `updatePosition`, `deletePosition`, `materializeTemplateShifts`, ו-`loadTeam` מחזיר מפתח `positions`.
    - `grep -c 'order("created_at")' src/lib/api.js` גדל ב-1 לעומת HEAD — שאילתת העמדות ממוינת מפורשות.
    - `updatePosition` זורק שגיאה בעברית כאשר `.select()` חוזר ריק (שומר עיוורון-RLS), בדיוק כמו `setGuardQualifications`.
    - `src/hooks/useGuardian.js` מכיל `positions: []` ב-`EMPTY` וארבעה actions חדשים; רשימת התלויות של `useMemo` ה-actions לא השתנתה.
    - `ensurePositionsForWeek` על שבוע שכבר מומש מחזיר `{created: 0}` בלי לקרוא ל-`refresh()` — ניתן לאימות בבדיקת יחידה שמחליפה את שכבת ה-API בכפילה.
    - `npm run build` עובר.
  </acceptance_criteria>

  <done>עמדת תבנית אחת עוברת את כל הנתיב: הגדרה נשמרת, שורות השבוע נוצרות בדיוק פעם אחת, והמנוע הקיים מאייש אותן מבריכת הכשירים — הכול מוכח ב-`npm test`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| דפדפן → Supabase (PostgREST) | כל כתיבה וקריאה של עמדות חוצה כאן. מפתח ה-anon גלוי בקוד הלקוח, ולכן RLS היא הגבול היחיד. |
| Supabase Auth → `gs_profiles.role` | הקביעה "האם המשתמש הזה אחמ"ש" נעשית בתוך מדיניות ה-RLS, לא בדפדפן. |
| קוד לקוח → מנוע הכשירות | `isQualified` רץ בדפדפן; אכיפת שרת נדחתה במפורש ל-QUAL-V2-03 (פאזה 3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | `gs_positions` — כתיבה | high | mitigate | מדיניות `gs_positions_write` מוגבלת ל-`role = 'supervisor'` בתוך אותה מיגרציה שיוצרת את הטבלה (Task 2 ג'/ב'). אומת חי: `npm run test:backend` מוכיח ששומר לא-אחמ"ש נדחה. |
| T-04-02 | Information Disclosure | `gs_positions` — קריאה | high | mitigate | מדיניות `gs_positions_read` מצומצמת ל-`team_code in (select ... where user_id = auth.uid())`. ניחוש `position_id` של צוות אחר אינו מחזיר שורה. אומת חי ב-`verify-backend.mjs`. |
| T-04-03 | Tampering | `shape` / `weekdays` — קלט | medium | mitigate | `check (shape in ('template','weekly'))` ברמת העמודה; `weekdays` נכתב רק דרך `positionToRow` ומאומת כמערך שלמים 0–6 בשכבת הטופס (04-02). ASVS V5. |
| T-04-04 | Tampering | אכיפת כשירות בצד לקוח בלבד | medium | accept | פשרה מתועדת שנורשה מפאזה 3 ללא שינוי — QUAL-04/QUAL-05 נאכפות בדפדפן, ואכיפת שרת נדחתה ל-QUAL-V2-03 (REQUIREMENTS.md). הפאזה הזאת אינה מרחיבה את הפשרה: היא משתמשת באותו `isQualified` על אותם נתונים. |
| T-04-05 | Denial of Service | `ensurePositionsForWeek` | low | mitigate | הפעולה חוזרת מיד בלי כתיבה ובלי `refresh()` כשאין חוסרים; בלי השער הזה כל שינוי שבוע היה מייצר סבב כתיבה+רענון. |
| T-04-SC | Tampering | התקנות npm | n/a | accept | הפאזה אינה מוסיפה חבילה חיצונית אחת (04-RESEARCH.md §"Package Legitimacy Audit": Not applicable). אין משימת התקנה, ולכן אין שער legitimacy לעבור. |
</threat_model>

## Artifacts this phase produces

**מסד נתונים**
- טבלה `gs_positions` — עמודות: `id` (uuid, PK), `team_code` (text, FK ל-`gs_teams.code`), `shape` (text, `template`/`weekly`), `title` (text), `category` (text), `weekdays` (jsonb), `start_time` (time), `end_time` (time), `required_guards` (int, ברירת מחדל 1), `active` (boolean, ברירת מחדל true), `created_at` (timestamptz)
- מדיניויות RLS: `gs_positions_read`, `gs_positions_write`
- עמודות חדשות: `gs_shifts.position_id` (uuid, nullable, FK), `gs_tasks.position_id` (uuid, nullable, FK)
- אינדקסים: `gs_shifts_position_date_idx`, `gs_tasks_position_date_idx`

**פונקציות טהורות (`src/lib/positions.js`)**
- `expectedDatesForWeek(position, sundayISO)`
- `plannedRowsForWeek(position, sundayISO)`
- `missingRowsForWeek(position, sundayISO, realized)`
- `qualifiedGuardsForPosition(position, guards)`
- `workingGuardIdsForWeek(position, { shifts, tasks }, sundayISO)`

**שכבת נתונים (`src/lib/api.js`)**
- `positionFromRow`, `positionToRow`
- `createPosition`, `updatePosition`, `deletePosition`, `materializeTemplateShifts`
- `positions` בתוך תוצאת `loadTeam`; `positionId` על `shiftFromRow`/`shiftToRow`/`taskFromRow`/`taskColumns`

**State (`src/hooks/useGuardian.js`)**
- `positions` ב-`EMPTY` ובנתונים המוחזרים
- `actions.addPosition`, `actions.updatePosition`, `actions.deletePosition`, `actions.ensurePositionsForWeek`

**בדיקות**
- `scripts/verify-positions.mjs` (חדש, מחווט ל-`npm test`)
- סעיף `standing positions` ב-`scripts/verify-backend.mjs`

<verification>
- `npm test` יוצא בקוד 0 ומריץ שלושה סקריפטים, כולל `verify-positions.mjs`.
- `npm run test:backend` מדפיס ok על סעיף `gs_positions` — קריאה מצומצמת לצוות, כתיבה מצומצמת לאחמ"ש, ואינדקס ייחודי שחוסם כפילות.
- `npm run build` עובר.
- `git diff --stat src/lib/autoAssign.js src/lib/fairness.js` ריק — שני המנועים הטהורים נצרכים כפי שהם (D-02, D-04).
- `grep -vE '^\s*(//|\*|/\*)' src/lib/positions.js | grep -cE 'Math\.random|Date\.now|new Date\(|todayISO\('` = 0.
</verification>

<success_criteria>
- עמדת תבנית אחת שהוגדרה פעם אחת מייצרת את שורות השבוע שלה, ורק פעם אחת, בכל מספר הרצות ובכל סדר קלט.
- שורה שנוצרה כך מאוישת על ידי `autoAssign()` הקיים ללא שינוי בו, ואף שומר לא-כשיר אינו נבחר אליה.
- `gs_positions` חיה עם RLS מוכחת, ומחיקת הגדרת עמדה משאירה את ההיסטוריה שלה על כנה.
- כל ההכרעות של Task 1 מתועדות ב-SUMMARY כך ש-04-02 ו-04-03 יורשות אותן.
</success_criteria>

<output>
Create `.planning/phases/04-standing-positions/04-01-SUMMARY.md` when done
</output>
