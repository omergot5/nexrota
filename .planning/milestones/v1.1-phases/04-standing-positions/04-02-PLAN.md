---
phase: 04-standing-positions
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/supervisor/PositionsScreen.jsx
  - src/components/SupervisorApp.jsx
  - src/components/supervisor/views.jsx
  - src/components/GuardApp.jsx
  - src/lib/terms.js
autonomous: true
requirements: [POS-01, POS-02, POS-05]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "POS-01 · מנהל מגדיר עמדה במסך ייעודי פעם אחת — טופס אחד, שמירה אחת — ומשם והלאה אין כפתור 'צור שבוע'. מעבר לשבוע אחר מייצר את השורות לבד."
    - "POS-05 · 'מי כשיר לעמדה' ו'מי עובד בה השבוע' הן שתי רשימות נפרדות על המסך, שנגזרות משני שדות שונים לחלוטין ומסומנות בארבעה ערוצים בלתי-תלויים: כותרת שונה, אייקון שונה, צורת פריט שונה, ותווית טקסט שונה על כל פריט."
    - "POS-02/D-04 · המסך אינו מציע שום פעולה של 'שייך אדם לעמדה'. הרשימה הכשירה היא גזירה קריאה בלבד מעל `qualifiedCategories` הקיים; מי שרוצה לשנות כשירות עושה זאת בעורך הכשירויות של הרוסטר מפאזה 3."
    - "ROADMAP §4.4 · משתתף שהוא בבריכת הכשירים של עמדה רואה זאת במסך שלו תחת כותרת נפרדת ומסומנת אחרת מ'התורנויות שלי' — הוא אינו יכול לקרוא את זה כשיבוץ."
    - "D-01 · טופס ההגדרה מציג את שתי הצורות כבחירה מפורשת בזמן ההגדרה; שדות היום/שעה מופיעים רק בצורת התבנית."
    - "D-05 · המסך נעצר כאן. אין תצוגה של לוז שרץ קדימה על פני שבועות, ואין איחוד עם לוח המשמרות — שניהם BOARD-02/BOARD-01 בפאזה 5."
  artifacts:
    - "src/components/supervisor/PositionsScreen.jsx — המסך הייעודי המינימלי: טופס הגדרה, רשימת העמדות, ושתי הרשימות של POS-05"
    - "src/components/SupervisorApp.jsx — פריט ניווט `positions` תחת 'עוד', חיווט ה-view, וקריאת `ensurePositionsForWeek` בשינוי שבוע"
    - "src/components/supervisor/views.jsx — `categoryOptions` הופכת למיוצאת (בלי שינוי בגוף הפונקציה)"
    - "src/components/GuardApp.jsx — בלוק 'העמדות שאני כשיר/ה להן' בתוך המסך של המשתתף"
    - "src/lib/terms.js — מפתחות `nav.positions`, `positions.qualified`, `positions.working`, `positions.shape.template`, `positions.shape.weekly`, `positions.mine`, כולל דריסות `army`"
  key_links:
    - "`state.positions` (מ-useGuardian, פאזה 04-01) → `SupervisorApp` → `PositionsScreen`."
    - "`qualifiedGuardsForPosition(position, guards)` → הרשימה השמאלית; `workingGuardIdsForWeek(position, {shifts, tasks}, sunday)` → הרשימה הימנית. שתי פונקציות, שני שדות מקור, אפס חפיפה."
    - "`categoryOptions(shifts, tasks)` (views.jsx, פאזה 3 D-01) → בוחר הקטגוריה בטופס העמדה — טקסונומיה אחת משותפת, לא רשימה שנייה."
    - "`weekDates[0]` ב-`SupervisorApp` → `actions.ensurePositionsForWeek` → מימוש עצל בצפייה בשבוע (POS-01)."
  prohibitions:
    - "אדם שנמצא בבריכת הכשירים של עמדה לעולם אינו נקרא על המסך כמי שעובד בה השבוע — לא במסך המנהל ולא במסך המשתתף."
    - "ההבחנה בין 'כשיר' ל'עובד השבוע' לעולם אינה נשענת על צבע בלבד."
    - "המסך לעולם אינו מציע כתיבה שמשמעותה 'שייך אדם לעמדה' — שיוך הוא הצהרת כשירות ולא שיבוץ, ולכן הוא כבר קיים בעורך הכשירויות."
    - "מחרוזת ממשק שנשמעת אחרת בצבא ובאזרחות לעולם אינה מקודדת קשיח בגוף רכיב."
    - "תצוגת לוז שרץ קדימה על פני שבועות אינה נבנית בפאזה הזאת."
  flagged_assumptions:
    - "POS-05 — בדיקת הקצה האוטומטית נמנעה מסיווג (unclassified/unresolved). הדרישה נגזרה כאן ידנית מ-REQUIREMENTS.md §POS-05 יחד עם ROADMAP קריטריון 4, ששניהם מדברים על אותה הפרדה משני צדדים שונים (מנהל ומשתתף) — ולכן שניהם ממומשים."
    - "POS-01 — בדיקת הקצה האוטומטית נמנעה מסיווג. ההנחה הממומשת: 'בלי שנגע בכלום' = טריגר בצפייה בשבוע, ולא כפתור. אם המשתמש מצפה לכפתור מפורש, זו סטייה שיש להעלות ב-UAT."
    - "ROADMAP קריטריון 4 מדבר על המשתתף, בעוד REQUIREMENTS §POS-05 מדבר על המנהל. שניהם מכוסים כאן; אילו כוסה רק אחד, מבחן הכניסה היה נופל בדיוק בנקודה שהפאזה קיימת כדי לסגור."
---

<objective>
לתת לעמדה הקבועה פנים: מסך ייעודי מינימלי שבו מנהל מגדיר עמדה פעם אחת (D-01, שתי הצורות), ורואה עליה שתי רשימות שאי אפשר לבלבל ביניהן — מי כשיר, ומי עובד השבוע (POS-05, D-04). ובצד השני, המשתתף רואה את אותה הבחנה בדיוק במסך שלו.

Purpose: זו הנקודה שבה הפאזה נופלת או עומדת במבחן הכניסה. אדם שקורא "אני ברשימה" ומבין "אני עובד" הוא בדיוק סוג הכשל שהמחזור הזה קיים כדי לסגור — שקיפות שמדווחת דבר לא-נכון. פאזה 3 כבר פתרה את הבעיה המקבילה (QUAL-08: מועמד חסום נושא שלושה סימנים מיותרים כדי ש"לא כשיר" לא יקרוס ל"לא זמין"), והמסך הזה יורש את אותה משמעת ומרחיב אותה לארבעה ערוצים.

Output: `PositionsScreen.jsx`, פריט ניווט, בלוק במסך המשתתף, ומילון מונחים מורחב. שום דבר מעבר — התצוגה המשוכללת של לוז שרץ קדימה היא BOARD-02 בפאזה 5 (D-05).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-standing-positions/04-CONTEXT.md
@.planning/phases/04-standing-positions/04-RESEARCH.md
@.planning/phases/04-standing-positions/04-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: מסך העמדות — טופס ההגדרה, רשימת העמדות, וחיווט הניווט</name>

  <files>src/components/supervisor/PositionsScreen.jsx, src/components/SupervisorApp.jsx, src/components/supervisor/views.jsx, src/lib/terms.js</files>

  <read_first>
    - `.planning/phases/04-standing-positions/04-01-SUMMARY.md` — ההכרעות שננעלו ב-Task 1 של 04-01 (שמות עמודות, תאריך הזהות של הצורה השבועית, `required_guards`)
    - `src/lib/positions.js` — החתימות המדויקות שהמסך צורך: `qualifiedGuardsForPosition`, `workingGuardIdsForWeek`, `expectedDatesForWeek`
    - `src/components/supervisor/views.jsx` שורות 1495–1530 — `categoryOptions` וההנמקה שלה (פאזה 3, D-01); ושורות 2335–2420 — `TeamView` כדוגמה למסך משני שמשלב טופס ורשימה
    - `src/components/SupervisorApp.jsx` שורות 1–50 (`navItems`/`moreItems`/`titles`) ושורות 70–170 (`state` destructuring, `weekDates`, `common`, `views`)
    - `src/components/ui.jsx` — `Card`, `Btn`, `IconBtn`, `Field`, `Input`, `Select`, `Segmented`, `Badge`, `EmptyState`, `PageHeader`, `Modal`
    - `src/lib/terms.js` — `BASE`, `PROFILE_TERMS.army`, ו-`t()`; והכלל בראש הקובץ: מונח הוא שם של פעולה, לא שם של יכולת
    - `src/lib/dates.js` — `DAYS_HE_SHORT` (מוסכמת `Date.getDay()` 0–6), `rangeLabelHe`
    - `CLAUDE.md` — "כל מילה שהמשתמש רואה" ב-`terms.js`; ניגודיות WCAG AA; RTL
  </read_first>

  <action>
    **(א) `src/components/supervisor/views.jsx`** — הוסף `export` לפני `const categoryOptions` (שורה ~1513). אל תיגע בגוף הפונקציה ואל תשכפל אותה: שתי טקסונומיות שרק *נראות* דומות הן בדיוק מה שההערה מעל הפונקציה מזהירה מפניו (פאזה 3, D-01).

    **(ב) `src/lib/terms.js`** — הוסף ל-`BASE`: `"nav.positions": "עמדות קבועות"`, `"positions.shape.template": "תבנית משמרת"`, `"positions.shape.weekly": "שבועית ללא שעות"`, `"positions.qualified": "מי כשיר לעמדה"`, `"positions.working": "מי עובד בה השבוע"`, `"positions.mine": "העמדות שאני כשיר/ה להן"`. הוסף ל-`PROFILE_TERMS.army` את הדריסות שבאמת שונות בלשון פיקוד — לכל הפחות `"nav.positions"` ו-`"positions.working"`. אל תדרוס מפתח שהמילה בו זהה; פרופיל שדורס הכול הוא מוצר שני שמתחזה לפרופיל.

    **(ג) `src/components/supervisor/PositionsScreen.jsx`** — רכיב חדש, `export default function PositionsScreen({ positions = [], guards = [], shifts = [], tasks = [], weekDates = [], actions, busy })`. `dir` נשלט מהמעטפת ב-`SupervisorApp`, כמו כל מסך משני אחר.

    טופס הגדרה בתוך `Modal` שנפתח מכפתור "עמדה חדשה", עם `Field` לכל שדה:
    - `title` — `Input`, חובה.
    - `shape` — `Segmented` עם שני ערכים בדיוק, `template` ו-`weekly`, בתוויות `t("positions.shape.template")` ו-`t("positions.shape.weekly")` (D-01). זו בחירה בזמן ההגדרה, לא הגדרה גלובלית.
    - `category` — `Select` שאפשרויותיו הן `categoryOptions(shifts, tasks)` המיוצאת מ-(א). זהו שדה הכשירות היחיד של העמדה (D-04); אל תבנה רשימת קטגוריות משלך.
    - `weekdays` — שבעה כפתורי החלפה ממוספרים 0–6 עם תוויות `DAYS_HE_SHORT`, מוצגים **רק** כאשר `shape === "template"`. הערך הנשמר הוא מערך מספרים שלמים בטווח 0–6 בלבד; סנן כל ערך אחר לפני השמירה (ASVS V5 / T-04-03).
    - `startTime`/`endTime` — `Input type="time"`, מוצגים ונדרשים **רק** כאשר `shape === "template"`. בצורה השבועית הם נשמרים `null` — עמדה שנפרשת על שבוע בלי שעות לעולם אינה נכתבת עם שעות מומצאות, אחרת היא הייתה מופיעה בגרפי הנטל עם חלון זמן שאיש לא הזין.
    - `requiredGuards` — `Input type="number"`, ברירת מחדל 1, מינימום 1.
    - `active` — תיבת סימון, ברירת מחדל דלוקה.

    שמירה קוראת ל-`actions.addPosition(form)` (או `actions.updatePosition(id, form)` בעריכה) ומשם והלאה אין שום פעולה נוספת שהמנהל צריך לעשות — זה מה ש-POS-01 דורש. מחיקה קוראת ל-`actions.deletePosition(id)`, שכבר עוברת דרך `deferred` ולכן מקבלת שורת ביטול של שמונה שניות; אל תוסיף `confirm()`.

    מתחת לטופס: רשימת העמדות המוגדרות. כל עמדה כרטיס אחד עם הכותרת, תג הצורה, תג הקטגוריה, ובצורת התבנית — הימים והשעות. עמדה עם `active === false` נושאת תג "מושבתת" ואינה נספרת במימוש. מצב ריק דרך `EmptyState` עם `icon="shield"` והסבר במשפט אחד מה עמדה קבועה עושה.

    **(ד) `src/components/SupervisorApp.jsx`** — הוסף `positions` לפירוק ה-`state`. הוסף ל-`moreItems()` פריט `{ id: "positions", label: t("nav.positions"), icon: "shield", hint: "עמדה שחוזרת כל שבוע לבד" }`. הוסף ל-`views` את `positions: <PositionsScreen positions={positions} guards={guards} shifts={shifts} tasks={tasks} weekDates={weekDates} actions={actions} busy={busy} />`.

    הוסף `useEffect` שקורא ל-`actions.ensurePositionsForWeek(weekDates[0])` כאשר `weekDates[0]` משתנה, עם `[weekDates[0], actions]` כרשימת תלויות. `actions` יציב רפרנציאלית ולכן אינו מייצר ריצה מחדש; `ensurePositionsForWeek` כבר חוזר בלי כתיבה ובלי `refresh()` כשאין חוסרים (04-01), ולכן האפקט אינו לולאה. זה הטריגר שהופך את POS-01 ל"בלי שנגע בכלום" — לא כפתור.
  </action>

  <verify>
    <automated>npm run build — עובר</automated>
    <automated>npm test — יוצא בקוד 0 (שלושת הסקריפטים כולל verify-positions.mjs)</automated>
    <automated>grep -c 'export const categoryOptions' src/components/supervisor/views.jsx — חייב להחזיר 1</automated>
    <automated>grep -c 't("nav.positions")' src/components/SupervisorApp.jsx — חייב להחזיר לפחות 1</automated>
    <automated>grep -c 'ensurePositionsForWeek' src/components/SupervisorApp.jsx — חייב להחזיר לפחות 1</automated>
    <automated>grep -c 'categoryOptions' src/components/supervisor/PositionsScreen.jsx — חייב להחזיר לפחות 1</automated>
    <human-check>`npm run dev`, היכנס כאחמ"ש, "עוד" ⟵ "עמדות קבועות". הגדר עמדת תבנית (א'–ה', 08:00–16:00, קטגוריה קיימת). עבור לשבוע הבא ובחזרה, ואמת ביומן שהמשמרות של העמדה נוצרו — בלי שלחצת על שום כפתור נוסף. עבור שוב קדימה ואחורה ואמת שלא נוצרו כפילויות. סמן כלא-נבדק אם לא נצפה בפועל בדפדפן.</human-check>
  </verify>

  <acceptance_criteria>
    - `src/components/supervisor/PositionsScreen.jsx` קיים ומייצא רכיב ברירת מחדל.
    - `grep -c 'export const categoryOptions' src/components/supervisor/views.jsx` = 1, ו-`git diff` על גוף הפונקציה `categoryOptions` מראה שינוי של מילת המפתח `export` בלבד.
    - `grep -c 'categoryOptions' src/components/supervisor/PositionsScreen.jsx` ≥ 1 — בוחר הקטגוריה נשען על הטקסונומיה המשותפת ולא על רשימה מקומית.
    - `grep -cE 't\("positions\.(shape\.template|shape\.weekly|qualified|working|mine)"\)|"positions\.(shape\.template|shape\.weekly|qualified|working|mine)"' src/lib/terms.js` ≥ 5 — חמשת המפתחות קיימים ב-`BASE`.
    - `grep -c '"nav.positions"' src/lib/terms.js` ≥ 2 — המפתח קיים גם ב-`BASE` וגם בדריסות `army`.
    - שדות `startTime`/`endTime`/`weekdays` אינם מוצגים כאשר `shape === "weekly"`, ובשמירה הם נכתבים `null`/`[]` — ניתן לאימות בקריאת המקור סביב הבדיקה על `shape`.
    - `grep -c 'ensurePositionsForWeek' src/components/SupervisorApp.jsx` ≥ 1, והקריאה יושבת בתוך `useEffect` שתלוי ב-`weekDates[0]`.
    - `grep -c 'confirm(' src/components/supervisor/PositionsScreen.jsx` = 0 — מחיקה עוברת דרך `deferred`/`UndoBar` הקיימים, לא דרך דיאלוג אישור.
    - `npm run build` ו-`npm test` עוברים.
  </acceptance_criteria>

  <done>מנהל יכול להגדיר עמדה משתי הצורות במסך ייעודי, לראות אותה ברשימה, ולראות את שורות השבוע נוצרות מעצמן במעבר שבוע.</done>
</task>

<task type="auto">
  <name>Task 2: POS-05 — שתי רשימות שאי אפשר לבלבל ביניהן</name>

  <files>src/components/supervisor/PositionsScreen.jsx, src/lib/terms.js</files>

  <read_first>
    - `src/lib/positions.js` — `qualifiedGuardsForPosition` ו-`workingGuardIdsForWeek`: מאילו שדות בדיוק כל אחת נגזרת
    - `src/components/supervisor/views.jsx` — `AssignView` (שורה ~918), ובתוכו הטיפול במועמד חסום: התקדים של פאזה 3 (QUAL-08) שבו מועמד נושא שלושה סימנים מיותרים — מצב מושבת, תווית מוחלפת, וסמל מנעול — ועטרת ניטרלית ולא סמל סכנה
    - `src/components/ui.jsx` — `Badge` (המנעד `tone`), `Avatar`, `Card`, `EmptyState`
    - `.planning/STATE.md` שורה 79 — הניסוח המדויק של הכרעת QUAL-08, שהמסך הזה יורש
    - `.planning/phases/04-standing-positions/04-RESEARCH.md` — Pitfall 2 ("'Qualified' collapsing into 'assigned' on screen") ו-Pattern 2
    - `CLAUDE.md` — ניגודיות WCAG AA 4.5:1; "סימון קריטי לעולם לא בצבע בלבד"
  </read_first>

  <action>
    בתוך כרטיס העמדה ב-`PositionsScreen.jsx`, הצג שתי רשימות זו לצד זו (או זו מתחת לזו במסך צר), עבור השבוע הנוכחי שנבחר (`weekDates[0]`):

    **רשימה א' — `t("positions.qualified")`.** נגזרת אך ורק מ-`qualifiedGuardsForPosition(position, guards)`, כלומר מ-`guard.qualifiedCategories` מול `position.category` (D-04, POS-02). זו רשימה קריאה בלבד: אין בה כפתור, אין בה תיבת סימון, ואין בה שום דבר שנקרא כ"שייך אותו לעמדה". מי שרוצה לשנות כשירות עושה זאת בעורך הכשירויות ב-`TeamView` מפאזה 3, והמסך אומר את זה במשפט אחד מתחת לרשימה.

    **רשימה ב' — `t("positions.working")`.** נגזרת אך ורק מ-`workingGuardIdsForWeek(position, { shifts, tasks }, weekDates[0])`, כלומר מ-`assignedGuards`/`assignees` על השורות שהתממשו. לצד כל שם — תאריך השורה שבה הוא עובד.

    שתי הרשימות חייבות להיות מובחנות בארבעה ערוצים בלתי-תלויים, כך שגם קורא שרואה את המסך בגווני אפור בלבד יבחין ביניהן:
    1. **כותרת שונה** — שני המחרוזות מ-`terms.js`, לא וריאציה של אותה מילה.
    2. **אייקון שונה** — `<Icon name="key" />` לרשימת הכשירים, `<Icon name="calendar" />` לרשימת העובדים. שניהם קיימים ברישום האייקונים.
    3. **צורת פריט שונה** — כשיר מוצג כ"צ'יפ" מתוחם בקו מתאר (`ring-1 ring-inset`, רקע שקוף); עובד השבוע מוצג כשורה מלאה עם `Avatar` ורקע מלא. ההבדל הוא בצורה, לא בגוון.
    4. **תווית טקסט לכל פריט** — פריט ברשימת הכשירים נושא סיומת מילולית שאומרת שזו כשירות; פריט ברשימת העובדים נושא את התאריך שבו הוא עובד. אחרי כיסוי הכותרות, הטקסט לבדו עדיין מבדיל.

    שני מצבי ריק שאומרים שני דברים שונים ולא אותו משפט: רשימת כשירים ריקה = "אין כשירים לקטגוריה הזאת" (זו התקלה שמונעת מהעמדה להתמלא); רשימת עובדים ריקה = "עוד לא שובץ אף אחד השבוע" (זה מצב נורמלי לפני הרצת השיבוץ). הראשונה היא אזהרה, השנייה אינה.

    שים לב במפורש לתרחיש שמוכיח שהרשימות אינן נגזרות מאותו שדה: אדם שאינו כשיר אך משובץ ידנית משורות עבר יופיע ברשימה ב' ולא ברשימה א'. אל תסנן אותו החוצה כדי ש"ייראה נכון" — הוא באמת עובד שם, וההצגה הכפולה הזאת היא בדיוק מה ש-POS-05 מבקש.

    כל מחרוזת ממשק חדשה שנשמעת אחרת בצבא ובאזרחות עוברת דרך `terms.js`; מחרוזות שהן תיאור ולא מונח (למשל הסבר במצב הריק) יכולות להישאר ברכיב, כמו בשאר המסכים.
  </action>

  <verify>
    <automated>npm run build — עובר</automated>
    <automated>npm test — יוצא בקוד 0</automated>
    <automated>grep -c 'qualifiedGuardsForPosition' src/components/supervisor/PositionsScreen.jsx — חייב להחזיר לפחות 1</automated>
    <automated>grep -c 'workingGuardIdsForWeek' src/components/supervisor/PositionsScreen.jsx — חייב להחזיר לפחות 1</automated>
    <automated>grep -c 'name="key"' src/components/supervisor/PositionsScreen.jsx — חייב להחזיר לפחות 1</automated>
    <automated>grep -c 'name="calendar"' src/components/supervisor/PositionsScreen.jsx — חייב להחזיר לפחות 1</automated>
    <automated>grep -c 't("positions.qualified")' src/components/supervisor/PositionsScreen.jsx — חייב להחזיר לפחות 1</automated>
    <automated>grep -c 't("positions.working")' src/components/supervisor/PositionsScreen.jsx — חייב להחזיר לפחות 1</automated>
    <human-check>בדפדפן: פתח עמדה שיש לה גם כשירים וגם משובצים. (1) צלם את המסך והמר לגווני אפור — שתי הרשימות עדיין מובחנות. (2) כסה את שתי הכותרות בכף היד — עדיין אפשר לומר איזו רשימה היא איזו, מהאייקון ומצורת הפריטים. (3) צמצם כשירות של אדם שכבר משובץ בעמדה ואמת שהוא עובר להופיע ברשימת העובדים בלבד ולא ברשימת הכשירים. סמן כלא-נבדק אם לא נצפה בפועל.</human-check>
  </verify>

  <acceptance_criteria>
    - `PositionsScreen.jsx` מייבא ומשתמש בשתי פונקציות נפרדות: `qualifiedGuardsForPosition` ו-`workingGuardIdsForWeek`; אין בקובץ שום נתיב שבו רשימה אחת נגזרת מהשנייה.
    - `grep -c 'name="key"' src/components/supervisor/PositionsScreen.jsx` ≥ 1 ו-`grep -c 'name="calendar"' src/components/supervisor/PositionsScreen.jsx` ≥ 1 — שני אייקונים שונים, ערוץ הבחנה שאינו צבע.
    - שתי הכותרות מגיעות מ-`t("positions.qualified")` ו-`t("positions.working")`, לא ממחרוזות מקודדות.
    - כל פריט ברשימת הכשירים נושא סיומת טקסט שמציינת כשירות; כל פריט ברשימת העובדים נושא את התאריך — כלומר גם ערוץ הטקסט מבדיל, לא רק הכותרת.
    - שני מצבי הריק מדפיסים שני משפטים שונים, ורק זה של "אין כשירים" מסומן כאזהרה.
    - אדם לא-כשיר שמשובץ בפועל מופיע ברשימת העובדים ואינו מסונן החוצה.
    - `npm run build` ו-`npm test` עוברים.
  </acceptance_criteria>

  <done>מנהל מסתכל על עמדה ורואה שתי רשימות שאי אפשר לבלבל ביניהן, גם בלי צבע וגם בלי כותרות.</done>
</task>

<task type="auto">
  <name>Task 3: הצד של המשתתף — "אני כשיר/ה לזה" אינו "אני עובד/ת בזה"</name>

  <files>src/components/GuardApp.jsx, src/lib/terms.js</files>

  <read_first>
    - `src/components/GuardApp.jsx` שורות 129–330 — `MySchedule` במלואו: איך תורנויות המשתתף מוצגות היום, אילו כותרות `<h2>`/`<h3>` יש, ואיפה מסתיים הבלוק של המשמרות
    - `src/components/GuardApp.jsx` שורות 781–830 — `GuardApp` עצמו, פירוק ה-`state` ומפת ה-`views`
    - `src/lib/positions.js` — `qualifiedGuardsForPosition`, `workingGuardIdsForWeek`
    - `src/components/supervisor/PositionsScreen.jsx` (אחרי Task 2) — ארבעת ערוצי ההבחנה שנקבעו שם, כדי לחזור עליהם ולא להמציא שפה ויזואלית שנייה
    - `.planning/ROADMAP.md` §"Phase 4" קריטריון הצלחה 4 — הניסוח המדויק שמדבר על המשתתף
    - `src/lib/terms.js` — המפתח `positions.mine` שנוסף ב-Task 1, ו-`guard.nav.schedule`
  </read_first>

  <action>
    ב-`GuardApp.jsx`, הוסף `positions` לפירוק ה-`state` והעבר אותו ל-`MySchedule`.

    בתוך `MySchedule`, מתחת לבלוק התורנויות הקיים (ולא בתוכו), הוסף בלוק קריאה-בלבד תחת הכותרת `t("positions.mine")`. הוא מציג את העמדות הפעילות שבהן המשתמש הנוכחי נמצא בבריכת הכשירים — כלומר `positions.filter((p) => p.active && qualifiedGuardsForPosition(p, [user]).length > 0)`. הצג לכל עמדה את הכותרת ואת הקטגוריה, ותו לא.

    הבלוק הזה חייב להיות מובחן מבלוק התורנויות באותם ארבעה ערוצים שנקבעו ב-Task 2: כותרת שונה, `<Icon name="key" />` (ולא האייקון של התורנויות), פריטים בצורת צ'יפ מתוחם ולא בצורת כרטיס משמרת מלא, ומשפט הסבר אחד מתחתיו שאומר במפורש שזו רשימת כשירות ולא לוח עבודה — הרשימה מספרת מה *מותר* לו לעשות, ומה שהוא *עושה* השבוע כבר מופיע למעלה בין התורנויות.

    אין להוסיף כאן פעולה, כפתור, או קישור שמאפשר למשתתף לשנות כשירות. אין להציג כאן תאריכים או שעות — תאריך על פריט בבלוק הזה הוא בדיוק הרמז שהופך אותו למשמרת בעיני הקורא. עמדה שהמשתמש באמת עובד בה השבוע כבר מופיעה למעלה כמשמרת או כמשימה רגילה, כי היא התממשה לשורה רגילה; הבלוק הזה לא משכפל אותה.

    אם רשימת העמדות הפעילות ריקה, אל תציג את הבלוק כלל — מסך משתתף שמראה כותרת ריקה מוסיף רעש ולא מידע.
  </action>

  <verify>
    <automated>npm run build — עובר</automated>
    <automated>npm test — יוצא בקוד 0</automated>
    <automated>grep -c 't("positions.mine")' src/components/GuardApp.jsx — חייב להחזיר לפחות 1</automated>
    <automated>grep -c 'qualifiedGuardsForPosition' src/components/GuardApp.jsx — חייב להחזיר לפחות 1</automated>
    <automated>grep -c 'name="key"' src/components/GuardApp.jsx — חייב להחזיר לפחות 1</automated>
    <human-check>היכנס כמשתתף שמוגדר כשיר לקטגוריה של עמדה קיימת אך אינו משובץ בה השבוע. אמת: (1) העמדה מופיעה תחת הכותרת החדשה ולא בין התורנויות; (2) אין לצידה תאריך או שעה; (3) בקש מאדם שלא ראה את המסך לקרוא אותו בקול ולומר במילים שלו אם הוא עובד בעמדה הזאת השבוע — התשובה חייבת להיות "לא, אני רק יכול". סמן כלא-נבדק אם לא נצפה בפועל.</human-check>
  </verify>

  <acceptance_criteria>
    - `grep -c 't("positions.mine")' src/components/GuardApp.jsx` ≥ 1 — הכותרת מגיעה ממילון המונחים.
    - `grep -c 'qualifiedGuardsForPosition' src/components/GuardApp.jsx` ≥ 1 — הרשימה נגזרת מהפונקציה הטהורה ולא מחישוב מקומי כפול.
    - `grep -c 'name="key"' src/components/GuardApp.jsx` ≥ 1 — אותו אייקון שמסמן כשירות במסך המנהל, לא אייקון שני.
    - בבלוק החדש אין `<Btn`, אין `onClick`, ואין תאריך או שעה — קריאה בלבד.
    - הבלוק אינו מרונדר כלל כאשר אין עמדות פעילות שהמשתמש כשיר להן.
    - העמדות שהמשתמש עובד בהן בפועל השבוע ממשיכות להופיע בבלוק התורנויות הקיים, ואינן משוכפלות לבלוק החדש.
    - `npm run build` ו-`npm test` עוברים.
  </acceptance_criteria>

  <done>משתתף שנמצא בבריכת הכשירים של עמדה קורא זאת כהיתר ולא כשיבוץ — ROADMAP קריטריון 4 מתקיים משני צדדי המוצר.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| טופס בדפדפן → `api.js` → Supabase | ערכי `shape` ו-`weekdays` שהמשתמש מזין חוצים כאן; הם נכתבים לעמודות שיש להן `check` constraint ברמת בסיס הנתונים. |
| נתוני צוות → מסך המשתתף | `GuardApp` מציג נתוני עמדות של הצוות; ה-RLS כבר צמצמה את מה שהגיע ללקוח, והמסך אינו מבקש דבר נוסף. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | טופס העמדה — `shape` / `weekdays` | medium | mitigate | `shape` מוגבל ל-`Segmented` עם שני ערכים בלבד ונאכף שוב ב-`check` constraint (04-01 Task 2). `weekdays` מסונן למספרים שלמים 0–6 לפני השמירה. ASVS V5. |
| T-04-06 | Information Disclosure | רשימת "מי כשיר" במסך המנהל | low | accept | הרשימה נגזרת מנתוני `gs_profiles` של אותו צוות בלבד, שכבר הגיעו ללקוח דרך `loadTeam` תחת RLS. המסך אינו חושף שדה שלא היה נגיש קודם. |
| T-04-07 | Repudiation | קריאת "מי עובד השבוע" | low | accept | המסך קורא בלבד; מקור האמת נשאר `gs_assignments`/`gs_tasks.assignees` עם `assignmentMeta.source` הקיים. אין כאן כתיבה שיש לתעד. |
| T-04-08 | Elevation of Privilege | פעולות בטופס העמדה | high | mitigate | הטופס נגיש רק מתוך `SupervisorApp`, אך זו אינה ההגנה — ההגנה היא מדיניות `gs_positions_write` המוגבלת ל-`role = 'supervisor'` (04-01). משתתף שיקרא ל-API ישירות יידחה ברמת בסיס הנתונים. |
| T-04-04 | Tampering | אכיפת כשירות בצד לקוח בלבד | medium | accept | פשרה שנורשה מפאזה 3 ללא הרחבה; אכיפת שרת נדחתה ל-QUAL-V2-03. |
| T-04-SC | Tampering | התקנות npm | n/a | accept | התוכנית אינה מתקינה חבילה חיצונית; אין שער legitimacy לעבור. |
</threat_model>

## Artifacts this phase produces

**רכיבים**
- `src/components/supervisor/PositionsScreen.jsx` — רכיב ברירת מחדל חדש: טופס הגדרת עמדה, רשימת עמדות, ושתי רשימות POS-05
- בלוק "העמדות שאני כשיר/ה להן" בתוך `MySchedule` ב-`src/components/GuardApp.jsx`

**ניווט**
- פריט `positions` ב-`moreItems()` ורשומת `positions` ב-`views` בתוך `src/components/SupervisorApp.jsx`
- `useEffect` שקורא ל-`actions.ensurePositionsForWeek(weekDates[0])`

**סמלים מיוצאים**
- `categoryOptions` — הופכת ממקומית למיוצאת ב-`src/components/supervisor/views.jsx` (הגוף ללא שינוי)

**מילון מונחים (`src/lib/terms.js`)**
- `nav.positions`, `positions.shape.template`, `positions.shape.weekly`, `positions.qualified`, `positions.working`, `positions.mine` — ב-`BASE` ובדריסות `army`

<verification>
- `npm test` ו-`npm run build` עוברים.
- שתי הרשימות של POS-05 נגזרות משתי פונקציות שונות ומשני שדות מקור שונים — ניתן לאימות בקריאת המקור ובבדיקות ה-grep.
- ארבעת ערוצי ההבחנה קיימים במסך המנהל ובמסך המשתתף כאחד: כותרת, אייקון, צורת פריט, תווית טקסט.
- אין בפאזה הזאת שום תצוגת לוז שרץ קדימה על פני שבועות (D-05) — `grep -ri "BOARD-02" src/` ריק.
- אימות בדפדפן בוצע ותועד; מה שלא נצפה מדווח כלא-נבדק.
</verification>

<success_criteria>
- מנהל מגדיר עמדה פעם אחת ורואה אותה מתממשת בלי פעולה נוספת.
- "מי כשיר" ו"מי עובד השבוע" אינם ניתנים לבלבול, גם בגווני אפור וגם בלי כותרות.
- משתתף מבין מהמסך שהוא כשיר לעמדה ולא שהוא עובד בה השבוע.
- הפאזה נעצרת במסך המינימלי; הלוח המאוחד נשאר כולו לפאזה 5.
</success_criteria>

<output>
Create `.planning/phases/04-standing-positions/04-02-SUMMARY.md` when done
</output>
