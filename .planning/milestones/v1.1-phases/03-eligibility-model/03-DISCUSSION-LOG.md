# Phase 3 Discussion Log — מודל כשירויות

**תאריך:** 2026-08-26
**תחומים שנבחרו:** טקסונומיית הקטגוריות · מה "כל הקטגוריות" בפועל אומרת
**תחומים שנסקרו אך לא נבחרו לדיון:** פער האכיפה בשיבוץ הידני (toggleAssignment לא בודק שום אילוץ קשיח היום), תצוגת הכשירות בשיבוץ הידני (QUAL-07)

## טקסונומיית הקטגוריות

**ש:** על משמרות יש כבר טקסונומיה של תיקיות משימות (כוננות/מטבח/שמירות/סיור) — אבל היא קיימת רק על משימות. הכשירות של QUAL-03 צריכה לחול על שתיהן. טקסונומיה משותפת אחת, או שתי רשימות נפרדות?
**ת:** טקסונומיה משותפת אחת למשמרות ולמשימות.

**ש:** במשימות יש כבר עמודת `category` שמשמשת תיקייה וגם נכנסת למטריצת ההתנגשויות (`gs_role_compatibility`). עם טקסונומיה משותפת — אותה עמודה הופכת לשדה הכשירות, או משימות מקבלות עמודה שנייה?
**ת:** אותה העמודה משרתת.

## מה "כל הקטגוריות" בפועל אומרת

**ש:** אדם חדש כשיר לכל הקטגוריות בלי שאף אחד נגע. איך המצב הזה צריך להיות מיוצג במסד הנתונים?
**ת:** (המשתמש דחה את השאלה המקורית וניסח בעצמו:) שיהיה כשיר לכל כברירת מחדל, אבל שתהיה אפשרות לערוך כל שומר בנפרד עם התנאים המספיקים לו.

**ש (אישור):** רשימה ריקה/`null` = כשיר לכל, בהתאם לדפוס הקיים ב-`gs_role_compatibility` (היעדר שורה = מותר) — רק ברמת אדם ולא ברמת צוות. אפשר לנעול?
**ת:** אישר מחדש את אותה הכרעה.

## תוצר

הכרעות אלה נוסחו ל-[03-CONTEXT.md](03-CONTEXT.md) עבור מחקר ותכנון פאזה 3. שני נקודות פער מהותיות (אכיפה בשיבוץ ידני, תצוגת QUAL-07) הועברו ל-CONTEXT.md כפערים גלויים למחקר/תכנון, לא כהחלטות נעולות — לא נדונו עם המשתמש בכוונה.
