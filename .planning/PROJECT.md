# NexRota

## What This Is

מערכת שבונה סידור משמרות או סד"כ תורנויות בלחיצה, **ומסבירה כל החלטה שהיא מקבלת** — למנהל שבנה את הסידור וגם לאדם שנפגע ממנו. היא משרתת שני תפקידים בלבד (מנהל ומשתתף) על פני תחומים שונים — אבטחה, צבא, מוקדים, מסעדנות — כשההבדל ביניהם הוא **אוצר מילים וכשירויות, לא מבנה**.

**v1.1 (כיול ואיחוד) שוחרר.** המוצר מסכים עכשיו עם עצמו: הוגנות נמדדת בנטל בכל מקום (מנוע, דוחות, לוח בקרה, שורת המשתתף), משמרת ומשימה חיות באותו מנוע שיבוץ, כשירות חוסמת בכל מסלול בלי עקיפה, עמדה קבועה חוזרת אוטומטית מדי שבוע, ולוח אחד מציג הכול יחד — מאומת גם מול צופה שלא ראה את האפליקציה מעולם.

## Core Value

אדם שמקבל את האפליקציה לידיו מסיים סידור שבועי מלא **בלי שאף אחד יסביר לו כלום** — ומה שהמערכת אומרת לו על עצמה הוא נכון.

הפסקה השנייה היתה החידוש של מחזור v1.1, ועברה מבחן: שקיפות שמדווחת מספר שגוי גרועה מאי-שקיפות, כי היא קונה אמון שהיא לא יכולה לכבד. הבאג שהניע את המחזור (ראו Context) נסגר, ונבדק מול שלוש חזיתות — המנוע, המסך, והצופה הנאיבי.

## Requirements

### Validated

<!-- נגזר מהקוד הקיים ומ-docs/product/CONTEXT_PACK.md. עובד בייצור. -->

- ✓ הרשמה/כניסה דרך Supabase Auth, והצטרפות לצוות בקוד בן 6 תווים — existing
- ✓ מסך "השבוע" — ארבעה שלבים ברצף במקום תפריט פריטים — existing
- ✓ שיבוץ אוטומטי דטרמיניסטי עם נימוק לכל שיבוץ ולכל משבצת ריקה — existing
- ✓ אילוצים קשיחים: מנוחה 8ש', רצף 12ש', תקרת 6 משמרות, תקרת 3 לילות, איסור חפיפה — existing
- ✓ סולם זמינות בן ארבע דרגות, שבו `preferred` רך ובלתי ניתן למשחק — existing
- ✓ שיבוץ ידני עם בדיקת אילוצים חיה, דרך אותו `checkAssignment` של המנוע — existing
- ✓ תצוגת שבוע על ציר שעות עם סימון חורים, סינון והדפסה — existing
- ✓ לוח חודשי, ניהול אנשים, בקשות החלפה, אנליטיקה — existing
- ✓ משימות בתיקיות, עם תבניות לפי תחום הפעילות — existing
- ✓ מטריצת התנגשויות בין קטגוריות, עם עקיפה שדורשת נימוק — existing
- ✓ המלצת איזון על חלון מתגלגל של שבועיים (`fairness.js`) — existing
- ✓ שורת ההוגנות למשתתף — מול ממוצע הצוות — existing
- ✓ מילון מונחים לפי `gs_teams.mode` (civil/army) — מילים משתנות, מבנה לא — existing
- ✓ ביטול פעולה במקום דיאלוג אישור, RTL מלא, מצב בהיר/כהה/מערכת — existing
- ✓ שיתוף הסידור כתמונה (Canvas) — existing
- ✓ **לוח אחד מסודר.** תצוגה שמראה את כל המשימות והמשמרות יחד (UnifiedBoard), ולצידה תצוגה לכל עמדה קבועה שבה רואים את הלוז שלה רץ ארבעה שבועות קדימה, קריא לזר בלי הסבר (BOARD-04/D-13, מאומת לייב) — Phase 5
- ✓ **מדד הוגנות אחד — נטל בכל מקום.** מעבר האיזון והציון שמוצג למנהל עברו לנטל, בכל ארבע החזיתות (מנוע, SmartAssign, אנליטיקה, כרטיס הלוח) — v1.1 Phase 1
- ✓ **מודל כשירויות.** לכל אדם סט קטגוריות שטוח שהוא רשאי לבצע. חסימה מוחלטת בלי עקיפה בכל מסלול (שיבוץ אוטומטי, מעבר איזון, שיבוץ ידני, אישור החלפה). ברירת מחדל: כשיר להכול — v1.1 Phase 3
- ✓ **משימה נושאת שעות ונכנסת למנוע השיבוץ.** משימה עם שעה מקבלת מנוחה, רצף, תקרה ונטל בדיוק כמו משמרת, דרך פונקציית חפיפה משותפת אחת; משימה שנוצרה לפני המיגרציה נשארת קפואה מחוץ למנוע — v1.1 Phase 2
- ✓ **עמדה קבועה שחוזרת.** עמדה שנכנסת לכל שבוע אוטומטית ומתממשת לשורות דטרמיניסטיות; שיוך אליה = הצהרת כשירות, לא שיבוץ — מי עובד בפועל נקבע ע"י המנוע — v1.1 Phase 4

### Active

<!-- המחזור הבא. ריק — טרם הוגדר. -->

(אין דרישות פעילות. הרץ `/gsd-new-milestone` להגדרת המחזור הבא.)

### Out of Scope

- **היררכיית תפקידים** — סט שטוח של קטגוריות לכל אדם מספיק לשני המקרים: צבא הוא קבוצות מוכלות (קרבי ⊃ לא-קרבי) ומסעדה היא קבוצות זרות (שף ∦ מלצר). היררכיה נשברת על המסעדה; רב-מלצר פשוט נושא שני תגים.
- **עקיפת חסימת כשירות** — בניגוד למטריצת ההתנגשויות, שהיא השערה על העולם וניתנת לעקיפה עם נימוק, כשירות היא עובדה על האדם. מלצר לא יכול לבשל, ואין נימוק שמשנה זאת.
- **התראות (push/SMS/וואטסאפ)** — זוהה כפער אמיתי שסוגר את הלולאה בין "מייצר סידור" ל"מריץ את השבוע". נדחה במודע: המחזור הזה מכוון לכיול פנימי, ופיצ'ר חיצוני היה מסיט אותו.
- **היעדרויות כישות נפרדת (חופשה/מחלה)** — חשוב, ונדחה מאותה סיבה. היום מיוצג דרך "לא זמין".
- **מודל ארגון והזמנות** — `team_code` נשאר מפתח הבידוד היחיד במחזור הזה. זהו החסם המרכזי למכירה לארגון גדול, והוא ידרוש מחזור משלו.
- **עורך למטריצת ההתנגשויות ולמשקלי הנטל** — הטבלאות קיימות; העריכה נשארת SQL בשלב הזה.
- **פיצול `useGuardian.js` לארבעה hooks** — נשקל ונדחה. `actions` חייב להישאר יציב רפרנציאלית, ופיצול כזה שובר בדיוק את התכונה הזו.

## Context

**זהו פרויקט brownfield.** אבן דרך א' (טרום-GSD) ואבן דרך v1.1 (כיול ואיחוד) הושלמו והמוצר עובד בייצור (Vercel + Supabase). מפת קודבייס מלאה נמצאת ב-`.planning/codebase/`, ארכיון v1.1 נמצא ב-`.planning/milestones/v1.1-*`, ומסמך המצב הסמכותי הוא `docs/product/CONTEXT_PACK.md` — כשהוא סותר את `PRD.md`, הוא הנכון.

**שלושת הפערים ש-v1.1 סגר (עם הראיה שהניעה אותו):**

1. **כיול הוגנות.** ההבטחה המרכזית "נטל, לא ספירה" (לילה שוקל 1.4, סופ"ש 1.25) נמדדה נכון ב-`fairness.js`, אבל מעבר האיזון ו-`fairnessScore` מיינו וחישבו לפי ספירת משמרות — שבוע של שלושה לילות/חמישה בקרים/ארבעה אנשים דיווח "הוגנות מושלמת" בזמן שאדם אחד לא נשא אף לילה. תוקן ב-Phase 1: שני המנועים נגזרים עכשיו מ`perShiftLoad` בפועל, עם 22 בדיקות רגרסיה מבניות.
2. **עיוורון בין משמרות למשימות.** `checkHardConstraints` ראה רק משמרות ו-`findConflicts` רק משימות — אדם יכול היה להיות משובץ למטבח ולתורנות לילה באותו יום בלי שאף מנוע יגיד מילה. תוקן ב-Phase 2: פונקציית חפיפה אחת (`windowsOverlap`) משרתת את שני המנועים, ומשימה עם שעות עוברת דרך אותם אילוצים בדיוק כמו משמרת.
3. **היעדר כשירויות.** `activeGuards` היתה רשימה שטוחה עם זמינות כמסנן היחיד — המערכת יכלה לשבץ מלצר למטבח. תוקן ב-Phase 3: סט קטגוריות שטוח לכל אדם, חסימה מוחלטת בכל ארבעת מסלולי השיבוץ, ברירת מחדל "כשיר להכול".

על הבסיס הזה נבנו עמדות קבועות (Phase 4) ולוח מאוחד (Phase 5) — ראו `.planning/milestones/v1.1-ROADMAP.md` לפירוט מלא.

## Constraints

- **דטרמיניזם**: אין `Math.random()` במנוע — אותם נתונים תמיד מייצרים אותו סידור. זה מה שהופך את המנוע לכלי שאפשר להתווכח איתו במקום קופסה שחורה.
- **מנועים טהורים**: `autoAssign`, `fairness` ו-`conflicts` לא מכירים React ולא רשת, ולכן נבדקים ב-Node ישירות. כל תוספת חייבת לשמר זאת.
- **Tech stack**: React 18, Vite, Tailwind, Supabase (Postgres + RLS + Auth), Recharts, Vercel — קיים ועובד, לא בדיון.
- **בדיקות**: אין test runner ואין linter. `npm test` מריץ סקריפטי Node עצמאיים שמדפיסים `ok`/`FAIL`. כל מנוע חדש חייב בדיקה מאותו סוג.
- **נגישות**: ניגודיות WCAG AA (4.5:1) על כל טקסט; סימון קריטי לעולם לא בצבע בלבד.
- **עיצוב**: כל הצבעים חיים ב-`src/design/tokens.css`. הלוגו הוא המקור — צבע שלא יושב בו לא נכנס.
- **RTL ועברית** בכל הממשק.
- **`api.js` היא נקודת חנק** — המקום היחיד שמכיר שמות עמודות. עדכון בלי `.select()` הוא באג: כש-RLS מסננת הכול, Supabase מחזירה `error: null` ומערך ריק.

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| כשירות = סט קטגוריות שטוח, לא היררכיה | צבא הוא קבוצות מוכלות ומסעדה קבוצות זרות; סט מחזיק את שתיהן, היררכיה נשברת על המסעדה | ✓ שוחרר — v1.1 Phase 3 |
| חסימת כשירות מוחלטת, בלי עקיפה | כשירות היא עובדה על האדם (כמו "לא זמין"), ולא השערה על העולם (כמו מטריצת ההתנגשויות) | ✓ שוחרר — v1.1 Phase 3 |
| ברירת מחדל: אדם חדש כשיר להכול | עקבי עם "היעדר שורה = מותר" שכבר קיים ב-`conflicts.js`; שומר על "עובד בלחיצה בלי הקמה" | ✓ שוחרר — v1.1 Phase 3 |
| משימה נושאת שעות ונכנסת לאותו מנוע | שני מנועים עיוורים זה לזה הם בדיוק "המוצר לא מסכים עם עצמו" | ✓ שוחרר — v1.1 Phase 2 |
| נטל הוא המדד היחיד להוגנות | ההבטחה כבר נאמרה במוצר ודלפה בשני מקומות; שקיפות שמדווחת מספר שגוי גרועה מאי-שקיפות | ✓ שוחרר — v1.1 Phase 1 |
| הכיול קודם להרחבה | תוספת פיצ'רים על בסיס שסותר את עצמו מכפילה את הסתירה | ✓ אושר — כל 5 הפאזות עקבו אחר הסדר הזה |
| תג "מחוץ למנוע" (glyph) נפרד מהמנעול של חסימת כשירות | אותו סמל לשתי משמעויות שונות על אותו לוח בילבל צופה נאיבי (G-05-1, BOARD-04) — נדרש סמל מובחן | ✓ נפתר — v1.1 Phase 5 |
| גרסת GSD ראשונה מתייגת את המחזור כ-v1.1, לא v1.0 | הפרויקט כבר עבד בייצור לפני שכלי ה-GSD נכנס (`docs/` מכנים זאת "אבן דרך ב'") — v1.1 ממשיך את המספור הפרודוקטי, לא מתחיל GSD tracking מ-v1.0 | ✓ הוחלט עם המשתמש בסגירת המחזור |

## Current State

**v1.1 (כיול ואיחוד) שוחרר 2026-09-03.** 5 פאזות, 17 תוכניות, 46 משימות. 29/29 דרישות v1 הושלמו. ראו `.planning/MILESTONES.md` לפירוט מלא ו-`.planning/milestones/v1.1-*` לארכיון (roadmap + requirements + phase directories).

חוב טכני ידוע שנשא: bytes NUL קיימים מראש ב-`pairKey` של `conflicts.js` (ראו `.planning/phases/02-task-shift-unification/deferred-items.md`) — ללא השפעה נצפית, מוכר במודע.

## Next Milestone Goals

טרם הוגדר. הרץ `/gsd-new-milestone` כדי לפתוח שאלות → מחקר → דרישות → roadmap עבור המחזור הבא. מועמדים גלויים מ-v2 Requirements (טרם הועברו ל-Active): תוקף לכשירות (רישיון שפג), עורך כשירויות מלא, אכיפת כשירות בצד השרת, התראות, היעדרויות כישות נפרדת.

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-09-03 after v1.1 (כיול ואיחוד) milestone close*
